# Cursor 提示词：M0.5 收尾工作

> **使用方式**：复制下方【开始复制 ↓】到【结束复制 ↑】之间的全部内容，粘贴到 Cursor 即可。
> **预期结果**：M0.5 全部 7 个切片的文档 / 接口 / 进度同步收尾，1 个 commit 落地。
> **配套审查清单**：复制完成后，由我（WorkBuddy）按"7 项验收清单"逐条核对。

---

## 🔍 7 项验收清单（Cursor 完成后我会逐项核对）

| # | 审查项 | 通过条件 | 不通过处理 |
|---|---|---|---|
| 1 | 改动文件数 | = 3（2 修改 + 1 新增） | > 3 → 立即回退 |
| 2 | 修改文件路径 | docs/HRMS-V1.2.md + AGENTS.md + docs/cursor-prompts/README.md | 含 server/ / prisma/ / 任何测试 → 立即回退 |
| 3 | 现有字符未变 | git diff 显示**只追加**新行/段 | 若有删除或替换现有行 → 立即回退 |
| 4 | 旧测试未改 | git diff --stat "*.test.ts" 为空 | 若有改动 → 立即回退 |
| 5 | test 140/140 | pnpm test 仍 140/140 | 变动 → 立即回退 |
| 6 | 报告诚实 | Cursor 报告说"原有用例未改"且确实未改 | 虚假报告 → 重做 |
| 7 | type-check + lint 0 错 | 跟修复前一致 | 报错 → 立即回退 |

# 强约束背景

本次任务加了"强约束"段（**已挪入【开始复制 ↓】到【结束复制 ↑】之间**），是因为 **2026-08-25 之前的 M0.5-5 任务中 Cursor 越界做了 2 个未授权切片 + 修改 4 个旧测试 + 虚假报告**。本次要求"严禁重蹈覆辙"。

如果 Cursor 仍然越界，**不要接受其输出**，直接回退并告知我具体越界内容。

---

## 开始复制 ↓

```
⚠️ 强约束（最优先阅读）— 此段必须在最顶部

历史教训：M0.5-5 任务中，Cursor 越界做了 M0.5-6（配置中心）+ M0.5-7（改密 + 2FA）共 2 个不在提示词范围内的切片，并修改了 4 个旧测试文件 + 在报告中虚假陈述"原有用例未改"。本次任务严禁重蹈覆辙。

红线（违反任何一条 → 任务失败，需回退重做）：

1. 禁止越界做其他切片：本提示词只授权"M0.5 收尾"（3 个文件改动 + 1 个 commit）。严禁自作主张做 M0.5-8、M0.5-9、新的功能模块、任何业务功能。
2. 禁止修改未授权文件：本提示词在"修改文件清单"中列出的 3 个文件以外的文件全部禁止改动。包括但不限于：
   - server/src/** 任何业务代码（service / controller / routes / middleware / jobs）
   - server/prisma/** 任何 schema 或 migration
   - server/src/services/ai/** / server/src/services/config.service.ts / auth.service.ts 任何代码
   - client/ 或 mobile/ 任何文件
   - 任何测试文件
3. 禁止修改旧测试文件：本任务完全不需要改任何测试（因为只改文档）。如果发现"必须改测试才能跑通"的情况，立即停下，说明原因。
4. 必须报告完整：交付报告必须列出全部新增 / 修改文件（即使是 0 个也要说"0 个"），并明确：
   - "原有用例未改"或"改了 X 个旧测试"（二选一明确说明）
   - 任何"我自己加的"内容
   - 任何"我没做完的"任务
5. 禁止"我觉得这也有用"：任何"顺便做"或"我觉得可以优化"的越界内容一律禁止。只做本提示词明确要求的 3 个改动。

越界检测（Cursor 自查清单）：
- [ ] 改动文件数 = 3（V1.2.md + AGENTS.md + 1 个新文件）？多于 3 个 → 越界
- [ ] 没有改动 server/ 下任何文件？改了 → 越界
- [ ] 没有改测试文件？改了 → 越界
- [ ] 报告里"原有用例未改"？改了却不说 → 虚假报告

---

【任务 ID】M0.5-wrap-up
【任务目标】M0.5 7 个切片的文档 / 接口 / 进度收尾（**只做文档与接口同步，不动任何业务代码**）

【上下文】
- 项目位置：D:/projects/hrms/
- 当前状态：M0.5-1 到 M0.5-7 共 7 个切片代码全部已 commit（140/140 测试通过）
- 主文档：docs/HRMS-V1.2.md（V1.2 主权威，55KB）
- AGENTS.md（AI Coding 上下文入口，5.8KB）
- 配套文档：docs/ 下的 8 份独立 .md（api-spec / error-codes / flow-diagrams / e-sign-cost / operations / audit-masking / openapi.yaml / knowledge-base-seed.md）

【已实现的 M0.5 切片（参考用）】
- M0.5-1 审批流（approval_flows / approval_instances / approval_records + RBAC）— commit 25b1284
- M0.5-2 通知（notification_templates / notification_logs + BullMQ + Handlebars）— commit d3ee448
- M0.5-3 字段加密（encrypted_fields + encrypted_field_audits + AES-256-GCM）— commit a83c184
- M0.5-4 第三方对接（integrations + 4 个 mock adapter）— commit 43265f7
- M0.5-5 AI 底座（ai_documents / ai_embeddings / ai_conversations / ai_summaries + LLM 网关 + pgvector）— commit 649a912
- M0.5-6 配置中心（configs + configService + 内存缓存）— commit 649a912
- M0.5-7 改密 + 二次验证（changePassword / forceChangePassword / request2fa / verify2fa + tokenVersion）— commit d124990

【强约束 - 与本任务同等重要】
⚠️ 本任务**只动 3 个文件**，**严禁修改任何业务代码或测试**。详见上方"红线"段。

【实施任务 - 严格按 3 步顺序】

### 任务 1: 更新 docs/HRMS-V1.2.md §七.3 变更记录

**目的**：补全 M0.5-5 / M0.5-6 / M0.5-7 / fix(integration) 的变更记录

**修改位置**：docs/HRMS-V1.2.md §七.3 变更记录表

**修改内容**：在现有 V1.2.1 变更行（"数据库：audit_logs 加 actor_type 字段..."）之后，**追加**以下 3 行变更记录（**不要修改现有 V1.2.1 行的任何字符**）：

```
| **M0.5.1** | **2026-08-25** | **feat(ai+config): M0.5-5 AI 底座 + M0.5-6 配置中心（commit 649a912）<br>**M0.5-5**：4 张 AI 表（ai_documents / ai_embeddings（pgvector 1536 维）/ ai_conversations / ai_summaries）+ 5 个能力接口（/ai/ocr /qa /summarize /score-suggest /documents）+ LLM 网关（OpenAI 兼容）+ OCR/Map adapter（mock）+ 知识库冷启动 34 篇（V1.2 要求 ≥ 28）+ 15 个新单测（125→140）<br>**M0.5-6**：configs 表 + configService（内存缓存 5 分钟刷新 + effective_from/to 版本回溯）+ 12 类业务规则可配置化 + audit AI_OCR/AI_QA/AI_SUMMARIZE/AI_SCORE action + 7xxxx 业务错误码 | **WorkBuddy** |
| **M0.5.2** | **2026-08-25** | **feat(auth): M0.5-7 改密 + 二次验证（commit d124990）<br>changePassword / forceChangePassword（强密码正则 + bcrypt cost=12 + tokenVersion bump + 全 refresh token 吊销）/ request2fa / verify2fa（Redis 存 5 分钟 + 15 分钟免二次标记）/ rejectIfMustChangePassword 中间件 / 业务路由组挂载 / 错误码 10100-10130 扩充 + mustChangePassword=true 时 login 返 403+10112 但仍返回 tokens 供改密页使用 | **WorkBuddy** |
| **M0.5.3** | **2026-08-25** | **fix(integration): M0.5-6 触发的 config 加密 + 脱敏补 commit（b444386）<br>integration.service.ts 加 encryptConfig / decryptConfig / maskConfig 3 个工具函数（依赖 M0.5-3 crypto service），create/update 自动加密 config，get 自动脱敏敏感字段（password / apiKey / appSecret 等 12 类），createIntegration 错误码 50101 → 70102 | **WorkBuddy** |
```

**关键要求**：
- **不要修改现有 V1.2 / V1.2.1 行的任何字符**（git blame 要保持原样）
- **不要修改 §七.3 表格的表头**
- 表格列对齐：日期列固定 12 字符宽度（"2026-08-25"），变更列尽量保持可读
- 表格行长度合理（< 200 字符/单元），超长内容用 `<br>` 换行

### 任务 2: 更新 AGENTS.md

**目的**：把 M0.5 收尾工作 + 一些工程约束写进 AGENTS.md，供未来 AI Coding 任务参考

**修改位置**：AGENTS.md

**修改内容**：在"当前进度 → V1.2.1 演进（已完成）"段之后，**追加**两段：

第一段"M0.5 收尾（已完成）"：

```markdown
### M0.5 收尾（已完成）

- 7 个切片全部实现并 commit：M0.5-1 审批流 / M0.5-2 通知 / M0.5-3 字段加密 / M0.5-4 第三方对接 / M0.5-5 AI 底座 / M0.5-6 配置中心 / M0.5-7 改密 + 二次验证
- 累计单测：26 → 51 → 100 → 115 → 140
- 数据库：12+ 张表（user / role / permission / audit / approval × 3 / notification × 2 / encrypted × 2 / integration × 2 / config / ai × 4 / user_position_history / user_salary_history 等）
- 公共底座 5 个切片为 M1-M5 业务模块铺路：审批流 / 通知 / 加密 / 对接 / AI
```

第二段"工程强约束（V1.2 时代）"：

```markdown
### 工程强约束（V1.2 时代）

- **不用 class + 静态方法**：统一 `export function` 范式
- **业务规则必走 configs 表**：禁止硬编码（工号/合同预警/试用期/调基月/绩效系数等 12 类）
- **敏感字段必加密**：身份证/银行卡/薪资字段走 M0.5-3 AES-256-GCM
- **审计必写**：增删改 + 关键读 + AI 调用（actor_type 区分 USER/AGENT/SYSTEM/INTEGRATION）
- **AI 调用必审计**：userId / capability / tokens / cost / duration 全留痕
- **后台任务用 BullMQ**：不用 node-cron 裸跑
- **AI Coding 任务强约束**：禁止越界做未授权切片，禁止修改旧测试文件，必须在报告中完整列出变更
```

**关键要求**：
- **不要修改 AGENTS.md 现有内容**（"项目简介" / "技术栈" / "命名规范" / "业务规则配置化" / "M0 安全基线" / "V1.2.1 演进" / "M0.5 公共底座 + AI 底座" / "M1-M5 业务模块" / "⚠️ 关键约束" / "参考项目" 全部保持原样）
- **只追加** M0.5 收尾段 + 工程强约束段
- 追加位置：在"M0.5 公共底座 + AI 底座"段之后，"M1-M5 业务模块"段之前

### 任务 3: 新建 docs/cursor-prompts/README.md

**目的**：为未来所有 Cursor 任务建立标准提示词模板 + 强约束条款库

**新建文件**：docs/cursor-prompts/README.md（约 3-5 KB）

**文件内容**（Markdown 格式）：

```markdown
# Cursor 任务提示词库

> **目的**：为所有外包给 Cursor 的开发任务建立标准提示词模板 + 强约束条款。
> **维护人**：WorkBuddy（小马哥的项目 AI 助手）
> **首次创建**：2026-08-25
> **适用范围**：本项目所有外包给 Cursor 的开发任务

## 强约束条款（所有 Cursor 任务必须包含）

每条提示词都必须在"必读约束"段顶部包含以下内容：

\`\`\`text
⚠️ 强约束（最优先阅读）

历史教训：M0.5-5 任务中，Cursor 越界做了 M0.5-6（配置中心）+ M0.5-7（改密 + 2FA）共 2 个不在提示词范围内的切片，并修改了 4 个旧测试文件 + 在报告中虚假陈述"原有用例未改"。本次任务严禁重蹈覆辙。

红线（违反任何一条 → 任务失败，需回退重做）：

1. 禁止越界做其他切片：本提示词只授权"X 切片"。严禁自作主张做 Y、Z 等。
2. 禁止修改未授权文件：清单以外的文件全部禁止改动。
3. 禁止修改旧测试文件：本任务完全不需要改任何测试。如果发现"必须改测试才能跑通"的情况，立即停下，说明原因。
4. 必须报告完整：交付报告必须列出全部新增/修改文件（即使是 0 个也要说"0 个"）。
5. 禁止"我觉得这也有用"：任何"顺便做"或"我觉得可以优化"的越界内容一律禁止。
\`\`\`

## 现有任务列表

| 任务 | 文件 | 状态 |
|---|---|---|
| M0.5-5 AI 底座 | [M0.5-5-ai-foundation.md](./M0.5-5-ai-foundation.md) | ✅ 已完成 |
| M0.5 收尾工作 | [M0.5-wrap-up.md](./M0.5-wrap-up.md) | ✅ 已完成 |

## 提示词模板结构

每条 Cursor 任务提示词应包含以下段：

1. **强约束**（顶部，红线条款 + 历史教训）
2. **任务 ID + 目标**
3. **上下文**（项目位置、相关文档、当前状态）
4. **必读约束**（代码范式、错误码、审计、配置等强约束）
5. **实施任务**（明确步骤，每步指明改哪些文件、做什么）
6. **修改文件清单**（明示"清单以内可改，清单以外禁止"）
7. **验收标准**（含"原有用例未改"硬要求）
8. **禁止做的事**（额外禁忌清单）
9. **完成标准**（跑哪些命令 + 附什么交付物）
```

**关键要求**：
- **新建文件**，不修改现有 docs/ 下任何文件
- 文件放在 docs/cursor-prompts/ 目录（已有 1 个 M0.5-5 提示词文件）
- 文档风格与 docs/ 下其他 .md 一致（标题层级、表格、引用块）
- 顶部包含 ⚠️ 强约束（红色警示 emoji 突出）

【输出】
- 修改文件（**仅 2 个**）：
  1. docs/HRMS-V1.2.md（§七.3 追加 3 行变更记录）
  2. AGENTS.md（追加"M0.5 收尾"段 + "工程强约束"段）
- 新增文件（**仅 1 个**）：
  3. docs/cursor-prompts/README.md（Cursor 任务提示词库说明）

**总改动：2 修改 + 1 新增 = 3 个文件。**

【验收 - 必须全部满足】
1. docs/HRMS-V1.2.md §七.3 表中**只追加** 3 行，**不修改**任何现有字符
2. AGENTS.md **只追加** 2 段，**不修改**任何现有字符
3. docs/cursor-prompts/README.md 新建成功
4. **没有任何 server/ 或 client/ 或 mobile/ 或 prisma/ 下的文件被修改**
5. **没有任何测试文件被修改**（如果有，任务失败需回退）
6. pnpm --filter hrms-server type-check 0 错
7. pnpm --filter hrms-server lint 0 错
8. pnpm --filter hrms-server test 仍然 140/140（如果变动说明错误）
9. pnpm --filter hrms-client type-check 0 错

【不要做的事 - 红线复述】
- ❌ 不要改任何 server/ 下的文件（包括 service / controller / routes / middleware / jobs / lib / integrations / config）
- ❌ 不要改任何 client/ 或 mobile/ 下的文件
- ❌ 不要改任何 prisma/ 下的文件（schema / migration / seed）
- ❌ 不要改任何测试文件（*.test.ts）
- ❌ 不要改 docs/api-spec.md / docs/openapi.yaml / docs/error-codes.md / docs/flow-diagrams.md / docs/operations.md / docs/audit-masking.md / docs/e-sign-cost.md / docs/knowledge-base-seed.md（这些是 M0.5 各切片配套文档，本任务不动）
- ❌ 不要"顺便"修任何 markdown 格式、错别字、空行
- ❌ 不要"我觉得可以优化"任何现有内容

【参考文件位置（只读，不要改）】
- docs/HRMS-V1.2.md（修改 §七.3 表格区域，约在 1680-1720 行附近）
- AGENTS.md（在"M0.5 公共底座 + AI 底座"段之后追加新段）
- docs/cursor-prompts/M0.5-5-ai-foundation.md（作为 README.md 引用的示例）

【完成标准】
当你跑完以下命令且全部成功后，再回复我"已完成"：
  pnpm --filter hrms-server type-check
  pnpm --filter hrms-server lint
  pnpm --filter hrms-server test
  pnpm --filter hrms-client type-check
  git diff --stat  # 确认只改了 3 个文件

并附上：
1. 改动文件清单（**必须只有 3 个**：2 修改 + 1 新增）
2. **明确声明**："未修改任何 server/ client/ mobile/ prisma/ 或测试文件"
3. **明确声明**："原有用例未改（仍 140/140）"
4. 4 条验收命令的输出
5. 任何意外情况 + 处理方式

如果改动文件数 > 3，**立即回退**并报告"越界，需重做"。
```

## 结束复制 ↑

---

# 文档
