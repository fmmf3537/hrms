# M5-2-D1 M5 绩效前端 · 基础配置（考核周期 + 指标库 + 考核方案 + 绩效系数 + 等级阈值）执行提示词

> **任务 ID**：M5-2-D1
> **任务名称**：M5-2 第 7 个业务子切片 — M5 绩效前端 · 基础配置（M5-2-D 拆 5 子切片之第 1 片；D2 考核记录流程 / D3 等级与奖金 / D4 销售提成 / D5 申请联动 待后续）
> **任务范围**：3 子任务 / 2d 工时 / V1.2 §四.9 D1-D2 前端部分（任务 ID 优先，不与 V1.2 切片表强对齐）
> **提示词生成日期**：2026-09-01
> **执行器**：DeepSeek Harness headless（dsh），非 Cursor；执行环境 Windows + cmd，**pnpm/node 若不在 PATH 需先 `set PATH=C:\Users\fmmf\AppData\Roaming\npm;C:\Users\fmmf\AppData\Roaming\pnpm;%PATH%`**
> **目标单测增量**：约 4-6 个（1 API 文件 + 1 视图聚合，每文件 ≥2 用例）
> **交付方式**：最终回复必须包含完整交付报告（§7.2 模板），报告随 stdout 落入日志文件，**不要执行 git commit**（提交由审核方统一做）
> **依赖**：M5-2-C3 commit `a23ea80` 已完成（后端 876/876 + 前端 70/70 基线）

---

## ⚠️ 强约束（最优先阅读）

**历史教训**（24 次，完整历史见同目录 M5-2-C1/C2/C3 提示词，以下是仍然适用全集）：
1. 严禁越界做其他切片（M0.5-5 曾越界做 2 个切片）。本提示词只授权 M5-2-D1。
2. 详尽模式写作，禁止推断补全范围外内容（M1-A6 教训）。
3. **5 角色 RBAC 无 finance**（admin / hr / dept_head / executive / employee）。
4. 任务 ID 优先，不与 V1.2 切片表强对齐。
5. **36 service + 40 表 0 行改动**（后端业务代码一律不碰）。
6. 涉日期断言用固定日期字符串比较（本切片测试**不 import vitest**，见红线 10）。
7. **后端实际端点/权限以代码为准**：本提示词 §1.3 / §3.3 已逐一核对 `server/src/routes/performance.ts` + `server/src/constants/permissions.ts`，但若发现不符，**以代码为准并在交付报告说明**。
8. M5-2-B/C1/C2/C3 已验证设计沿用：0 修改后端 / 0 修改 client/package.json / **一个模块一个独立 Layout** / 无详情的模块在 List 内 Dialog 展开 / 状态×权限判定抽导出纯函数供测试。
9. **M5-2-D1 后端端点确认**（**12 端点，已逐一核对 `server/src/routes/performance.ts`**，挂载前缀 `/api/performance`）：
   - **考核周期**（3）：POST /cycles + GET /cycles + **PATCH** /cycles/:id（状态变更也走 PATCH body.status，**没有独立状态端点**）
   - **指标库**（2）：POST /indicators + GET /indicators
   - **考核方案**（3）：POST /schemes + GET /schemes + POST /schemes/:id/clone
   - **绩效系数**（2）：GET /coefficients + **PATCH** /coefficients（**整体对象 PATCH，不是逐项**）
   - **等级阈值**（2）：GET /grade/thresholds + **PATCH** /grade/thresholds（同上整体 PATCH）
   - **总计消费 12 后端端点**（**0 新增**）
10. **M5-2-D1 关键设计**：
    - **0 修改后端任何文件**（纯前端）
    - **0 修改 `client/package.json`**（严禁装依赖，含图表库）
    - **新建独立 `PerformanceLayout.vue`**（参照 `SalaryLayout.vue` 结构，自带 icon 映射表；**不改 SalaryLayout**）
    - **新建 `router/performance.ts`**；`router/index.ts` **仅追加** import + 展开 performanceRoutes（C1 同款模式）
    - **类型独立到 `api/types/performance.ts`**（不改既有 5 个 types 文件）
    - **本切片无 employee 角色页面**（D1 权限点 employee 全无，菜单对其整体不可见）
11. **薪酬模块实测事实沿用**：金额 `formatAmount`（无 formatMoney）；图标名以 `@element-plus/icons-vue` 实际导出为准；列表分页解包形态**以 service 实际返回为准**（`{items,total,page,pageSize}` 用 `unwrapSalaryPage`，数组形态用共享 `unwrapPage`——**§5.1 先确认再选**）。
12. **执行器差异（dsh 特注）**：你在 headless 环境执行，没有人工交互；遇到提示词与代码冲突时**按代码事实处理并在交付报告"主动识别与处理"中逐条说明**，严禁停下来等确认。

**红线**（违反任何一条 → 任务失败，需回退重做）：

1. **禁止越界**：严禁做 M5-2-D2（records 考核流程，/records 16 端点）/ D3（grade calculate + payouts）/ D4（sales）/ D5（applications）。本切片只允许消费 §1.3 的 12 端点。
2. **禁止修改后端文件**：**严禁修改 `server/` 任何文件**。
3. **禁止修改 `client/package.json`** / `pnpm-lock.yaml`：不装新依赖。
4. **禁止修改共享基础设施**：`api/http.ts` / `api/types.ts` / `stores/` / `utils/` / `components/` 全部 0 修改；`layouts/` 仅**新增** PerformanceLayout.vue。
5. **禁止修改 M5-2-0/A1/A2/B/C1/C2/C3 已建文件**：
   - ❌ `api/types/{organization,attendance,salary,payroll,compensation}.ts`（新类型放 `api/types/performance.ts`）
   - ❌ `api/` 已有 18 个业务 API 文件 + `http.ts`
   - ❌ `router/{organization,attendance,salary}.ts`（**仅允许 `router/index.ts` 追加**）
   - ❌ `layouts/` 已有 4 个 layout；`views/{organization,attendance,salary}/**`
   - ❌ `client/src/**/__tests__/` 已有 33 个测试文件（**不修改旧测试**）
6. **必须报告完整**：交付报告列出全部新增/修改文件，明确"原有用例是否改动"。
7. **禁止"我觉得这也有用"**：任何越界优化一律禁止。
8. **禁止臆测字段**：所有响应/请求字段以 §2.4 所列 service 与 zod schema 实际为准。
9. **范围强约束**：0 修改后端 / 0 修改 package.json / **12 个 client 文件（11 新增 + 1 修改 router/index.ts）** / 5 角色无 finance。
10. **测试范式强约束（`ac43074` 规则）**：
    - 新测试文件放 `src/**/__tests__/`，自包含 shim：文件内自定义 `describe`/`it` 收集用例 + `expectEqual` 抛 Error + 导出 `run*Tests(): number | Promise<number>`（命名匹配 `/^run.*Tests$/`）
    - **严禁 `import ... from 'vitest'`**（`client/tests/run-all.test.ts` glob 自动聚合，**无需修改**）
11. **不要执行 git commit / git add**（审核方统一提交）；完成后保持工作区改动原样。

---

## 1. 任务 ID + 目标

### 1.1 任务 ID

**M5-2-D1**（M5-2 第 7 个业务子切片 — M5 绩效前端 · 基础配置 · **考核周期 + 指标库 + 考核方案 + 绩效系数 + 等级阈值**）

### 1.2 任务目标

| 子任务 | 范围 | 端点 | 实际后端路径（/api/performance 前缀） | 新增 |
|---|---|---|---|---|
| 7.2.D1.1 考核周期 | 周期 CR U（PATCH 含状态流转 draft/active/closed）| 3 | POST/GET /cycles + PATCH /cycles/:id | 1 页面 |
| 7.2.D1.2 指标库 + 考核方案 | 指标 CR + 方案 CR+克隆（方案内嵌指标权重数组）| 5 | POST/GET /indicators + POST/GET /schemes + POST /schemes/:id/clone | 2 页面 |
| 7.2.D1.3 系数与阈值 | 绩效系数 S/A/B/C/D + 等级阈值，整体读取/整体 PATCH | 4 | GET/PATCH /coefficients + GET/PATCH /grade/thresholds | 2 页面 |
| **共用** | types/performance.ts + api/performance.ts + PerformanceLayout + router/performance.ts + router/index.ts 追加 + 2 测试 | — | — | 1 types + 1 API + 1 layout + 1 router + 1 修改 + 2 测试 |
| **M5-2-D1 合计** | **12 端点消费** | **12** | **11 新增 + 1 修改 = 12 文件** |

### 1.3 端点清单（**0 新增后端端点**，消费 D1 已有 12 端点）

| # | 方法 + 路径 | 权限点 | body / query（zod 实际） |
|---|---|---|---|
| 1 | POST /api/performance/cycles | performance:cycle:write | `{code(1-50), name(1-200), type: monthly\|quarterly\|yearly, startDate, endDate, description?(≤2000)}` |
| 2 | GET /api/performance/cycles | performance:cycle:read | query `type? / status?(draft\|active\|closed) / year?(int) / page / pageSize` |
| 3 | PATCH /api/performance/cycles/:id | performance:cycle:write | 全部可选：`{code?, name?, startDate?, endDate?, description?, status?: draft\|active\|closed}`（**状态流转走这里**） |
| 4 | POST /api/performance/indicators | performance:indicator:write | `{code, name, type: KPI\|OKR\|BSC\|360, category?, description?, defaultWeight?(>0,≤100), target?, unit?, scoringRule?}` |
| 5 | GET /api/performance/indicators | performance:indicator:read | query `type? / status?(active\|archived) / category? / page / pageSize` |
| 6 | POST /api/performance/schemes | performance:scheme:write | `{code, name, cycleId?(uuid), applicableScope: company\|department\|position, applicableDeptId?, applicablePositionLevel?, description?, indicators: [{indicatorId(uuid), weight(>0,≤100), target?, sortOrder?}] min 1}` |
| 7 | GET /api/performance/schemes | performance:scheme:read | query `cycleId? / status?(draft\|active\|archived) / applicableScope? / page / pageSize` |
| 8 | POST /api/performance/schemes/:id/clone | performance:scheme:write | `{newCode(1-50), newName(1-200)}` |
| 9 | GET /api/performance/coefficients | performance:coefficient:read | — |
| 10 | PATCH /api/performance/coefficients | performance:coefficient:write | `{S, A, B, C, D}` 全部必填，number >0 ≤10 |
| 11 | GET /api/performance/grade/thresholds | performance:grade:threshold:read | — |
| 12 | PATCH /api/performance/grade/thresholds | performance:grade:threshold:write | **以 `performance_grade.service.ts` 的 `GradeThresholds` 类型为准**（§5.1 必读） |

---

## 2. 上下文

### 2.1 项目位置

- 仓库根：`D:\projects\hrms`（pnpm monorepo：`client/` Vue3 + `server/` Express+Prisma+PG）
- 前端源码：`client/src/`
- 本提示词：`docs/cursor-prompts/M5-2-D1.md`

### 2.2 当前状态 + 必增文件清单

**当前状态**（M5-2-C3 commit `a23ea80`）：后端 876/876；前端 70/70（`ac43074` 脚手架）；组织/考勤/薪酬三模块前端已完整落地（各有独立 Layout + 独立 router 文件 + `router/index.ts` 追加挂载）。

**本切片必增 11 文件 + 必改 1 文件**：
- 新增：`api/types/performance.ts`、`api/performance.ts`、`layouts/PerformanceLayout.vue`、`router/performance.ts`、`views/performance/cycle/CycleList.vue`、`views/performance/indicator/IndicatorList.vue`、`views/performance/scheme/SchemeList.vue`、`views/performance/coefficient/CoefficientPanel.vue`、`views/performance/grade/GradeThresholds.vue`、`api/__tests__/performance.test.ts`、`views/__tests__/performance.test.ts`
- 修改：`router/index.ts`（**仅追加** import + performanceRoutes 展开，C1 挂载 salaryRoutes 同款模式）

### 2.3 可复用模块（**严禁重写**）

| 模块 | 路径 | 用法 |
|---|---|---|
| http 实例 | `client/src/api/http.ts` | 默认导出 axios 实例，baseURL `/api`，拦截器返回 `response.data`，业务错误自动 ElMessage |
| 分页解包 | `api/types.ts` 的 `unwrapPage`（数组形态）/ `api/types/salary.ts` 的 `unwrapSalaryPage`（items 对象形态） | **§5.1 确认绩效 list 返回形态后二选一，import 复用，不重写** |
| 布局范式 | `layouts/SalaryLayout.vue` | **整体复制改造**为 PerformanceLayout.vue（侧栏菜单由本模块 router 文件的 filter 函数驱动；icon 映射表自带） |
| 路由+菜单范式 | `router/salary.ts` | `PERFORMANCE_MENU: XxxMenuItem[]` + `filterPerformanceMenu(user)` + `resolvePerformanceActiveKey(path)` + default export performanceRoutes（路径前缀 `/performance`） |
| 权限判断 | `utils/permission.ts` 的 `hasPermission(user, point)` | 按钮/菜单显隐 |
| 当前用户 | `stores/user.ts` | `useUserStore().userInfo` |
| 页面范式 | 参照 `views/salary/grade/GradeList.vue`（C1） | PageHeader + 筛选栏 + ElTable + ElPagination + Dialog 表单 |
| 测试范式 | 参照 `api/__tests__/payroll.test.ts`（C2） | 自包含 shim，`run*Tests` 导出 |

### 2.4 业务规则来源（**写类型前必读**）

- `server/src/services/performance_cycle.service.ts`（list 分页形态 / update 返回 / status 流转约束）
- `server/src/services/performance_indicator.service.ts`
- `server/src/services/performance_scheme.service.ts`（方案详情是否内嵌 indicators / clone 返回）
- `server/src/services/performance_coefficient.service.ts`（GET 返回结构：是 `{S,A,B,C,D}` 还是带元数据）
- `server/src/services/performance_grade.service.ts`（**`GradeThresholds` 类型定义 + getGradeThresholds 返回**，阈值字段名/范围一律以此为准）

---

## 3. 必读约束

### 3.1 代码范式

- Vue3 `<script setup lang="ts">` + Element Plus + Pinia，与 C1-C3 页面同风格
- API 层只做端点拼装与解包；页面操作成功后 ElMessage.success + 刷新
- 状态列统一 ElTag + 映射常量（导出供测试）

### 3.2 命名

- API 函数 camelCase 动宾：`createCycle / listCycles / updateCycle / createIndicator / listIndicators / createScheme / listSchemes / cloneScheme / getCoefficients / updateCoefficients / getGradeThresholds / updateGradeThresholds`
- `PERFORMANCE_PATHS as const` 单文件集中
- 类型 PascalCase：`PerformanceCycle / CycleType / CycleStatus / Indicator / IndicatorType / Scheme / SchemeIndicator / ApplicableScope / Coefficients / GradeThresholds`
- 菜单 key：`cycles / indicators / schemes / coefficients / grade-thresholds`

### 3.3 RBAC 权限矩阵（**已实读 `permissions.ts` ROLE_PERMISSIONS**）

| 权限点 | admin | hr | dept_head | executive | employee |
|---|---|---|---|---|---|
| performance:cycle:read / indicator:read / scheme:read / coefficient:read | ✅* | ✅ | ✅ | ✅ | ❌ |
| performance:cycle:write / indicator:write / scheme:write | ✅* | ✅ | ❌ | ❌ | ❌ |
| performance:coefficient:write | ✅* | ✅ | ❌ | **✅** | ❌ |
| performance:grade:threshold:read | ✅* | ✅ | ❌ | ✅ | ❌ |
| performance:grade:threshold:write | ✅* | ✅ | ❌ | **❌** | ❌ |

**反直觉点（显隐必须遵守）**：
1. **executive 能改绩效系数（coefficient:write）但不能改等级阈值（threshold:write 仅 admin/hr）**——阈值页的保存按钮对 executive 隐藏（只读展示）
2. **dept_head 有 cycle/indicator/scheme/coefficient 的 read，但没有 grade:threshold:read**——阈值菜单对 dept_head 不可见
3. **employee 本切片权限全无**——绩效模块菜单整体不可见（filter 函数天然处理，无需特判）

### 3.4 错误码 / 3.5 审计 / 3.6 configs

0 新增。系数与阈值虽然本质是配置，但**走 performance 专用端点**（本切片消费），不动 configs 模块。

---

## 4. 实施任务

### 4.1 `api/types/performance.ts`（新增，**先做 §5.1 确认再动笔**）

- `CycleType = 'monthly'|'quarterly'|'yearly'`；`CycleStatus = 'draft'|'active'|'closed'`；`PerformanceCycle`（字段以 service 为准）；`CycleListQuery`；`CreateCycleRequest` / `UpdateCycleRequest`（对齐 zod，含 status）
- `IndicatorType = 'KPI'|'OKR'|'BSC'|'360'`；`IndicatorStatus = 'active'|'archived'`；`PerformanceIndicator`；`IndicatorListQuery`；`CreateIndicatorRequest`
- `ApplicableScope = 'company'|'department'|'position'`；`SchemeStatus = 'draft'|'active'|'archived'`；`SchemeIndicator = {indicatorId, weight, target?, sortOrder?}`；`PerformanceScheme`；`SchemeListQuery`；`CreateSchemeRequest`（indicators min 1）；`CloneSchemeRequest = {newCode, newName}`
- `Coefficients = {S, A, B, C, D: number}`（若 service GET 返回带元数据则另立 `CoefficientsResponse`）
- `GradeThresholds`：**直接参考 service 导出类型手写等价 TS 接口**（字段以 service 为准）
- 状态→中文/ElTag type 映射常量：`CYCLE_STATUS_MAP` / `SCHEME_STATUS_MAP` / `INDICATOR_STATUS_MAP`（**导出**，供页面与测试共用）

### 4.2 `api/performance.ts`（新增，12 函数）

`PERFORMANCE_PATHS as const`：`cycles / cycle(id) / indicators / schemes / schemeClone(id) / coefficients / gradeThresholds`。
12 个函数一一对应 §1.3，每函数单行 JSDoc（端点 + 方法 + 权限点）。分页函数（listCycles / listIndicators / listSchemes）的解包方式**以 §5.1 结论为准**并在文件头注释注明形态。

### 4.3 `layouts/PerformanceLayout.vue`（新增）

- **整体参照 `SalaryLayout.vue`** 改造：侧栏菜单从 `router/performance.ts` 的 `filterPerformanceMenu(userInfo)` 取，activeKey 用 `resolvePerformanceActiveKey(route.path)`
- icon 映射表自带（本模块菜单图标自定，从 `@element-plus/icons-vue` 实际导出中选 5 个，如 Calendar / Aim / Files / SetUp / Medal——**import 前核实存在**）
- 员工角色进入 `/performance` 时菜单为空 → 显示 EmptyState（参照既有 layout 的空菜单处理，没有则加简单 v-if）

### 4.4 `router/performance.ts`（新增）

- `PERFORMANCE_MENU`（5 项）：cycles（考核周期）/ indicators（指标库）/ schemes（考核方案）/ coefficients（绩效系数）/ grade-thresholds（等级阈值），permission 分别对应 cycle:read / indicator:read / scheme:read / coefficient:read / grade:threshold:read
- `filterPerformanceMenu` + `resolvePerformanceActiveKey`（5 前缀分支）
- default export `performanceRoutes`：`/performance` → PerformanceLayout，children 5 条（redirect '' → '/performance/cycles'）
- **`router/index.ts` 仅追加**：import + 在 routes 数组展开 performanceRoutes（照抄 salaryRoutes 挂载行的写法）

### 4.5 五个页面（新增）

**CycleList.vue**：筛选 type/status/year；表格 code/name/type/起止日期/status ElTag/操作；「新建周期」Dialog（type Radio + 起止 ElDatePicker date `value-format="YYYY-MM-DD"`）；编辑 Dialog 复用表单 + **状态流转 Select（draft/active/closed）**（同一 PATCH 端点，提交时只带变更字段）；写按钮 `cycle:write`（admin/hr）显隐

**IndicatorList.vue**：筛选 type/status/category；表格 code/name/type/category/defaultWeight/unit/操作；新建/编辑 Dialog（type Radio：KPI/OKR/BSC/360；defaultWeight ElInputNumber 1-100）；**注意没有 PATCH 指标端点**——编辑不在本切片（无端点），只新建 + 列表 + 展开行看详情

**SchemeList.vue**：筛选 cycleId/status/applicableScope；表格 code/name/scope/周期/状态/操作；新建 Dialog（**动态指标行编辑器**：ElSelect 选指标（从 listIndicators 拉 active 列表）+ weight ElInputNumber + 可增删行，至少 1 行，提交前校验权重数组）；「克隆」按钮 → Dialog 填 newCode/newName → cloneScheme

**CoefficientPanel.vue**：GET 加载 S/A/B/C/D 五个 ElInputNumber（0-10，步长 0.1）；「保存」PATCH；保存按钮 `coefficient:write`（admin/hr/**executive**）显隐，无权限时只读 + 提示文案

**GradeThresholds.vue**：同 CoefficientPanel 结构（字段以 §5.1 为准）；保存按钮 `grade:threshold:write`（**仅 admin/hr**）显隐，executive/dept_head 只读（dept_head 连 read 都没有 → 菜单不可见）

### 4.6 测试（2 个新文件，`ac43074` 自包含范式）

1. `api/__tests__/performance.test.ts`（≥2）：`PERFORMANCE_PATHS` 拼接断言（含 `/performance/grade/thresholds` 与 `/performance/coefficients` 不混淆）；`CreateSchemeRequest.indicators` min 1 语义 / `Coefficients` 五键齐全断言
2. `views/__tests__/performance.test.ts`（≥2）：`CYCLE_STATUS_MAP` / `SCHEME_STATUS_MAP` 完整映射断言；`filterPerformanceMenu` 角色可见性（employee → 空数组；dept_head → 4 项无 grade-thresholds；executive → 5 项全有）

导出 `runPerformanceApiTests / runPerformanceViewTests`。**严禁 import vitest / @vue/test-utils**。

---

## 5. 关键决策点（详尽）

### 5.1 响应结构确认（**最优先**）

写 types 前逐一打开 §2.4 五个 service，记录：
- 三个 list 方法的分页返回形态（`{items,total,page,pageSize}` → `unwrapSalaryPage`；`{list/array}` → 共享 `unwrapPage`）
- `GradeThresholds` 的确切字段（等级划分阈值，可能形如 `{S: 90, A: 80, ...}` 下限分数，或含版本/生效期）
- coefficients GET 返回是否带 updatedAt 等元数据
- scheme GET list 是否内嵌 indicators 数组（决定列表页是否直接展示指标数）
- **结论写进交付报告「主动识别与处理」**

### 5.2 周期状态流转

- 无独立状态端点：编辑 Dialog 内放 status Select，PATCH 提交 `{status}`（可与其他字段同提）
- 流转合法性（如 closed 不可逆）以 service 为准；前端**不做硬限制**，失败后端错误信息由拦截器弹出

### 5.3 指标"编辑"缺失的处理

- 后端**没有** PATCH /indicators/:id——本切片不做指标编辑与归档按钮（archive 无端点），交付报告"已知问题"注明

### 5.4 方案指标编辑器

- `indicators` 数组 min 1；权重每项 >0 ≤100；**是否校验总和=100 以后端 service 为准**（service 若校验则在表单加实时合计提示；不校验则不加）
- sortOrder 默认按行序 0,1,2… 自动填充

### 5.5 dsh 执行环境特注

- shell 为 cmd：多步命令用 `&&`；pnpm 不在 PATH 时先执行 `set PATH=C:\Users\fmmf\AppData\Roaming\npm;C:\Users\fmmf\AppData\Roaming\pnpm;%PATH%`
- **不要执行 git add / commit / push**
- 验收命令逐条跑，真实输出摘要进交付报告

### 5.6 不做清单

- ❌ records / grade calculate / payouts / sales / applications 任何页面与 API（D2-D5）
- ❌ DefaultLayout 侧栏「绩效管理」占位（若 disabled，属 M5-2-0 范畴，列入已知问题）
- ❌ 指标编辑/归档（无端点，§5.3）
- ❌ 修改 `ROLE_PERMISSIONS` 或任何后端文件

---

## 6. 修改文件清单

### 6.1 必改文件（12 个）

**新增 11 个**：
1. `client/src/api/types/performance.ts`
2. `client/src/api/performance.ts`
3. `client/src/layouts/PerformanceLayout.vue`
4. `client/src/router/performance.ts`
5. `client/src/views/performance/cycle/CycleList.vue`
6. `client/src/views/performance/indicator/IndicatorList.vue`
7. `client/src/views/performance/scheme/SchemeList.vue`
8. `client/src/views/performance/coefficient/CoefficientPanel.vue`
9. `client/src/views/performance/grade/GradeThresholds.vue`
10. `client/src/api/__tests__/performance.test.ts`
11. `client/src/views/__tests__/performance.test.ts`

**修改 1 个**：
12. `client/src/router/index.ts`（**仅追加** import + performanceRoutes 展开）

### 6.2 禁止修改文件（清单以外）

- ❌ `server/**`（全部）
- ❌ `client/package.json` / `pnpm-lock.yaml` / `vitest.config.ts` / `tests/run-all.test.ts` / `vite.config.ts` / `tsconfig*.json`
- ❌ `client/src/api/types.ts` / `api/types/` 既有 5 文件 / `api/` 既有 18 业务 API + `http.ts`
- ❌ `client/src/router/{organization,attendance,salary}.ts`
- ❌ `client/src/layouts/` 既有 4 个 layout / `components/**` / `stores/**` / `utils/**`
- ❌ `client/src/views/{organization,attendance,salary}/**`
- ❌ `client/src/**/__tests__/` 已有 33 个测试文件

### 6.3 越界检测（交付时自检）

```bash
cd D:\projects\hrms
git status --short
# 必须只出现 §6.1 列出的 12 个文件
git diff --stat server/
# 必须 0 行
git diff client/package.json
# 必须 0 行
git diff client/src/components/ client/src/stores/ client/src/utils/
# 必须 0 行
```

---

## 7. 验收标准

### 7.1 硬性验收（必须 100% 通过）

```bash
cd D:\projects\hrms

# 0. 环境（pnpm 不在 PATH 时先执行）
set PATH=C:\Users\fmmf\AppData\Roaming\npm;C:\Users\fmmf\AppData\Roaming\pnpm;%PATH%

# 1. 后端测试（0 改动，沿用 876）
pnpm --filter hrms-server test
# 预期：876/876 通过

# 2. 后端 type-check
pnpm --filter hrms-server type-check
# 预期：0 错

# 3. 前端 type-check
pnpm --filter hrms-client type-check
# 预期：0 错

# 4. 前端测试（自动聚合新用例）
pnpm --filter hrms-client test
# 预期：70 + 新增（2 文件 × ≥2）= 74 以上全通过，0 failed

# 5. 全仓 lint
pnpm lint
# 预期：0 error（server 既有 73 条 warning 不新增超过 5 个）

# 6. 前端生产构建
pnpm --filter hrms-client build
# 预期：构建成功
```

### 7.2 交付报告模板（**写入你的最终回复**）

```markdown
# M5-2-D1 交付报告

## 完成范围
- 子任务 7.2.D1.1/2/3 完成状态 + 端点消费清单（12/12）
## 新增文件（11）
（逐一列出）
## 修改文件（1）
- client/src/router/index.ts（+N 行）
## 主动识别与处理
（§5.1 响应结构确认结论逐 service 列出 / 分页形态选择 / GradeThresholds 实际字段 / 与提示词不一致处，逐一说明处理方式）
## 测试
- 新增 2 个测试文件 / N 个用例；pnpm --filter hrms-client test 结果
- 原有用例是否改动：0（必须）
## 验收命令输出摘要
（§7.1 六条命令的实际结果）
## 已知问题
（如有，逐一列出 + 建议处理阶段）
## 红线自检
- 0 修改后端 / 0 修改 package.json / 0 修改旧测试 / 0 修改共享基础设施与 C1-C3 文件（除 router/index.ts 追加）/ 5 角色无 finance / 未执行 git commit
```

---

**提示词结束。按本提示词直接执行（headless 环境无人工确认环节），先输出实施计划（文件创建顺序 + §5.1 确认结论），然后动手，最终回复给出完整交付报告。**
