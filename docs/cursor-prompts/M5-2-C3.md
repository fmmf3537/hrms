# M5-2-C3 M4 薪酬前端 · 提成·成本预警·调薪（C6 提成 + C7 成本预警 + C8 调薪）Cursor 提示词

> **任务 ID**：M5-2-C3
> **任务名称**：M5-2 第 6 个业务子切片 — M4 薪酬前端 · 提成/成本预警/调薪（提成汇总与结算 + 成本预警扫描处理 + 调薪全流程），**M4 薪酬前端收官切片**
> **任务范围**：3 子任务 / 3d 工时 / V1.2 §四.8 C6-C8（任务 ID 优先，不与 V1.2 切片表强对齐）
> **提示词生成日期**：2026-09-01
> **目标单测增量**：约 6-10 个（3 API 文件 + 1 视图聚合，每文件 ≥2 用例）
> **目标 commit**：1 个业务 commit + 1 个提示词 commit
> **依赖**：[M5-2-C2 算薪与工资条前端](M5-2-C2.md) commit `aa00620` 已完成（后端 876/876 + 前端 62/62 基线）

---

## ⚠️ 强约束（最优先阅读）

**历史教训**（23 次）：
1. M0.5-5 任务中，Cursor 越界做了 M0.5-6 + M0.5-7 共 2 个不在提示词范围内的切片。本次任务严禁重蹈覆辙。
2. M1-A6 提示词第一次瘦身到 15KB，Cursor 推断/补全消耗大量 token，返工 1 次。本次任务按详尽模式写。
3. D1 提示词 §3.3 误列 5 角色包含 finance，**Cursor 主动识别 + 正确处理**。此后各切片已修正。**M5-2-C3 沿用 5 角色 RBAC 无 finance**。
4. M2-wrap-up.md §7 误写 D3 = "季度校准会议"。M5-2-C3 不重蹈覆辙，严格按 V1.2 §四.8 C6-C8 实际范围（**任务 ID 优先，不与 V1.2 切片表强对齐**，M4 错位专题见 AGENTS.md）。
5. C8 红线 36 service 0 行改动。**M5-2-C3 沿用 36 service 0 行改动**。
6. D4 验收发现 2 个 B3/B5 跨 UTC 边界日期敏感旧测。涉日期断言用固定日期字符串比较（**本切片测试不 import vitest**，见红线 10）。
7. M5-2-A1/A2 起反复验证：**后端实际端点与提示词可能不同 → M5-2-C3 必须以 `server/src/routes/salary.ts` 实际代码为准（本提示词 §1.3 已逐一核对）**；真实权限点以 `server/src/constants/permissions.ts` ROLE_PERMISSIONS 为准（本提示词 §3.3 已实读）。
8. **M5-2-B/C1/C2 关键设计沿用**（commit `ff433c7` / `787036f` / `aa00620`）：0 修改后端 / 0 修改 client/package.json / 独立 SalaryLayout / List 内 Dialog 展开详情 / 状态×权限双维按钮显隐抽纯函数导出供测试。
9. **M5-2-C3 后端端点确认**（**22 端点，已逐一核对 `server/src/routes/salary.ts` 实际代码**，挂载前缀 `/api/salary`）：
   - **C6 提成**（8 端点，`/api/salary/commissions` 前缀）：GET /summary + GET /employees/:employeeId + GET /departments/:departmentId + POST /settlements + GET /settlements + GET /settlements/:id + POST /settlements/:id/confirm + POST /settlements/:id/cancel
   - **C7 成本预警**（6 端点，`/api/salary/cost-alerts` 前缀）：POST /scan + GET /summary + GET / + GET /:id + POST /:id/acknowledge + POST /:id/close
   - **C8 调薪**（8 端点，`/api/salary/adjustments` 前缀）：POST /execute-pending + POST / + GET / + GET /:id + POST /:id/submit + POST /:id/approve + POST /:id/execute + POST /:id/cancel
   - **M5-2-C3 总计消费 22 后端端点**（**0 新增**）
10. **M5-2-C3 关键设计**：
    - **0 修改后端任何文件**（**M5-2-C3 是纯前端**）
    - **0 修改 `client/package.json`**（**严禁再装依赖**）
    - **0 新增 D1-D6 / C1-C8 / M0.5 service**（沿用 36 service 0 改动）
    - **0 修改 M5-2-0 + M5-2-A1/A2/B + M5-2-C1/C2 已建文件**（唯一例外：`router/salary.ts` **追加**菜单与路由，沿用 C2 的 `SALARY_C3_MENU` 独立数组模式，**不得破坏 C1 旧测 `SALARY_MENU.length === 7`**）
    - **SalaryLayout.vue 0 修改**（菜单图标白名单仅 Money/Coin/Tickets/Wallet/Postcard/Operation/DataLine，**新增菜单项 icon 只能从中选**——C2 实测结论）
    - **5 角色 RBAC 严格遵守**（admin / hr / dept_head / executive / employee，**无 finance**）
    - **类型独立到 `api/types/compensation.ts` 新文件**（不改 C1 `types/salary.ts` / C2 `types/payroll.ts`；`unwrapSalaryPage` 从 `./types/salary` import 复用）
11. **M5-2-C1/C2 实测验证沿用的前端事实**：
    - 薪酬模块 list 响应 `data` 是 `{items,total,page,pageSize}` 对象 → **必须用 `unwrapSalaryPage`**
    - 金额格式化用 **`formatAmount`**（`utils/format.ts`），**不存在 `formatMoney`**
    - 图标名以 `@element-plus/icons-vue` 实际导出 + SalaryLayout 白名单为准
    - 按钮可见性判定写页面/类型文件内**导出的纯函数**（C2 `canPayrollRunAction` 模式），供测试断言
12. **M5-2-C2 遗留事实（本切片知道即可，不修）**：`salary:payslip:write` 后端未授 hr（C2 已知问题 4）；**本切片严禁借修改 `ROLE_PERMISSIONS` 之机改后端**。

**红线**（违反任何一条 → 任务失败，需回退重做）：

1. **禁止越界做其他切片**：本提示词只授权 "M5-2-C3 切片（提成/成本预警/调薪）"。严禁做 M5-2-D（绩效前端）/ M5-3~M5-8。严禁回头改 C1/C2 页面。
2. **禁止修改后端文件**：纯前端，**严禁修改 `server/` 任何文件**（包括 `constants/permissions.ts`）。
3. **禁止修改 `client/package.json`**：不装新依赖（图表用 ElTable/ElProgress/描述列表实现，**不装 echarts**）。
4. **禁止修改 M5-2-0 共享基础设施**：`api/http.ts` / `api/types.ts` / `stores/` / `utils/` / `components/` / `layouts/` 全部 0 修改。
5. **禁止修改 M5-2-A1/A2/B/C1/C2 已建文件**：
   - ❌ 严禁修改 `api/types/{organization,attendance,salary,payroll}.ts`（新类型放 `api/types/compensation.ts`）
   - ❌ 严禁修改 `api/` 已有 17 个业务 API 文件 + `http.ts`
   - ❌ 严禁修改 `router/{index,organization,attendance}.ts`（**仅允许追加修改 `router/salary.ts`**）
   - ❌ 严禁修改 `SalaryLayout.vue` 与 `views/salary/{grade,plan,insurance,tax,payroll,ess}/**`（C1/C2 已建 14 页面）
   - ❌ 严禁修改 `client/src/**/__tests__/` 已有 29 个测试文件（**不修改旧测试**，包括 C1 的 `SALARY_MENU.length === 7` 断言）
6. **必须报告完整**：交付报告必须列出全部新增/修改文件（即使是 0 个也要说"0 个"），并明确说明"原有用例是否改动"。
7. **禁止"我觉得这也有用"**：任何"顺便做"或"我觉得可以优化"的越界内容一律禁止。
8. **禁止 `employees.baseSalary` 臆测**：调薪的 `toBaseSalary` 是**调薪单字段**（zod 实测存在），与 employees 表无关；员工当前薪资一律以 service 返回为准，不臆测字段。
9. **M5-2-C3 范围强约束**：
   - **0 修改后端任何文件**
   - **0 修改 `client/package.json` / `vitest.config.ts` / `tests/run-all.test.ts`**
   - **16 个 client 文件**（**15 新增 + 1 修改 router/salary.ts**）
   - **D1-D6 + C1-C8 业务代码 0 行改动**
   - **5 角色 RBAC 严格遵守**（**无 finance 角色**）
10. **测试范式强约束（`ac43074` 规则）**：
    - 新测试文件**沿用自包含内联断言模式**：文件内自定义 `describe`/`it` + `expectEqual` 抛 Error + 导出 `run*Tests(): number | Promise<number>`（命名匹配 `/^run.*Tests$/`）
    - **严禁 `import ... from 'vitest'`**（glob 自动聚合 `src/**/__tests__/*.test.ts`）
    - 涉日期断言用固定日期字符串比较

---

## 1. 任务 ID + 目标

### 1.1 任务 ID

**M5-2-C3**（M5-2 第 6 个业务子切片 — M4 薪酬前端 · **C6 提成 + C7 成本预警 + C8 调薪**，M4 前端收官）

### 1.2 任务目标

实现 M5 联调 Vue3 前端的**第六个业务子切片**（M4 薪酬 · 提成/预警/调薪），包含 3 子任务：

| 子任务 | 范围 | 端点 | 实际后端路径（/api/salary 前缀） | 新增/修改 |
|---|---|---|---|---|
| 7.2.C3.1 提成（C6）| 汇总（4 种 groupBy）+ 员工/部门明细 + 结算单全流程（创建/确认/作废）| 8 | /commissions 系列 | 1 API + 3 页面 + 1 测试 |
| 7.2.C3.2 成本预警（C7）| 扫描 + 汇总 + 列表 + 详情 + 确认 + 关闭 | 6 | /cost-alerts 系列 | 1 API + 2 页面 + 1 测试 |
| 7.2.C3.3 调薪（C8）| 创建 + 列表（含 read-self）+ 详情 + 提交/审批/执行/作废 + 批量执行 | 8 | /adjustments 系列 | 1 API + 2 页面 + 1 测试 |
| **共用** | api/types/compensation.ts 新文件 + router/salary.ts 追加（4 菜单 + 6-7 路由）| — | — | 1 新 types + 1 修改 router |
| **M5-2-C3 合计** | **22 端点消费** | **22** | **15 新增 + 1 修改 = 16 文件** |

**M5-2-C3 业务量**：
- 0 后端文件改动（**仅消费 22 端点**）
- 0 `client/package.json` 改动
- 0 M5-2-0 + M5-2-A1/A2/B + M5-2-C1/C2 已建文件改动（除 `router/salary.ts` 追加）
- 16 client 文件改动（**15 新增 + 1 修改**）
- ~2500-3500 行 Vue3 + TypeScript 代码
- 6-10 单元测试

### 1.3 端点清单（**0 新增后端端点**，消费 C6-C8 已有 22 端点）

**C6 提成**（`/api/salary/commissions` 前缀，8 端点）：

| # | 方法 + 路径 | 权限点 | body / query（zod 实际） | 备注 |
|---|---|---|---|---|
| 1 | GET /summary | salary:commission:read | query `groupBy?: employee\|department\|product\|report` + `period?` + `quarter?` | `groupBy=report` 时后端改走 commissionReportService.getReport |
| 2 | GET /employees/:employeeId | salary:commission:read | query `period? / quarter?` | 员工提成明细 |
| 3 | GET /departments/:departmentId | salary:commission:read | query `period? / quarter?` | 部门提成明细 |
| 4 | POST /settlements | salary:commission:**settle** | `{year: int, quarter: 1\|2\|3\|4, initialStatus?: 'draft'\|'pending_confirm'}` | 创建结算单 |
| 5 | GET /settlements | salary:commission:read | query `year? / status? / periodStart? / periodEnd? / page / pageSize`；status 枚举 `draft\|pending_confirm\|confirmed\|cancelled` | 分页 |
| 6 | GET /settlements/:id | salary:commission:read | — | 结算单详情 |
| 7 | POST /settlements/:id/confirm | salary:commission:**confirm** | `{remark?: string(max2000)}` | 确认 |
| 8 | POST /settlements/:id/cancel | salary:commission:**cancel** | `{reason: string(min1, max2000)}` **必填** | 作废 |

**C7 成本预警**（`/api/salary/cost-alerts` 前缀，6 端点）：

| # | 方法 + 路径 | 权限点 | body / query（zod 实际） | 备注 |
|---|---|---|---|---|
| 9 | POST /scan | salary:cost-alert:**scan** | `{period: string(min7,max7)=YYYY-MM, alertType?: 'overtime_ratio'\|'attrition_monthly'\|'all'}` | 触发扫描 |
| 10 | GET /summary | salary:cost-alert:read | query `period: string(min7,max7)` **必填** | 期间汇总 |
| 11 | GET / | salary:cost-alert:read | query `alertType? / period? / status? / severity? / departmentId? / page / pageSize`；status `active\|acknowledged\|closed`，severity `warning\|critical` | 分页 |
| 12 | GET /:id | salary:cost-alert:read | — | 预警详情 |
| 13 | POST /:id/acknowledge | salary:cost-alert:**ack** | `{note?: string(max2000)}` | 确认知晓 |
| 14 | POST /:id/close | salary:cost-alert:**close** | `{reason: string(min1,max2000)` **必填** `, remark?: string(max2000)}` | 关闭 |

**C8 调薪**（`/api/salary/adjustments` 前缀，8 端点）：

| # | 方法 + 路径 | 权限点 | body / query（zod 实际） | 备注 |
|---|---|---|---|---|
| 15 | POST /execute-pending | salary:adjustment:**execute** | `{asOfDate: string}` 正则 **`^\d{4}-\d{2}-\d{2}$`** | 批量执行到期调薪 |
| 16 | POST / | salary:adjustment:write | `{employeeId: uuid, adjustmentType, toBaseSalary: number>0, toPerformanceSalary?: number>=0, effectiveDate: YYYY-MM-DD 正则, reason: min1 max2000, remark?: max2000}` | 201 创建；adjustmentType 枚举 `promotion\|annual_adjust\|performance\|market_adjustment` |
| 17 | GET / | salary:adjustment:read **或** salary:adjustment:**read-self**（双权限其一即可） | query `employeeId? / status? / adjustmentType? / period? / page / pageSize`；status 枚举 `draft\|pending\|approved\|rejected\|executed\|cancelled` | 分页；employee 角色走 read-self |
| 18 | GET /:id | 同上双权限 | — | 详情 |
| 19 | POST /:id/submit | salary:adjustment:write | `{comment?: string(max2000)}` | 提交 |
| 20 | POST /:id/approve | salary:adjustment:**approve** | `{action: 'approve'\|'reject', comment?: string(max2000)}` | **单端点双动作**，驳回也走这里 |
| 21 | POST /:id/execute | salary:adjustment:**execute** | 无 body | 单笔执行 |
| 22 | POST /:id/cancel | salary:adjustment:**cancel** | `{reason: string(min1, max2000)}` **必填** | 作废 |

**0 新增后端端点**

---

## 2. 上下文

### 2.1 项目位置

- 仓库根：`D:\projects\hrms`（pnpm monorepo：`client/` Vue3 + `server/` Express+Prisma+PG）
- 前端源码：`client/src/`
- 本提示词：`docs/cursor-prompts/M5-2-C3.md`

### 2.2 当前状态 + 必增文件清单

**当前状态**（M5-2-C2 commit `aa00620` 已完成）：
- 后端测试 876/876；前端测试 62/62（`ac43074` 脚手架 glob 聚合）
- `SalaryLayout.vue` + `router/salary.ts`（含 `SALARY_MENU` + `SALARY_C2_MENU` + `filterSalaryMenu`）已就绪
- 薪酬模块已有 14 页面：grade×2 / plan×1 / insurance×3 / tax×2 / payroll×4 / ess×2

**本切片必增 13 文件 + 必改 1 文件**（逐一见 §4 与 §6.1）：
- 新增：`api/types/compensation.ts`、`api/{commission,costAlert,adjustment}.ts`、`views/salary/commission/{CommissionSummary,SettlementList,SettlementDetail}.vue`、`views/salary/cost-alert/{CostAlertList,CostAlertDetail}.vue`、`views/salary/adjustment/{AdjustmentList,AdjustmentDetail}.vue`、`api/__tests__/{commission,costAlert,adjustment}.test.ts`、`views/__tests__/compensation.test.ts`
- 修改：`router/salary.ts`（**仅追加** `SALARY_C3_MENU` 4 项 + `filterSalaryMenu` 拼接 + 4 个 activeKey 分支 + 7 子路由）

### 2.3 可复用模块（**严禁重写**）

| 模块 | 路径 | 用法 |
|---|---|---|
| http 实例 | `client/src/api/http.ts` | 默认导出 axios 实例，baseURL `/api`，拦截器返回 `response.data` |
| 分页解包 | `api/types/salary.ts` 的 **`unwrapSalaryPage`** | 三个 list 端点（settlements / cost-alerts / adjustments）都用它 |
| 金额格式化 | `utils/format.ts` 的 **`formatAmount`** | 提成额 / 调薪额 / 薪资字段 |
| 权限判断 | `utils/permission.ts` 的 `hasPermission(user, point)` | 按钮显隐；**双权限端点用 `hasPermission(a) \|\| hasPermission(b)`** |
| 当前用户 | `stores/user.ts` 的 `useUserStore().userInfo` | employeeId = `userInfo.employee?.id`；为空显示 EmptyState「尚未关联员工档案」 |
| 菜单过滤 | `router/salary.ts` | **C2 模式**：新建 `SALARY_C3_MENU` 独立数组 + `filterSalaryMenu` 内拼接（**不改 `SALARY_MENU`**，C1 旧测断言 length===7） |
| 状态×权限判定 | 参照 `api/types/payroll.ts` 的 `canPayrollRunAction`（C2）| 本切片同样抽导出纯函数 `canSettlementAction` / `canCostAlertAction` / `canAdjustmentAction` |
| 员工选择 | 沿用 C1 参保登记页的员工选择方式（既有 `api/employee.ts` 的 `listEmployees`）| **不允许新写员工 API 文件** |
| 页面范式 | 参照 `views/salary/payroll/PayrollRunList.vue`（C2）| PageHeader + 筛选栏 + ElTable + ElPagination + Dialog |

### 2.4 业务规则来源

- PRD：`docs/prd/` 内 M4 薪酬章节（C6 提成 / C7 成本预警 / C8 调薪）
- **响应字段唯一事实来源**（写类型前必读）：
  - `server/src/services/commission_summary.service.ts` / `commission_report.service.ts` / `commission_settlement.service.ts`
  - `server/src/services/cost_alert.service.ts`
  - `server/src/services/adjustment.service.ts` / `adjustment_query.service.ts` / `adjustment_execute.service.ts`

---

## 3. 必读约束

### 3.1 代码范式

- Vue3 `<script setup lang="ts">` + Element Plus + Pinia，**与 C1/C2 页面同风格**
- API 层只做端点拼装与解包，不写业务逻辑
- 金额列统一 `formatAmount`；状态列统一 ElTag + 映射常量；动作成功后 ElMessage.success + 刷新
- **汇总/报表数据不装图表库**：用 ElTable + ElDescriptions + ElProgress（占比）呈现

### 3.2 命名

- API 函数：camelCase 动宾（`getCommissionSummary` / `createSettlement` / `scanCostAlerts` / `acknowledgeAlert` / `createAdjustment` / `executePendingAdjustments`）
- PATHS 常量：`COMMISSION_PATHS` / `COST_ALERT_PATHS` / `ADJUSTMENT_PATHS`（`as const`）
- 类型：PascalCase（`CommissionSummary` / `Settlement` / `SettlementStatus` / `CostAlert` / `CostAlertStatus` / `Adjustment` / `AdjustmentStatus` / `AdjustmentType`）
- 文件：kebab-case API（`costAlert.ts` 沿用 camelCase 文件名惯例，与既有 `businessTrip.ts` 一致）+ PascalCase.vue 页面

### 3.3 RBAC 权限矩阵（**已实读 `server/src/constants/permissions.ts` ROLE_PERMISSIONS**）

| 权限点 | admin | hr | dept_head | executive | employee |
|---|---|---|---|---|---|
| salary:commission:read | ✅* | ✅ | ✅ | ✅ | **✅** |
| salary:commission:settle | ✅* | ✅ | ❌ | ❌ | ❌ |
| salary:commission:confirm | ✅* | ✅ | ❌ | ✅ | ❌ |
| salary:commission:cancel | ✅* | ✅ | ❌ | ❌ | ❌ |
| salary:cost-alert:read | ✅* | ✅ | ✅ | ✅ | ❌ |
| salary:cost-alert:scan | ✅* | ✅ | ❌ | ❌ | ❌ |
| salary:cost-alert:ack | ✅* | ✅ | ❌ | ✅ | ❌ |
| salary:cost-alert:close | ✅* | ✅ | ❌ | ❌ | ❌ |
| salary:adjustment:read | ✅* | ✅ | ✅ | ✅ | ❌ |
| salary:adjustment:read-self | ❌ | ❌ | ❌ | ❌ | ✅ |
| salary:adjustment:write | ✅* | ✅ | ❌ | ❌ | ❌ |
| salary:adjustment:approve | ✅* | ✅ | ❌ | ✅ | ❌ |
| salary:adjustment:execute | ✅* | ✅ | ❌ | ✅ | ❌ |
| salary:adjustment:cancel | ✅* | ✅ | ❌ | ❌ | ❌ |

（admin 为通配 `*`）

**四个反直觉点（按钮/菜单显隐必须严格遵守）**：
1. **employee 有 `commission:read`** —— 后端service 层大概率按本人数据收敛，前端 ESS 视角只查自己（用 `/commissions/employees/{自己的employeeId}`），**不展示全量 summary 入口**
2. **executive 有 `commission:confirm` 但没有 `settle`/`cancel`** —— 创建与作废按钮对 executive 不可见
3. **executive 有 `cost-alert:ack` 但没有 `scan`/`close`**；**`scan`/`close` 只有 admin/hr**
4. **调薪列表/详情是双权限端点**（`adjustment:read` 或 `adjustment:read-self` 其一即可）——employee 用 read-self 进入「我的调薪」，页面强制 `employeeId=自己` 且隐藏员工选择器（C1 参保登记页同款模式）
5. （附加）**`adjustment:approve` 单端点双动作**：驳回 = `POST /:id/approve {action:'reject'}`，**不存在独立 reject 端点**

### 3.4 错误码

0 新增。业务错误由 http 拦截器自动 ElMessage；页面只处理成功路径与空态。

### 3.5 审计

0 新增（后端已覆盖，前端无感知）。

### 3.6 configs

0 新增。提成比例 / 预警阈值已在 configs 表（后端消费），前端本切片只做展示与动作。

---

## 4. 实施任务

### 4.1 `api/types/compensation.ts`（新增，**先做 §5.1 响应确认再动笔**）

导出类型（字段以 §2.4 六个 service 实际返回为准）：

- **提成**：`CommissionGroupBy = 'employee'|'department'|'product'|'report'`；`CommissionSummary`（汇总行：维度名/提成额/占比等，以 service 为准）；`CommissionQuery = {groupBy?, period?, quarter?}`；`SettlementStatus = 'draft'|'pending_confirm'|'confirmed'|'cancelled'`；`Settlement`（id/year/quarter/status/金额汇总/明细/时间戳/操作人，以 service 为准）；`SettlementListQuery`；`CreateSettlementRequest = {year: number; quarter: 1|2|3|4; initialStatus?: 'draft'|'pending_confirm'}`；`canSettlementAction(action: 'confirm'|'cancel', settlement, user): boolean`（**导出纯函数**，权限+状态双维）
- **成本预警**：`CostAlertType = 'overtime_ratio'|'attrition_monthly'`；`CostAlertStatus = 'active'|'acknowledged'|'closed'`；`CostAlertSeverity = 'warning'|'critical'`；`CostAlert`（id/type/severity/status/departmentId/period/指标数值/说明/时间戳，以 service 为准）；`CostAlertSummary`；`CostAlertListQuery`；`canCostAlertAction(action: 'acknowledge'|'close', alert, user): boolean`（导出纯函数）
- **调薪**：`AdjustmentType = 'promotion'|'annual_adjust'|'performance'|'market_adjustment'`；`AdjustmentStatus = 'draft'|'pending'|'approved'|'rejected'|'executed'|'cancelled'`；`Adjustment`（id/employeeId/type/status/toBaseSalary/toPerformanceSalary/effectiveDate/reason/remark/时间戳，以 service 为准）；`AdjustmentListQuery`；`CreateAdjustmentRequest`（对齐 zod：employeeId/adjustmentType/toBaseSalary/toPerformanceSalary?/effectiveDate `YYYY-MM-DD`/reason/remark?）；`ApproveAdjustmentRequest = {action: 'approve'|'reject'; comment?: string}`；`canAdjustmentAction(action: 'submit'|'approve'|'execute'|'cancel', adjustment, user): boolean`（导出纯函数；approve 动作覆盖 approve+reject 两态）
- `import { unwrapSalaryPage } from './types/salary'`（**不重写**）

### 4.2 `api/commission.ts`（新增，8 函数）

`COMMISSION_PATHS as const`：`summary` / `employee(id)` / `department(id)` / `settlements` / `settlement(id)` / `confirm(id)` / `cancel(id)`。
函数：`getCommissionSummary(query)`（非分页，unwrapData）/ `getEmployeeCommission(employeeId, query)` / `getDepartmentCommission(departmentId, query)` / `createSettlement(body)` / `listSettlements(query)`（**unwrapSalaryPage**）/ `getSettlement(id)` / `confirmSettlement(id, body:{remark?})` / `cancelSettlement(id, body:{reason})`。每函数单行 JSDoc（端点+方法+权限点）。

### 4.3 `api/costAlert.ts`（新增，6 函数）

`COST_ALERT_PATHS as const`：`scan` / `summary` / `list`（`/salary/cost-alerts`，注意列表是 **GET /** 根路径）/ `detail(id)` / `acknowledge(id)` / `close(id)`。
函数：`scanCostAlerts(body:{period, alertType?})` / `getCostAlertSummary(period)` / `listCostAlerts(query)`（**unwrapSalaryPage**）/ `getCostAlert(id)` / `acknowledgeCostAlert(id, body:{note?})` / `closeCostAlert(id, body:{reason, remark?})`。

### 4.4 `api/adjustment.ts`（新增，8 函数）

`ADJUSTMENT_PATHS as const`：`executePending`（`/salary/adjustments/execute-pending`）/ `list` / `create` / `detail(id)` / `submit(id)` / `approve(id)` / `execute(id)` / `cancel(id)`。
函数：`executePendingAdjustments(asOfDate: string)`（body `{asOfDate}`）/ `createAdjustment(body)`（201）/ `listAdjustments(query)`（**unwrapSalaryPage**）/ `getAdjustment(id)` / `submitAdjustment(id, body:{comment?})` / `approveAdjustment(id, body: ApproveAdjustmentRequest)`（**驳回也走此函数，action:'reject'**）/ `executeAdjustment(id)` / `cancelAdjustment(id, body:{reason})`。

### 4.5 `views/salary/commission/CommissionSummary.vue`（新增）

- 筛选：groupBy（Radio：employee/department/product/report）+ period（ElDatePicker month）或 quarter（年份+季度）
- groupBy=employee/department/product → `getCommissionSummary`；groupBy=report → 同端点（后端自动切 report service），展示整体报表结构（以 service 返回为准）
- 员工维度行操作「明细」→ 内嵌 Drawer 调 `getEmployeeCommission`；部门维度同理
- 金额列 `formatAmount`；占比列 ElProgress
- employee 角色**不进入此页**（菜单隐藏，见 §4.8）

### 4.6 `views/salary/commission/SettlementList.vue` + `SettlementDetail.vue`（新增）

**SettlementList.vue**：
- 「发起结算」按钮（`commission:settle` = admin/hr 才显示）：Dialog 选年份（ElDatePicker year）+ 季度（Select 1-4）+ initialStatus（Radio draft/pending_confirm，默认 draft）
- 筛选：年份 / 状态（4 态）/ 期间范围；表格：年份+季度 / 状态 ElTag / 人数 / 提成总额 / 创建时间 / 操作（查看）
- 分页 `unwrapSalaryPage`

**SettlementDetail.vue**：
- 描述列表 + 结算明细表格（字段以 service 为准）
- 动作按钮（`canSettlementAction` 纯函数驱动）：
  - 「确认」confirm：`commission:confirm`（admin/hr/**executive**）+ status=pending_confirm（或 draft，以 service 状态机为准）；Dialog 填 remark 可选
  - 「作废」cancel：`commission:cancel`（**仅 admin/hr**）+ status ∈ {draft, pending_confirm}；Dialog 填 reason **必填**

### 4.7 `views/salary/cost-alert/CostAlertList.vue` + `CostAlertDetail.vue`（新增）

**CostAlertList.vue**：
- 顶部汇总卡：period（month，必填）→ `getCostAlertSummary`，展示 active/acknowledged/closed 计数与 critical 计数
- 「扫描」按钮（`cost-alert:scan` = **仅 admin/hr**）：Dialog 选 period（month）+ alertType（Radio：all/overtime_ratio/attrition_monthly，默认 all）→ `scanCostAlerts` → 刷新
- 筛选：alertType / status / severity / 部门；表格：类型 / 严重度（warning=warning, critical=danger）/ 状态 ElTag / 部门 / 期间 / 说明摘要 / 操作（查看）
- employee 无 `cost-alert:read`，菜单隐藏

**CostAlertDetail.vue**（或 List 内 Drawer，二选一，全切片一致）：
- 完整指标数值 + 说明 + 处理记录（以 service 返回为准）
- 「确认知晓」acknowledge：`cost-alert:ack`（admin/hr/executive）+ status=active；Dialog 填 note 可选
- 「关闭」close：`cost-alert:close`（**仅 admin/hr**）+ status ∈ {active, acknowledged}；Dialog 填 reason **必填** + remark 可选

### 4.8 `views/salary/adjustment/AdjustmentList.vue` + `AdjustmentDetail.vue`（新增）

**AdjustmentList.vue**：
- 双模式：有 `adjustment:read`（admin/hr/dept_head/executive）→ 全量列表 + 员工筛选；只有 `read-self`（employee）→ 强制 `employeeId=userInfo.employee?.id`，**隐藏员工选择器**，标题文案「我的调薪」（C1 plan 页 employeeLabel 同款）
- 「新建调薪」按钮（`adjustment:write` = admin/hr）：Dialog 表单对齐 zod——员工选择 / adjustmentType（Select 4 枚举：promotion=晋升 / annual_adjust=年度普调 / performance=绩效调薪 / market_adjustment=市场对标）/ toBaseSalary（ElInputNumber，min>0）/ toPerformanceSalary（可选，min 0）/ effectiveDate（ElDatePicker date，`value-format="YYYY-MM-DD"`，**匹配后端正则**）/ reason（textarea 必填 maxlength 2000）/ remark（可选）
- 「批量执行」按钮（`adjustment:execute` = admin/hr/executive）：Dialog 选 asOfDate（date）→ `executePendingAdjustments` → ElMessage 展示执行笔数（以 service 返回为准）
- 表格：员工 / 类型 / 调整后基本工资 / 生效日期 / 状态 ElTag（6 态）/ 操作（查看）

**AdjustmentDetail.vue**：
- 描述列表全字段 + 状态 ElTag
- 动作按钮（`canAdjustmentAction` 纯函数驱动）：
  - 「提交」submit：write + status=draft；Dialog comment 可选
  - 「审批通过」/「驳回」：**同一个 approve 端点**，`adjustment:approve`（admin/hr/executive）+ status=pending；Dialog 内 Radio 选 action（approve/reject）+ comment，选 reject 时 comment **必填**（前端校验，后端 zod 未强制但业务必须）
  - 「执行」execute：`adjustment:execute`（admin/hr/executive）+ status=approved；二次确认
  - 「作废」cancel：`adjustment:cancel`（**仅 admin/hr**）+ status ∈ {draft, pending}；Dialog reason **必填**

### 4.9 `router/salary.ts`（**唯一允许修改的既有文件，仅追加，沿用 C2 模式**）

1. **新建 `SALARY_C3_MENU: SalaryMenuItem[]`** 独立数组（**不动 `SALARY_MENU` / `SALARY_C2_MENU`**，C1 旧测断言 length===7）：
   ```ts
   { key: 'commission', label: '提成查询', employeeLabel: '我的提成', icon: 'Coin', to: '/salary/commissions', permission: 'salary:commission:read' },
   { key: 'settlements', label: '提成结算', icon: 'Tickets', to: '/salary/commission-settlements', permission: 'salary:commission:read', hideForEmployee: true },
   { key: 'cost-alerts', label: '成本预警', icon: 'Operation', to: '/salary/cost-alerts', permission: 'salary:cost-alert:read', hideForEmployee: true },
   { key: 'adjustments', label: '调薪管理', employeeLabel: '我的调薪', icon: 'Money', to: '/salary/adjustments', permission: 'salary:adjustment:read' },
   ```
   （icon 必须从 SalaryLayout 白名单 Money/Coin/Tickets/Wallet/Postcard/Operation/DataLine 中选）
   **注意 employee 的 adjustments 菜单**：employee 只有 `read-self` 没有 `adjustment:read`，`filterSalaryMenu` 的 `hasPermission` 会把它滤掉 → `filterSalaryMenu` 拼接 C3 菜单时，对 `key==='adjustments'` 的项，权限判断改为 `hasPermission(user,'salary:adjustment:read') || hasPermission(user,'salary:adjustment:read-self')`（**仅此一项特判，在 filterSalaryMenu 内实现，不改通用签名**）。commission 项 employee 有 `commission:read` 无需特判。
2. **filterSalaryMenu 拼接**：返回 `[...head, ...SALARY_MENU, ...c2Tail, ...SALARY_C3_MENU].filter(...)` 结构（保持 C2 的 my-payslips 首位逻辑不变，C3 菜单追加在尾部）
3. **resolveSalaryActiveKey 追加 4 分支**：`/salary/commissions` → `commission`；`/salary/commission-settlements` → `settlements`；`/salary/cost-alerts` → `cost-alerts`；`/salary/adjustments` → `adjustments`（**插在 return '' 之前**；注意 `/salary/commissions` 与 `/salary/commission-settlements` 前缀不互相包含，顺序安全）
4. **children 追加 7 路由**：`commissions`（CommissionSummary）/ `commission-settlements`（SettlementList）/ `commission-settlements/:id`（SettlementDetail）/ `cost-alerts`（CostAlertList）/ `cost-alerts/:id`（CostAlertDetail，若详情用 Drawer 则不加此路由，改 5 路由亦可，交付报告说明）/ `adjustments`（AdjustmentList）/ `adjustments/:id`（AdjustmentDetail）

### 4.10 测试（4 个新文件，`ac43074` 自包含范式）

1. `api/__tests__/commission.test.ts`（≥2）：`COMMISSION_PATHS` 拼接断言；`canSettlementAction` 纯函数（confirm+executive+pending_confirm→true；cancel+executive→**false**；confirm+employee→false）
2. `api/__tests__/costAlert.test.ts`（≥2）：`COST_ALERT_PATHS` 断言（list 是根路径 `/salary/cost-alerts`）；`canCostAlertAction`（ack+executive+active→true；close+executive→**false**；scan 权限 admin/hr 专属断言）
3. `api/__tests__/adjustment.test.ts`（≥2）：`ADJUSTMENT_PATHS` 断言；`ApproveAdjustmentRequest` action 字面量；`canAdjustmentAction`（approve+hr+pending→true；cancel+executive→**false**；submit+executive→false）
4. `views/__tests__/compensation.test.ts`（≥2）：状态→ElTag type 映射（Settlement 4 态 / CostAlert 3 态 / Adjustment 6 态至少断其一组完整映射）；`filterSalaryMenu` 对 employee 的 C3 菜单可见性（employee 可见 commission/adjustments，不可见 settlements/cost-alerts）——**注意此断言对象是导出纯函数，不 import 组件**

导出 `runCommissionApiTests / runCostAlertApiTests / runAdjustmentApiTests / runCompensationViewTests`（命名匹配 `/^run.*Tests$/`）。**严禁 import vitest / @vue/test-utils**。

---

## 5. 关键决策点（详尽）

### 5.1 响应结构确认（**最优先，写 types 前必做**）

逐一打开以下文件，记录每个 service 方法的返回对象字段，再定 `api/types/compensation.ts`：
- `commission_summary.service.ts`：`getSummary` / `getEmployeeSummary` / `getDepartmentSummary` 返回结构（维度行字段名）
- `commission_report.service.ts`：`getReport`（groupBy=report 时的整体报表结构）
- `commission_settlement.service.ts`：`createSettlement` / `listSettlements`（确认是否 `{items,total,page,pageSize}`）/ `getSettlement`（明细嵌套结构）/ `confirmSettlement` / `cancelSettlement` 返回，以及**状态机允许的状态迁移**（confirm 是否允许从 draft 直接确认）
- `cost_alert.service.ts`：`scanCostAlerts`（返回扫描出几条？）/ `getAlertSummary` / `listAlerts` / `getAlert`（指标数值字段名）/ `acknowledgeAlert` / `closeAlert`
- `adjustment.service.ts` / `adjustment_query.service.ts` / `adjustment_execute.service.ts`：create/list/get/submit/approve/execute/cancel/executePending 返回，以及**状态机允许的迁移**（execute 是否要求 approved；cancel 允许哪些状态；read-self 是否在 service 层收敛 employee 数据）

**禁止凭猜写字段**；service 返回里有的才进类型。

### 5.2 三组状态机按钮显隐（初版假定，以 service 状态机为准）

**Settlement**：
| 动作 | 权限 | 可见状态（假定） |
|---|---|---|
| confirm | commission:confirm（admin/hr/executive） | draft / pending_confirm |
| cancel | commission:cancel（**仅 admin/hr**） | draft / pending_confirm |

**CostAlert**：
| 动作 | 权限 | 可见状态（假定） |
|---|---|---|
| acknowledge | cost-alert:ack（admin/hr/executive） | active |
| close | cost-alert:close（**仅 admin/hr**） | active / acknowledged |

**Adjustment**：
| 动作 | 权限 | 可见状态（假定） |
|---|---|---|
| submit | adjustment:write（admin/hr） | draft |
| approve（含 reject） | adjustment:approve（admin/hr/executive） | pending |
| execute | adjustment:execute（admin/hr/executive） | approved |
| cancel | adjustment:cancel（**仅 admin/hr**） | draft / pending |

**实现要求**：三张表各抽一个导出纯函数（`canSettlementAction` / `canCostAlertAction` / `canAdjustmentAction`）放 `api/types/compensation.ts`，页面与测试共用；service 实际状态迁移与上表不同时**以 service 为准**并在交付报告说明。

### 5.3 期间与日期格式

- 提成 period：`YYYY-MM`（ElDatePicker month）；quarter：年份 number + 季度 1-4（settlementCreate 的 year 是 **int**，`value-format` 出来的是字符串，**提交前 `Number()` 转换**）
- 成本预警 period：`YYYY-MM`（zod min7 max7 正好匹配）
- 调薪 effectiveDate / asOfDate：`YYYY-MM-DD`（ElDatePicker date + `value-format="YYYY-MM-DD"`，匹配后端两个日期正则）
- **不要手拼日期字符串**

### 5.4 employee 的 ESS 视角

- **我的提成**：employee 有 `commission:read`，菜单可见「我的提成」；页面进入时检测角色，纯 employee（无 admin/hr/dept_head/executive）则跳过 CommissionSummary，直接展示 `getEmployeeCommission(自己的employeeId)` 结果（在 CommissionSummary.vue 内做模式分支，**不新增页面文件**）
- **我的调薪**：AdjustmentList 双模式（§4.8），employee 固定 employeeId
- employeeId 取 `useUserStore().userInfo.employee?.id`；为空 → EmptyState「尚未关联员工档案」

### 5.5 filterSalaryMenu 的 C3 拼接

- 保持 C2 结构：my-payslips 首位 + SALARY_MENU + SALARY_C2_MENU 尾部 + **SALARY_C3_MENU 最尾**
- adjustments 菜单项的权限特判（read 或 read-self）在 filterSalaryMenu 内联实现
- **不改 `SALARY_MENU`**（C1 旧测 `length === 7` 红线）

### 5.6 汇总/报表的可视化取舍

- **不装 echarts**（红线 3）；汇总页用 ElTable + ElProgress（占比条）+ ElDescriptions
- 若 service 返回含时间序列，用 ElTable 按期间分行展示即可，**不画趋势图**

### 5.7 不做清单（明确排除）

- ❌ 修改 `ROLE_PERMISSIONS` 补授 hr 的 `payslip:write`（C2 已知问题 4，留待联调阶段决策）
- ❌ DefaultLayout 侧栏 disabled 占位 / Node 25 localStorage 警告（C1 已知问题 1/4）
- ❌ M5-2-D 绩效前端任何页面与 API
- ❌ 提成比例 / 预警阈值 / 调薪规则的 configs 配置页（后端消费 configs，前端配置界面不在本切片）

---

## 6. 修改文件清单

### 6.1 必改文件（16 个）

**新增 15 个**：
1. `client/src/api/types/compensation.ts`
2. `client/src/api/commission.ts`
3. `client/src/api/costAlert.ts`
4. `client/src/api/adjustment.ts`
5. `client/src/views/salary/commission/CommissionSummary.vue`
6. `client/src/views/salary/commission/SettlementList.vue`
7. `client/src/views/salary/commission/SettlementDetail.vue`
8. `client/src/views/salary/cost-alert/CostAlertList.vue`
9. `client/src/views/salary/cost-alert/CostAlertDetail.vue`
10. `client/src/views/salary/adjustment/AdjustmentList.vue`
11. `client/src/views/salary/adjustment/AdjustmentDetail.vue`
12. `client/src/api/__tests__/commission.test.ts`、`client/src/api/__tests__/costAlert.test.ts`、`client/src/api/__tests__/adjustment.test.ts`（**3 个文件**）
13. `client/src/views/__tests__/compensation.test.ts`

（编号 12 为 3 个同目录测试文件，合计 1+3+7+4 = **15 个物理文件**；若 CostAlertDetail 改用 Drawer 内嵌则为 14 个，交付报告必须如实列出实际文件数）

**修改 1 个**：
16. `client/src/router/salary.ts`（**仅追加** `SALARY_C3_MENU` + filterSalaryMenu 拼接与特判 + 4 activeKey 分支 + 6-7 children 路由，不改既有行）

### 6.2 禁止修改文件（清单以外）

- ❌ `server/**`（全部，含 `constants/permissions.ts`）
- ❌ `client/package.json` / `pnpm-lock.yaml`（**不装依赖，含 echarts**）
- ❌ `client/vitest.config.ts` / `client/tests/run-all.test.ts`
- ❌ `client/vite.config.ts` / `tsconfig*.json` / `.eslintrc.cjs`
- ❌ `client/src/api/types.ts` / `api/types/{organization,attendance,salary,payroll}.ts`
- ❌ `client/src/api/` 已有 17 个业务 API 文件 + `http.ts`
- ❌ `client/src/router/{index,organization,attendance}.ts`
- ❌ `client/src/layouts/**`（含 SalaryLayout.vue）
- ❌ `client/src/components/**` / `stores/**` / `utils/**`
- ❌ `client/src/views/organization/**` / `views/attendance/**` / `views/salary/{grade,plan,insurance,tax,payroll,ess}/**`
- ❌ `client/src/**/__tests__/` 已有 29 个测试文件（**不修改旧测试**）

### 6.3 越界检测（交付时自检）

```bash
cd D:\projects\hrms
git status --short
# 必须只出现 §6.1 列出的 16 个文件
# 如果出现其他文件 → 越界，必须回退

git diff --stat server/
# 必须 0 行

git diff client/package.json
# 必须 0 行

git diff client/src/components/ client/src/stores/ client/src/utils/ client/src/layouts/
# 必须 0 行

git diff client/src/api/types/salary.ts client/src/api/types/payroll.ts
# 必须 0 行（unwrapSalaryPage 只 import 不修改）
```

---

## 7. 验收标准

### 7.1 硬性验收（必须 100% 通过）

```bash
cd D:\projects\hrms

# 1. 后端测试（0 改动，沿用 876）
pnpm --filter hrms-server test
# 预期：876/876 通过

# 2. 后端 type-check
pnpm --filter hrms-server type-check
# 预期：0 错

# 3. 前端 type-check
pnpm --filter hrms-client type-check
# 预期：0 错

# 4. 前端测试（ac43074 脚手架，自动聚合新用例）
pnpm --filter hrms-client test
# 预期：62 + 新增（4 文件 × ≥2）= 70 以上全通过，0 failed

# 5. 全仓 lint
pnpm lint
# 预期：0 error（warning 不新增超过 5 个）

# 6. 前端生产构建
pnpm --filter hrms-client build
# 预期：构建成功（chunk 增大可接受，但不允许构建错误）
```

### 7.2 交付报告模板

```markdown
# M5-2-C3 交付报告

## 完成范围
- 子任务 7.2.C3.1/2/3 完成状态 + 端点消费清单（22/22）
## 新增文件（15）
（逐一列出）
## 修改文件（1）
- client/src/router/salary.ts（+N 行：SALARY_C3_MENU + 拼接 + 4 activeKey 分支 + 6-7 路由）
## 主动识别与处理
（§5.1 响应结构确认结论逐 service 列出 / 三组状态机实际迁移与 §5.2 假定差异 / 字段命名差异 / read-self 数据收敛方式，逐一说明处理方式）
## 测试
- 新增 4 个测试文件 / N 个用例；pnpm --filter hrms-client test 结果
- 原有用例是否改动：0（必须）
## 验收命令输出摘要
（§7.1 六条命令的实际结果）
## 已知问题
（如有，逐一列出 + 建议处理阶段）
## 红线自检
- 0 修改后端 / 0 修改 package.json / 0 修改旧测试 / 0 修改共享基础设施与 C1/C2 文件（除 router/salary.ts 追加）/ 5 角色无 finance
```

---

**提示词结束。开始执行前，请先回复你的实施计划（文件创建顺序 + §5.1 响应结构确认结论），确认无误后再动手。**
