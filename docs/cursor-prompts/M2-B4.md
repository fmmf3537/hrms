# Cursor 提示词：M2-B4 加班管理（加班申请 + 补偿二选一 + 加班上限校验）

> **使用方式**：复制下方【开始复制 ↓】到【结束复制 ↑】之间全部内容，粘贴到 Cursor。
> **预期结果**：B4 加班管理完整实现，1 个 commit 落地，336 → 355 测试。
> **审查清单**：复制完成后，由我按"7 项验收清单"逐条核对。
> **依赖前置**：M1 全部 7 切片 + M0.5 全部 7 切片 + M2-B1/B2/B3（ed23a62 / f390fe1 / dfd6b81）。
>
> **优化理念**：本提示词**故意写得详尽**（沿用 A5/A6/A7/B1/B2/B3 验证通过的 45KB 模式）。
> 关键决策点全部锁死，Cursor 一次到位，token 反而少。

---

## 开始复制 ↓

```
⚠️ 强约束（最优先阅读）— 此段必须在最顶部

历史教训：
- M0.5-5 任务中，Cursor 越界做了 M0.5-6（配置中心）+ M0.5-7（改密 + 2FA）共 2 个
  不在提示词范围内的切片，并修改了 4 个旧测试文件 + 在报告中虚假陈述"原有用例未改"。
- M1 + M2-B1/B2/B3 全部 0 越界（用户验收通过），本次严禁重蹈 M0.5-5 覆辙。
- A6 第一次"瘦身 53%"（15KB）是错误方向；A5/A6/A7/B1/B2/B3 沿用 45KB 详尽模式验证成功。
  本提示词**沿用 45KB 详尽模式**，**禁止再次瘦身**。

红线（违反任一 → 任务失败，需回退重做）：

1. 禁止越界做其他切片：本提示词只授权 "M2-B4（加班）" 单切片，1 个 commit。
   严禁自作主张做 B1/B2/B3（已实现）/ B5（出差）/ B6（月度汇总），
   不做移动端 / 前端 / 任何业务功能。

2. 禁止修改未授权文件：本提示词在"修改文件清单"中列出的文件以外的文件全部禁止改动。
   包括但不限于：
   - server/src/services/{auth,approval,notification,crypto,encryptedField,integration,
     ai,config,audit,company,department,employee,employeeAI,onboarding,
     regularization,offboarding,transfer,contract,shift,attendance,leave}/*
     任何已有文件
   - server/src/controllers/ 已有 19 个文件全部黑名单
   - server/src/routes/ 已有 19 个文件全部黑名单
   - server/src/middleware/* / server/src/jobs/* / server/src/integrations/**
   - client/ 或 mobile/ 任何文件
   - 任何已有测试文件（*.test.ts，包括 M0/M0.5/M1/M2-B1/M2-B2/M2-B3 所有 336 个用例）
   - 任何已有 migration

3. **错误码段位 72101-72110**（72 段位 B4 子区，**B3 用了 72001-72010**）：
   B3 用了 72001-72010，B4 继续 72 段位 72101-72110。
   注意：与 M0 阶段 10xxx-12xxx、M0.5 阶段 20xxx-70xxx、M1 71xxx 段位不冲突。

4. 必须复用既有能力（**严禁重写 / 严禁改源**）：
   - 审批流：调 approval.submitApproval / withdraw（**B4 加班强依赖**）
   - 通知：调 notification.sendNotification（**bypassTemplate fallback**，参考 A4/A5/A6/A7/B1/B2/B3 模式）
   - 字段加密：调 crypto.encrypt / decrypt（如有新增字段）
   - 配置读取：调 configService.getValue（已 export）
   - 审计：调 auditService.auditLog（已 export）
   - 员工关联：通过 prisma.employee.findFirst（**不调 employee.service**）
   - 部门关联：通过 prisma.department.findUnique（**不调 department.service**）
   严禁在 B4 中重写任何以上逻辑。如发现"必须改 B3 才能跑通"，**立即停下并报告**。

5. **不 import 跨 service**（A4/A5/A6/A7/B1/B2/B3 红线沿用）：
   overtime.service.ts 的 import 只允许：
   - './approval.service'
   - './audit.service'
   - './config.service'
   - './notification.service'
   - '../middleware/errorHandler' (AppError)
   - '../lib/prisma' (prisma)
   - '@prisma/client' (Prisma 类型)
   严禁 import './leave.service' / './shift.service' / './attendance.service'
   / './employee.service' / './department.service' / './company.service'
   / './onboarding.service' / './regularization.service' / './offboarding.service'
   / './transfer.service' / './contract.service' / './employeeAI.service'
   / './auth.service'。

6. 禁止修改旧测试文件：本任务 B4 涉及的新测试，**只新建**
   `overtime.service.test.ts`，**严禁修改**任何已有测试。

7. **B4 范围严格界定**（V1.2 §二.3.3 加班管理 + §四.6.1）：
   - 加班申请（事前申请，注明加班事由 + 预计时长）—— **B4 实现**
   - 加班补偿：加班费（按法定标准）/ 调休（1:1），员工自选 —— **B4 实现**
   - 加班上限：每日 ≤ 3 小时 / 每月 ≤ 36 小时（劳动法）—— **B4 实现**
   - 走 M0.5-1 审批流 —— **B4 实现**
   - 加班费计算（按倍数，service 函数 calculateOvertimePay）—— **B4 实现**
   - 调休 1:1 转换（service 函数 calculateOvertimeCompDays）—— **B4 实现**
   - **不实现：**
     - 出差（B5 范围）
     - 月度汇总（B6 范围）
     - **调休余额累计**（**B3 调休余额留 B4**的承诺，**B4 不实现累计到 leave_balances**——
       调休余额由 B6 月度汇总或独立任务从 overtime_requests 表聚合，**B4 不实现**）
     - BullMQ 加班预警（留独立任务）
     - 调休到期清理（6 个月有效期，留独立任务）

8. **3 端点约束**（V1.2 §四.6.1 B4 估时 2d / 3 子任务）：
   - POST /api/overtime/requests             提交加班申请
   - GET  /api/overtime/requests             加班记录列表
   - POST /api/overtime/requests/:id/cancel 撤回
   - **不实现 GET /api/overtime/requests/:id 详情端点**（如需详情合并入列表返回）

9. **审批流不暴露独立端点**（V1.2 §二.3.3）：
   - 提交：POST /api/overtime/requests 触发 approval.submitApproval
   - 审批节点操作：/api/approval-instances/:id/approve（已存在）
   - 审批回调：confirmOvertimeRequest / rejectOvertimeRequest（系统内部）

10. 必须报告完整：交付报告必须列出**全部**新增 / 修改文件（即使是 0 个也要说"0 个"），
    并明确 7 条声明（旧测试未改 / 新单测数 / 未做 B1/B2/B3（已实现）/ B5-B6 /
    不 import 跨 service / 不实现调休余额累计 / 通知走 bypassTemplate）。

11. 禁止"我觉得这也有用"：任何"顺便做"或"我觉得可以优化"的越界内容一律禁止。
    只做本提示词明确要求的 B4 内容。

越界检测（Cursor 自查清单，跑完前自查）：
- [ ] 改动文件数 ≤ 18？超过 → 越界
- [ ] 没有改动 M0/M0.5/M1/M2-B1/B2/B3 全部 22 个 service / controller / routes？改了 → 越界
- [ ] 没有改 336 个旧测试文件？改了 → 越界
- [ ] 没有做 B1/B2/B3（已实现）/ B5/B6 任何业务？做了 → 越界
- [ ] overtime.service.ts 仅 import 黑名单以外的 service？黑名单内 import → 越界
- [ ] **未修改 leave.service.ts 的 calculateLeaveBalance 函数**？改了 → 越界
- [ ] **未实现调休余额累计**（B6 或独立任务范围）？实现了 → 越界
- [ ] 通知 / 加密 / 配置 / 审计 4 类能力是调用已有 export，没自己重写？重写 → 越界
- [ ] 报告里 7 条声明齐全？声明虚假 → 越界

---

【任务 ID】M2-B4
【任务目标】实现 M2-B4（加班）单切片，1 个 commit 落地
【依赖前置】M1 全部 7 切片 + M0.5 全部 7 切片 + M2-B1/B2/B3

【上下文】
- 项目位置：D:/projects/hrms/
- 当前状态：M0/M0.5/M1/M2-B1/B2/B3 全部已 commit，336/336 测试通过；M2-B4 启动
- 主文档：docs/HRMS-V1.2.md §二.3.3 加班管理（申请 + 补偿二选一 + 上限 ≤3h/d ≤36h/m）
  + §四.6.1 B4 切片（3 子任务 / 2d）+ §四.6.2 关键约束 + §三.5.2 业务规则配置化
- API 契约：docs/api-spec.md §3.16 待补 B4 端点（Cursor 任务里补）
- 错误码：docs/error-codes.md §7xxxx（**B4 用 72101-72110**，72 段位 B4 子区）
- 数据模型：docs/flow-diagrams.md §3.2 ER 图

【B4 业务背景（V1.2 §二.3.3 加班管理）】
- 加班申请：**事前申请**，注明加班事由 + 预计时长
- 加班补偿（二选一，员工自选）：
  - `pay`（加班费，按法定倍数计算）
  - `comp`（调休，1:1 转换）
- 加班上限（劳动法）：
  - 每日 ≤ 3 小时（configs.overtime.max_daily_hours）
  - 每月 ≤ 36 小时（configs.overtime.max_monthly_hours）
- 加班费倍数：
  - 工作日：1.5x（configs.overtime.pay_multiplier_weekday）
  - 周末：2.0x（configs.overtime.pay_multiplier_weekend）
  - 法定假日：3.0x（configs.overtime.pay_multiplier_holiday）
- 走 M0.5-1 审批流

【B4 业务规则必走 configs 表（V1.2 §三.5.2 强约束）— 6 类配置】

| 配置键 | 类型 | 默认值 | 用途 |
|---|---|---|---|
| configs.overtime.max_daily_hours | number | 3 | 单日加班上限（劳动法） |
| configs.overtime.max_monthly_hours | number | 36 | 单月加班上限（劳动法） |
| configs.overtime.pay_multiplier_weekday | number | 1.5 | 工作日加班费倍数 |
| configs.overtime.pay_multiplier_weekend | number | 2.0 | 周末加班费倍数 |
| configs.overtime.pay_multiplier_holiday | number | 3.0 | 法定假日加班费倍数 |
| configs.overtime.approval_flow_key | string | 'overtime:overtime_approval' | 加班审批流 key |
| configs.overtime.min_advance_hours | number | 4 | 最少提前申请小时数 |

**这些值先用 M0.5-6 configService 读取，如果 config 表没有就 hardcoded fallback +
TODO 注释**。不要直接 hardcoded 写死（业务规则强约束）。

【必读约束】（本切片差异点，模板同 A3/A4/A5/A6/A7/B1/B2/B3）
1. 代码范式：export function 导出（参考 leave.service.ts / attendance.service.ts）
2. 所有方法含 JSDoc
3. 错误统一抛 AppError(message, statusCode, code)，code 用 72101-72110 段位
4. 业务规则走 configService 读取
5. **不 import 跨 service**（A4/A5/A6/A7/B1/B2/B3 红线沿用，见强约束第 5 条）
6. 通知走 bypassTemplate fallback（notification.service.ts 无 TEMPLATE_KEYS）
7. **B4 不实现出差 / 月度汇总**（B5-B6 范围）
8. **B4 不实现调休余额累计**（**B3 调休余额留 B4 的承诺**——但 B4 仅提供 overtime_requests 数据，**B4 不调 leave.service 累计**。调休余额由 B6 月度汇总或独立任务实现）
9. 员工 / 部门关联走 prisma 直接查询（不 import 跨 service）
10. 审计必写：create/cancel/confirm/reject 必写 audit_logs
11. 状态机严格：draft → submitted → approved / rejected / cancelled，不可跳级
12. 不实现 BullMQ 定时任务（仅暴露 service 函数）
13. 不实现前端，只做后端 service + controller + routes + 单测
14. 不动 336 个已有测试文件
15. 不修改 docs/HRMS-V1.2.md 的现有内容（**只允许在 §七.3 变更记录追加新行** +
    §四.6.1 B4 切片行追加"已实现"标记）
16. 不修改 AGENTS.md 现有内容（**只允许在 M2-B3 进度段之后追加 B4 描述**）
17. 不修改 M0.5 + M1 + M2-B1 + M2-B2 + M2-B3 任何 service / controller / routes
    （**特别强调：leave.service.ts 的 calculateLeaveBalance 函数不修改**，
    调休余额累计留 B6 或独立任务）

【实施任务 - 严格按 13 步顺序】

### 任务 1: 更新 Prisma schema（+ 1 张表 + 索引）

**目标文件**：server/prisma/schema.prisma

**新增模型**（**完整定义**，不要"列关键字段"）：

```prisma
// ==================== M2-B4 加班 ====================
// 状态机：draft → submitted → approved / rejected / cancelled
// 关联 employee（A2 已有）+ approvalInstance（M0.5-1 已有）
// 加班补偿二选一：pay（加班费）/ comp（调休 1:1）
// 加班费计算在 service 函数（**不存表**），调休 1:1 转换（service 函数）
model OvertimeRequest {
  id            String   @id @default(uuid()) @db.Uuid
  // 关联员工
  employeeId    String   @map("employee_id") @db.Uuid
  companyId     String   @map("company_id") @db.Uuid
  departmentId  String   @map("department_id") @db.Uuid
  // 加班时间
  startTime     DateTime @map("start_time") @db.Timestamptz(6)
  endTime       DateTime @map("end_time") @db.Timestamptz(6)
  // 加班时长（小时，service 层 calculateOvertimeHours 计算）
  totalHours    Decimal  @map("total_hours") @db.Decimal(5, 2)
  // 加班类型（工作日 / 周末 / 法定假日，service 层判定）
  overtimeType  String   @map("overtime_type") @db.VarChar(20)
  // weekday / weekend / holiday
  // 补偿方式（pay / comp，员工自选）
  compensationType String  @map("compensation_type") @db.VarChar(10)
  // pay / comp
  // 加班费金额（如果 compensationType='pay'，service 层 calculateOvertimePay 计算）
  overtimePay   Decimal? @map("overtime_pay") @db.Decimal(10, 2)
  // 调休天数（如果 compensationType='comp'，service 层 calculateOvertimeCompDays 计算 = totalHours / 8）
  compDays      Decimal? @map("comp_days") @db.Decimal(4, 1)
  // 加班事由
  reason        String   @db.Text
  // 附件（可选）
  attachments   Json?    // [{ name, url, type, size }]（简化，与 M1-A7 附件同模式）
  // 状态机
  status        String   @default("draft") @db.VarChar(20)
  // draft / submitted / approved / rejected / cancelled
  // 审批流（M0.5-1）
  approvalInstanceId String? @unique @map("approval_instance_id") @db.Uuid
  // 审批结果
  approvedAt    DateTime? @map("approved_at") @db.Timestamptz(6)
  approvedBy    String?  @map("approved_by") @db.Uuid
  rejectedAt    DateTime? @map("rejected_at") @db.Timestamptz(6)
  rejectedBy    String?  @map("rejected_by") @db.Uuid
  rejectedReason String? @map("rejected_reason") @db.Text
  // 取消
  cancelledAt   DateTime? @map("cancelled_at") @db.Timestamptz(6)
  cancelledBy   String?  @map("cancelled_by") @db.Uuid
  cancelledReason String? @map("cancelled_reason") @db.Text
  // 元数据
  createdBy     String?  @map("created_by") @db.Uuid
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt     DateTime @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@index([employeeId, status])
  @@index([companyId, status, startTime])
  @@index([departmentId, status, startTime])
  @@index([status, startTime])
  @@index([compensationType, status])
  @@map("overtime_requests")
}
```

**注意**：
- `overtimePay` / `compDays` 互斥（要么 pay 要么 comp，但都存冗余方便查询）
- 不加 Employee / Department 反向 relation（避免改 A2 model）
- 5 个 index 覆盖：employee 维度 / 时间维度 / company 维度 / department 维度 / 补偿类型维度
- **不修改 leave.service.ts 的 calculateLeaveBalance**（B3 调休余额留 B6 或独立任务）

### 任务 2: 创建 migration

**新建文件**：
server/prisma/migrations/20260830070000_add_overtime/migration.sql

参考 B3 风格（CREATE TABLE + indexes + foreign keys），用 IF NOT EXISTS 幂等。
包含 1 张表 + 5 个 index + 外键。

### 任务 3: 跑 db:generate

```bash
cd D:/projects/hrms
pnpm --filter hrms-server db:generate
```

### 任务 4: 写 overtime.service.ts（核心业务）

**新建**：server/src/services/overtime.service.ts（约 600-700 行）

**业务函数**（**B4 特有，详细列参数 + 返回值 + 关键调用**）：

```typescript
// ==================== 1. 提交加班申请 ====================
/**
 * 提交加班申请（draft → submitted）
 * @param input { employeeId, startTime, endTime, overtimeType?, compensationType,
 *                reason, attachments? }
 * @param operatorId 申请人 user.id
 * @returns OvertimeRequest
 *
 * 校验链：
 *  1. employee 存在 → 71201
 *  2. startTime < endTime → 72102
 *  3. **最少提前申请**（configs.overtime.min_advance_hours，默认 4 小时）→ 72108
 *  4. 调 calculateOvertimeHours(startTime, endTime) → totalHours
 *  5. **单日上限**：totalHours <= configs.overtime.max_daily_hours（默认 3 小时）→ 72103
 *  6. **单月上限**：configs.overtime.max_monthly_hours（默认 36 小时）→ 72104
 *     - 调 prisma.overtimeRequest.findMany（当月已 approved 部分 + 当前申请）
 *     - 总和 > 36 → 72104
 *  7. **补偿方式校验**：compensationType='pay'|'comp' → 72107
 *  8. **加班类型判定**（如未传 overtimeType）：service 层判定
 *     - weekday（周一到周五）/ weekend（周六日）/ holiday（需调 calendar，B4 简化用周末判定）
 *  9. **加班费计算**（如 compensationType='pay'）：
 *     - 调 calculateOvertimePay(employeeId, totalHours, overtimeType)
 *     - overtimePay = baseSalary / 21.75 / 8 * totalHours * multiplier
 *  10. **调休转换**（如 compensationType='comp'）：
 *     - 调 calculateOvertimeCompDays(totalHours)
 *     - compDays = totalHours / 8
 *  11. 调 approval.submitApproval({ flowKey: configs.overtime.approval_flow_key
 *     （fallback 'overtime:overtime_approval'），businessType: 'overtime',
 *     businessId: id, initiatorId: operatorId,
 *     title: `${employee.name} 加班申请 - ${startTime}`,
 *     data: { employeeId, totalHours, compensationType, overtimeType } })
 *  12. 状态：draft → submitted
 *  13. 通知员工 + 直属上级
 *  14. 审计 action=CREATE
 */
export async function createOvertimeRequest(
  input: CreateOvertimeRequestInput,
  operatorId: string
): Promise<OvertimeRequest> { /* ... */ }

// ==================== 2. 单查 / 列表 ====================
/**
 * 列表（分页 + employeeId/companyId/departmentId/compensationType/status/dateRange 过滤）
 *
 * **B4 不实现 GET /:id 详情端点**（如需详情合并入列表返回）
 */
export async function listOvertimeRequests(query: ListOvertimeRequestsQuery): Promise<{
  data: OvertimeRequest[];
  total: number;
  page: number;
  pageSize: number;
}> { /* ... */ }

// ==================== 3. 撤回 ====================
/**
 * 撤回（draft / submitted 状态可撤回）
 * - draft 状态：直接 cancelled
 * - submitted 状态：调 approval.withdraw(approvalInstanceId) → cancelled
 * - approved 状态：→ 72106（已批准不可撤回；如需"撤销加班"留独立流程）
 * - rejected 状态：→ 72106
 */
export async function cancelOvertimeRequest(
  id: string,
  reason: string,
  operatorId: string
): Promise<OvertimeRequest> { /* ... */ }

// ==================== 4. 审批回调 ====================
/**
 * 审批通过回调（submitted → approved）—— 系统内部调用
 * @param id overtime_request id
 * @param approvalResult { approved: boolean, instanceId?: string }
 * @param operatorId 审批人 user.id
 *
 * 流程：
 *  1. record 存在 → 72101
 *  2. status === 'submitted' → 否则 72105
 *  3. approvalResult.approved:
 *    - 状态：submitted → approved
 *    - approvedAt / approvedBy 写入
 *    - 通知员工
 *  4. !approvalResult.approved:
 *    - 状态：submitted → rejected
 *    - rejectedAt / rejectedBy / rejectedReason 写入
 *    - 通知员工
 *  5. 审计 action=APPROVE / REJECT
 */
export async function confirmOvertimeRequest(
  id: string,
  approvalResult: { approved: boolean; instanceId?: string },
  operatorId: string
): Promise<OvertimeRequest> { /* ... */ }

// ==================== 5. 内部：加班时长计算 ====================
/**
 * 内部：加班时长计算（小时，保留 2 位小数）
 * @param startTime 开始时间
 * @param endTime 结束时间
 * @returns 时长（小时）
 */
function calculateOvertimeHours(
  startTime: Date,
  endTime: Date
): number { /* ... */ }

// ==================== 6. 内部：加班费计算 ====================
/**
 * 内部：加班费计算
 * @param employeeId
 * @param totalHours
 * @param overtimeType weekday/weekend/holiday
 * @returns 加班费金额
 *
 * 公式：baseSalary / 21.75 / 8 * totalHours * multiplier
 *  - baseSalary：调 prisma.employeeSalaryHistory.findFirst（最近一条）
 *  - multiplier：configs.overtime.pay_multiplier_weekday/weekend/holiday
 *  - 21.75 = 月平均工作日（劳动法）
 *  - 8 = 每天工作小时
 */
async function calculateOvertimePay(
  employeeId: string,
  totalHours: number,
  overtimeType: string
): Promise<number> { /* ... */ }

// ==================== 7. 内部：调休转换 ====================
/**
 * 内部：调休转换（1:1）
 * @param totalHours
 * @returns 调休天数
 */
function calculateOvertimeCompDays(totalHours: number): number { /* ... */ }

// ==================== 8. 内部：加班类型判定 ====================
/**
 * 内部：加班类型判定
 * @param startTime
 * @returns weekday / weekend / holiday
 *
 * B4 简化：仅 weekday / weekend（B4 不实现法定假日判定，configs.holiday 留二期）
 * - 工作日（周一到周五）：weekday
 * - 周末（周六日）：weekend
 */
function determineOvertimeType(startTime: Date): 'weekday' | 'weekend' { /* ... */ }

// ==================== 9. BullMQ 调用：即将加班 ====================
/**
 * 给 BullMQ 调用的"即将加班"列表
 * @param days N 天内开始
 * @returns status='approved' AND startTime BETWEEN now AND now+days 的加班
 */
export async function listUpcomingOvertime(days: number): Promise<OvertimeRequest[]> { /* ... */ }
```

**集成调用**（**严禁重写 / 严禁 import 跨 service**）：
- `approvalService.submitApproval({ flowKey, businessType, businessId, initiatorId, title, data })`
  - B4 强依赖：flowKey = configs.overtime.approval_flow_key
- `approvalService.withdraw({ instanceId })`
- `notificationService.sendNotification({ ..., bypassTemplate: { ... } })`
  - B4 走 bypassTemplate fallback（参考 A4/A5/A6/A7/B1/B2/B3 模式）
- `configService.getValue('overtime', '*')` 6 类配置
- `auditService.auditLog({ action, resourceType, resourceId, description, actorId, actorType })`

**员工 / 部门 / 薪资关联**（**通过 prisma 直接查询，不调 service**）：
- `prisma.employee.findFirst({ where: { id, deletedAt: null } })`
- `prisma.department.findUnique({ where: { id } })`
- `prisma.employeeSalaryHistory.findFirst({ where: { employeeId }, orderBy: { effectiveDate: 'desc' } })`
- `prisma.overtimeRequest.findMany / count`（直接查询）

**错误码段位 72101-72110**（**B4 专用**，**含触发条件**）：

| code | HTTP | 名称 | 触发条件 | 客户端处理 |
|---|---|---|---|---|
| 72101 | 404 | OVERTIME_NOT_FOUND | overtime_request id 不存在 | 提示"加班记录不存在" |
| 72102 | 400 | OVERTIME_INVALID_TIME_RANGE | startTime >= endTime | 提示"加班时间不合法" |
| 72103 | 400 | OVERTIME_EXCEED_DAILY_LIMIT | totalHours > 3 小时 | 提示"单日加班不能超过 3 小时" |
| 72104 | 400 | OVERTIME_EXCEED_MONTHLY_LIMIT | 当月加班 > 36 小时 | 提示"单月加班不能超过 36 小时" |
| 72105 | 409 | OVERTIME_NOT_PENDING | record 状态非 submitted | 提示"加班已审批" |
| 72106 | 409 | OVERTIME_NOT_CANCELLABLE | approved/rejected 状态撤回 | 提示"已批准/已拒绝不可撤回" |
| 72107 | 400 | OVERTIME_INVALID_COMPENSATION | compensationType 非法 | 提示"补偿方式不合法" |
| 72108 | 400 | OVERTIME_TOO_LATE | 未提前 4 小时申请 | 提示"加班需提前 4 小时申请" |
| 72109 | 409 | OVERTIME_OVERLAPPING | 同员工同日已有 overtime | 提示"加班区间重叠" |
| 72110 | 500 | OVERTIME_PAY_CALCULATION_ERROR | 加班费计算异常（如 baseSalary 缺失） | 提示"加班费计算失败" |

### 任务 5: 写 controller + routes

**新建 2 个文件**：
- server/src/controllers/overtime.controller.ts
- server/src/routes/overtime.ts

**端点清单**（3 端点，§3.16 Overtime API）：

```
# Overtime（3 端点）
POST   /api/overtime/requests             提交加班申请（draft → submitted）
GET    /api/overtime/requests             加班记录列表
POST   /api/overtime/requests/:id/cancel  撤回
```

**注意**：
- 审批通过 / 拒绝**不暴露独立端点**，由 approval callback 触发 confirmOvertimeRequest
- 审批节点操作走 /api/approval-instances/:id/approve（已存在）
- 所有路由都用 Zod 校验（参考 server/src/routes/leave.ts 风格）
- 所有端点都加 authenticate + rejectIfMustChangePassword + requirePermission

**权限点**（新增 3 个）：
- OVERTIME_READ: 'overtime:read' → admin + hr + dept_head
- OVERTIME_REQUEST: 'overtime:request' → admin + hr + dept_head + employee（员工自加班）
- OVERTIME_CANCEL: 'overtime:cancel' → admin + hr + dept_head
（**不允许新建 RBAC 角色**，仅在 5 角色权限列表追加）
（**审批动作走 approval 端点**，不需新加 OVERTIME_APPROVE）

### 任务 6: 挂载到 routes/index.ts

**修改**：server/src/routes/index.ts

**追加**（在 B3 之后）：
```typescript
import overtimeRoutes from './overtime';
// ...
router.use('/overtime', overtimeRoutes);
```

### 任务 7: 加权限点

**修改**：server/src/constants/permissions.ts

**追加**（在 B3 权限点之后）：
```typescript
// M2-B4 加班
OVERTIME_READ: 'overtime:read',
OVERTIME_REQUEST: 'overtime:request',
OVERTIME_CANCEL: 'overtime:cancel',
```

并在 ROLE_PERMISSIONS 的 admin / hr / dept_head / employee 数组中追加（**5 角色结构不变**）：
- admin: [..., OVERTIME_READ, OVERTIME_REQUEST, OVERTIME_CANCEL]
- hr: [..., OVERTIME_READ, OVERTIME_REQUEST, OVERTIME_CANCEL]
- dept_head: [..., OVERTIME_READ, OVERTIME_CANCEL]
- employee: [..., OVERTIME_REQUEST]  // 员工自加班

### 任务 8: 写单测（19+ 个）

**新建**：server/src/services/overtime.service.test.ts

**测试覆盖**（**含断言细节**，不要只列测试名）：

```typescript
describe('createOvertimeRequest', () => {
  it('正常路径：工作日 2 小时加班 + pay 补偿', async () => {
    // mock: prisma.employee.findFirst → { id: 'emp-1', status: 'active' }
    // mock: prisma.employeeSalaryHistory.findFirst → { baseSalary: 10000 }
    // mock: configService.getValue → configs（max_daily=3, pay_multiplier_weekday=1.5, ...）
    // mock: prisma.overtimeRequest.findMany → []（当月无加班）
    // mock: approval.submitApproval → { id: 'instance-1' }
    // mock: notification.sendNotification
    // mock: auditService.auditLog
    
    const result = await createOvertimeRequest({
      employeeId: 'emp-1',
      startTime: '2026-08-30T19:00:00Z',
      endTime: '2026-08-30T21:00:00Z',
      compensationType: 'pay',
      reason: '项目紧急'
    }, 'user-1');
    
    expect(result.status).toBe('submitted');
    expect(result.totalHours).toBe(2.0);
    expect(result.overtimePay).toBeGreaterThan(0);
    expect(approval.submitApproval).toHaveBeenCalled();
  });
  
  it('周末加班 + comp 补偿 → compDays = totalHours / 8', async () => {
    // mock: 周末日期（如 2026-09-05 周六）
    // mock: compensationType='comp'
    const result = await createOvertimeRequest({
      startTime: '2026-09-05T10:00:00Z',
      endTime: '2026-09-05T18:00:00Z',
      compensationType: 'comp',
      ...
    }, 'user-1');
    
    expect(result.compDays).toBe(1.0);  // 8 小时 = 1 天
    expect(result.overtimePay).toBeNull();
  });
  
  it('startTime >= endTime → 72102', async () => {
    await expect(createOvertimeRequest({
      startTime: '2026-08-30T21:00:00Z', endTime: '2026-08-30T19:00:00Z',
      ...
    }, 'user-1')).rejects.toThrow(expect.objectContaining({ code: 72102 }));
  });
  
  it('单日 > 3 小时 → 72103', async () => {
    await expect(createOvertimeRequest({
      startTime: '2026-08-30T19:00:00Z', endTime: '2026-08-30T23:00:00Z',  // 4 小时
      ...
    }, 'user-1')).rejects.toThrow(expect.objectContaining({ code: 72103 }));
  });
  
  it('单月 > 36 小时 → 72104', async () => {
    // mock: prisma.overtimeRequest.findMany → 当月已有 35 小时
    await expect(createOvertimeRequest({
      startTime: '2026-08-30T19:00:00Z', endTime: '2026-08-30T21:30:00Z',  // 2.5 小时，累计 37.5
      ...
    }, 'user-1')).rejects.toThrow(expect.objectContaining({ code: 72104 }));
  });
  
  it('未提前 4 小时 → 72108', async () => {
    // 2 小时后开始（当前时间 + 2 小时）
    const soon = new Date(Date.now() + 2 * 3600 * 1000);
    const end = new Date(soon.getTime() + 2 * 3600 * 1000);
    await expect(createOvertimeRequest({
      startTime: soon.toISOString(),
      endTime: end.toISOString(),
      ...
    }, 'user-1')).rejects.toThrow(expect.objectContaining({ code: 72108 }));
  });
  
  it('compensationType 非法 → 72107', async () => {
    await expect(createOvertimeRequest({
      compensationType: 'invalid', ...
    }, 'user-1')).rejects.toThrow(expect.objectContaining({ code: 72107 }));
  });
  
  it('加班区间重叠 → 72109', async () => {
    (prisma.overtimeRequest.findFirst as any).mockResolvedValue({
      id: 'ov-existing', startTime: '2026-08-30T19:00:00Z', endTime: '2026-08-30T21:00:00Z'
    });
    await expect(createOvertimeRequest({
      startTime: '2026-08-30T20:00:00Z', endTime: '2026-08-30T22:00:00Z',
      ...
    }, 'user-1')).rejects.toThrow(expect.objectContaining({ code: 72109 }));
  });
});

describe('cancelOvertimeRequest', () => {
  it('draft 直接取消', async () => {
    (prisma.overtimeRequest.findUnique as any).mockResolvedValue({ 
      id: 'ov-1', status: 'draft' 
    });
    const result = await cancelOvertimeRequest('ov-1', '撤回', 'user-1');
    expect(result.status).toBe('cancelled');
  });
  
  it('submitted 调 approval.withdraw', async () => {
    (prisma.overtimeRequest.findUnique as any).mockResolvedValue({ 
      id: 'ov-1', status: 'submitted', approvalInstanceId: 'i-1' 
    });
    const result = await cancelOvertimeRequest('ov-1', '撤回', 'user-1');
    expect(approval.withdraw).toHaveBeenCalledWith(
      expect.objectContaining({ instanceId: 'i-1' })
    );
  });
  
  it('approved 状态 → 72106', async () => {
    (prisma.overtimeRequest.findUnique as any).mockResolvedValue({ 
      id: 'ov-1', status: 'approved' 
    });
    await expect(cancelOvertimeRequest('ov-1', '原因', 'user-1'))
      .rejects.toThrow(expect.objectContaining({ code: 72106 }));
  });
});

describe('confirmOvertimeRequest', () => {
  it('approved → 通知 + 审计', async () => {
    (prisma.overtimeRequest.findUnique as any).mockResolvedValue({ 
      id: 'ov-1', status: 'submitted' 
    });
    const result = await confirmOvertimeRequest('ov-1', { approved: true }, 'user-1');
    expect(result.status).toBe('approved');
  });
  
  it('rejected → 状态 rejected + 通知', async () => {
    const result = await confirmOvertimeRequest('ov-1', { approved: false }, 'user-1');
    expect(result.status).toBe('rejected');
  });
  
  it('非 submitted 状态 → 72105', async () => {
    (prisma.overtimeRequest.findUnique as any).mockResolvedValue({ 
      id: 'ov-1', status: 'approved' 
    });
    await expect(confirmOvertimeRequest('ov-1', { approved: true }, 'user-1'))
      .rejects.toThrow(expect.objectContaining({ code: 72105 }));
  });
});

describe('calculateOvertimePay', () => {
  it('工作日 2 小时 + baseSalary=10000 → pay = 10000/21.75/8 * 2 * 1.5 ≈ 172.41', async () => {
    (prisma.employeeSalaryHistory.findFirst as any).mockResolvedValue({ baseSalary: 10000 });
    (configService.getValue as any).mockResolvedValue(1.5);  // pay_multiplier_weekday
    const pay = await calculateOvertimePay('emp-1', 2.0, 'weekday');
    expect(pay).toBeCloseTo(172.41, 1);
  });
  
  it('周末 4 小时 + baseSalary=20000 → pay = 20000/21.75/8 * 4 * 2.0 ≈ 919.54', async () => {
    (prisma.employeeSalaryHistory.findFirst as any).mockResolvedValue({ baseSalary: 20000 });
    (configService.getValue as any).mockResolvedValue(2.0);  // pay_multiplier_weekend
    const pay = await calculateOvertimePay('emp-1', 4.0, 'weekend');
    expect(pay).toBeCloseTo(919.54, 1);
  });
  
  it('baseSalary 缺失 → 72110', async () => {
    (prisma.employeeSalaryHistory.findFirst as any).mockResolvedValue(null);
    await expect(calculateOvertimePay('emp-1', 2.0, 'weekday'))
      .rejects.toThrow(expect.objectContaining({ code: 72110 }));
  });
});

describe('calculateOvertimeCompDays', () => {
  it('8 小时 → 1 天', () => {
    expect(calculateOvertimeCompDays(8)).toBe(1.0);
  });
  
  it('12 小时 → 1.5 天', () => {
    expect(calculateOvertimeCompDays(12)).toBe(1.5);
  });
  
  it('4 小时 → 0.5 天', () => {
    expect(calculateOvertimeCompDays(4)).toBe(0.5);
  });
});

describe('determineOvertimeType', () => {
  it('2026-09-01（周二）→ weekday', () => {
    expect(determineOvertimeType(new Date('2026-09-01T19:00:00Z'))).toBe('weekday');
  });
  
  it('2026-09-05（周六）→ weekend', () => {
    expect(determineOvertimeType(new Date('2026-09-05T19:00:00Z'))).toBe('weekend');
  });
  
  it('2026-09-06（周日）→ weekend', () => {
    expect(determineOvertimeType(new Date('2026-09-06T19:00:00Z'))).toBe('weekend');
  });
});
```

**总测试数预估**：336 + 19 = 355

**【必做】在测试里 mock 的依赖**（不调真实）：
- prisma（mock 整个 client）
- approvalService（mock submitApproval/withdraw）
- notificationService（mock sendNotification）
- auditService（mock auditLog）
- configService（mock getValue 返回 fixture）

**禁止**：调用真实 prisma（用 vi.mock），调用真实审批/通知/审计/配置（用 vi.mock）。

### 任务 9: 更新 seed.ts

**修改**：server/prisma/seed.ts

**追加**（在 B3 seed 之后）：
- 6 类 overtime configs seed
- 3 个 demo overtime requests：
  1. draft 状态工作日加班 pay
  2. submitted 状态周末加班 comp
  3. approved 状态工作日加班 pay（overtimePay 已计算）

**注意**：B4 seed 引用 A2 已 seed 的 employee + M1+A2 已 seed 的 employee_salary_history，**不要重复 seed**。

### 任务 10: 更新 API Spec

**修改**：docs/api-spec.md

**追加** §3.16 Overtime API（3 端点表格）

**修改**：docs/openapi.yaml

**追加**：3 个新端点 path（overtime 3），含 schema 定义。

### 任务 11: 更新 V1.2 主文档

**修改**：docs/HRMS-V1.2.md

**两处修改**：
1. §四.6.1 M2 切片清单 B4 行标记"**已实现**" + 链接到
   server/src/services/overtime/README.md
2. §七.3 变更记录**追加** 1 行：
```
| **M2.0.4** | **2026-08-30** | **feat(overtime): M2-B4 加班管理（Cursor 交付）<br>**范围**：overtime_requests 1 张表 + 加班补偿二选一（pay 加班费 / comp 调休 1:1）+ 单日 ≤3h / 单月 ≤36h 加班上限（劳动法）+ 加班费倍数计算（工作日 1.5x / 周末 2.0x，**法定假日 3.0x 留二期**）+ 至少提前 4h 申请 + 6 类 configs 业务规则 + 3 个新端点 + 19 个新单测（336→355）<br>**依赖**：M1 全部 + M0.5 全部 + M2-B1/B2/B3 + employee_salary_history（加班费计算）<br>**强约束**：员工/部门关联走 prisma 直接操作（不 import 跨 service），**未实现调休余额累计**（B3 调休余额留 B6 月度汇总或独立任务），未修改 leave.service.ts 的 calculateLeaveBalance 函数 | **Cursor + WorkBuddy** |
```

### 任务 12: 更新 AGENTS.md

**修改**：AGENTS.md

**追加 1 段**（在 M2 段 B3 描述之后）：
```markdown
- **B4 加班管理（已完成）**：overtime_requests 1 张表
  + 加班补偿二选一（pay 加班费 / comp 调休 1:1）
  + 加班上限（单日 ≤3h / 单月 ≤36h，劳动法）
  + 加班费倍数（工作日 1.5x / 周末 2.0x，**法定假日 3.0x 留二期**）
  + 至少提前 4h 申请
  + 3 个新端点 + 19 个新单测（336 → 355）
  + **B4 不实现调休余额累计**（B3 调休余额留 B6 月度汇总或独立任务）
  + **未修改 leave.service.ts 的 calculateLeaveBalance 函数**
- 后续：B5（出差）→ B6（月度汇总）
```

### 任务 13: 创建 README

**新建**：server/src/services/overtime/README.md（约 3-5 KB）

**包含**：
- B4 业务说明（V1.2 §二.3.3 加班管理 + §四.6.1 B4 切片）
- 状态机图（draft / submitted / approved / rejected / cancelled）
- 加班补偿选择（pay / comp 1:1 转换）
- 加班上限（单日 3h / 单月 36h）
- 3 端点清单
- 业务规则（6 类 configs）
- M0.5 + M1 + M2-B1/B2/B3 集成点（审批 / 通知 / 员工关联 / 薪资历史）
- 测试覆盖（19 个）
- 已知限制（**调休余额累计留 B6** / 法定假日判定留二期 / BullMQ 加班预警留独立任务）
- 后续 B5/B6 集成计划

【输出 - 完整文件清单】

**新增文件（6 个）**：
1. server/prisma/migrations/20260830070000_add_overtime/migration.sql
2. server/src/services/overtime.service.ts
3. server/src/services/overtime.service.test.ts
4. server/src/controllers/overtime.controller.ts
5. server/src/routes/overtime.ts
6. server/src/services/overtime/README.md

**修改文件（8 个）**：
7. server/prisma/schema.prisma（追加 1 张表 overtime_requests）
8. server/prisma/seed.ts（追加 6 类 overtime configs + 3 demo records）
9. server/src/routes/index.ts（追加 1 import + 1 router.use）
10. server/src/constants/permissions.ts（追加 3 个新权限点 + 5 角色权限追加）
11. docs/api-spec.md（追加 §3.16 Overtime API）
12. docs/openapi.yaml（追加 3 个新端点 path + schema）
13. docs/HRMS-V1.2.md（§四.6.1 B4 标"已实现" + §七.3 追加 1 行变更记录）
14. AGENTS.md（M2 段追加 B4 描述）

**总改动：6 新增 + 8 修改 = 14 文件**

【验收 - 必须全部满足】
1. pnpm --filter hrms-server type-check 0 错
2. pnpm --filter hrms-server lint 0 错
3. pnpm --filter hrms-server test 355/355 通过（336 + 19 新增）
4. pnpm --filter hrms-client type-check 0 错
5. 改动文件数 ≤ 18
6. 没有任何 M0/M0.5/M1/M2-B1/M2-B2/M2-B3 旧测试文件被修改
   （用 `git diff --stat "*.test.ts"` 确认仅新增 overtime.service.test.ts）
7. 没有做 B1/B2/B3（已实现）/ B5/B6 任何业务
8. 没有改 M0.5 + M1 + M2-B1 + M2-B2 + M2-B3 任何 service / controller / routes（grep 验证）
9. overtime.service.ts **仅 import 黑名单以外的 service**
   （即只 import approval/audit/config/notification/prisma，不 import 跨 service）
10. 审批调用走 approval.submitApproval / withdraw（已 export），没有自己重写审批逻辑
11. 通知走 notification.sendNotification + **bypassTemplate fallback**（已验证 A4/A5/A6/A7/B1/B2/B3 模式）
12. 员工/部门/薪资关联走 prisma 直接操作（**不 import 跨 service**）
13. **未实现调休余额累计**（B6 月度汇总或独立任务范围）
14. **未修改 leave.service.ts 的 calculateLeaveBalance 函数**（红线 17 强约束）
15. 所有业务规则走 M0.5-6 configService（不要 hardcoded）
16. 没有新建 RBAC 角色（5 角色结构不变，仅追加权限点）

【不要做的事 - 红线复述】
- ❌ 不要做 B1/B2/B3（已实现）/ B5/B6 任何业务
- ❌ 不要做 client/ 或 mobile/ 任何文件
- ❌ 不要改任何 M0/M0.5/M1/M2-B1/M2-B2/M2-B3 已有测试
- ❌ 不要改 M0.5 + M1 + M2-B1 + M2-B2 + M2-B3 已有 service / controller / routes
- ❌ **不要 import 跨 service**（leave/shift/attendance/employee/department/company/onboarding/regularization/offboarding/transfer/contract/employeeAI/auth）
- ❌ **不要修改 leave.service.ts 的 calculateLeaveBalance 函数**（红线 17 强约束）
- ❌ **不要实现调休余额累计**（B6 月度汇总或独立任务范围）
- ❌ 不要修改 notification.service.ts / approval.service.ts / crypto.service.ts 已有函数签名
- ❌ 不要改 server/src/jobs/* 任何文件
- ❌ 不要"顺便"加 B5-B6 的接口 / 表 / 服务
- ❌ 不要"我觉得可以优化"任何已有代码
- ❌ 不要重写审批 / 通知 / 加密 / 配置 / 审计
- ❌ 不要新建 RBAC 角色
- ❌ **不要实现法定假日判定**（留二期）
- ❌ **不要实现出差**（留 B5）
- ❌ **不要实现月度汇总**（留 B6）
- ❌ **不要实现 BullMQ 加班预警**（留独立任务）
- ❌ **不要实现 BullMQ 调休到期清理**（留独立任务）

【参考实现位置】
- service 模式：server/src/services/leave.service.ts（**最完整参考**）
- controller 模式：server/src/controllers/leave.controller.ts
- routes 模式：server/src/routes/leave.ts（含 zod schema + authenticate +
  rejectIfMustChangePassword + requirePermission）
- 业务集成调用：server/src/services/leave.service.ts
  （import approvalService / auditService / configService / notificationService）
- 状态机：参考 leave.service.ts 的 draft → submitted → approved/rejected
- 单测模式：server/src/services/leave.service.test.ts（mock + describe 风格）
- bypassTemplate fallback 模式：参考 regularization.service.ts 第 80-100 行
- 加班费计算公式：参考 V1.2 §二.3.3 + §二.4.2 固浮比
- 员工关联：参考 leave.service.ts 的 createLeaveRequest

【完成标准】
当你跑完以下命令且全部成功后，再回复我"已完成"：
  pnpm --filter hrms-server type-check
  pnpm --filter hrms-server lint
  pnpm --filter hrms-server test
  pnpm --filter hrms-client type-check
  git diff --stat  # 确认改动文件数

并附上：
1. 改动文件清单（**必须只有 14 个**：6 新增 + 8 修改）
2. **明确声明**："未修改任何 M0/M0.5/M1/M2-B1/M2-B2/M2-B3 旧测试（仍 336 个）"
3. **明确声明**："新单测 19 个，总计 355/355 通过"
4. **明确声明**："未做 B1/B2/B3（已实现）/ B5/B6 任何业务"
5. **明确声明**："overtime.service.ts 仅 import approval/audit/config/notification/prisma，
   未 import 跨 service"
6. **明确声明**："**未实现调休余额累计**（B3 调休余额留 B6 月度汇总或独立任务）"
7. **明确声明**："**未修改 leave.service.ts 的 calculateLeaveBalance 函数**"
8. **明确声明**："通知走 bypassTemplate fallback（参考 A4/A5/A6/A7/B1/B2/B3 模式）"
9. 4 条验收命令的输出
10. 任何意外情况 + 处理方式

如果改动文件数 > 18 或动了 336 个旧测试或重写审批/通知/加密
或 import 跨 service 或实现 B5-B6 业务或修改 leave.service.ts
或实现调休余额累计，**立即回退**并报告"越界，需重做"。
```

## 结束复制 ↑

---

# 文档

| 项 | 值 |
|---|---|
| 任务 ID | M2-B4 |
| 任务名 | 加班管理（加班申请 + 补偿二选一 + 加班上限校验） |
| 估时 | 2d（V1.2 §四.6.1） |
| 依赖前置 | M1 + M0.5 + M2-B1/B2/B3 |
| **M2 第 4 个切片** | 阶段继续 |
| 改动文件 | 6 新增 + 8 修改 = 14 |
| 端点 | 3（提交/列表/撤回） |
| 新单测 | 19（336 → 355） |
| 错误码段位 | **72101-72110**（72 段位 B4 子区） |
| 提示词大小 | ~45KB（沿用 A5/A6/A7/B1/B2/B3 详尽模式） |
| 关键约束 | 不 import 跨 service / 未实现调休余额累计 / 未改 leave.service.ts |
| 越界红线 | 不做 B5-B6 / 不改旧测试 / 不改 M0.5+M1+B1+B2+B3 已有代码 / 不改 leave.service.ts |
