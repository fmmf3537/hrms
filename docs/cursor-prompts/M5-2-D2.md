# M5-2-D2 M5 绩效前端 · 考核记录全流程（自评→经理评分→部门校准→HR 汇总→CEO 审批→归档）执行提示词

> **任务 ID**：M5-2-D2
> **任务名称**：M5-2 第 8 个业务子切片 — M5 绩效前端 · 考核记录全流程（M5-2-D 第 2 片；D3 等级与奖金 / D4 销售提成 / D5 申请联动 待后续）
> **任务范围**：2 子任务 / 2.5d 工时 / V1.2 §四.9 D3 考核流程前端部分（任务 ID 优先，不与 V1.2 切片表强对齐）
> **提示词生成日期**：2026-09-01
> **执行器**：opencode run（headless）；执行环境 Windows + cmd，**pnpm/node 若不在 PATH 需先 `set PATH=C:\Users\fmmf\AppData\Roaming\npm;C:\Users\fmmf\AppData\Roaming\pnpm;%PATH%`**
> **提速约定（本切片起生效）**：**不要运行 §7.1 的验收命令**（测试/type-check/lint/build 由审核方统一重跑）；你只需完成代码 + 静态自检 + 交付报告。**也不要执行 git add / commit**。
> **目标单测增量**：约 4-6 个（1 API 文件 + 1 视图聚合，每文件 ≥2 用例）
> **依赖**：M5-2-D1 commit `0a0a5dd` 已完成（后端 876/876 + 前端 74/74 基线；PerformanceLayout + router/performance.ts + api/performance.ts 已就绪）

---

## ⚠️ 强约束（最优先阅读）

**历史教训**（沿用 M5-2-C1/C2/C3/D1 提示词全集，以下为仍然适用要点）：
1. 严禁越界做其他切片。本提示词只授权 M5-2-D2（/records 16 端点）。
2. **5 角色 RBAC 无 finance**（admin / hr / dept_head / executive / employee）。
3. **36 service + 40 表 0 行改动**（后端业务代码一律不碰）。
4. **后端实际端点/权限以代码为准**：§1.3 / §3.3 已逐一核对 `server/src/routes/performance.ts` + `server/src/constants/permissions.ts`；发现不符以代码为准并在交付报告说明。
5. **M5-2-D2 后端端点确认**（**16 端点，已逐一核对**；挂载前缀 `/api/performance`）：
   - 记录 CRU 主线（3）：POST /records + GET /records + GET /records/:id
   - 自评（2）：**PATCH** /records/:id/self + POST /records/:id/submit-self
   - AI 建议（2）：POST /records/:id/ai-suggest + GET /records/:id/ai-suggestions
   - 经理评分（2）：**PATCH** /records/:id/manager-score + POST /records/:id/submit-manager
   - 部门校准（2）：**PATCH** /records/:id/calibrate + POST /records/:id/submit-calibrate
   - HR 汇总（2）：**PATCH** /records/:id/hr-summary + POST /records/:id/submit-hr
   - CEO 审批（1）：**PATCH** /records/:id/ceo-approve
   - 归档 + 驳回（2）：POST /records/:id/archive + POST /records/:id/reject
   - **总计 16 端点消费（0 新增）**
6. **M5-2-D2 关键设计**：
   - **0 修改后端 / 0 修改 client/package.json**（严禁装依赖）
   - **0 修改 D1 已建文件**，唯一例外：`router/performance.ts` **追加**（沿用 salary 模块 C2/C3 的独立菜单数组模式，见 §4.4）
   - **类型独立到 `api/types/performanceRecord.ts`**（不改 D1 的 `types/performance.ts`）
   - **保存用 PATCH、提交用 POST 的"两段式"是本切片核心范式**：每个评分环节都是「PATCH 存草稿 → POST submit 定稿推进状态」
7. **dsh D1 实测教训沿用**：金额/分数字段以 service 实际返回为准；状态枚举**不要猜**，§5.1 先读 `performance_record.service.ts` 确认 status 字符串全集；分页形态确认后复用解包函数。
8. **执行器差异（headless 特注）**：没有人工交互；冲突按代码事实处理并写入交付报告；**不跑验收命令、不 git commit**。

**红线**（违反任何一条 → 任务失败，需回退重做）：

1. **禁止越界**：严禁做 D1（cycles/indicators/schemes/coefficients/thresholds，已完成）/ D3（grade calculate + payouts）/ D4（sales）/ D5（applications）。只消费 §1.3 的 16 端点。
2. **禁止修改后端文件**：**严禁修改 `server/` 任何文件**。
3. **禁止修改 `client/package.json`** / `pnpm-lock.yaml`。
4. **禁止修改共享基础设施**：`api/http.ts` / `api/types.ts` / `stores/` / `utils/` / `components/` / `layouts/`（含 D1 新建的 PerformanceLayout.vue）全部 0 修改。
5. **禁止修改既有前端文件**：
   - ❌ `api/types/{organization,attendance,salary,payroll,compensation,performance}.ts`（新类型放 `api/types/performanceRecord.ts`）
   - ❌ `api/` 已有 19 个业务 API 文件 + `http.ts`
   - ❌ `router/{index,organization,attendance,salary}.ts`（**仅允许 `router/performance.ts` 追加**）
   - ❌ `views/{organization,attendance,salary}/**` 与 `views/performance/{cycle,indicator,scheme,coefficient,grade}/**`（D1 已建 5 页面）
   - ❌ `client/src/**/__tests__/` 已有 35 个测试文件（**不修改旧测试**；**注意 D1 测试可能断言 PERFORMANCE_MENU 长度或 filter 行为，见 §4.4**）
6. **必须报告完整**：交付报告列出全部新增/修改文件，明确"原有用例是否改动"。
7. **禁止"我觉得这也有用"**。
8. **禁止臆测字段**：响应/请求字段以 §2.4 所列 service 与 zod 实际为准。
9. **范围强约束**：0 修改后端 / 0 修改 package.json / **7 个 client 文件（6 新增 + 1 修改 router/performance.ts）** / 5 角色无 finance。
10. **测试范式强约束（`ac43074` 规则）**：新测试文件放 `src/**/__tests__/`，自包含 shim（文件内 describe/it + expectEqual + 导出 `run*Tests()`，命名匹配 `/^run.*Tests$/`）；**严禁 `import ... from 'vitest'`**。
11. **不要执行 git add / commit / push**；**不要运行 §7.1 验收命令**（提速约定）。

---

## 1. 任务 ID + 目标

### 1.1 任务 ID

**M5-2-D2**（M5-2 第 8 个业务子切片 — M5 绩效前端 · 考核记录全流程）

### 1.2 任务目标

| 子任务 | 范围 | 端点 | 新增 |
|---|---|---|---|
| 7.2.D2.1 考核记录管理（HR/管理视角）| 创建记录 + 列表 + 详情 + 归档 + 驳回 | 5（POST/GET records + GET :id + archive + reject）| 1 页面（RecordList）|
| 7.2.D2.2 五环评分工作区（详情页）| 自评/AI 建议/经理评分/校准/HR 汇总/CEO 审批 | 11 | 1 页面（RecordDetail）|
| **共用** | types/performanceRecord.ts + api/performanceRecord.ts + router/performance.ts 追加 + 2 测试 | — | 1 types + 1 API + 1 修改 + 2 测试 |
| **M5-2-D2 合计** | **16 端点消费** | **16** | **6 新增 + 1 修改 = 7 文件** |

### 1.3 端点清单（**0 新增后端端点**）

| # | 方法 + 路径 | 权限点 | body / query（zod 实际） |
|---|---|---|---|
| 1 | POST /api/performance/records | performance:record:write | `{cycleId: uuid, employeeIds: uuid[] min1, schemeId?: uuid}` |
| 2 | GET /api/performance/records | performance:record:read | query `cycleId? / employeeId? / status?(string ≤30) / deptId? / page / pageSize` |
| 3 | GET /api/performance/records/:id | performance:record:read | — |
| 4 | PATCH /records/:id/self | performance:**self:submit** | saveScoreSchema（见下） |
| 5 | POST /records/:id/submit-self | performance:**self:submit** | 无 body |
| 6 | POST /records/:id/ai-suggest | performance:**ai:request** | 无 body |
| 7 | GET /records/:id/ai-suggestions | performance:**ai:read** | — |
| 8 | PATCH /records/:id/manager-score | performance:**manager:score** | saveScoreSchema |
| 9 | POST /records/:id/submit-manager | performance:**manager:score** | 无 body |
| 10 | PATCH /records/:id/calibrate | performance:**dept:calibrate** | saveScoreSchema |
| 11 | POST /records/:id/submit-calibrate | performance:**dept:calibrate** | 无 body |
| 12 | PATCH /records/:id/hr-summary | performance:**hr:summary** | saveScoreSchema |
| 13 | POST /records/:id/submit-hr | performance:**hr:summary** | 无 body |
| 14 | PATCH /records/:id/ceo-approve | performance:**ceo:approve** | `{finalGrade: S\|A\|B\|C\|D, finalScore: 0-100, comment?(≤2000)}` |
| 15 | POST /records/:id/archive | performance:record:write | 无 body |
| 16 | POST /records/:id/reject | performance:record:write | `{reason: string(min **5**, max 2000)}` 必填 |

**saveScoreSchema（4/8/10/12 四个 PATCH 共用）**：
```json
{ "comment?": "string ≤2000",
  "items": [{ "indicatorId": "uuid", "score": "number 0-100", "comment?": "string ≤2000" }]（min 1）,
  "basedOnAiSuggestionId?": "uuid" }
```

---

## 2. 上下文

### 2.1 项目位置

- 仓库根：`D:\projects\hrms`；前端源码 `client/src/`；本提示词 `docs/cursor-prompts/M5-2-D2.md`

### 2.2 当前状态 + 必增文件清单

**当前状态**（D1 commit `0a0a5dd`）：PerformanceLayout / router/performance.ts（`PERFORMANCE_MENU` 5 项 + `filterPerformanceMenu` + `resolvePerformanceActiveKey`）/ api/performance.ts / api/types/performance.ts（含 `unwrapPerformancePage`）已就绪；后端 876/876，前端 74/74。

**本切片必增 6 文件 + 必改 1 文件**：
- 新增：`api/types/performanceRecord.ts`、`api/performanceRecord.ts`、`views/performance/record/RecordList.vue`、`views/performance/record/RecordDetail.vue`、`api/__tests__/performanceRecord.test.ts`、`views/__tests__/performance-record.test.ts`
- 修改：`router/performance.ts`（**仅追加** D2 菜单 + activeKey 分支 + 2 路由）

### 2.3 可复用模块（**严禁重写**）

| 模块 | 路径 | 用法 |
|---|---|---|
| http 实例 | `api/http.ts` | 拦截器返回 response.data，业务错误自动 ElMessage |
| 分页解包 | **`api/types/performance.ts` 的 `unwrapPerformancePage`**（D1 已建，绩效模块统一用它）| listRecords 分页形态 §5.1 确认后复用 |
| 周期/方案数据源 | `api/performance.ts` 的 `listCycles` / `listSchemes` | 创建记录 Dialog 的下拉数据源，**不重写** |
| 员工数据源 | `api/employee.ts` 的 `listEmployees` | 创建记录的员工多选 |
| 菜单/布局 | `router/performance.ts` + PerformanceLayout | 追加菜单即自动出现在侧栏 |
| 页面范式 | 参照 `views/salary/payroll/PayrollRunDetail.vue`（C2 状态机详情页）| ElSteps + 权限按钮区 + 分环节面板 |
| 纯函数判定 | 参照 `api/types/payroll.ts` 的 `canPayrollRunAction`（C2）| 本切片抽 `canRecordStageAction` 导出 |

### 2.4 业务规则来源（**写类型前必读**）

- `server/src/services/performance_record.service.ts`（**status 字符串全集与状态机**、list 分页形态、getRecord 嵌套结构、reject 打回到哪个状态）
- `server/src/services/performance_score.service.ts`（save/submit 语义、分数汇总字段）
- `server/src/services/performance_ai_suggestion.service.ts`（ai-suggest 返回 / suggestions 列表结构）

---

## 3. 必读约束

### 3.1 代码范式

- Vue3 `<script setup lang="ts">` + Element Plus + Pinia，与 D1/C1-C3 同风格
- 「PATCH 存草稿 → POST submit 定稿」两段式按钮：保存（可多次）+ 提交（二次确认 ElMessageBox.confirm）
- 每个环节动作成功后重新 `getRecord` 刷新整页

### 3.2 命名

- API 函数：`createRecord / listRecords / getRecord / saveSelfScore / submitSelf / requestAiSuggest / listAiSuggestions / saveManagerScore / submitManager / saveCalibration / submitCalibration / saveHrSummary / submitHrSummary / ceoApprove / archiveRecord / rejectRecord`（16 个）
- `RECORD_PATHS as const`
- 类型：`PerformanceRecord / RecordStatus / ScoreItem / SaveScoreRequest / ScoreStage / AiSuggestion / CreateRecordRequest / RecordListQuery / CeoApproveRequest`
- `ScoreStage = 'self' | 'manager' | 'calibrate' | 'hr' | 'ceo'`（页面内部环节标识）

### 3.3 RBAC 权限矩阵（**已实读 ROLE_PERMISSIONS**）

| 权限点 | admin | hr | dept_head | executive | employee |
|---|---|---|---|---|---|
| performance:record:read | ✅* | ✅ | ✅ | ✅ | ✅ |
| performance:record:write（创建/归档/驳回）| ✅* | ✅ | ❌ | ❌ | ❌ |
| performance:self:submit（自评）| ✅* | ❌ | ❌ | ❌ | ✅ |
| performance:manager:score | ✅* | ❌ | ✅ | ❌ | ❌ |
| performance:dept:calibrate | ✅* | ❌ | ✅ | ❌ | ❌ |
| performance:hr:summary | ✅* | ✅ | ❌ | ❌ | ❌ |
| performance:ceo:approve | ✅* | ❌ | ❌ | ✅ | ❌ |
| performance:ai:request | ✅* | ❌ | ✅ | ✅ | ❌ |
| performance:ai:read | ✅* | ✅ | ✅ | ✅ | ❌ |

**反直觉点（显隐必须遵守）**：
1. **hr 不能发起 AI 建议**（ai:request 仅 dept_head/executive），但能看建议列表
2. **自评仅 employee**（hr/dept_head 都不行；admin 通配除外）——RecordDetail 的自评面板只对「当前用户 employeeId === 记录员工」且有 self:submit 的人可编辑
3. **manager:score 与 dept:calibrate 都是 dept_head 专属**（同一人两个环节）
4. **ceo:approve 仅 executive**
5. **employee 有 record:read 但没有 ai:read**——员工看不到 AI 建议面板

### 3.4 错误码 / 3.5 审计 / 3.6 configs

0 新增。

---

## 4. 实施任务

### 4.1 `api/types/performanceRecord.ts`（新增，**先做 §5.1 确认再动笔**）

- `RecordStatus`：**字符串字面量联合，全集以 `performance_record.service.ts` 实际状态机为准**（预期含自评/经理/校准/HR/CEO/归档/已驳回各环节，**严禁猜**）
- `PerformanceRecord`：id / cycleId / employeeId / schemeId / status / 各环节分数与评论字段（selfScore/managerScore/…/finalGrade/finalScore，以 service 为准）/ items（ScoreItem[] 若内嵌）/ 时间戳 / 操作人
- `ScoreItem = {indicatorId, score, comment?}`（+ 若 service 返回 indicator 名称则扩展只读字段）
- `SaveScoreRequest`（对齐 saveScoreSchema，含 basedOnAiSuggestionId?）
- `AiSuggestion`（以 ai_suggestion service 返回为准）
- `RECORD_STATUS_MAP: Record<string, StatusTagInfo>`（导出，供测试；key 覆盖全部 RecordStatus）
- `canRecordStageAction(stage, record, user): boolean`（导出纯函数：环节 × 权限 × 状态三维判定；stage ∈ 'self'|'manager'|'calibrate'|'hr'|'ceo'|'archive'|'reject'|'ai'）
- 从 `./types/performance` import `unwrapPerformancePage` / `StatusTagInfo`（**不重写**）

### 4.2 `api/performanceRecord.ts`（新增，16 函数）

`RECORD_PATHS as const`：`list / detail(id) / self(id) / submitSelf(id) / aiSuggest(id) / aiSuggestions(id) / managerScore(id) / submitManager(id) / calibrate(id) / submitCalibrate(id) / hrSummary(id) / submitHr(id) / ceoApprove(id) / archive(id) / reject(id)`。
16 函数对应 §1.3，每函数单行 JSDoc（端点+方法+权限点）；`listRecords` 用 `unwrapPerformancePage`。

### 4.3 `views/performance/record/RecordList.vue`（新增）

- 双模式：有 `record:write` 或管理角色（admin/hr/dept_head/executive）→ 全量列表 + 筛选（cycleId Select 来自 `listCycles` / status / deptId）；**employee → 「我的考核」模式**：强制 `employeeId = userInfo.employee?.id`，隐藏筛选中的员工维度，为空显示 EmptyState「尚未关联员工档案」
- 「发起考核」按钮（`record:write` = admin/hr）：Dialog——周期 Select（active 周期优先）+ 员工**多选**（`listEmployees`）+ 方案 Select（`listSchemes`，可选）→ createRecord
- 表格：员工 / 周期 / 状态 ElTag（RECORD_STATUS_MAP）/ 当前得分（以 service 字段为准）/ 创建时间 / 操作（查看 → RecordDetail）
- 分页 `unwrapPerformancePage`

### 4.4 `views/performance/record/RecordDetail.vue`（新增，**本切片核心页面**）

- 顶部：员工 + 周期 + 状态 ElTag；`ElSteps` 展示六环节进度（自评 → 经理评分 → 部门校准 → HR 汇总 → CEO 审批 → 归档），当前环节按 record.status 映射；被驳回记录显示 ElAlert（原因 + 被打回环节）
- **环节面板区（按权限 + 状态显隐，`canRecordStageAction` 驱动）**：
  - **自评面板**（self:submit 且当前环节=自评 且记录属于本人）：指标评分明细表（每行：指标名 + score ElInputNumber 0-100 + comment 输入）+「保存草稿」（PATCH self）+「提交自评」（POST submit-self，confirm）
  - **AI 建议面板**：「生成 AI 建议」按钮（ai:request：dept_head/executive）→ requestAiSuggest；建议列表（ai:read，**employee 不可见**）→ listAiSuggestions；经理评分时可「采纳建议」（把建议分数填入评分表并记录 basedOnAiSuggestionId）
  - **经理评分面板**（manager:score = dept_head）：同结构评分表 + saveManagerScore / submitManager
  - **部门校准面板**（dept:calibrate = dept_head）：同上 + saveCalibration / submitCalibration
  - **HR 汇总面板**（hr:summary = hr）：同上 + saveHrSummary / submitHrSummary
  - **CEO 审批面板**（ceo:approve = executive）：finalGrade Radio（S/A/B/C/D）+ finalScore ElInputNumber（0-100）+ comment → ceoApprove（PATCH，一次定稿）
  - **归档 / 驳回**（record:write = hr）：归档 confirm；驳回 Dialog 填 reason（**min 5 字符**，前端校验）
- 只读角色看到各环节已保存的分数与评论（只读表格）

### 4.5 `router/performance.ts`（**唯一允许修改的既有文件，仅追加**）

- **新建 `PERFORMANCE_D2_MENU` 独立数组**（**不动 `PERFORMANCE_MENU`**——D1 测试可能断言其长度/行为，与 salary 模块 C1 旧测教训同款）：
  ```ts
  { key: 'records', label: '考核记录', employeeLabel: '我的考核', icon: '<从 PerformanceLayout icon 映射表中选>', to: '/performance/records', permission: 'performance:record:read' },
  ```
- `filterPerformanceMenu` 内拼接 `PERFORMANCE_D2_MENU`（追加在既有项之后；employee 经 employeeLabel 显示「我的考核」）
- `resolvePerformanceActiveKey` 追加 `/performance/records` 分支
- children 追加 2 路由：`records`（RecordList，title 考核记录）+ `records/:id`（RecordDetail，title 考核详情）
- **若 PerformanceLayout 的 icon 映射表缺少所选图标**：本切片**不允许修改 layout 文件** → 从 layout 已有映射表中挑一个已注册的图标复用

### 4.6 测试（2 个新文件，`ac43074` 自包含范式）

1. `api/__tests__/performanceRecord.test.ts`（≥2）：`RECORD_PATHS` 拼接断言（**重点区分 PATCH 与 POST 路径**，如 self(id) vs submitSelf(id)）；`SaveScoreRequest.items` min 1 / score 0-100 类型断言
2. `views/__tests__/performance-record.test.ts`（≥2）：`RECORD_STATUS_MAP` 覆盖全部 RecordStatus 断言；`canRecordStageAction` 三维判定用例（employee+自评环节+本人记录 → true；hr+ai 请求 → **false**；dept_head+manager 环节 → true；executive+ceo 环节 → true；hr+ceo 环节 → **false**）

导出 `runPerformanceRecordApiTests / runPerformanceRecordViewTests`。**严禁 import vitest / @vue/test-utils**。

---

## 5. 关键决策点（详尽）

### 5.1 响应结构确认（**最优先**）

写类型前逐一打开 §2.4 三个 service，记录：
- **RecordStatus 字符串全集**（状态机每个环节的 status 值 + reject 后打回值 + archive 后终态）→ 决定 `RECORD_STATUS_MAP` 与 ElSteps 映射
- getRecord 返回的嵌套结构（是否含 items / 各环节 score 与 comment / employee·cycle·scheme 关联对象）
- listRecords 分页形态（确认 `{items,total,page,pageSize}` → `unwrapPerformancePage`）
- save PATCH 与 submit POST 的返回（更新后 record 还是摘要）
- ai-suggest 返回（异步任务回执还是同步建议）与 ai-suggestions 列表元素结构
- **结论写进交付报告「主动识别与处理」**

### 5.2 两段式保存/提交语义

- PATCH = 存草稿（可反复），POST submit = 推进状态机；**提交前不强制先保存**（service 若要求先存再提，以前端提示引导，以 service 实际校验为准）
- 提交按钮一律 ElMessageBox.confirm 二次确认

### 5.3 驳回语义

- reject 后 status 变化与可重新编辑的环节以 service 为准；前端按新 status 重新计算各环节面板显隐（不要硬编码"驳回=回到自评"）

### 5.4 AI 建议交互

- 生成可能是异步：requestAiSuggest 返回后轮询一次 listAiSuggestions 或提示稍后刷新（以 service 返回形态为准，**不做 setInterval 长轮询**，最多手动刷新按钮）

### 5.5 执行器环境特注（opencode / headless）

- shell 为 cmd；pnpm 不在 PATH 时先 `set PATH=C:\Users\fmmf\AppData\Roaming\npm;C:\Users\fmmf\AppData\Roaming\pnpm;%PATH%`
- **不要执行 git add / commit**；**不要运行验收命令**（提速约定，审核方统一执行）
- 完成后最终回复 = 完整交付报告

### 5.6 不做清单

- ❌ cycles/indicators/schemes/coefficients/thresholds 任何改动（D1 已完成）
- ❌ grade calculate / payouts / sales / applications（D3-D5）
- ❌ DefaultLayout 侧栏占位（M5-2-0 范畴）
- ❌ 修改 PerformanceLayout.vue（icon 复用既有映射表）

---

## 6. 修改文件清单

### 6.1 必改文件（7 个）

**新增 6 个**：
1. `client/src/api/types/performanceRecord.ts`
2. `client/src/api/performanceRecord.ts`
3. `client/src/views/performance/record/RecordList.vue`
4. `client/src/views/performance/record/RecordDetail.vue`
5. `client/src/api/__tests__/performanceRecord.test.ts`
6. `client/src/views/__tests__/performance-record.test.ts`

**修改 1 个**：
7. `client/src/router/performance.ts`（**仅追加** PERFORMANCE_D2_MENU + 拼接 + 1 activeKey 分支 + 2 路由，不改既有行）

### 6.2 禁止修改文件（清单以外）

- ❌ `server/**`（全部）
- ❌ `client/package.json` / `pnpm-lock.yaml` / `vitest.config.ts` / `tests/run-all.test.ts` / `vite.config.ts` / `tsconfig*.json`
- ❌ `client/src/api/types.ts` / `api/types/` 既有 6 文件 / `api/` 既有 19 业务 API + `http.ts`
- ❌ `client/src/router/{index,organization,attendance,salary}.ts`
- ❌ `client/src/layouts/**`（含 PerformanceLayout.vue）/ `components/**` / `stores/**` / `utils/**`
- ❌ `client/src/views/{organization,attendance,salary}/**` / `views/performance/{cycle,indicator,scheme,coefficient,grade}/**`
- ❌ `client/src/**/__tests__/` 已有 35 个测试文件
- ❌ **禁止新建根目录/其他目录的辅助脚本**（如 mock-run-tests.mjs 之类一律禁止；D1 教训）

### 6.3 越界检测（交付时自检，写入报告）

```bash
cd D:\projects\hrms
git status --short
# 必须只出现 §6.1 列出的 7 个文件
git diff --stat server/
# 必须 0 行
git diff client/package.json
# 必须 0 行
```
（若沙箱内 git 不可用，用文件清单比对等价说明）

---

## 7. 验收标准

### 7.1 硬性验收（**审核方执行，你不跑**；列出仅供你了解通过线）

基线：后端 876/876；前端 74 + 新增（2 文件 × ≥2）= **≥78**；两端 type-check 0 错；lint 0 error；build 成功。

### 7.2 交付报告模板（**你的最终回复必须完整包含**）

```markdown
# M5-2-D2 交付报告

## 完成范围
- 子任务 7.2.D2.1/2 完成状态 + 端点消费清单（16/16）
## 新增文件（6）
（逐一列出）
## 修改文件（1）
- client/src/router/performance.ts（+N 行）
## 主动识别与处理
（§5.1 逐 service 确认结论 / RecordStatus 全集与状态机 / 分页形态 / AI 建议同步或异步 / 与提示词不一致处，逐一说明处理方式）
## 测试
- 新增 2 个测试文件 / N 个用例（未运行，由审核方执行）
- 原有用例是否改动：0（必须）
## 已知问题
（如有，逐一列出 + 建议处理阶段）
## 红线自检
- 0 修改后端 / 0 修改 package.json / 0 修改旧测试 / 0 修改共享基础设施与 D1 文件（除 router/performance.ts 追加）/ 5 角色无 finance / 未执行 git commit / 未运行验收命令
```

---

**提示词结束。按本提示词直接执行（headless 环境无人工确认环节）：先输出实施计划（文件创建顺序 + §5.1 确认结论），然后动手，最终回复给出完整交付报告。**
