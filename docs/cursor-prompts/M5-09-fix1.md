# M5-09 Fix 1：E2E 残留数据清理 + 沙箱红线再强化

> **关联任务**：M5-09 缺陷修复
> **Fix 轮次**：fix1（第 1 次修订）
> **根因**：
> 1. E2E 03-leave 失败：admin 账号在旧库中已有 2 条 draft + 1 条 submitted 请假记录（日期 09-08、09-09、09-14），再次提交同日期触发 409 区间重叠
> 2. E2E 04-payroll：lock 逻辑已修改（approve 后 autoLock），但未重跑验证
> 3. opencode 调试时写 `/tmp/` 触发沙箱拒绝，任务被中止

---

## 上一轮状态

- 5 个缺陷修复代码**全部已完成**
- server vitest 876/876 ✅
- client vitest 90/90 ✅
- server/client type-check ✅（client 的 coords null 问题已修）
- lint 0 error ✅
- E2E：3 passed（01-login/02-attendance/05-performance），2 failed（03-leave 409 / 04-payroll lock），1 did not run

## 修复要求

### 1. 跑 E2E 前清理 admin 的请假记录

在 `e2e/specs/03-leave.spec.ts` 的 `test.beforeEach` 或开头增加：用 API 删除 admin 的所有 **draft** 状态请假记录（保留 submitted/approved 的不影响）。或者改为用动态未来日期（如 `Date.now()` 偏移）避免与既有记录冲突。

**推荐做法**：test 开头先用 API 清理所有 `status='draft'` 的请假记录，再填写表单。

### 2. 04-payroll 重跑验证

`e2e/specs/04-payroll.spec.ts` 的 lock 逻辑修改已完成（approve 后 autoLock，去掉显式 lock 调用）。只需重跑 `pnpm test:e2e --grep "算薪主流程"` 验证通过。

### 3. 完整 E2E 6/6 验证

清理 draft 记录 + 重跑后，执行完整 `pnpm test:e2e`，确保 6/6 全绿。

### 4. 缺陷 5 压测重跑

E2E 全绿后，执行 `pnpm test:load`，验证 100 VU 阶段业务接口 0 个 429、错误率 < 1%、p95 < 2000ms。把 artillery 汇总写进报告。

### 5. 沙箱红线（再次强调）

**严禁使用 `/tmp/` 或任何工作区外路径**。所有调试脚本、probe 文件、临时解压**必须**写在：
- `server/scripts/fixes/tmp/`（已在 .gitignore）
- 或 `e2e/`、`loadtest/tmp/` 等工作区内目录

**允许命令白名单**：
- `node` / `pnpm` / `pnpm exec playwright` / `npx`
- `curl`（不落文件到工作区外）
- `cat` / `ls` / `grep` / `rm` / `cd`

**严禁**：
- `mkdir -p /tmp/...` / `unzip -d /tmp/...` / 任何 `/tmp` 路径
- `python` / `python3`
- 读取 `.env`
- `powershell`（Git Bash 中不可用）

### 6. 交付报告

按 M5-09 原提示词 §7.2 模板输出完整交付报告（含「红线自检」）。

---

**执行顺序**：
1. 修改 `e2e/specs/03-leave.spec.ts` 增加 draft 清理逻辑
2. 重跑 `pnpm test:e2e --grep "算薪主流程"` 验证 04-payroll
3. 重跑完整 `pnpm test:e2e` 验证 6/6
4. 跑 `pnpm test:load` 验证缺陷 5
5. 输出交付报告
