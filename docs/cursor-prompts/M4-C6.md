# M4-C6 销售提成核算（与 D5 联动 + 季度结算）Cursor 提示词

> **任务 ID**：M4-C6
> **任务名称**：M4 薪酬核算 C6 切片 — 销售提成季度结算（与 D5 联动 + 4 维度汇总 + 报表）
> **任务范围**：5 子任务 / 3d 工时 / V1.2 §二.5.4 + §四.8 遗留 + §四.7 D5 联动
> **提示词生成日期**：2026-08-27
> **目标单测增量**：约 26-32 个（M4 中段，753 → 779-785）
> **目标 commit**：1 个业务 commit + 1 个提示词 commit

---

## ⚠️ 强约束（最优先阅读）

**历史教训**（13 次）：
1. M0.5-5 任务中，Cursor 越界做了 M0.5-6（配置中心）+ M0.5-7（改密 + 2FA）共 2 个不在提示词范围内的切片。本次任务严禁重蹈覆辙。
2. M1-A6 提示词第一次瘦身到 15KB，Cursor 推断/补全消耗大量 token，返工 1 次。本次任务按 55-65KB 详尽模式写。
3. D1 提示词 §3.3 误列 5 角色包含 finance，**Cursor 主动识别 + 正确处理**。D2-D6 + C1-C5 已修正。C6 沿用 5 角色 = admin / hr / dept_head / executive / employee，**无 finance**（销售提成 "财务确认" 由 hr 兼任）。
4. M2-wrap-up.md §7 误写 D3 = "季度校准会议"。C6 不重蹈覆辙，严格按 V1.2 §四.8 + §二.5.4 实际范围（销售提成核算）。
5. D3 业务代码 0 行改动 D1+D2。D4 升级到 9 service。D5 升级到 11 service。D6 升级到 14 service。C1 升级到 17 service。C2 升级到 20 service。C3 升级到 23 service。C4 升级到 23 service（**D1-D6 + C1+C2+C3 = 23**）。C5 报告声明 "27 service"，**实际为 32 service**（D1-D6 14 + C1-C5 18 = 32，C3 实际 3 个非 4 个，tax_declaration 是 C5 独有）。C6 沿用 **32 service 0 行改动**。
6. D4 验收发现 2 个 B3/B5 跨 UTC 边界日期敏感旧测。已用 vi.useFakeTimers 修复。C6 测试**沿用**此模式。
7. C5 报告"已知问题 2"：V1.2 切片表 C5=劳务费/C6=工资条与本任务 ID「M4-C5」不一致。**C6 沿用 M4-Cn 任务 ID 习惯**，不与 V1.2 切片表强对齐。V1.2 §二.5.4 销售提成 + D5 联动是 M4-C6 的实际范围。
8. C1 路由**首次非 performance 路由**（独立 `server/src/routes/salary.ts`）。C2+C3+C4+C5 路由**追加**在 salary.ts 内（不分新文件）。**C6 路由继续追加**在 salary.ts 内。
9. C1 验证：D1-D6 14 service 0 行改动 + 5 角色无 finance。C2 验证：17 service 0 行改动 + 5 角色无 finance + 不写 employee_salary_history。C3 验证：20 service 0 行改动 + 5 角色无 finance + 0 新表。C4 验证：23 service 0 行改动 + 5 角色无 finance + AI 复用 M0.5-5。C5 验证：32 service 0 行改动（修正 C3 = 3 / C5 = 5）+ 5 角色无 finance + 0 新表 + 银企/个税/报表留 mock。
10. C3 已知问题：Cursor 主动识别提示词 §5.1.4 / §5.3.3 部分"预期税额"与 §3.7 实际税率表算术不一致，按 §3.7 实际算法实现（**更准确**）。C4 类似：算法细节应以 §3 描述为准。**C6 类似**：D5 公式 `finalAmount = baseAmount × (commissionRate + targetBonusRate)` 是加法（vs 实际乘法上浮 20%），**C6 沿用 D5 公式**，**不重新计算**，仅做季度汇总。
11. **C6 关键设计**：
    - **C6 1 新表**（`commission_settlements` 季度结算单），C4 已建 `payroll_runs` + `payslips` + `payslip_items` + D5 已建 `performance_sales_commissions` + `performance_sales_payments` + `performance_sales_products` 全部**只读复用**。
    - **C6 0 联动 C4 算薪**：`payroll_calculation.service.ts` 的 `salesCommissionAmount` 字段继续默认 0，**C6 不主动写 payslip_items**。真正的"季度结算→工资条合并"留二期或 M4-wrap-up 收尾。
    - **C6 0 改 D5 service**：`payoutCommission` 仅标记 + audit（D5 已实现），C6 季度结算单**只读 D5 commissions**，不调用 D5 任何 service 函数。
    - **C6 财务确认由 hr 兼任**（无 finance 角色，按 D1-D5 + C1-C5 教训）。
12. **C6 范围**（V1.2 §二.5.4 销售提成 + M3-wrap-up 遗留"D5 提成发放联动 M4"）：
    - **5 子任务**：4 维度汇总 + 季度结算单生成 + 财务确认 + 失败回滚 + 销售提成报表
    - **1 新表**：`commission_settlements` 季度结算单（status 状态机 + 季度 + 汇总金额 + 财务确认）
    - **8 端点**：4 汇总（summary + 员工/部门/产品维度）+ 4 结算单 CRUD/状态机（create/list/get/confirm/cancel）= 实际 8 端点
    - **4 权限点**：READ / SETTLE / CONFIRM / CANCEL
    - **10 错误码**（73601-73610）
    - **6 configs**（季度起始月 / 确认窗口 / 调整比例 / 默认 groupBy / 报表字段 / mock 模式）
13. **任务 ID 与 V1.2 切片表错位说明**：
    - V1.2 切片表 C5=劳务费（**未实现**，留 M4-wrap-up 二期）
    - V1.2 切片表 C6=工资条+ESS 自助（**已用 M4-C5 实现**，工资条已做，ESS 自助 tab 留前端）
    - V1.2 切片表 C7=薪酬审批流（**已用 M4-C4 实现**，3 级审批 HR→财务→CEO）
    - V1.2 切片表 C8=薪酬数据加密+二次授权（**已用 M0.5-3 + M0.5-7 实现**）
    - **C6 任务 ID「M4-C6」= 销售提成核算（与 D5 联动）**，与 V1.2 切片表 C6 范围不一致
    - **C6 业务交付时**在 V1.2 §四.8.1 **C5 行**（劳务费）末尾追加 "本切片由 M4-C6 业务 commit 在 C5 行标注 '销售提成核算联动'（**未实现**），劳务费通道留二期"

**红线**（违反任何一条 → 任务失败，需回退重做）：

1. **禁止越界做其他切片**：本提示词只授权 "C6 切片（销售提成季度结算 + D5 联动）"。严禁自作主张做 C1+C2+C3+C4+C5（已完成）/ C7（人力成本预算）/ C8（调薪实际执行）/ V1.2 切片表 C5 劳务费。
2. **禁止修改未授权文件**：清单以外的文件全部禁止改动（清单见 §6）。**特别是 D1-D6 + C1 + C2 + C3 + C4 + C5 业务代码（32 个 service）严禁改动**。
3. **禁止修改旧测试文件**：本任务完全不需要改任何测试。如果发现"必须改测试才能跑通"的情况，立即停下，说明原因。
4. **必须报告完整**：交付报告必须列出全部新增/修改文件（即使是 0 个也要说"0 个"），并明确说明"原有用例是否改动"。
5. **禁止"我觉得这也有用"**：任何"顺便做"或"我觉得可以优化"的越界内容一律禁止。包括但不限于：顺手做 C7 人力成本预算 / 顺手做 C8 调薪实际执行 / 顺手做 V1.2 切片表 C5 劳务费通道 / 顺手联动 C4 算薪（`salesCommissionAmount` 继续默认 0） / 顺手写 `employee_salary_history` / 顺手做 ESS 自助 tab / 顺手重写 D5 `payoutCommission` / 顺手加 finance 角色。
6. **C6 范围强约束**（红线 9）：
   - **C6 1 新表**（仅 `commission_settlements` 季度结算单，复用 C4 `payroll_runs`/`payslips`/`payslip_items` + D5 `performance_sales_commissions` + `performance_sales_payments` + `performance_sales_products` 全部只读）
   - **C6 不实现季度结算→工资条合并**（**C4 `salesCommissionAmount` 继续默认 0**，**不写 payslip_items**）
   - **C6 不写 `employee_salary_history`**（C8 调薪实际执行范围）
   - **C6 不重写 D5 service**（D5 14 service 中 D5 3 service `performance_sales_product` / `performance_sales_payment` / `performance_sales_commission` 全部 0 行改动，C6 仅 prisma 读 D5 表）
   - **C6 不联动 C4 算薪**（不调用 `payroll_calculation.service` 任何函数，不写 `payroll_runs` / `payslips` / `payslip_items`）
   - **D1-D6 + C1 + C2 + C3 + C4 + C5 业务代码 0 行改动**（32 service + D1-D6 14 表 + C1+C2+C4+C5 12 表 = 26 表 0 行改动）
   - **复用 M0.5-1 approval 基础设施**（财务确认如需审批，**仅调用** `submitApproval` / `withdraw`）
   - **复用 M0.5-6 configService**（6 项 configs）— 严禁重写
   - **5 角色 RBAC 不含 finance**（D1-D6 + C1-C5 教训延续，财务确认由 hr 兼任）

---

## 1. 任务 ID + 目标

### 1.1 任务 ID

**M4-C6**（薪酬核算 C6 切片 — **销售提成季度结算 + D5 联动 + 4 维度汇总 + 报表**）

### 1.2 任务目标

实现薪酬核算 C6 切片（V1.2 §二.5.4 销售提成 + M3-wrap-up §7 "D5 提成发放联动 M4" 遗留 + §四.7 D5 联动范围），包含 5 子任务：

| 子任务 | 范围 | Service 函数 | 复用表 |
|---|---|---|---|
| 6.1 4 维度汇总 | 员工 / 部门 / 产品 / 期间 4 维度聚合 | `getSummary` / `getEmployeeSummary` / `getDepartmentSummary` | D5 commissions + payments + products + C3 employees |
| 6.2 季度结算单生成 | 状态机 draft → pending_confirm → confirmed / cancelled | `createSettlement` / `listSettlements` / `getSettlement` | 1 新表 `commission_settlements` |
| 6.3 财务确认 | hr 兼任财务确认（无 finance 角色） | `confirmSettlement` | 1 新表 `commission_settlements` |
| 6.4 失败回滚 | 取消已 draft / pending_confirm 状态的结算单 | `cancelSettlement` | 1 新表 `commission_settlements` |
| 6.5 销售提成报表 | 按维度导出 + 汇总字段计算 | `getReport` | D5 commissions（只读聚合） |

**V1.2 §二.5.4 销售提成**：
- 提成 = 回款金额 × 提成比例（D5 已实现，**C6 沿用 D5 公式**）
- 季度结算 = 季度内所有 `status=paid` 的 commissions 汇总（**C6 季度结算单核心逻辑**）
- 提成报表 = 按员工 / 部门 / 产品 / 期间 多维度分析

**V1.2 §四.8 遗留**（M3-wrap-up §7）：
- "D5 提成发放联动 M4"：C6 实现**季度结算单生成 + 财务确认**（取代 D5 单条 payoutCommission）
- "D4 payout 实际扣工资留 M4"：与 C6 无关，**留 C8**

**D5 联动模式（C6 仅读 D5）**：
- C6 通过 prisma 直接读 D5 `performance_sales_commissions` 表（**不 import D5 service**）
- C6 通过 prisma 直接读 D5 `performance_sales_payments` + `performance_sales_products` + `employees` 关联表
- C6 不调用 D5 `calculateCommission` / `payoutCommission` / `listCommissions` 任何 service 函数
- C6 不修改 D5 任何 commission 的 `status` / `paidAt` 字段

**C4 联动模式（C6 不写 C4）**：
- C6 不写 `payroll_runs` / `payslips` / `payslip_items`
- C6 不调用 `payroll_calculation.service` 任何函数
- C6 不调用 `payroll_run.service` 任何函数
- C6 不调用 `payslip.service` 任何函数
- C4 算薪引擎的 `salesCommissionAmount = 0`（C4 已实现，**C6 不联动**）

### 1.3 端点清单（8 个新端点，全部挂到 `/api/salary/commissions/*`）

| # | Method | Path | 子任务 | 权限点 |
|---|---|---|---|---|
| 1 | `GET` | `/api/salary/commissions/summary?groupBy=employee\|department\|product&period=YYYY-MM&quarter=YYYY-Q1` | 6.1 | `SALARY_COMMISSION_READ` |
| 2 | `GET` | `/api/salary/commissions/employees/:employeeId?period=YYYY-MM&quarter=YYYY-Q1` | 6.1 | `SALARY_COMMISSION_READ` |
| 3 | `GET` | `/api/salary/commissions/departments/:departmentId?period=YYYY-MM&quarter=YYYY-Q1` | 6.1 | `SALARY_COMMISSION_READ` |
| 4 | `POST` | `/api/salary/commissions/settlements` | 6.2 | `SALARY_COMMISSION_SETTLE` |
| 5 | `GET` | `/api/salary/commissions/settlements?year=YYYY&status=draft` | 6.2 / 6.3 / 6.4 | `SALARY_COMMISSION_READ` |
| 6 | `GET` | `/api/salary/commissions/settlements/:id` | 6.2 | `SALARY_COMMISSION_READ` |
| 7 | `POST` | `/api/salary/commissions/settlements/:id/confirm` | 6.3 | `SALARY_COMMISSION_CONFIRM` |
| 8 | `POST` | `/api/salary/commissions/settlements/:id/cancel` | 6.4 | `SALARY_COMMISSION_CANCEL` |

**实际端点数**：8 个（不重复 6.1 的 groupBy 维度展开为 4 个端点，3 个明细端点 + 5 个 settlement 端点）

**注**：
- 6.5 销售提成报表**复用** 6.1 端点 + `groupBy=report` 参数（**不增加新端点**）
- 端点 1（`/commissions/summary`）支持 `groupBy=employee|department|product|report` 4 种聚合模式

### 1.4 单测目标

26-32 个新单测，按 3 service 分布：

| Service | describe | it 数 | 目标 |
|---|---|---|---|
| `commission_settlement.service` | 季度结算单 | 12-15 | 创建 / 列表 / 详情 / 状态机 / 财务确认 / 取消 / 重复创建 / 越界 / 越期 |
| `commission_summary.service` | 4 维度汇总 | 8-10 | 4 groupBy / period / quarter / employeeId 过滤 / 无数据 / 汇总字段 |
| `commission_report.service` | 销售提成报表 | 6-8 | 按维度 / 期间 / 汇总字段 / mock |

---

## 2. 上下文

### 2.1 项目位置

- **项目根**：`D:\projects\hrms`
- **主文档**：[`docs/HRMS-V1.2.md`](../../HRMS-V1.2.md) §二.5.4 + §四.8 遗留 + §四.7 D5
- **API 契约**：[`docs/api-spec.md`](../../api-spec.md) §6.x（追加 salary/commissions 端点）
- **OpenAPI**：[`docs/openapi.yaml`](../../openapi.yaml)（追加 /salary/commissions 端点）
- **错误码**：[`docs/error-codes.md`](../../error-codes.md) §8.6（C6 用 73601-73610）
- **AI Coding 上下文**：[`AGENTS.md`](../../../AGENTS.md)

### 2.2 当前状态（M0+M0.5+M1+M2+M3+C1+C2+C3+C4+C5 已全部完成）

- **C1 业务 commit**：`265316b feat(salary): M4-C1 薪级薪档 + 员工薪酬方案`
- **C2 业务 commit**：`5704c0b feat(salary): M4-C2 社保公积金方案`
- **C3 业务 commit**：`92406f1 feat(salary): M4-C3 个税引擎`
- **C4 业务 commit**：`a71f7c9 feat(salary): M4-C4 算薪引擎 + 算薪流程 + AI 算薪校验摘要`
- **C5 业务 commit**：`8485d08 feat(salary): M4-C5 工资条 + 银企代发 + 个税申报 + 工资表导出`
- **测试**：753/753 通过
- **C1+C2+C4+C5 已建 12 张 salary 表**（C3 0 表）— C6 1 新表（`commission_settlements`），其余**只读复用** D5 `performance_sales_commissions` + `performance_sales_payments` + `performance_sales_products` + D1 `employees` + D1 `departments`

**C6 严禁修改**：
- ❌ C1 3 service（salary_grade / salary_grade_level / salary_plan）
- ❌ C2 3 service（social_insurance_scheme / housing_fund_scheme / employee_insurance）
- ❌ C3 3 service（tax_calculation / tax_labor_income / tax_year_end_bonus）
- ❌ C4 4 service（payroll_calculation / payroll_run / payslip / payroll_ai_summary）
- ❌ C5 5 service（payslip_generator / payslip_delivery / banking_export / tax_declaration / report_export）
- ❌ D1 4 service（performance_cycle / performance_indicator / performance_scheme / performance_coefficient）
- ❌ D2 3 service（performance_record / performance_score / performance_ai_suggestion）
- ❌ D3 2 service（performance_grade / performance_calibration）
- ❌ D4 2 service（performance_payout_config / performance_payout）
- ❌ D5 3 service（performance_sales_product / performance_sales_payment / performance_sales_commission）
- ❌ C1+C2+C4+C5 共 12 张表
- ❌ D1-D6 共 14 张表（含 D5 3 张 sales_commissions + sales_payments + sales_products）
- ❌ C1 4 + C2 4 + C3 3 + C4 5 + C5 4 = 20 权限点（仅追加 4 个 C6 权限点）
- ❌ C1 7 + C2 9 + C3 6 + C4 12 + C5 7 = 41 端点（仅追加 8 个 C6 端点）
- ❌ C1 5 + C2 6 + C3 10 + C4 6 + C5 6 = 33 configs（仅追加 6 项 C6 configs）
- ❌ C1+C2+C3+C4+C5 路由（`server/src/routes/salary.ts` 内 C1-C5 子路由，C6 路由**追加**在 commissions 子路由）
- ❌ M0.5-1 approval.service.ts（**仅调用** `submitApproval` / `withdraw`）
- ❌ M0.5-2 notification.service.ts（**仅调用** `sendNotification`，C6 不发邮件，留 mock 模式）
- ❌ M0.5-5 ai/score.service.ts（仅调用，C6 报表不调 AI）
- ❌ M0.5-6 config.service.ts（**仅调用** `getValue` / `setValue`）

### 2.3 已有可复用模块（C6 必复用，零重写）

| 复用对象 | 来源 | 用途 | C6 用法 |
|---|---|---|---|
| `auditService.auditLog` | M0.5-8 | 增删改 + 关键读 | 必用（8 个端点全部写审计） |
| `configService.getValue` | M0.5-6 | 6 项 configs | 必用（季度起始月 / 确认窗口 / 调整比例 / 默认 groupBy / 报表字段 / mock 模式） |
| `prisma` | server | 30+ 张表直接查询 | 必用（commissions + payments + products + employees + departments + 新表 commission_settlements） |
| `approvalService.submitApproval` | M0.5-1 | 财务确认（可选，**C6 留审批流走 mock**，直接 status 切换） | **仅调用**，**不实现审批**（避免增加 M0.5-1 改动） |
| D5 `performance_sales_commissions` | M3-D5 | 提成记录（status=paid 已发放） | **prisma 只读**，不 import D5 service |
| D5 `performance_sales_payments` | M3-D5 | 回款记录 | **prisma 只读**，关联查询 |
| D5 `performance_sales_products` | M3-D5 | 产品字典 | **prisma 只读**，关联查询 |
| D1 `employees` | M3-D1 | 员工档案 | **prisma 只读**（姓名 / 部门 / 离职状态） |
| D1 `departments` | M3-D1 | 部门档案 | **prisma 只读**（部门名称） |

### 2.4 业务规则来源

- **V1.2 §二.5.4 销售提成**：
  - 提成 = 回款金额 × 提成比例（D5 公式，C6 沿用）
  - 提成记录状态：calculated（已计算） / paid（已发放） / cancelled（已取消）
  - 季度结算 = 季度内所有 paid commissions 汇总
- **V1.2 §四.8 遗留**（M3-wrap-up §7）：
  - "D5 提成发放联动 M4" → C6 实现季度结算单
- **V1.2 §四.7 D5 销售提成**：
  - 提成计算公式 = baseAmount × (commissionRate + targetBonusRate)
  - targetBonusRate 默认 0（D5 暂不实现销售目标表）
  - 财务确认由 hr 兼任（D1 教训延续，**无 finance 角色**）

---

## 3. 必读约束（项目级）

### 3.1 代码范式

- **不用 class + 静态方法**：统一 `export function xxx() {}` 范式
- **JSDoc + 校验链**：每个 service 函数必须含 JSDoc（参数 + 返回值 + 校验链 + 算法步骤 + 错误码 + D5 联动说明）
- **错误统一三参**：抛 `new AppError(message, statusCode, code)`，code 必查错误码表
- **业务规则必走 configService**：禁止硬编码（V1.2 §三.5 强约束 + mock 模式必须走 `configs.salary.commission.settlement.mock_mode`）
- **审计必写**：8 个端点全部写 `auditService.auditLog(...)`（含 `actorType='USER'`）

### 3.2 数据模型约定

- **C6 1 新表**：`commission_settlements`（季度结算单）
  - 完整 schema 详见 §5.2
  - 状态机：`draft → pending_confirm → confirmed / cancelled`（不能再 → confirmed）
  - 关联：C6 仅 prisma 关联 D5 commissions（**不写外键**，避免 D5 schema 改动）
- **TS 类型**：PascalCase（`CommissionSettlement` / `CommissionSummary` / `CommissionReport`）
- **API 路径**：kebab-case（`/api/salary/commissions/settlements/:id/confirm`）

### 3.3 RBAC 5 角色（**无 finance**）

```
admin / hr / dept_head / executive / employee
```

**C6 新增 4 个权限点**（追加到 `permissions` 数组，不修改 ROLE_PERMISSIONS 结构）：
- `SALARY_COMMISSION_READ`（查看汇总 + 详情 + 报表）
- `SALARY_COMMISSION_SETTLE`（创建季度结算单）
- `SALARY_COMMISSION_CONFIRM`（财务确认，hr 兼任）
- `SALARY_COMMISSION_CANCEL`（取消结算单）

**C6 权限点分配**（5 角色）：
- admin：`['*']` WILDCARD 通配
- hr：4 个全开（HR 创建结算 + 财务确认 + 取消 + READ）
- executive：READ（决策权 + 审计）+ CONFIRM（CEO 决策权）
- dept_head：READ（看本部门员工提成）
- employee：仅 READ（看本人提成）

**注**：C6 仍保持 5 角色 = admin / hr / dept_head / executive / employee，**无 finance**。销售提成"财务确认"由 hr / executive 兼任（D1-D5 + C1-C5 教训延续）。

### 3.4 错误码段位（C6 用 73601-73610，10 个）

| code | HTTP | 名称 | 触发条件 | 客户端处理 |
|---|---|---|---|---|
| 73601 | 400 | `COMMISSION_SUMMARY_FAILED` | 4 维度汇总查询失败 | 提示重试 |
| 73602 | 404 | `COMMISSION_SETTLEMENT_NOT_FOUND` | settlementId 不存在 | 提示 ID 错误 |
| 73603 | 400 | `COMMISSION_SETTLEMENT_ALREADY_CONFIRMED` | 已 confirmed 不能再次 confirm | 提示状态 |
| 73604 | 400 | `COMMISSION_SETTLEMENT_INVALID_STATUS` | 状态机非法（draft/confirmed 不能 cancel 已 cancelled） | 提示状态 |
| 73605 | 400 | `COMMISSION_SETTLEMENT_QUARTER_INVALID` | quarter 格式错（不是 1-4 或 year-quarter） | 提示格式 |
| 73606 | 400 | `COMMISSION_SETTLEMENT_NO_COMMISSIONS` | 季度内无 paid commissions | 提示无数据 |
| 73607 | 400 | `COMMISSION_SETTLEMENT_ALREADY_EXISTS` | 同一 (year, quarter) 已存在 draft/pending_confirm 结算单 | 提示存在 |
| 73608 | 400 | `COMMISSION_SETTLEMENT_CANCEL_FAILED` | 取消失败（已 confirmed 不能 cancel） | 提示状态 |
| 73609 | 400 | `COMMISSION_SETTLEMENT_CONFIRM_WINDOW_EXPIRED` | 确认窗口已过（默认 7 天） | 提示已过期 |
| 73610 | 400 | `COMMISSION_SETTLEMENT_D5_DATA_MISSING` | D5 commissions 数据缺失（关联查询失败） | 提示 D5 异常 |

### 3.5 审计埋点（C6 5 类 action）

| action | resource | 触发 |
|---|---|---|
| `COMMISSION_SUMMARY_QUERY` | `commission_summary` | GET /commissions/summary 或 /employees/:id 或 /departments/:id |
| `COMMISSION_SETTLEMENT_CREATE` | `commission_settlement` | POST /commissions/settlements |
| `COMMISSION_SETTLEMENT_CONFIRM` | `commission_settlement` | POST /commissions/settlements/:id/confirm |
| `COMMISSION_SETTLEMENT_CANCEL` | `commission_settlement` | POST /commissions/settlements/:id/cancel |
| `COMMISSION_REPORT_QUERY` | `commission_report` | GET /commissions/summary?groupBy=report |

`actorType`：HR/executive/管理员/dept_head = 'USER'；employee 查本人 = 'USER'；BullMQ 自动 = 'SYSTEM'

### 3.6 业务规则配置化（6 项 configs，C6 全配齐）

所有 6 项必须走 `configService.getValue(...)` + fallback + TODO 注释：

| config key | 类型 | 默认值 | 用途 |
|---|---|---|---|
| `salary.commission.settlement.fiscal_quarter_start` | number | `1` | 财年 Q1 起始月（1-12，默认 1=自然年） |
| `salary.commission.settlement.confirm_window_days` | number | `7` | 结算单确认窗口（默认 7 天，超期 73609） |
| `salary.commission.settlement.max_adjustment_ratio` | number | `0.5` | 最大调整比例（默认 50%，超过抛 73606） |
| `salary.commission.summary.default_group_by` | string | `'employee'` | 默认 groupBy 维度（employee / department / product / report） |
| `salary.commission.report.summary_fields` | string[] | `['totalAmount', 'recordCount', 'employeeCount']` | 报表汇总字段 |
| `salary.commission.settlement.mock_mode` | boolean | `true` | **mock 模式**（C6 强制 true，**不联动 C4 算薪**） |

**总 configs**：86（C1+C2+C3+C4+D1-D6 84 + C5 6 = 90）+ 6（C6）= **96 项**

**注**：C6 留 mock 模式（`salary.commission.settlement.mock_mode = true`），**不联动 C4 算薪**（不写 `payroll_runs` / `payslips` / `payslip_items`）。真正的"季度结算→工资条合并"留二期或 M4-wrap-up 收尾。

---

## 4. 实施任务

### 4.1 子任务 6.1：4 维度汇总（commission_summary.service.ts）

**0 新表**（仅读 D5 commissions + D1 employees + D1 departments）

**Service 函数**：
- `getSummary(actorId, filter)` — 4 维度汇总查询（groupBy: employee | department | product | report）
- `getEmployeeSummary(actorId, employeeId, filter)` — 员工维度汇总（含员工姓名 + 部门）
- `getDepartmentSummary(actorId, departmentId, filter)` — 部门维度汇总（含部门名称 + 员工数）

**4 维度定义**：
- 员工维度：按 `commission.employeeId` 聚合，含 `employeeName` / `departmentId` / `departmentName` / `totalAmount` / `recordCount` / `periods[]`
- 部门维度：按 `employee.departmentId` 聚合，含 `departmentName` / `employeeCount` / `totalAmount` / `recordCount` / `periods[]`
- 产品维度：按 `commission.productId` 聚合，含 `productCode` / `productName` / `category` / `totalAmount` / `recordCount` / `periods[]`
- 报表维度：所有 paid commissions 跨维度汇总，含 `totalAmount` / `totalBaseAmount` / `totalCommissionRate` / `recordCount` / `employeeCount` / `productCount` / `periods[]`

**完整 schema + JSDoc + 错误码触发条件 + 测试断言**：见 §5.1。

### 4.2 子任务 6.2：季度结算单生成（commission_settlement.service.ts）

**1 新表**（`commission_settlements` 季度结算单）

**Service 函数**：
- `createSettlement(actorId, input)` — 创建季度结算单（status=draft 或 pending_confirm）
- `listSettlements(actorId, filter)` — 列表查询（按 year / status / periodStart / periodEnd 过滤）
- `getSettlement(actorId, id)` — 详情查询（含状态机 + 关联 commissions 数量）

**状态机**：
```
draft → pending_confirm（创建时可指定 initialStatus，**默认 pending_confirm**）
pending_confirm → confirmed（财务确认）
draft / pending_confirm → cancelled（取消）
confirmed → (终态，不能再切换)
cancelled → (终态，不能再切换)
```

**完整 schema + JSDoc + 错误码触发条件 + 测试断言**：见 §5.2。

### 4.3 子任务 6.3：财务确认（commission_settlement.service.ts confirmSettlement）

**0 新表**（更新 `commission_settlements.status`）

**Service 函数**：
- `confirmSettlement(actorId, id, input?)` — 财务确认（status pending_confirm → confirmed）

**校验链**：
1. 查询 settlement → 不存在抛 73602
2. 状态检查：status === 'pending_confirm' → 否则抛 73603 / 73604
3. 校验 confirm_window：当前时间 - settlement.createdAt <= `salary.commission.settlement.confirm_window_days`（默认 7 天）→ 超期抛 73609
4. 校验 mock_mode = true（C6 强制 mock，**不联动 C4 算薪**）
5. update { status: 'confirmed', confirmedBy: actorId, confirmedAt: now() }
6. 写 audit（COMMISSION_SETTLEMENT_CONFIRM，actorType='USER'）

**完整 JSDoc + 错误码触发条件 + 测试断言**：见 §5.3。

### 4.4 子任务 6.4：失败回滚（commission_settlement.service.ts cancelSettlement）

**0 新表**（更新 `commission_settlements.status`）

**Service 函数**：
- `cancelSettlement(actorId, id, input)` — 取消（status draft / pending_confirm → cancelled）

**校验链**：
1. 查询 settlement → 不存在抛 73602
2. 状态检查：status ∈ ['draft', 'pending_confirm'] → 否则抛 73608
3. update { status: 'cancelled', cancelledBy: actorId, cancelledAt: now(), cancelReason: input.reason }
4. 写 audit（COMMISSION_SETTLEMENT_CANCEL，actorType='USER'）
5. **不修改 D5 commissions 表**（D5 0 改动）

**完整 JSDoc + 错误码触发条件 + 测试断言**：见 §5.4。

### 4.5 子任务 6.5：销售提成报表（commission_report.service.ts）

**0 新表**（复用 6.1 端点 + `groupBy=report`）

**Service 函数**：
- `getReport(actorId, filter)` — 销售提成报表（按维度 / 期间 / 汇总字段）

**汇总字段**（来自 `salary.commission.report.summary_fields`，默认 `['totalAmount', 'recordCount', 'employeeCount']`）：
- `totalAmount` = sum(commissions.finalAmount)
- `totalBaseAmount` = sum(commissions.baseAmount)
- `recordCount` = count(commissions.id)
- `employeeCount` = count(distinct commissions.employeeId)
- `productCount` = count(distinct commissions.productId)
- `periods[]` = distinct commissions.period 列表

**完整 JSDoc + 错误码触发条件 + 测试断言**：见 §5.5。

### 4.6 路由挂载

C6 路由**追加**在 `server/src/routes/salary.ts` 内（**不分新文件**，沿用 C1+C2+C3+C4+C5 模式）：

```ts
// 已有 C1 + C2 + C3 + C4 + C5 路由
salaryRouter.use('/grades', ...);
salaryRouter.use('/grade-levels', ...);
salaryRouter.use('/plans', ...);
salaryRouter.use('/insurances/social', ...);
salaryRouter.use('/insurances/housing-fund', ...);
salaryRouter.use('/insurances/employees', ...);
salaryRouter.use('/tax', ...);
salaryRouter.use('/payrolls', ...);
salaryRouter.use('/payslips', ...);
salaryRouter.use('/banking', ...);
salaryRouter.use('/tax', taxDeclarationRouter);  // C5 /tax-declare
salaryRouter.use('/reports', ...);

// C6 新增（1 个子路由）
salaryRouter.use('/commissions', commissionRouter);  // 6.1 + 6.2 + 6.3 + 6.4 + 6.5
```

**注**：
- `server/src/routes/index.ts` 已挂载 `/api/salary`（C1 业务已做），C6 不需改 index.ts
- C6 路由设计：`/api/salary/commissions/*`（8 端点全部在 commissions 子路由下）

### 4.7 权限常量追加

在 `server/src/constants/permissions.ts` 追加 4 个 C6 权限点（`SALARY_COMMISSION_READ` / `SALARY_COMMISSION_SETTLE` / `SALARY_COMMISSION_CONFIRM` / `SALARY_COMMISSION_CANCEL`），并在 5 角色（**不**含 finance）中按 §3.3 分配。

### 4.8 Migration

C6 创建 1 个 migration：`server/prisma/migrations/20260911000000_add_salary_c6_commission_settlements/migration.sql`

**仅 commission_settlements 表**，**不修改 D5 + C4 + C5 任何表**。

### 4.9 seed.ts 追加

在 `server/prisma/seed.ts` 追加：
- 6 项 salary.commission.* configs
- 2 组 demo commission_settlements（Q1 2026 confirmed + Q2 2026 draft）
- **不创建 demo D5 commissions / products / payments**（D5 seed 已存在，**C6 不重 seed D5**）

---

## 5. 关键决策点详尽（4 类必须详尽）

### 5.1 4 维度汇总 JSDoc + 测试（子任务 6.1）

#### 5.1.1 不创建新表（C6 子任务 6.1 0 新表）

C6 子任务 6.1 **0 新表**，全部**只读**：
- D5 `performance_sales_commissions`（提成记录，status='paid' 过滤）
- D5 `performance_sales_payments`（回款记录）
- D5 `performance_sales_products`（产品字典）
- D1 `employees`（员工姓名 + 部门 + 离职状态）
- D1 `departments`（部门名称）

#### 5.1.2 JSDoc + 校验链（每个函数 15-30 行）

**`getSummary`**

```ts
/**
 * 4 维度汇总查询（员工 / 部门 / 产品 / 报表）
 * @param actorId 操作人 ID（hr/admin/executive/dept_head/employee）
 * @param filter { groupBy: 'employee' | 'department' | 'product' | 'report', period?: 'YYYY-MM', quarter?: 'YYYY-Q1' }
 * @returns { groupBy, period, totalAmount, items: [...] }
 * @throws AppError(400, 73601) 4 维度汇总查询失败
 * 校验链：
 *  1. 校验 groupBy ∈ ['employee', 'department', 'product', 'report'] → 默认走 configs.salary.commission.summary.default_group_by（默认 'employee'）
 *  2. 校验 period 格式（YYYY-MM）或 quarter 格式（YYYY-Q1/Q2/Q3/Q4）→ 互斥，仅能传一个
 *  3. 校验 quarter 不在白名单（Q1/Q2/Q3/Q4）→ 否则抛 73605
 *  4. 查询 D5 commissions（status='paid' 过滤 + period/quarter 过滤）+ prisma 关联 payments + products + employees + departments
 *  5. 按 groupBy 维度聚合：
 *     - employee: group by commission.employeeId，含 employeeName / departmentId / departmentName / totalAmount / recordCount
 *     - department: group by employee.departmentId，含 departmentName / employeeCount / totalAmount / recordCount
 *     - product: group by commission.productId，含 productCode / productName / category / totalAmount / recordCount
 *     - report: 整体汇总，含 totalAmount / totalBaseAmount / recordCount / employeeCount / productCount / periods[]
 *  6. 返回 { groupBy, period, quarter, totalAmount, items }
 *  7. 写 audit（COMMISSION_SUMMARY_QUERY，actorType='USER'，details: { groupBy, period, quarter, recordCount }）
 *  注：employee 角色仅看自己（filter 加 where.employeeId = actorId.employeeId），dept_head 仅看本部门
 */
export async function getSummary(
  actorId: string,
  filter: { groupBy?: string; period?: string; quarter?: string }
): Promise<CommissionSummaryResult> { ... }
```

**`getEmployeeSummary`**

```ts
/**
 * 员工维度汇总（含员工姓名 + 部门 + 提成明细）
 * @param actorId 操作人 ID
 * @param employeeId 员工 ID
 * @param filter { period?: 'YYYY-MM', quarter?: 'YYYY-Q1' }
 * @returns { employeeId, employeeName, departmentId, departmentName, totalAmount, recordCount, items: [{ commissionId, period, productName, baseAmount, commissionRate, finalAmount }] }
 * @throws AppError(400, 73601) 查询失败
 * 校验链：
 *  1. 查 employee（prisma）→ 不存在抛 404
 *  2. 校验权限：employee 角色仅看自己（actorId.employeeId === employeeId），dept_head 仅看本部门，hr/executive/admin 全开
 *  3. 查 D5 commissions（employeeId + status='paid' + period/quarter 过滤）
 *  4. prisma 关联 payments + products + employees + departments
 *  5. 汇总 totalAmount / recordCount
 *  6. 返回明细 items
 *  7. 写 audit
 */
export async function getEmployeeSummary(
  actorId: string,
  employeeId: string,
  filter?: { period?: string; quarter?: string }
): Promise<CommissionEmployeeSummary> { ... }
```

**`getDepartmentSummary`**

```ts
/**
 * 部门维度汇总（含部门名称 + 员工数 + 提成汇总）
 * @param actorId 操作人 ID
 * @param departmentId 部门 ID
 * @param filter { period?, quarter? }
 * @returns { departmentId, departmentName, employeeCount, totalAmount, recordCount, items: [{ employeeId, employeeName, totalAmount, recordCount }] }
 * @throws AppError(400, 73601) 查询失败
 * 校验链：
 *  1. 查 department（prisma）→ 不存在抛 404
 *  2. 校验权限：dept_head 仅看本部门
 *  3. 查 D5 employees（where departmentId）
 *  4. 查 D5 commissions（employeeId in [...] + status='paid' + period/quarter 过滤）
 *  5. 按 employeeId 聚合
 *  6. 返回汇总
 *  7. 写 audit
 */
export async function getDepartmentSummary(
  actorId: string,
  departmentId: string,
  filter?: { period?: string; quarter?: string }
): Promise<CommissionDepartmentSummary> { ... }
```

#### 5.1.3 错误码触发条件

| code | 触发函数 | 触发条件 | HTTP | 客户端处理 |
|---|---|---|---|---|
| 73601 | getSummary / getEmployeeSummary / getDepartmentSummary | prisma 查询失败 / groupBy 非法 | 400 | 提示重试 |
| 73605 | getSummary | quarter 格式错（不是 Q1/Q2/Q3/Q4） | 400 | 提示格式 |

#### 5.1.4 测试用例（8-10 个 it）

```ts
describe('commission_summary.service', () => {
  describe('getSummary', () => {
    it('employee 维度汇总（含姓名 + 部门 + totalAmount + recordCount）', async () => { ... });
    it('department 维度汇总（含 employeeCount + totalAmount）', async () => { ... });
    it('product 维度汇总（含 productCode + category）', async () => { ... });
    it('report 维度汇总（含 totalBaseAmount + employeeCount + productCount）', async () => { ... });
    it('quarter=2026-Q1 过滤（Q1=1-3月）', async () => { ... });
    it('period=2026-03 过滤（仅 3 月）', async () => { ... });
    it('groupBy 非法抛 73601', async () => { ... });
    it('quarter 格式错抛 73605', async () => { ... });
  });

  describe('getEmployeeSummary', () => {
    it('员工维度汇总（含部门名 + 提成明细 items）', async () => { ... });
    it('employee 角色仅看自己（actorId 校验）', async () => { ... });
    it('员工不存在抛 404', async () => { ... });
  });

  describe('getDepartmentSummary', () => {
    it('部门维度汇总（含 employeeCount + 按员工聚合）', async () => { ... });
    it('dept_head 仅看本部门', async () => { ... });
    it('部门不存在抛 404', async () => { ... });
  });
});
```

### 5.2 季度结算单生成 JSDoc + 测试（子任务 6.2）

#### 5.2.1 1 新表 commission_settlements 完整 schema

```prisma
model CommissionSettlement {
  id              String                    @id @default(uuid()) @db.Uuid
  year            Int                       @db.SmallInt
  quarter         Int                       @db.SmallInt  // 1-4
  status          CommissionSettlementStatus @default(draft)
  totalAmount     Decimal                   @map("total_amount") @db.Decimal(14, 2)
  recordCount     Int                       @map("record_count")
  employeeCount   Int                       @map("employee_count")
  productCount    Int                       @map("product_count")
  periodStart     DateTime                  @map("period_start") @db.Date
  periodEnd       DateTime                  @map("period_end") @db.Date
  confirmedBy     String?                   @map("confirmed_by") @db.Uuid
  confirmedByUser User?                     @relation("CommissionSettlementConfirmer", fields: [confirmedBy], references: [id], onDelete: SetNull)
  confirmedAt     DateTime?                 @map("confirmed_at") @db.Timestamptz(6)
  cancelledBy     String?                   @map("cancelled_by") @db.Uuid
  cancelledByUser User?                     @relation("CommissionSettlementCanceller", fields: [cancelledBy], references: [id], onDelete: SetNull)
  cancelledAt     DateTime?                 @map("cancelled_at") @db.Timestamptz(6)
  cancelReason    String?                   @map("cancel_reason") @db.Text
  remark          String?                   @db.Text
  createdBy       String                    @map("created_by") @db.Uuid
  createdByUser   User                      @relation("CommissionSettlementCreator", fields: [createdBy], references: [id], onDelete: Restrict)
  createdAt       DateTime                  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime                  @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@unique([year, quarter])  // 同一 (year, quarter) 仅允许一个 settlement
  @@index([status, year, quarter])
  @@index([year, quarter])
  @@index([createdBy])
  @@map("commission_settlements")
}

enum CommissionSettlementStatus {
  draft
  pending_confirm
  confirmed
  cancelled
}
```

**User model 关系追加**（**C6 1 处** schema 改动，仅追加 relation 字段，**不动 User 已有字段**）：

```prisma
model User {
  // ... 已有字段 ...
  commissionSettlementsCreated  CommissionSettlement[] @relation("CommissionSettlementCreator")
  commissionSettlementsConfirmed CommissionSettlement[] @relation("CommissionSettlementConfirmer")
  commissionSettlementsCancelled CommissionSettlement[] @relation("CommissionSettlementCanceller")
}
```

#### 5.2.2 JSDoc + 校验链（每个函数 15-30 行）

**`createSettlement`**

```ts
/**
 * 创建季度结算单（V1.2 §二.5.4 销售提成 + §四.8 遗留 D5 联动）
 * @param actorId 操作人 ID（hr/admin）
 * @param input { year: number, quarter: 1|2|3|4, initialStatus?: 'draft' | 'pending_confirm' }
 * @returns 新创建的 CommissionSettlement
 * @throws AppError(400, 73605) quarter 不在 1-4
 * @throws AppError(400, 73606) 季度内无 paid commissions
 * @throws AppError(400, 73607) 同一 (year, quarter) 已存在 draft/pending_confirm 结算单
 * 校验链：
 *  1. 校验 year ∈ [2020, 2099]（防越界）
 *  2. 校验 quarter ∈ {1, 2, 3, 4} → 否则抛 73605
 *  3. 校验 initialStatus ∈ ['draft', 'pending_confirm']（默认 'pending_confirm'，C6 推荐直接进入待确认）
 *  4. 计算 periodStart / periodEnd（按 fiscal_quarter_start 配置，默认 1=自然年）
 *     - Q1: month=fiscal_quarter_start, periodStart=year-MM-01, periodEnd=year-MM+2-月末
 *     - Q2/Q3/Q4 类似
 *  5. 查 D5 commissions（status='paid' + periodStart ≤ period ≤ periodEnd）
 *     - 若无数据抛 73606
 *  6. 校验同一 (year, quarter) 不存在 draft/pending_confirm 状态的 settlement → 否则抛 73607
 *  7. 汇总 totalAmount / recordCount / employeeCount / productCount
 *  8. 事务：create CommissionSettlement（status=initialStatus，createdBy=actorId）
 *  9. 写 audit（COMMISSION_SETTLEMENT_CREATE，actorType='USER'，details: { settlementId, year, quarter, totalAmount, recordCount, employeeCount })
 *  注：
 *  - **不联动 C4 算薪**（C4 `salesCommissionAmount` 继续默认 0）
 *  - **不写 payslip_items**（季度结算→工资条合并留二期）
 *  - **不写 D5 commissions**（D5 0 改动）
 */
export async function createSettlement(
  actorId: string,
  input: { year: number; quarter: 1 | 2 | 3 | 4; initialStatus?: 'draft' | 'pending_confirm' }
): Promise<CommissionSettlement> { ... }
```

**`listSettlements`**

```ts
/**
 * 季度结算单列表查询
 * @param actorId 操作人 ID
 * @param filter { year?, status?, periodStart?, periodEnd?, page?, pageSize? }
 * @returns { items, total, page, pageSize }
 * @throws AppError(400) 分页参数不合法
 * 校验链：
 *  1. 校验 page ≥ 1 / pageSize ∈ [1, 100]
 *  2. 查 commission_settlements（按 filter 过滤）
 *  3. 按 createdAt desc 排序
 *  4. 返回分页
 *  5. 写 audit
 */
export async function listSettlements(
  actorId: string,
  filter: { year?: number; status?: string; periodStart?: string; periodEnd?: string; page?: number; pageSize?: number }
): Promise<PaginatedSettlements> { ... }
```

**`getSettlement`**

```ts
/**
 * 季度结算单详情查询
 * @param actorId 操作人 ID
 * @param id settlement ID
 * @returns CommissionSettlement（含 confirmedBy / cancelledBy 用户名 + 关联 commissions 数量）
 * @throws AppError(404, 73602) settlement 不存在
 * 校验链：
 *  1. 查 settlement（含 confirmedByUser / cancelledByUser / createdByUser）→ 不存在抛 73602
 *  2. 校验权限：employee 仅看自己作为受益人的（**C6 留 mock，employee 通常无 view settlement 权限**，但兜底校验）
 *  3. 关联查询：prisma 聚合 D5 commissions 数量（不返回 commissions 明细，避免数据量过大）
 *  4. 返回 settlement
 *  5. 写 audit
 */
export async function getSettlement(
  actorId: string,
  id: string
): Promise<CommissionSettlementDetail> { ... }
```

#### 5.2.3 错误码触发条件

| code | 触发函数 | 触发条件 | HTTP | 客户端处理 |
|---|---|---|---|---|
| 73602 | getSettlement / confirmSettlement / cancelSettlement | settlementId 不存在 | 404 | 提示 ID |
| 73605 | createSettlement | quarter 不在 1-4 | 400 | 提示格式 |
| 73606 | createSettlement | 季度内无 paid commissions | 400 | 提示无数据 |
| 73607 | createSettlement | 同一 (year, quarter) 已存在 draft/pending_confirm 结算单 | 400 | 提示存在 |

#### 5.2.4 测试用例（6-8 个 it）

```ts
describe('commission_settlement.service', () => {
  describe('createSettlement', () => {
    it('创建 Q1 2026 季度结算单成功（默认 pending_confirm）', async () => { ... });
    it('创建时 initialStatus=draft 成功', async () => { ... });
    it('quarter 不在 1-4 抛 73605', async () => { ... });
    it('季度内无 paid commissions 抛 73606', async () => { ... });
    it('同一 (year, quarter) 已存在 draft settlement 抛 73607', async () => { ... });
    it('汇总 totalAmount / recordCount / employeeCount / productCount 正确', async () => { ... });
    it('periodStart / periodEnd 按 fiscal_quarter_start 计算（默认 1=自然年）', async () => { ... });
  });

  describe('listSettlements', () => {
    it('按 year + status 过滤列表', async () => { ... });
    it('分页正确（page=1 / pageSize=10）', async () => { ... });
  });

  describe('getSettlement', () => {
    it('详情查询（含关联 commissions 数量）', async () => { ... });
    it('settlement 不存在抛 73602', async () => { ... });
  });
});
```

### 5.3 财务确认 JSDoc + 测试（子任务 6.3）

#### 5.3.1 JSDoc + 校验链

**`confirmSettlement`**

```ts
/**
 * 财务确认季度结算单（status pending_confirm → confirmed，hr 兼任）
 * @param actorId 操作人 ID（hr/admin/executive，**无 finance 角色**）
 * @param id settlement ID
 * @param input { remark?: string }
 * @returns 更新后的 CommissionSettlement
 * @throws AppError(404, 73602) settlement 不存在
 * @throws AppError(400, 73603) settlement 已 confirmed
 * @throws AppError(400, 73604) settlement 状态非法（cancelled 不能 confirm）
 * @throws AppError(400, 73609) 确认窗口已过（默认 7 天）
 * @throws AppError(400, 73610) D5 数据缺失（关联查询失败）
 * 校验链：
 *  1. 查询 settlement → 不存在抛 73602
 *  2. 状态检查：status === 'pending_confirm' → 否则抛 73603 / 73604
 *  3. 校验 confirm_window：当前时间 - settlement.createdAt <= configs.salary.commission.settlement.confirm_window_days（默认 7 天）→ 超期抛 73609
 *  4. 校验 mock_mode = true（C6 强制 mock，**不联动 C4 算薪**）
 *  5. 校验 D5 commissions 数据完整性（重新查询一次）→ 缺失抛 73610
 *  6. 事务：update { status: 'confirmed', confirmedBy: actorId, confirmedAt: now() }
 *  7. 写 audit（COMMISSION_SETTLEMENT_CONFIRM，actorType='USER'，details: { settlementId, totalAmount, recordCount, mockMode: true }）
 *  注：
 *  - **不联动 C4 算薪**（mock 模式，**C4 `salesCommissionAmount` 继续默认 0**）
 *  - **不写 payslip_items**（季度结算→工资条合并留二期）
 *  - **不调用 M0.5-1 approval**（C6 留 mock，财务确认由 hr/executive 直接确认，不走审批流）
 *  - **hr 兼任财务确认**（D1-D5 + C1-C5 教训延续，**无 finance 角色**）
 */
export async function confirmSettlement(
  actorId: string,
  id: string,
  input?: { remark?: string }
): Promise<CommissionSettlement> { ... }
```

#### 5.3.2 测试用例（4-5 个 it，**mock 模式**）

```ts
describe('confirmSettlement', () => {
  it('pending_confirm → confirmed 成功', async () => { ... });
  it('已 confirmed 抛 73603', async () => { ... });
  it('cancelled 状态抛 73604', async () => { ... });
  it('超 confirm_window（默认 7 天）抛 73609', async () => { ... });
  it('audit 记录 mockMode: true + confirmedBy', async () => { ... });
  it('不写 payslip_items（mock 模式）', async () => { ... });
});
```

### 5.4 失败回滚 JSDoc + 测试（子任务 6.4）

#### 5.4.1 JSDoc + 校验链

**`cancelSettlement`**

```ts
/**
 * 取消季度结算单（status draft / pending_confirm → cancelled）
 * @param actorId 操作人 ID（hr/admin）
 * @param id settlement ID
 * @param input { reason: string }
 * @returns 更新后的 CommissionSettlement
 * @throws AppError(404, 73602) settlement 不存在
 * @throws AppError(400, 73608) 已 confirmed 不能 cancel
 * 校验链：
 *  1. 查询 settlement → 不存在抛 73602
 *  2. 状态检查：status ∈ ['draft', 'pending_confirm'] → 否则抛 73608
 *  3. 校验 reason 非空（必填）
 *  4. 事务：update { status: 'cancelled', cancelledBy: actorId, cancelledAt: now(), cancelReason: input.reason }
 *  5. 写 audit（COMMISSION_SETTLEMENT_CANCEL，actorType='USER'，details: { settlementId, reason }）
 *  注：
 *  - **不修改 D5 commissions 表**（D5 0 改动，C6 仅修改 commission_settlements 表）
 *  - **已 confirmed 的结算单不能 cancel**（状态机终态）
 *  - **不联动 C4 算薪**（mock 模式）
 */
export async function cancelSettlement(
  actorId: string,
  id: string,
  input: { reason: string }
): Promise<CommissionSettlement> { ... }
```

#### 5.4.2 测试用例（3-4 个 it）

```ts
describe('cancelSettlement', () => {
  it('draft → cancelled 成功', async () => { ... });
  it('pending_confirm → cancelled 成功', async () => { ... });
  it('已 confirmed 抛 73608', async () => { ... });
  it('reason 必填校验', async () => { ... });
});
```

### 5.5 销售提成报表 JSDoc + 测试（子任务 6.5）

#### 5.5.1 JSDoc + 校验链

**`getReport`**

```ts
/**
 * 销售提成报表（按维度 / 期间 / 汇总字段）
 * @param actorId 操作人 ID
 * @param filter { period?, quarter?, groupBy?: 'employee' | 'department' | 'product' | 'overall', summaryFields?: string[] }
 * @returns { filter, groupBy, totalAmount, summary: {...}, items: [...] }
 * @throws AppError(400, 73601) 查询失败
 * 校验链：
 *  1. 校验 groupBy ∈ ['employee', 'department', 'product', 'overall']（默认 'overall'）
 *  2. 校验 period 格式（YYYY-MM）或 quarter 格式（YYYY-Q1/Q2/Q3/Q4）→ 互斥
 *  3. 校验 summaryFields 走 configs.salary.commission.report.summary_fields（默认 ['totalAmount', 'recordCount', 'employeeCount']）
 *  4. 查 D5 commissions（status='paid' + period/quarter 过滤）
 *  5. 按 groupBy 维度聚合
 *  6. 计算 summary 字段（按 summaryFields）
 *  7. 返回报表
 *  8. 写 audit（COMMISSION_REPORT_QUERY，actorType='USER'，details: { groupBy, period, quarter, summaryFields, totalAmount, recordCount }）
 *  注：
 *  - **不调 M0.5-5 AI**（销售提成报表无 AI，C4 算薪 AI 摘要 + D2 评分建议 + M0.5-5 知识库问答，**C6 报表不调 AI**）
 *  - **不调 M0.5-2 notification**（报表不发邮件，前端直接展示）
 *  - **mock 模式**（`mockMode: true` 字段，**不联动 C4 算薪**）
 */
export async function getReport(
  actorId: string,
  filter: { groupBy?: string; period?: string; quarter?: string; summaryFields?: string[] }
): Promise<CommissionReport> { ... }
```

#### 5.5.2 测试用例（6-8 个 it，**mock 模式**）

```ts
describe('commission_report.service', () => {
  it('overall 维度报表（含 totalAmount + recordCount + employeeCount）', async () => { ... });
  it('employee 维度报表（含每员工明细）', async () => { ... });
  it('department 维度报表（含部门汇总 + 员工数）', async () => { ... });
  it('product 维度报表（含产品分类汇总）', async () => { ... });
  it('period=2026-Q1 过滤', async () => { ... });
  it('quarter=2026-Q2 过滤（Q2=4-6月）', async () => { ... });
  it('audit 记录 summaryFields + mockMode: true', async () => { ... });
  it('groupBy 非法抛 73601', async () => { ... });
});
```

---

## 6. 修改文件清单（明示边界）

### 6.1 必改文件（清单以内）

#### 新增文件（7 个）

1. `server/prisma/migrations/20260911000000_add_salary_c6_commission_settlements/migration.sql`
2. `server/src/services/commission_summary.service.ts`
3. `server/src/services/commission_summary.service.test.ts`
4. `server/src/services/commission_settlement.service.ts`
5. `server/src/services/commission_settlement.service.test.ts`
6. `server/src/services/commission_report.service.ts`
7. `server/src/services/commission_report.service.test.ts`
8. `server/src/services/salary/C6-README.md`（C6 模块说明）

#### 修改文件（8 个）

9. `server/prisma/schema.prisma` — 追加 1 张新表（`commission_settlements` + enum `CommissionSettlementStatus`）+ 1 个 model 关系（User 上 3 个 relation 字段）
10. `server/prisma/seed.ts` — 追加 6 项 salary.commission.* configs + 2 组 demo commission_settlements
11. `server/src/routes/salary.ts` — **追加** 1 个 C6 子路由（`/commissions`），**不删 C1-C5 路由**
12. `server/src/constants/permissions.ts` — 追加 4 个 C6 权限点 + 5 角色分配
13. `docs/api-spec.md` — §6.x salary C6 端点
14. `docs/openapi.yaml` — 8 端点 + 8 schema
15. `docs/error-codes.md` — §8.6 C6 错误码（73601-73610，10 个）
16. `docs/HRMS-V1.2.md` — §四.8 状态更新（V1.2 切片表 C5 行追加 "本切片由 M4-C6 业务 commit 在 C5 行标注 '销售提成核算联动'（**未实现**），劳务费通道留二期"）+ §七.3 追加 C6.0.1
17. `docs/flow-diagrams.md` — §4.x 追加销售提成季度结算流程图

**注**：
- `server/src/routes/index.ts` 0 改动（C1 已挂载 /api/salary）
- 实际清单数：8 新增 + 9 修改 = 17 个文件（C6 提示词 §1.4 目标 28 测试 实际可能 26-32 个）

### 6.2 禁止修改文件（清单以外）

- **D1-D6 + C1 + C2 + C3 + C4 + C5 业务代码**（除上述 8 个新增 + 9 个修改）：
  - ❌ 严禁修改 D1 4 service（performance_cycle / performance_indicator / performance_scheme / performance_coefficient）
  - ❌ 严禁修改 D2 3 service（performance_record / performance_score / performance_ai_suggestion）
  - ❌ 严禁修改 D3 2 service（performance_grade / performance_calibration）
  - ❌ 严禁修改 D4 2 service（performance_payout_config / performance_payout）
  - ❌ 严禁修改 D5 3 service（performance_sales_product / performance_sales_payment / performance_sales_commission）
  - ❌ 严禁修改 C1 3 service（salary_grade / salary_grade_level / salary_plan）
  - ❌ 严禁修改 C2 3 service（social_insurance_scheme / housing_fund_scheme / employee_insurance）
  - ❌ 严禁修改 C3 3 service（tax_calculation / tax_labor_income / tax_year_end_bonus）
  - ❌ 严禁修改 C4 4 service（payroll_calculation / payroll_run / payslip / payroll_ai_summary）
  - ❌ 严禁修改 C5 5 service（payslip_generator / payslip_delivery / banking_export / tax_declaration / report_export）
  - ❌ 严禁修改 D1+D2+D4+D5+C1+C2+C4+C5 共 12 张表
  - ❌ 严禁修改 D1-D6 38 + C1-C5 20 = 58 权限点
  - ❌ 严禁修改 D1-D6 55 + C1-C5 41 = 96 端点
  - ❌ 严禁修改 D1-D6 59 + C1-C5 33 = 92 configs
  - ❌ 严禁修改 `server/src/routes/salary.ts` 中 C1-C5 子路由
- **M0.5 公共底座**：
  - ❌ 严禁修改 M0.5-1 approval.service.ts（**仅调用** `submitApproval` / `withdraw`，C6 留 mock 不调用）
  - ❌ 严禁修改 M0.5-2 notification.service.ts（**仅调用**，C6 不发邮件）
  - ❌ 严禁修改 M0.5-5 ai/score.service.ts（仅调用，C6 报表不调 AI）
  - ❌ 严禁修改 M0.5-6 config.service.ts（**仅调用** `getValue` / `setValue`，**严禁重写** config service）
- **M1 + M2 业务代码**：
  - ❌ 严禁修改 M1 / M2 任何 service（C6 仅 prisma 读 D1 employees / departments 表）
- 任何旧测试文件（`*.test.ts`）除非在 §6.1 清单内（仅 3 个新测试）

### 6.3 越界检测（交付时自检）

```bash
git diff --name-only HEAD
# 必须只出现 §6.1 列出的 17 个文件
# 如果出现其他文件 → 越界，必须回退

# 特别检查：D1-D6 + C1-C5 共 32 service 0 行改动
git diff HEAD -- 'server/src/services/performance_*.service.ts' 'server/src/services/salary_*.service.ts' 'server/src/services/social_insurance_scheme.service.ts' 'server/src/services/housing_fund_scheme.service.ts' 'server/src/services/employee_insurance.service.ts' 'server/src/services/tax_*.service.ts' 'server/src/services/payroll_*.service.ts' 'server/src/services/payslip.service.ts' 'server/src/services/banking_export.service.ts' 'server/src/services/report_export.service.ts' 'server/src/services/payslip_generator.service.ts' 'server/src/services/payslip_delivery.service.ts'
# 必须为 0 行

# 验证 1 新表
ls server/prisma/migrations/ | grep -E "add_salary_c6"
# 必须为 1 结果（C6 1 migration）

# 验证 mock 模式
grep -rE "mockMode: true|mock_mode.*=.*true" server/src/services/commission_settlement.service.ts server/src/services/commission_report.service.ts
# 必须 ≥ 2 条结果

# 验证 5 角色无 finance
grep -i "finance" server/src/constants/permissions.ts
# 必须 0 命中（5 角色无 finance）

# 验证 D5 0 改动
git diff HEAD -- 'server/src/services/performance_sales_*.service.ts'
# 必须 0 行

# 验证 C4 0 联动
grep -rE "import.*payroll_calculation|import.*payroll_run|import.*payslip\b" server/src/services/commission_*.service.ts
# 必须 0 命中（C6 不 import C4 任何 service）
```

---

## 7. 验收标准

### 7.1 硬性验收（必须 100% 通过）

```bash
# 在项目根 D:\projects\hrms
pnpm --filter hrms-server type-check    # 必须 0 错
pnpm --filter hrms-server lint          # 必须 0 error
pnpm --filter hrms-server test          # 必须 779+ 个测试通过（753 旧 + 26-32 新）
pnpm --filter hrms-client type-check    # 必须 0 错
```

### 7.2 验收明细

- [ ] **1 新表**（`commission_settlements` 1 migration）
- [ ] 8 个端点全部可访问
- [ ] 10 个错误码（73601-73610）全部可触发
- [ ] 4 维度汇总正确（employee / department / product / report）
- [ ] 季度结算单状态机正确（draft → pending_confirm → confirmed / cancelled）
- [ ] 财务确认正确（**hr 兼任，mock 模式**）
- [ ] 失败回滚正确（**不修改 D5 commissions**）
- [ ] 销售提成报表正确（**mock 模式，不调 AI**）
- [ ] 96 项 configs 全部走 configService
- [ ] 5 角色 RBAC 权限分配正确（**无 finance**）
- [ ] audit log 写入正确（5 类 action）
- [ ] 0 旧测试改动
- [ ] 0 越界
- [ ] **D1-D6 + C1-C5 共 32 service 0 行改动**

### 7.3 红线 9 验证

- [ ] **C6 1 新表**（仅 `commission_settlements`，复用 C4 + D5 表）
- [ ] **C6 不实现季度结算→工资条合并**（**C4 `salesCommissionAmount` 继续默认 0**，**不写 payslip_items**）
- [ ] **C6 不写 `employee_salary_history`**（C8 调薪实际执行范围）
- [ ] **C6 不重写 D5 service**（D5 3 service `performance_sales_product` / `performance_sales_payment` / `performance_sales_commission` 全部 0 行改动）
- [ ] **C6 不联动 C4 算薪**（不调用 `payroll_calculation.service` 任何函数，不写 `payroll_runs` / `payslips` / `payslip_items`）
- [ ] **D1-D6 + C1-C5 业务代码 0 行改动**（32 service + 26 表 = 0 行改动）
- [ ] **复用 M0.5-6 configService**（6 项 configs）— 严禁重写
- [ ] **mock 模式**（`salary.commission.settlement.mock_mode = true`，**不联动 C4 算薪**）
- [ ] **5 角色 RBAC 不含 finance**（D1-D6 + C1-C5 教训延续，财务确认由 hr 兼任）

### 7.4 原有用例未改（硬要求）

```bash
git diff HEAD -- '*.test.ts' | grep -E '^[+-]' | grep -v '^[+-]{3}' | grep -v 'commission_summary|commission_settlement|commission_report'
# 必须为 0 行（原有用例 100% 未改）
```

---

## 8. 禁止做的事（额外禁忌清单）

1. ❌ 做 C7 人力成本预算（留独立任务）
2. ❌ 做 C8 调薪实际执行（留独立任务）
3. ❌ 做 V1.2 切片表 C5 劳务费（**留二期**，C6 不在 V1.2 切片表 C6 范围）
4. ❌ 联动 C4 算薪（**C4 `salesCommissionAmount` 继续默认 0**）
5. ❌ 写 `payslip_items`（**C6 季度结算→工资条合并留二期**）
6. ❌ 写 `payroll_runs` / `payslips`（同上）
7. ❌ 重写 D5 `payoutCommission` / `calculateCommission`（D5 0 改动）
8. ❌ 调用 D5 任何 service 函数（**C6 仅 prisma 读 D5 表**）
9. ❌ 调用 C4 任何 service 函数（**C6 不联动 C4 算薪**）
10. ❌ 写 `employee_salary_history`（C8 调薪实际执行范围）
11. ❌ 创建任何第 2 张新表（C6 仅 1 新表 `commission_settlements`）
12. ❌ 创建除 1 个 migration 外的额外 migration
13. ❌ 改 D1-D6 任何 service（不重做 M3）
14. ❌ 改 C1-C5 任何 service（不重做 M4 早期切片）
15. ❌ 改 M0.5-1 approval.service.ts（C6 留 mock，**不调用** approval）
16. ❌ 改 M0.5-2 notification.service.ts（**仅调用**，C6 不发邮件）
17. ❌ 改 M0.5-5 ai/score.service.ts（仅调用，C6 报表不调 AI）
18. ❌ 改 M0.5-6 config.service.ts（**仅调用**，**严禁重写** config service）
19. ❌ 改 M1 任何 service（不联动组织人事）
20. ❌ 改 M2 任何 service（C6 仅 prisma 读 D1 表）
21. ❌ 调 M0.5-5 AI 做销售提成报表（**C6 报表不调 AI**）
22. ❌ 调 M0.5-2 notification 发邮件（C6 报表不发邮件）
23. ❌ 实现真实 PDF/Excel 库（**留 mock**）
24. ❌ 修改任何 M0/M0.5/M1/M2/M3/C1/C2/C3/C4/C5 已存在的 .ts 文件（除 §6.1 清单内）
25. ❌ 修改任何旧测试文件（即使 type-check 报错也只新增 import）
26. ❌ 越界做 6 角色 RBAC（M0 锁定 5 角色：admin / hr / dept_head / executive / employee，**无 finance**）
27. ❌ 用 class + 静态方法范式（统一 `export function xxx() {}`）
28. ❌ 硬编码业务规则（6 项 C6 configs 全走 configService + fallback + TODO）
29. ❌ 写 magic number 错误码（必须用 73601-73610 错误码表）
30. ❌ "我觉得这也有用"（任何"顺便做"越界内容一律禁止）
31. ❌ 重写 notification / configService / aiSummary（**M0.5 已有，**严禁重写**）
32. ❌ 给 finance 角色分配权限（D1-D6 + C1-C5 教训延续，hr 兼任财务确认）
33. ❌ mock_mode = false（C6 强制 true，**不联动 C4 算薪**）
34. ❌ 调 M0.5-1 approval 做财务确认（C6 留 mock，hr 直接 confirm，不走审批流）

---

## 9. 完成标准

### 9.1 必跑命令（全部通过才交付）

```bash
cd D:\projects\hrms

# 1. 类型检查
pnpm --filter hrms-server type-check
# 预期：0 错

# 2. Lint
pnpm --filter hrms-server lint
# 预期：0 error（warnings 可接受）

# 3. 测试
pnpm --filter hrms-server test
# 预期：779+ 个测试通过（753 旧 + 26-32 新），0 failed

# 4. 客户端类型检查
pnpm --filter hrms-client type-check
# 预期：0 错
```

### 9.2 越界自检（必查）

```bash
# 1. 越界文件检测
git diff --name-only HEAD
# 预期：§6.1 列出 17 个文件

# 2. service 0 行改动检测
git diff HEAD -- 'server/src/services/performance_*.service.ts' 'server/src/services/salary_*.service.ts' 'server/src/services/social_insurance_scheme.service.ts' 'server/src/services/housing_fund_scheme.service.ts' 'server/src/services/employee_insurance.service.ts' 'server/src/services/tax_*.service.ts' 'server/src/services/payroll_*.service.ts' 'server/src/services/payslip.service.ts' 'server/src/services/banking_export.service.ts' 'server/src/services/report_export.service.ts' 'server/src/services/payslip_generator.service.ts' 'server/src/services/payslip_delivery.service.ts'
# 预期：空（0 行改动）

# 3. 1 新表 migration 检测
ls server/prisma/migrations/ | grep "add_salary_c6"
# 预期：1 个结果（20260911000000_add_salary_c6_commission_settlements）

# 4. mock 模式检测
grep -rE "mockMode: true|mock_mode.*=.*true" server/src/services/commission_settlement.service.ts server/src/services/commission_report.service.ts
# 预期：≥ 2 条结果

# 5. 5 角色无 finance 检测
grep -i "finance" server/src/constants/permissions.ts
# 预期：0 命中

# 6. D5 service 0 改动检测
git diff HEAD -- 'server/src/services/performance_sales_*.service.ts'
# 预期：空（0 行改动）

# 7. C4 不联动检测（C6 不 import C4 service）
grep -rE "import.*payroll_calculation|import.*payroll_run|import.*payslip\b" server/src/services/commission_*.service.ts
# 预期：0 命中

# 8. 旧测试 0 改动检测
git diff HEAD -- '*.test.ts' | grep -E '^[+-]' | grep -v '^[+-]{3}' | grep -v 'commission_summary|commission_settlement|commission_report'
# 预期：0 行
```

### 9.3 交付报告（Cursor 输出格式）

请在交付时输出以下内容（**M4-C5 交付报告格式**）：

```
# M4-C6 交付报告

C6 已落地：销售提成季度结算 + 4 维度汇总 + 财务确认 + 失败回滚 + 销售提成报表。1 新表，未联动 C4 算薪，未改 D1-D6 / C1-C5 业务代码。未提交 commit。

## 改动文件清单（X 新增 + X 修改 = X 个）

**新增（X）**：
1. server/prisma/migrations/20260911000000_add_salary_c6_commission_settlements/migration.sql
2. server/src/services/commission_summary.service.ts
3. server/src/services/commission_summary.service.test.ts
4. server/src/services/commission_settlement.service.ts
5. server/src/services/commission_settlement.service.test.ts
6. server/src/services/commission_report.service.ts
7. server/src/services/commission_report.service.test.ts
8. server/src/services/salary/C6-README.md

**修改（X）**：
9. server/prisma/schema.prisma
10. server/prisma/seed.ts
11. server/src/routes/salary.ts
12. server/src/constants/permissions.ts
13. docs/api-spec.md
14. docs/openapi.yaml
15. docs/error-codes.md
16. docs/HRMS-V1.2.md
17. docs/flow-diagrams.md

**未改（按约束）**：
- server/src/routes/index.ts
- D1-D6 + C1-C5 共 32 service（diff = 0）
- D1-D6 14 表 + C1+C2+C4+C5 12 表
- M0.5-1 approval.service.ts（M0.5 仅调用，C6 留 mock 不调用）
- M0.5-2 notification.service.ts（仅调用）
- M0.5-5 ai/score.service.ts（仅调用）
- M0.5-6 config.service.ts（仅调用）
- employee_salary_history（不写）

## 7 条强制声明

1. 未修改任何 M0/M0.5/M1/M2/M3/C1/C2/C3/C4/C5 旧测试（仍 753 个）
2. 新单测 X 个，总计 X/X 通过
3. 未做 C1-C5 / C7 / C8 / V1.2 切片表 C5 任何切片
4. 未 import 跨业务 service；D5 仅 prisma 读表
5. 5 角色 RBAC 结构不变（admin / hr / dept_head / executive / employee，**无 finance**）
6. **1 新表**（`commission_settlements` 季度结算单，复用 D5 + C4 + D1 表只读）
7. **D1-D6 + C1-C5 业务代码 0 行改动**（32 service diff = 0）

## 红线 9 自检

- [x] 1 新表（commission_settlements）
- [x] 不实现季度结算→工资条合并（C4 salesCommissionAmount 继续默认 0）
- [x] 不写 employee_salary_history
- [x] 不重写 D5 service
- [x] 不联动 C4 算薪
- [x] 32 service 0 行改动
- [x] 复用 M0.5-6 configService
- [x] mock 模式（mock_mode = true）
- [x] 5 角色不含 finance

## 验收命令输出

| 命令 | 结果 |
|---|---|
| pnpm --filter hrms-server type-check | 0 错 |
| pnpm --filter hrms-server lint | 0 error（XX warnings） |
| pnpm --filter hrms-server test | X/X 通过（753 旧 + X 新） |
| pnpm --filter hrms-client type-check | 0 错 |

单测分布：commission_settlement X + commission_summary X + commission_report X = X。

## 越界检查

git diff --name-only HEAD + untracked = 清单内 17 个文件，0 越界。
permissions.ts 无 finance。
旧 *.test.ts diff 为空。

## 已知问题

（如有边界场景未覆盖 / TODO 留二期 / mock 限制等，请详细说明）
```

---

## 10. 参考文档

- **V1.2 主文档** §二.5.4 销售提成：https://example.com/HRMS-V1.2.md#二-5-4-销售提成
- **V1.2 主文档** §四.8 M4 薪酬核算切片表：https://example.com/HRMS-V1.2.md#四-8-m4-薪酬核算
- **V1.2 主文档** §三.5 业务规则配置化：https://example.com/HRMS-V1.2.md#三-5-业务规则配置化
- **M3-wrap-up 报告** §7 遗留任务：https://example.com/cursor-prompts/M3-wrap-up.md
- **C5 业务 commit** 8485d08（28 测试 / 0 新表 / 4 权限点 / 10 错误码）：https://example.com/commit/8485d08
- **C5 提示词** docs/cursor-prompts/M4-C5.md（本任务范式参考）

---

**任务开始**：C6 业务实现 = 5 子任务 + 8 端点 + 1 新表 + 4 权限点 + 10 错误码 + 6 configs + 26-32 单测 + 32 service 0 行改动 + 5 角色无 finance + mock 模式。
