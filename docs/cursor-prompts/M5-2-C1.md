# M5-2-C1 M4 薪酬前端 · 基础配置（C1 薪级薪档与薪酬方案 + C2 社保公积金 + C3 个税工具）Cursor 提示词

> **任务 ID**：M5-2-C1
> **任务名称**：M5-2 第 4 个业务子切片 — M4 薪酬前端 · 基础配置（C1 薪级薪档与薪酬方案 + C2 社保公积金 + C3 个税工具）
> **任务范围**：3 子任务 / 3d 工时 / V1.2 §四.8 C1-C3（任务 ID 优先，不与 V1.2 切片表强对齐）
> **提示词生成日期**：2026-09-01
> **目标单测增量**：约 12-18 个（API 函数 + 视图内联断言 + 状态映射）
> **目标 commit**：1 个业务 commit + 1 个提示词 commit
> **依赖**：[M5-2-B 考勤假勤前端](M5-2-B.md) commit `ff433c7` + 前端测试脚手架 commit `ac43074` 已完成

---

## ⚠️ 强约束（最优先阅读）

**历史教训**（21 次）：
1. M0.5-5 任务中，Cursor 越界做了 M0.5-6 + M0.5-7 共 2 个不在提示词范围内的切片。本次任务严禁重蹈覆辙。
2. M1-A6 提示词第一次瘦身到 15KB，Cursor 推断/补全消耗大量 token，返工 1 次。本次任务按 70-90KB 详尽模式写。
3. D1 提示词 §3.3 误列 5 角色包含 finance，**Cursor 主动识别 + 正确处理**。D2-D6 + C1-C8 + M5-1 + M5-2-0/A1/A2/B 已修正。**M5-2-C1 沿用 5 角色 RBAC 无 finance**。
4. M2-wrap-up.md §7 误写 D3 = "季度校准会议"。M5-2-C1 不重蹈覆辙，严格按 V1.2 §四.8 C1-C3 实际范围（**任务 ID 优先，不与 V1.2 切片表强对齐**，M4 错位专题见 AGENTS.md）。
5. C8 红线 36 service 0 行改动。**M5-2-C1 沿用 36 service 0 行改动**。
6. D4 验收发现 2 个 B3/B5 跨 UTC 边界日期敏感旧测。M5-2-C1 沿用此模式（涉日期用 `vi.useFakeTimers` 锁日期，**但本切片测试不 import vitest**，见红线 10）。
7. M5-2-A1/A2 已落地 4 个关键点（A2 commit `fd9e4a7` 报告），M5-2-B 沿用并验证：**后端实际端点路径与提示词可能不同 → M5-2-C1 必须以 `server/src/routes/salary.ts` 实际代码为准**；路由顺序问题（**必须在列表前**写详情/动作子路由）；真实权限点；工号由后端生成（**不传**）。
8. **M5-2-B 关键设计沿用**（commit `ff433c7`）：0 修改后端 / 0 修改 client/package.json / 一个模块一个独立 Layout / 无详情的模块在 List 内 Dialog 展开。
9. **M5-2-C1 后端端点确认**（3 模块 22 端点，**已逐一核对 `server/src/routes/salary.ts` 实际代码**，挂载前缀 `/api/salary`）：
   - **C1 薪级**（2 端点）：POST /api/salary/grades + GET /api/salary/grades
   - **C1 薪档**（2 端点）：POST /api/salary/grade-levels + GET /api/salary/grade-levels
   - **C1 薪酬方案**（3 端点）：POST /api/salary/plans + GET /api/salary/plans + **PATCH** /api/salary/plans/:id/deactivate（**注意是 PATCH 不是 PUT/POST**）
   - **C2 社保方案**（3 端点）：POST + GET + **PATCH /:id** `/api/salary/insurances/social`
   - **C2 公积金方案**（3 端点）：POST + GET + **PATCH /:id** `/api/salary/insurances/housing-fund`
   - **C2 参保登记**（3 端点）：POST + GET + **PATCH /:id** `/api/salary/insurances/employees`
   - **C3 个税计算**（4 端点）：POST /api/salary/tax/calculate + /calculate-batch + /year-end-bonus + /labor-income
   - **C3 个税查询**（2 端点）：GET /api/salary/tax/history + GET /api/salary/tax/annual-summary
   - **M5-2-C1 总计消费 22 后端端点**（**0 新增**）
   - **关键调整**：grades / grade-levels / plans / insurances **均没有 GET /:id 详情端点，也没有 DELETE**；编辑走 PATCH；详情展示在 List 内展开行 / Dialog（**不创建独立详情页**，GradeDetail 除外，见 §4.1）
10. **M5-2-C1 关键设计**：
    - **0 修改后端任何文件**（**M5-2-C1 是纯前端**）
    - **0 修改 `client/package.json`**（vitest + happy-dom 已由 `ac43074` 装好，**严禁再装依赖**）
    - **0 新增 D1-D6 / C1-C8 / M0.5 service**（沿用 36 service 0 改动）
    - **0 修改 M5-2-0 + M5-2-A1/A2 + M5-2-B 已建文件**（除 `router/index.ts` **追加** salaryRoutes）
    - **独立 SalaryLayout**（**不合并**到 OrganizationLayout / AttendanceLayout，**沿用一模块一 layout 模式**）
    - **5 角色 RBAC 严格遵守**（admin / hr / dept_head / executive / employee，**无 finance**）
    - **不创建 `api/types/index.ts`**（M5-2-B 教训：会遮蔽同级 `api/types.ts`；类型从 `./types/salary` 直接 import）

**红线**（违反任何一条 → 任务失败，需回退重做）：

1. **禁止越界做其他切片**：本提示词只授权 "M5-2-C1 切片（M4 薪酬前端 · 基础配置 C1-C3）"。严禁做 M5-2-C2（算薪/工资条）/ M5-2-C3（提成/预警/调薪）/ M5-2-D / M5-3-M5-8。
2. **禁止修改后端文件**：纯前端，**严禁修改 `server/` 任何文件**。
3. **禁止修改 `client/package.json`**：不装新依赖。
4. **禁止修改 M5-2-0 共享基础设施**（除 `router/index.ts` 追加 salaryRoutes，沿用 M5-2-A1/B 模式）。
5. **禁止修改 M5-2-A1/A2/B 已建文件**：
   - ❌ 严禁修改 `api/types/organization.ts` / `api/types/attendance.ts`（类型独立到 `api/types/salary.ts`）
   - ❌ 严禁修改 `api/{company,department,employee,onboarding,regularization,transfer,offboarding,contract,shift,attendance,leave,overtime,businessTrip,monthlySummary}.ts`（14 API 文件）
   - ❌ 严禁修改 `router/organization.ts` / `router/attendance.ts`（**仅追加** salaryRoutes 到 `router/index.ts`）
   - ❌ 严禁修改 `OrganizationLayout.vue` / `AttendanceLayout.vue`（**仅**新增独立 `SalaryLayout.vue`）
6. **必须报告完整**：交付报告必须列出全部新增/修改文件（即使是 0 个也要说"0 个"），并明确说明"原有用例是否改动"。
7. **禁止"我觉得这也有用"**：任何"顺便做"或"我觉得可以优化"的越界内容一律禁止。
8. **禁止修改 `employees.baseSalary` 相关臆测**：`employees` 表**没有** baseSalary 字段（C8 强约束），员工实际薪资走 `employee_salary_plans`（本切片消费）+ `employee_salary_history`。**严禁**在前端类型里给 Employee 加 baseSalary。
9. **M5-2-C1 范围强约束**：
   - **0 修改后端任何文件**（**仅 client/src + client/tests 之外的 src**）
   - **0 修改 `client/package.json` / `vitest.config.ts` / `tests/run-all.test.ts`**（聚合入口用 `import.meta.glob` 自动发现新测试文件，**无需修改**）
   - **21 个 client 文件**（20 新增 + 1 修改 router/index.ts）
   - **D1-D6 + C1-C8 业务代码 0 行改动**（36 service + 40 表 = 0 行）
   - **M0.5 4 service 0 行改动**
   - **5 角色 RBAC 严格遵守**（**无 finance 角色**）
10. **测试范式强约束（`ac43074` 新规则）**：
    - 新测试文件**沿用自包含内联断言模式**：文件内自定义 `describe`/`it` 收集用例 + `expectEqual` 抛 Error + 导出 `run*Tests(): number | Promise<number>`
    - **严禁 `import ... from 'vitest'`**（会与文件内 shim 冲突；`client/tests/run-all.test.ts` 的 glob 会自动聚合 `src/**/__tests__/*.test.ts`）
    - 涉日期敏感断言：在 run 函数内用固定日期字符串比较，**不可用** `vi.useFakeTimers`（那是后端 vitest 测试的模式）

---

## 1. 任务 ID + 目标

### 1.1 任务 ID

**M5-2-C1**（M5-2 第 4 个业务子切片 — M4 薪酬前端 · 基础配置 · **C1 薪级薪档与薪酬方案 + C2 社保公积金 + C3 个税工具**）

### 1.2 任务目标

实现 M5 联调 Vue3 前端的**第四个业务子切片**（M4 薪酬 · 基础配置），包含 3 子任务：

| 子任务 | 范围 | 端点 | 实际后端路径（/api/salary 前缀） | 新增/修改 |
|---|---|---|---|---|
| 7.2.C1.1 薪级薪档与薪酬方案（C1）| 薪级 CR + 薪档 CR + 方案 CR + 停用 | 7 | POST/GET /grades + POST/GET /grade-levels + POST/GET /plans + PATCH /plans/:id/deactivate | 2 API + 3 页面 + 2 测试 |
| 7.2.C1.2 社保公积金（C2）| 社保方案 + 公积金方案 + 员工参保登记 | 9 | POST/GET/PATCH /insurances/social(/:id) + POST/GET/PATCH /insurances/housing-fund(/:id) + POST/GET/PATCH /insurances/employees(/:id) | 1 API + 3 页面 + 1 测试 |
| 7.2.C1.3 个税工具（C3）| 4 类计算 + 历史 + 年度汇总 | 6 | POST /tax/calculate + /calculate-batch + /year-end-bonus + /labor-income + GET /tax/history + /annual-summary | 1 API + 2 页面 + 1 测试 |
| **共用** | api/types/salary.ts 新文件 + router/salary.ts 新文件 + SalaryLayout 新文件 + router/index.ts 追加 + views 聚合测试 | — | 1 新 types + 1 新 router + 1 新 layout + 1 修改 router/index + 1 新 views 测试 |
| **M5-2-C1 合计** | **22 端点消费** | **22** | **20 新增 + 1 修改 = 21 文件** |

**关键调整**（**已核对 `server/src/routes/salary.ts` 实际代码**）：
- C1：**7 端点**（plans 停用是 **PATCH /plans/:id/deactivate**，**没有** PUT /plans/:id 全量更新，**没有** DELETE）
- C2：**9 端点**（3 组资源各有 POST 创建 + GET 列表 + **PATCH /:id** 部分更新；**没有** GET /:id 详情与 DELETE）
- C3：**6 端点**（4 计算 + 2 查询；history / annual-summary 是 **GET query 传参**：employeeId + year + settle?）
- 除 GradeDetail（薪档按薪级管理）外**不创建独立详情页**，详情/编辑在 List 内 Dialog 完成

**M5-2-C1 业务量**：
- 0 后端文件改动（**仅消费 22 端点**）
- 0 `client/package.json` 改动
- 0 M5-2-0 + M5-2-A1/A2 + M5-2-B 已建文件改动（除 `router/index.ts` 追加）
- 21 client 文件改动（**20 新增 + 1 修改**）
- ~3000-4000 行 Vue3 + TypeScript 代码
- 12-18 单元测试

### 1.3 端点清单（**0 新增后端端点**，消费 C1-C3 已有 22 端点）

**复用后端 M4 路由**（`server/src/routes/index.ts` 已挂载 `/api/salary`，`server/src/routes/salary.ts` 内分组）：

| 模块 | 路由前缀 | 端点 | 端点路径 | 权限点（后端实际） |
|---|---|---|---|---|
| C1 薪级 | `/api/salary/grades` | 2 | POST / + GET / | salary:grade:write / salary:grade:read |
| C1 薪档 | `/api/salary/grade-levels` | 2 | POST / + GET / | salary:grade:write / salary:grade:read |
| C1 薪酬方案 | `/api/salary/plans` | 3 | POST / + GET / + PATCH /:id/deactivate | salary:plan:write / salary:plan:read |
| C2 社保方案 | `/api/salary/insurances/social` | 3 | POST / + GET / + PATCH /:id | salary:insurance:write / salary:insurance:read |
| C2 公积金方案 | `/api/salary/insurances/housing-fund` | 3 | POST / + GET / + PATCH /:id | salary:housing-fund:write / salary:housing-fund:read |
| C2 参保登记 | `/api/salary/insurances/employees` | 3 | POST / + GET / + PATCH /:id | **以 salary.ts 实际权限点为准（主动识别）** |
| C3 个税计算 | `/api/salary/tax` | 4 | POST /calculate + /calculate-batch + /year-end-bonus + /labor-income | salary:tax:calculate |
| C3 个税查询 | `/api/salary/tax` | 2 | GET /history + GET /annual-summary | salary:tax:read |
| **M5-2-C1 合计** | — | **22** | — | — |

**0 新增后端端点**

### 1.4 单测目标

12-18 个新单测，按 3 子任务 + 通用测试分布：

| 文件 | it 数 | 目标 |
|---|---|---|
| `api/__tests__/salaryGrade.test.ts` | 2-3 | SALARY_GRADE_PATHS 路径 + listGrades / createGrade / listGradeLevels / createGradeLevel 类型 |
| `api/__tests__/salaryPlan.test.ts` | 2-3 | SALARY_PLAN_PATHS（deactivate 是 **PATCH** /plans/:id/deactivate）+ createPlan / listPlans / deactivatePlan |
| `api/__tests__/insurance.test.ts` | 2-3 | 3 组资源路径（social / housing-fund / employees）+ PATCH /:id + city / insuranceType 枚举值 |
| `api/__tests__/tax.test.ts` | 2-3 | TAX_PATHS 4 计算 + 2 查询 + year-end-bonus 的 `isAnnual: true` 字面量 |
| `views/__tests__/salary.test.ts` | 3-6 | 6 个列表页组件对象 + 状态映射（grade active/archived + plan active/inactive/superseded + registration active/inactive）+ 5 角色菜单过滤（无 finance） |

**沿用 M5-2-A1/A2/B 内联断言模式 + 导出 `run*Tests()`**（**不 import vitest**，由 `client/tests/run-all.test.ts` 自动聚合）

---

## 2. 上下文

### 2.1 项目位置

- **项目根**：`D:\projects\hrms`
- **主文档**：[`docs/HRMS-V1.2.md`](../HRMS-V1.2.md) §四.8 C1-C3
- **API 契约**：后端真实代码 [`server/src/routes/salary.ts`](../../../server/src/routes/salary.ts)（**唯一权威**，提示词与代码冲突时以代码为准并报告）
- **AI Coding 上下文**：[`AGENTS.md`](../../../AGENTS.md)（含 M4 任务 ID 错位专题 + 36 service 红线）
- **M5-2-B 考勤假勤前端**：[`docs/cursor-prompts/M5-2-B.md`](M5-2-B.md) commit `ff433c7`
- **前端测试脚手架**：commit `ac43074`（vitest 3 + happy-dom + `client/tests/run-all.test.ts` 聚合入口）

### 2.2 当前状态（M0+M0.5+M1+M2+M3+M4+M5-1+M5-2-0/A1/A2/B 已全部完成）

- **最近 commit**：`ac43074 test(client): 补建前端测试脚手架`
- **后端测试**：876/876 通过（73 文件）
- **前端测试**：46/46 通过（23 自包含用例文件 × 存在性 + 执行 2 层）
- **M5-2-A1/A2/B 已建 client 文件**：
  - `api/types.ts`（信封 + PaginatedResponse + UserInfo，**文件**非目录出口）
  - `api/types/organization.ts` + `api/types/attendance.ts`（2 类型文件，**无 index.ts**）
  - `api/*.ts`（14 业务 API 文件：company/department/employee/onboarding/regularization/transfer/offboarding/contract/shift/attendance/leave/overtime/businessTrip/monthlySummary）
  - `router/organization.ts` + `router/attendance.ts`（2 模块路由，含 `filterXxxMenu` / `resolveXxxActiveKey` 导出）
  - `layouts/{OrganizationLayout,AttendanceLayout}.vue`（2 模块布局）
  - `views/organization/*`（27 页面）+ `views/attendance/*`（15 页面）
  - `components/{StatusTag,TreeNode,Calendar}.vue` + M5-2-0 5 通用组件（PageHeader / DataTable / FormDialog / EmptyState / SearchForm）
- **M5-2-C1 严禁修改**：
  - ❌ `server/` 任何文件（0 修改）
  - ❌ `client/package.json` / `client/vitest.config.ts` / `client/tests/run-all.test.ts`（0 修改）
  - ❌ `client/vite.config.ts` / `tsconfig.json` / `.eslintrc.cjs`（0 修改）
  - ❌ M5-2-0 共享基础设施（除 `router/index.ts` 追加 `salaryRoutes`）
  - ❌ M5-2-A1/A2/B 已建 client 文件

**M5-2-C1 必须新增 client/ 下 20 个文件**：

- **API 类型**（1 新文件 `api/types/salary.ts`）：
  - SalarySequence（'M'/'T'/'P'/'S'/'A'，**注意高管 M 序列也在此枚举**，与薪级表 M1-M4/M5-M6 对齐）
  - SalaryGrade + SalaryGradeLevel + GradeStatus（active/archived）
  - SalaryPlan + PlanStatus（active/inactive/superseded）
  - CityCode（xi_an/bei_jing/si_chuan）+ InsuranceType（pension/medical/unemployment/work_injury/maternity）
  - SocialInsuranceScheme + HousingFundScheme + EmployeeInsuranceRegistration + RegistrationStatus（active/inactive）
  - TaxCalculateRequest / TaxBatchRequest / TaxYearEndBonusRequest（**isAnnual: true 字面量**）/ TaxLaborIncomeRequest
  - TaxResult / TaxHistoryItem / TaxAnnualSummary（**以服务端 service 返回结构为准，先读 `server/src/services/tax_calculation.service.ts` 等再定字段**）
- **API 函数**（4 新文件）：
  - `client/src/api/salaryGrade.ts`（grades 2 + grade-levels 2 端点函数 + `SALARY_GRADE_PATHS`）
  - `client/src/api/salaryPlan.ts`（plans 3 端点函数 + `SALARY_PLAN_PATHS`，**deactivate 用 http.patch**）
  - `client/src/api/insurance.ts`（social 3 + housing-fund 3 + employees 3 端点函数 + `INSURANCE_PATHS`）
  - `client/src/api/tax.ts`（6 端点函数 + `TAX_PATHS`）
- **页面**（8 新增）：
  - C1（3）：`views/salary/grade/{GradeList,GradeDetail}.vue` + `views/salary/plan/SalaryPlanList.vue`
  - C2（3）：`views/salary/insurance/{SocialSchemeList,HousingFundList,EmployeeInsuranceList}.vue`
  - C3（2）：`views/salary/tax/{TaxCalculator,TaxHistory}.vue`
- **路由 + 布局**（2 新增）：
  - `client/src/router/salary.ts`（薪酬子路由表 + `filterSalaryMenu` + `resolveSalaryActiveKey` 导出，**沿用 router/attendance.ts 模式**）
  - `client/src/layouts/SalaryLayout.vue`（模块布局：左侧子菜单 + 右侧 router-view，**沿用 AttendanceLayout 模式**）
- **测试**（5 新增）：
  - `client/src/api/__tests__/salaryGrade.test.ts`（2-3）
  - `client/src/api/__tests__/salaryPlan.test.ts`（2-3）
  - `client/src/api/__tests__/insurance.test.ts`（2-3）
  - `client/src/api/__tests__/tax.test.ts`（2-3）
  - `client/src/views/__tests__/salary.test.ts`（3-6）

**M5-2-C1 必须修改 1 个文件**：
- ✅ `client/src/router/index.ts`（**追加** `salaryRoutes`，**不删** login / dashboard / organizationRoutes / attendanceRoutes / 403 / 404）

**实际新增 20 个 + 修改 1 个 = 21 个文件**。

### 2.3 已有可复用模块（M5-2-C1 必复用，零重写）

| 复用对象 | 来源 | 用途 | M5-2-C1 用法 |
|---|---|---|---|
| `http` 实例 | M5-2-0 `client/src/api/http.ts` | axios 封装 + 401 refresh + 错误 toast | **4 个新 API 文件全部 import http** |
| `unwrapData` / `unwrapPage` | `api/types/organization.ts` 导出 | 拆 `{success,data}` 信封 / 分页 | **4 个新 API 文件复用**（shift.ts 已验证此 import 路径） |
| `useUserStore` | M5-2-0 `client/src/stores/user.ts` | 用户信息 + 权限 + 角色 | 5 角色权限判断 + employee 取自己 employeeId |
| `usePermissionStore` | M5-2-0 `client/src/stores/permission.ts` | v-permission 指令 | 元素级权限控制（写按钮仅 admin/hr 可见） |
| `PageHeader` / `DataTable` / `FormDialog` / `EmptyState` / `SearchForm` | M5-2-0 5 通用组件 | UI 通用组件 | **8 页面全部用** |
| `StatusTag` | M5-2-A1 `components/StatusTag.vue` | 状态标签 | 8 页面用（label/type 由页面传参，**0 修改 StatusTag**） |
| `formatDate` / `formatMoney` | M5-2-0 `utils/format.ts` | 日期 / 金额格式化 | **8 页面全部用**（薪资金额一律 formatMoney） |
| `filterAttendanceMenu` 模式 | M5-2-B `router/attendance.ts` | 菜单按权限过滤 | `router/salary.ts` 同款实现 `filterSalaryMenu` |
| 后端 `/api/salary` | server/src/routes/salary.ts | 22 端点 | 4 个新 API 文件调 |

### 2.4 业务规则来源

- **V1.2 §四.8 C1-C3**：薪级薪档 / 社保公积金 / 个税引擎
- **V1.2 §二.4.1 薪级表**：高管 M1-M4 / 部门负责人 M5-M6 / 技术 T1-T6 / 生产 P1-P6 / 销售 S1-S6 / 职能 A1-A6（前端 sequence 枚举 M/T/P/S/A 的下拉标签以此为准）
- **V1.2 §三.6 RBAC**：5 角色（**无 finance**，hr 兼任财务）
- **V1.2 §三.5.2 配置化**：前端仅消费，0 新 configs
- **PRD 社保比例表（V1.2 §二.4.3）**：西安/北京/四川三地比例仅为参考，**前端不硬编码默认值**，比例一律由用户录入（后端 zod 0-1 小数）

---

## 3. 必读约束（项目级）

### 3.1 代码范式

- **Vue3 Composition API + `<script setup lang="ts">`**（**不用** Options API，**不用** class 组件）
- **TypeScript 强类型**（`strict: true`，**严禁** `any`）
- **Element Plus 组件**：`ElButton` / `ElInput` / `ElInputNumber` / `ElTable` / `ElDialog` / `ElForm` / `ElSelect` / `ElTabs` / `ElDescriptions` / `ElTag`
- **ESLint 规范**：`@vue/eslint-config-airbnb` + `@vue/eslint-config-typescript`
- **API 类型**：`api/types/salary.ts` 独立文件，**不混** organization.ts / attendance.ts，**不建** `api/types/index.ts`

### 3.2 数据模型约定

- **TS 类型**：PascalCase（`SalaryGrade` / `SalaryPlan` / `SocialInsuranceScheme` / `TaxResult`）
- **组件命名**：PascalCase（`GradeList.vue` / `TaxCalculator.vue`）
- **文件命名**：camelCase 沿用现有 API 文件惯例（`salaryGrade.ts` / `salaryPlan.ts` / `insurance.ts` / `tax.ts`）
- **API 路径**：`/api/salary/<resource>`，kebab-case（**注意 housing-fund 带中划线**）

### 3.3 RBAC 5 角色（**无 finance**）

```
admin / hr / dept_head / executive / employee
```

**M5-2-C1 权限分配**（22 端点，**按 `server/src/constants/permissions.ts` ROLE_PERMISSIONS 实际分配**）：

| 端点组 | admin | hr | dept_head | executive | employee |
|---|---|---|---|---|---|
| **C1 薪级/薪档**（4）| ✅ 全开 | ✅ 读+写 | 🔍 只读 | 🔍 只读 | ❌ 无 |
| **C1 薪酬方案**（3）| ✅ 全开 | ✅ 读+写+停用 | 🔍 只读 | 🔍 只读 | 🟡 只读（salary:plan:read，**数据范围以后端为准**，前端默认传自己 employeeId） |
| **C2 社保/公积金方案**（6）| ✅ 全开 | ✅ 读+写 | 🔍 只读 | 🔍 只读 | ❌ 无（employee 无 housing-fund:read） |
| **C2 参保登记**（3）| ✅ 全开 | ✅ 读+写 | 🔍 只读（insurance:read） | 🔍 只读 | 🟡 只读本人（insurance:read，前端默认传自己 employeeId） |
| **C3 个税计算**（4）| ✅ | ✅ | ❌ 无 tax:calculate | ✅ | ❌ 无 |
| **C3 个税查询**（2）| ✅ | ✅ | ✅ | ✅ | 🟡 只读本人（tax:read，前端默认传自己 employeeId） |

**菜单过滤**（`filterSalaryMenu` 按权限点过滤，沿用 attendance 模式）：
- admin / hr：薪级薪档 + 薪酬方案 + 社保方案 + 公积金方案 + 参保登记 + 个税工具 + 个税查询（7 项）
- dept_head：薪级薪档(只读) + 薪酬方案(只读) + 社保方案(只读) + 公积金方案(只读) + 参保登记(只读) + 个税查询（6 项，**无个税工具**）
- executive：同 dept_head + 个税工具（7 项）
- employee：我的薪酬方案 + 我的参保 + 我的个税（3 项，**固定传自己 employeeId**，隐藏员工选择器）

**严禁** 给 finance 角色分配权限（D1 教训延续）

### 3.4 错误码段位（M5-2-C1 0 新错误码）

**M5-2-C1 不分配任何错误码**。前端仅显示后端 message（M4 C1-C3 段位 73001-73210，**前端不需记忆具体码**，沿用 http.ts 自动 ElMessage.error）。

### 3.5 审计埋点（M5-2-C1 0 审计）

**M5-2-C1 不写任何审计**（后端 service 已自动记录）。

### 3.6 业务规则配置化（M5-2-C1 0 新 configs）

**M5-2-C1 不新增任何 configs**。薪级区间、社保比例、个税税率表均由后端 configs / 表数据驱动，前端只录入与展示。

---

## 4. 实施任务

### 4.1 子任务 7.2.C1.1：薪级薪档与薪酬方案（C1）— 7 端点 + 3 页面

**2 新 API 文件** + 3 新页面 + 2 新测试

**`api/salaryGrade.ts`**（参考 `api/shift.ts` 范式）：
- `SALARY_GRADE_PATHS = { grades: '/salary/grades', gradeLevels: '/salary/grade-levels' }`（as const）
- `listGrades(filter: ListSalaryGradeFilter)` → GET /grades（query: sequence?/status?/page/pageSize）→ unwrapPage
- `createGrade(data: CreateSalaryGradeRequest)` → POST /grades → 201 unwrapData
- `listGradeLevels(filter: ListGradeLevelFilter)` → GET /grade-levels（query: gradeId?/status?/page/pageSize）
- `createGradeLevel(data: CreateGradeLevelRequest)` → POST /grade-levels
- **请求体字段**（后端 zod 实测）：grade = { sequence, gradeCode, name, minBaseSalary, maxBaseSalary, minPerformanceBase, maxPerformanceBase }；level = { gradeId, level(1-7), baseSalary, performanceBase }

**`api/salaryPlan.ts`**：
- `SALARY_PLAN_PATHS = { plans: '/salary/plans', deactivate: (id) => `/salary/plans/${id}/deactivate` }`
- `listPlans(filter)` → GET /plans（employeeId?/status?/effectiveFrom?/page/pageSize）
- `createPlan(data)` → POST /plans（employeeId/gradeId/levelId/baseSalary/performanceBase/allowance?/welfare?/effectiveFrom?）
- `deactivatePlan(id, data)` → **http.patch**（{ effectiveTo, reason? }）

**页面**：
1. **`GradeList.vue`**：SearchForm（序列下拉 M/T/P/S/A + 状态）+ DataTable（序列/薪级代码/名称/基本薪资区间/绩效基数区间/状态 StatusTag）+ 创建 Dialog（ElInputNumber 金额，**校验 min ≤ max 前端预检**）+ 行操作"薪档管理"跳 GradeDetail
2. **`GradeDetail.vue`**：顶部 ElDescriptions 展示薪级 + DataTable 薪档列表（档级 1-7/基本工资/绩效基数/状态）+ 新增薪档 Dialog（gradeId 从路由参数带，**不可改**）
3. **`SalaryPlanList.vue`**：SearchForm（员工选择 — admin/hr/dept_head/executive 用员工选择器，employee 固定自己）+ DataTable（员工/工号/薪级/薪档/基本工资/绩效基数/补贴/生效日期/状态）+ 创建 Dialog（选员工 → 选薪级 → 联动选薪档，**薪档下拉依赖薪级选择**，调 listGradeLevels({gradeId})）+ 停用 Dialog（effectiveTo 日期 + reason）

**测试**：
- `api/__tests__/salaryGrade.test.ts`：路径常量 + 类型枚举（sequence 5 值 / status 2 值）
- `api/__tests__/salaryPlan.test.ts`：deactivate 是 PATCH 路径形态 + PlanStatus 3 值

### 4.2 子任务 7.2.C1.2：社保公积金（C2）— 9 端点 + 3 页面

**1 新 API 文件**（`api/insurance.ts`）+ 3 新页面 + 1 新测试

**`api/insurance.ts`**：
- `INSURANCE_PATHS = { social: '/salary/insurances/social', housingFund: '/salary/insurances/housing-fund', employees: '/salary/insurances/employees', item: (base, id) => `${base}/${id}` }`
- 社保方案：`listSocialSchemes(filter)` / `createSocialScheme(data)` / `updateSocialScheme(id, data)`（**http.patch**）
  - create 字段：city（xi_an/bei_jing/si_chuan）+ insuranceType（pension/medical/unemployment/work_injury/maternity）+ companyRate（0-1 小数）+ personalRate + baseMin + baseMax + baseAdjustmentMonth?
  - update 字段：全部可选
- 公积金方案：`listHousingFundSchemes` / `createHousingFundScheme` / `updateHousingFundScheme`（无 insuranceType）
- 参保登记：`listEmployeeInsurances(filter)` / `createEmployeeInsurance(data)` / `updateEmployeeInsurance(id, data)`（{ effectiveTo?, baseSalary? }）

**页面**（3 个列表页同构，均 List + 创建/编辑 Dialog，**无详情页**）：
1. **`SocialSchemeList.vue`**：城市 + 险种 + 状态筛选；表格（城市/险种/单位比例/个人比例/基数上下限/调基月/状态）；**比例录入用百分比输入、提交时 ÷100 转小数**（展示 ×100，前端转换层写在页面内 utils 函数，**不改后端契约**）
2. **`HousingFundList.vue`**：城市 + 状态筛选；无险种列
3. **`EmployeeInsuranceList.vue`**：员工/城市/状态筛选；表格（员工/城市/社保方案/公积金方案/缴费基数/生效起止/状态）；创建 Dialog 联动两个方案下拉（按城市过滤，**选城市后才允许选方案**）

**测试**：
- `api/__tests__/insurance.test.ts`：3 组路径（**housing-fund 中划线**）+ city 3 枚举 + insuranceType 5 枚举 + PATCH item 路径拼接

### 4.3 子任务 7.2.C1.3：个税工具（C3）— 6 端点 + 2 页面

**1 新 API 文件**（`api/tax.ts`）+ 2 新页面 + 1 新测试

**`api/tax.ts`**：
- `TAX_PATHS = { calculate: '/salary/tax/calculate', calculateBatch: '/salary/tax/calculate-batch', yearEndBonus: '/salary/tax/year-end-bonus', laborIncome: '/salary/tax/labor-income', history: '/salary/tax/history', annualSummary: '/salary/tax/annual-summary' }`
- `calculateMonthlyTax(data)`：{ employeeId, period, baseAmount, cumulativePrepaid? }
- `calculateBatchTax(data)`：{ period, deptIds? }
- `calculateYearEndBonus(data)`：{ employeeId, bonusAmount, **isAnnual: true**（前端固定传 true，zod literal）}
- `calculateLaborIncomeTax(data)`：{ employeeId, incomeAmount }
- `getTaxHistory(query)`：GET history?employeeId&year
- `getTaxAnnualSummary(query)`：GET annual-summary?employeeId&year&settle?

**页面**：
1. **`TaxCalculator.vue`**：ElTabs 4 个计算 tab（月度工资薪金累计预扣 / 批量计算 / 年终奖 / 劳务报酬）+ 各 tab 表单 + 结果 ElDescriptions/ElTable 展示（**结果字段以服务端实际返回为准，先读 service 再写类型**）；批量计算 tab 用部门多选（复用 employee 模块的部门数据源，**只 import api/department.ts 的 list 函数，不改它**）
2. **`TaxHistory.vue`**：员工选择器（employee 角色固定自己 + 隐藏）+ 年份选择 + 历史明细表 + 年度汇总卡片（settle 切换为 ElSwitch，变更即重新查询）

**测试**：
- `api/__tests__/tax.test.ts`：6 路径 + year-end-bonus 请求体含 `isAnnual: true` + history/annual-summary 为 GET query 形态

### 4.4 共用新增（5 个新文件 + 1 个修改）

1. **`api/types/salary.ts`**：§2.2 列出的全部类型；每个 interface 带 JSDoc（引用 V1.2 §四.8 章节号）；**不确定的响应字段先读对应 server service 返回再定**
2. **`router/salary.ts`**：
   - 导出 `salaryRoutes: RouteRecordRaw`（path '/salary' + SalaryLayout + redirect 到第一个有权限的子页 + 8 子路由）
   - 导出 `filterSalaryMenu(userInfo)` / `resolveSalaryActiveKey(path)`（**逐行参考 router/attendance.ts 实现模式**）
   - 菜单项 7 个：grade（薪级薪档）/ plan（薪酬方案）/ social（社保方案）/ fund（公积金方案）/ registration（参保登记）/ tax-calc（个税工具）/ tax-history（个税查询）
   - **路由顺序**：静态子路径在前，带参（/salary/grades/:id）在后
3. **`layouts/SalaryLayout.vue`**：**逐行参考 AttendanceLayout.vue**，品牌名"薪酬核算"，图标用 Money/Coin/Tickets/Wallet/Postcard/Calculator/DataLine（@element-plus/icons-vue 实际存在的图标名，**以 node_modules/@element-plus/icons-vue 实际导出为准**）
4. **`api/__tests__/*.test.ts` × 4 + `views/__tests__/salary.test.ts`**：自包含内联断言 + `run*Tests()` 导出
5. **修改 `router/index.ts`**：import salaryRoutes + 追加到 routes（**仅此 1 处修改**）

---

## 5. 关键决策点详尽（4 类必须详尽）

### 5.1 响应结构确认（**最优先，动手写类型前必做**）

M5-2-B 教训：后端实际与提示词可能不同。**写 `api/types/salary.ts` 之前**，必须打开以下文件确认 list/get 返回结构（分页包裹形态 `{ items, total, page, pageSize }` 还是数组、字段命名 camelCase 或 snake_case）：

- `server/src/services/salary_grade.service.ts`（listGrades 返回）
- `server/src/services/salary_plan.service.ts`（listPlans / deactivatePlan 返回）
- `server/src/services/social_insurance_scheme.service.ts` / `housing_fund_scheme.service.ts` / `employee_insurance.service.ts`
- `server/src/services/tax_calculation.service.ts` / `tax_year_end_bonus.service.ts` / `tax_labor_income.service.ts`

**若实际返回与 unwrapPage 期望的分页形态不一致**：以实际为准写 unwrap（可参考 organization.ts 的 unwrapData/unwrapPage 实现另写 salary 本地 unwrap，**但不修改 organization.ts**），并在交付报告"主动识别"段说明。

### 5.2 金额与比例的展示约定

- 薪资金额：`formatMoney`（分/元口径**以服务端字段注释为准**，发现歧义就报告，**不猜**）
- 社保/公积金比例：后端 0-1 小数；页面录入 %（如 16%），提交 `value/100`，展示 `value*100 + '%'`；转换函数写在 `views/salary/insurance/` 页面内或 `api/types/salary.ts` 导出纯函数（**二选一，统一即可**）
- 个税 period：YYYY-MM 字符串（ElDatePicker type="month" + value-format="YYYY-MM"）

### 5.3 employee 角色的数据范围

- employee 菜单只有 3 项（我的薪酬方案 / 我的参保 / 我的个税），**页面复用同一组件**，通过 `useUserStore().userInfo.employee?.id` 固定 employeeId 并隐藏员工选择器
- 后端是否对 employee 做数据范围过滤**不在本切片验证范围**（纯前端切片）；前端只保证"默认传自己"
- `userInfo.employee` 为空（未关联员工档案的账号）→ 显示 EmptyState"尚未关联员工档案"，**不报错**

### 5.4 测试编写规范（`ac43074` 之后的新规则）

- 文件位置：`src/api/__tests__/` 与 `src/views/__tests__/`（**必须在这两个目录**，glob 只扫 `src/**/__tests__/*.test.ts`）
- 文件结构：顶部 JSDoc → import → `interface Case` → `cases` 数组 → 本地 `describe`/`it`/`expectEqual` shim → 用例 → `export function run*Tests(): number`（**或 async Promise<number>**，聚合器已兼容）
- **导出命名**：`runSalaryGradeTests` / `runSalaryPlanTests` / `runInsuranceTests` / `runTaxTests` / `runSalaryViewTests`（`/^run.*Tests$/` 必须匹配）
- **严禁** import vitest / @vue/test-utils
- 验证方式：`pnpm --filter hrms-client test` 总数从 46 增长（预期 46 + 新文件数×2 ≈ 56-64）

### 5.5 JSDoc 规范

每个新 API 文件顶部：

```ts
/**
 * 薪级薪档 API（M5-2-C1）
 * @module api/salaryGrade
 * @description 消费 /api/salary/grades + /grade-levels 4 端点；0 新增后端端点
 * @permission salary:grade:read / salary:grade:write（5 角色无 finance）
 */
```

每个导出函数带单行 JSDoc（端点 + 方法 + 权限点）。

---

## 6. 修改文件清单（明示边界）

### 6.1 必改文件（清单以内）

**新增 20 个**：
1. `client/src/api/types/salary.ts`
2. `client/src/api/salaryGrade.ts`
3. `client/src/api/salaryPlan.ts`
4. `client/src/api/insurance.ts`
5. `client/src/api/tax.ts`
6. `client/src/router/salary.ts`
7. `client/src/layouts/SalaryLayout.vue`
8. `client/src/views/salary/grade/GradeList.vue`
9. `client/src/views/salary/grade/GradeDetail.vue`
10. `client/src/views/salary/plan/SalaryPlanList.vue`
11. `client/src/views/salary/insurance/SocialSchemeList.vue`
12. `client/src/views/salary/insurance/HousingFundList.vue`
13. `client/src/views/salary/insurance/EmployeeInsuranceList.vue`
14. `client/src/views/salary/tax/TaxCalculator.vue`
15. `client/src/views/salary/tax/TaxHistory.vue`
16. `client/src/api/__tests__/salaryGrade.test.ts`
17. `client/src/api/__tests__/salaryPlan.test.ts`
18. `client/src/api/__tests__/insurance.test.ts`
19. `client/src/api/__tests__/tax.test.ts`
20. `client/src/views/__tests__/salary.test.ts`

**修改 1 个**：
21. `client/src/router/index.ts`（**仅追加** import + salaryRoutes）

### 6.2 禁止修改文件（清单以外）

- ❌ `server/**`（全部）
- ❌ `client/package.json` / `pnpm-lock.yaml`（**不装依赖**）
- ❌ `client/vitest.config.ts` / `client/tests/run-all.test.ts`
- ❌ `client/vite.config.ts` / `tsconfig*.json` / `.eslintrc.cjs`
- ❌ `client/src/api/types.ts` / `api/types/organization.ts` / `api/types/attendance.ts`
- ❌ `client/src/api/` 已有 14 个业务 API 文件 + `http.ts`
- ❌ `client/src/router/organization.ts` / `attendance.ts`
- ❌ `client/src/layouts/` 已有 3 个 layout
- ❌ `client/src/components/**` / `stores/**` / `utils/**` / `views/organization/**` / `views/attendance/**`
- ❌ `client/src/**/__tests__/` 已有 23 个测试文件（**不修改旧测试**）

### 6.3 越界检测（交付时自检）

```bash
cd D:\projects\hrms
git status --short
# 必须只出现 §6.1 列出的 21 个文件
# 如果出现其他文件 → 越界，必须回退

git diff --stat server/
# 必须 0 行

git diff client/package.json
# 必须 0 行

git diff client/src/components/ client/src/stores/ client/src/utils/
# 必须 0 行（36 service 红线的 client 侧等价约束）
```

---

## 7. 验收标准

### 7.1 硬性验收（必须 100% 通过）

```bash
cd D:\projects\hrms

# 1. 后端测试（0 改动，沿用 876）
pnpm --filter hrms-server test
# 预期：876/876 通过

# 2. 后端 type-check
pnpm --filter hrms-server type-check
# 预期：0 错

# 3. 前端 type-check
pnpm --filter hrms-client type-check
# 预期：0 错

# 4. 前端测试（ac43074 脚手架，自动聚合新用例）
pnpm --filter hrms-client test
# 预期：46 + 新增（5 文件 × 2）= 56 以上全通过，0 failed

# 5. 全仓 lint
pnpm lint
# 预期：0 error（warning 不新增超过 5 个）

# 6. 前端生产构建
pnpm --filter hrms-client build
# 预期：构建成功（chunk 增大可接受，但不允许构建错误）
```

### 7.2 交付报告模板

```markdown
# M5-2-C1 交付报告

## 完成范围
- 子任务 7.2.C1.1/2/3 完成状态 + 端点消费清单（22/22）
## 新增文件（20）
（逐一列出）
## 修改文件（1）
- client/src/router/index.ts（+N 行 import + salaryRoutes）
## 主动识别与处理
（响应结构与 unwrapPage 差异 / 字段命名差异 / 权限点差异，逐一说明处理方式）
## 测试
- 新增 5 个测试文件 / N 个用例；pnpm --filter hrms-client test 结果
- 原有用例是否改动：0（必须）
## 验收命令输出摘要
（§7.1 六条命令的实际结果）
## 已知问题
（如有，逐一列出 + 建议处理阶段）
## 红线自检
- 0 修改后端 / 0 修改 package.json / 0 修改旧测试 / 0 修改共享基础设施（除 router/index.ts）/ 5 角色无 finance
```

---

**提示词结束。开始执行前，请先回复你的实施计划（文件创建顺序 + 响应结构确认结论），确认无误后再动手。**
