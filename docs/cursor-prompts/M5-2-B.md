# M5-2-B M2 考勤假勤前端（B1 班次 + B2 打卡 + B3 请假 + B4 加班 + B5 出差 + B6 月度汇总）Cursor 提示词

> **任务 ID**：M5-2-B
> **任务名称**：M5-2 第 3 个业务子切片 — M2 考勤假勤前端（B1 班次 + B2 打卡 + B3 请假 + B4 加班 + B5 出差 + B6 月度汇总）
> **任务范围**：6 子任务 / 3d 工时 / V1.2 §四.6 B1-B6
> **提示词生成日期**：2026-08-31
> **目标单测增量**：约 16-20 个（API 函数 + 视图内联断言 + 状态机）
> **目标 commit**：1 个业务 commit + 1 个提示词 commit
> **依赖**：[M5-2-A1 基础数据](M5-2-A1.md) commit `7689a45` + [M5-2-A2 员工生命周期](M5-2-A2.md) commit `fd9e4a7` 已完成

---

## ⚠️ 强约束（最优先阅读）

**历史教训**（20 次）：
1. M0.5-5 任务中，Cursor 越界做了 M0.5-6（配置中心）+ M0.5-7（改密 + 2FA）共 2 个不在提示词范围内的切片。本次任务严禁重蹈覆辙。
2. M1-A6 提示词第一次瘦身到 15KB，Cursor 推断/补全消耗大量 token，返工 1 次。本次任务按 70-90KB 详尽模式写。
3. D1 提示词 §3.3 误列 5 角色包含 finance，**Cursor 主动识别 + 正确处理**。D2-D6 + C1-C8 + M5-1 + M5-2-0 + M5-2-A1 + M5-2-A2 已修正。**M5-2-B 沿用 5 角色 RBAC 无 finance**。
4. M2-wrap-up.md §7 误写 D3 = "季度校准会议"。M5-2-B 不重蹈覆辙，严格按 V1.2 §四.6 B1-B6 实际范围。
5. D3 → ... → C8 红线升级到 36 service 0 行改动。**M5-2-B 沿用 36 service 0 行改动**。
6. D4 验收发现 2 个 B3/B5 跨 UTC 边界日期敏感旧测。M5-2-B 沿用此模式。
7. C5 报告"已知问题 2"：V1.2 切片表 C5=劳务费/C6=工资条与本任务 ID「M4-C5」不一致。**M5-2-B 不涉及 V1.2 切片表错位**。
8. **M5-2-A1/A2 已落地 + M5-2-B 主动识别 4 个关键点**（M5-2-A2 commit `fd9e4a7` 报告）：
   - 后端 M1 实际端点路径与提示词不同（**M2-B 同理**）
   - 工号由后端生成（与 B 无关，但消费 employeeNo 时**不传**给后端）
   - 路由顺序问题（**必须在列表前**写详情子路由）
   - 真实权限点（dept_head 仅有 employee:read:self-dept）
   - **M5-2-B 必须沿用这些约束 + 主动识别后端真实端点**
9. **M5-2-B 后端端点确认**（6 模块 25 端点，**实际后端路径**）：
   - **B1 班次**（5 端点）：GET /shifts/ + POST /shifts/assignments + GET /shifts/:id + POST /shifts/ + PUT /shifts/:id
   - **B2 打卡**（5 端点）：POST /attendance/clock-in + GET /attendance/records + GET /attendance/records/:id + POST /attendance/manual + POST /attendance/import
   - **B3 请假**（5 端点）：GET /leaves/balance + POST /leaves/requests + GET /leaves/requests + GET /leaves/requests/:id + POST /leaves/requests/:id/cancel
   - **B4 加班**（**3 端点**）：POST /overtime/requests + GET /overtime/requests + POST /overtime/requests/:id/cancel（**没有 GET /:id 详情**）
   - **B5 出差**（**3 端点**）：POST /business-trips/requests + GET /business-trips/requests + POST /business-trips/requests/:id/cancel（**没有 GET /:id 详情**）
   - **B6 月度汇总**（4 端点）：POST /monthly-summaries/generate + GET /monthly-summaries/ + POST /monthly-summaries/:id/confirm + POST /monthly-summaries/:id/lock（**没有 GET /:id 详情**）
   - **M5-2-B 总计消费 25 后端端点**（**0 新增**）
   - **关键调整**：**B4/B5/B6 实际后端没有 GET /:id 详情端点**，前端**仅消费 25 端点**，详情展示在 List 内 Dialog 展开（**不创建独立详情页**）
10. **M5-2-B 关键设计**：
    - **0 修改后端任何文件**（**M5-2-B 是纯前端**）
    - **0 修改 `client/package.json`**
    - **0 新增 D1-D6 / C1-C8 / M0.5 service**（沿用 36 service 0 改动）
    - **沿用 M5-2-0 + M5-2-A1 + M5-2-A2 共享基础设施 + 已建文件**
    - **0 修改 M5-2-0 + M5-2-A1 + M5-2-A2 已建文件**（除**追加**菜单 + 路由 + types）
    - **独立 AttendanceLayout**（**不合并**到 OrganizationLayout，**沿用 M5-2-0 一个模块一个 layout 模式**）
    - **5 角色 RBAC 严格遵守**

**红线**（违反任何一条 → 任务失败，需回退重做）：

1. **禁止越界做其他切片**：本提示词只授权 "M5-2-B 切片（M2 考勤假勤前端 · B1-B6）"。严禁做 M5-2-A1/A2/C/D / M5-3-M5-8。
2. **禁止修改后端文件**：纯前端，**严禁修改 `server/` 任何文件**。
3. **禁止修改 `client/package.json`**：不装新依赖。
4. **禁止修改 M5-2-0 共享基础设施**（除 `router/index.ts` 追加 attendanceRoutes，**沿用 M5-2-A1 模式**）
5. **禁止修改 M5-2-A1/A2 已建文件**（除**追加**菜单 + 路由 + types）：
   - ❌ 严禁修改 `api/types/organization.ts`（A1+A2+A3+A3-A7 类型，**与 attendance 无关**）
   - ❌ 严禁修改 `api/{company,department,employee,onboarding,regularization,transfer,offboarding,contract}.ts`（7 API 文件）
   - ❌ 严禁修改 `router/organization.ts`（9 + 11 = 20 子路由，**仅追加** attendance 子路由到 `router/index.ts`）
   - ❌ 严禁修改 `OrganizationLayout.vue`（4 + 5 = 9 菜单项，**仅**新增独立 `AttendanceLayout.vue`）
6. **必须报告完整**：交付报告必须列出全部新增/修改文件（即使是 0 个也要说"0 个"），并明确说明"原有用例是否改动"。
7. **禁止"我觉得这也有用"**：任何"顺便做"或"我觉得可以优化"的越界内容一律禁止。
8. **M5-2-B 范围强约束**（红线 9）：
   - **0 修改后端任何文件**（**仅 client/src**）
   - **0 修改 `client/package.json`**
   - **0 修改 M5-2-0 共享基础设施**（除 `router/index.ts` 追加 `attendanceRoutes`）
   - **0 修改 M5-2-A1/A2 已建 30 文件**（除**追加**路由到 `router/index.ts` + types 索引）
   - **30 个 client 文件**（27 新增 + 3 修改）
   - **D1-D6 + C1-C8 业务代码 0 行改动**（36 service + 40 表 = 0 行）
   - **M0.5 4 service 0 行改动**
   - **复用 M5-2-0 + M5-2-A1 + M5-2-A2 通用组件 + Pinia stores + http + utils + 8 已建 API 文件 + types**
   - **5 角色 RBAC 严格遵守**（admin / hr / dept_head / executive / employee，**无 finance 角色**）
   - **16-20 单元测试**（内联断言 + 导出 `run*Tests()`，**不引入 vitest**）

---

## 1. 任务 ID + 目标

### 1.1 任务 ID

**M5-2-B**（M5-2 第 3 个业务子切片 — M2 考勤假勤前端 · **B1 班次 + B2 打卡 + B3 请假 + B4 加班 + B5 出差 + B6 月度汇总**）

### 1.2 任务目标

实现 M5 联调 Vue3 前端的**第三个业务子切片**（M2 考勤假勤），包含 6 子任务：

| 子任务 | 范围 | 端点 | 实际后端路径 | 新增/修改 |
|---|---|---|---|---|
| 7.2.B.1 班次（B1）| 班次模板 + 排班分配 | 5 | GET /shifts/ + GET /shifts/:id + POST /shifts/ + PUT /shifts/:id + POST /shifts/assignments | 1 API + 3 页面 + 1 测试 |
| 7.2.B.2 打卡（B2）| 上下班打卡 + 手动补卡 + 导入 | 5 | POST /attendance/clock-in + GET /attendance/records + GET /attendance/records/:id + POST /attendance/manual + POST /attendance/import | 1 API + 3 页面 + 1 测试 |
| 7.2.B.3 请假（B3）| 请假申请 + 余额查询 | 5 | GET /leaves/balance + POST /leaves/requests + GET /leaves/requests + GET /leaves/requests/:id + POST /leaves/requests/:id/cancel | 1 API + 3 页面 + 1 测试 |
| 7.2.B.4 加班（B4）| 加班申请 + 补休/调休 | 3 | POST /overtime/requests + GET /overtime/requests + POST /overtime/requests/:id/cancel | 1 API + 2 页面 + 1 测试 |
| 7.2.B.5 出差（B5）| 出差申请 + 差旅补助 | 3 | POST /business-trips/requests + GET /business-trips/requests + POST /business-trips/requests/:id/cancel | 1 API + 2 页面 + 1 测试 |
| 7.2.B.6 月度汇总（B6）| 月度报表生成 + 员工确认 + HR 锁定 | 4 | POST /monthly-summaries/generate + GET /monthly-summaries/ + POST /monthly-summaries/:id/confirm + POST /monthly-summaries/:id/lock | 1 API + 2 页面 + 1 测试 |
| **共用** | api/types/attendance.ts 新文件 + router/attendance.ts 新文件 + AttendanceLayout 新文件 + api/types/index.ts 追加 export + router/index.ts 追加 attendanceRoutes | — | 1 新 types + 1 新 router + 1 新 layout + 1 修改 types/index + 1 修改 router/index | 
| **M5-2-B 合计** | **25 端点消费** | **25** | **27 新增 + 3 修改 = 30 文件** |

**关键调整**（**M5-2-B 主动识别后端真实端点**）：
- B1 班次：**5 端点**（**没有** delete 端点，**没有** assign 列表端点）
- B2 打卡：**5 端点**（路径是 `/attendance/records` 而不是 `/attendance`）
- B3 请假：**5 端点**（路径是 `/leaves/requests` 而不是 `/leave`）
- **B4 加班 3 端点**（**没有 GET /:id 详情**）
- **B5 出差 3 端点**（**没有 GET /:id 详情**）
- **B6 月度汇总 4 端点**（**没有 GET /:id 详情**）
- **B4/B5/B6 详情展示在 List 内 Dialog 展开**（**不创建独立详情页**）

**M5-2-B 业务量**：
- 0 后端文件改动（**仅消费 25 端点**）
- 0 `client/package.json` 改动
- 0 M5-2-0 + M5-2-A1 + M5-2-A2 已建文件改动（除**追加**路由 + types）
- 30 client 文件改动（**27 新增 + 3 修改**）
- ~3500-4500 行 Vue3 + TypeScript 代码
- 16-20 单元测试

### 1.3 端点清单（**0 新增后端端点**，消费 B1-B6 已有 25 端点）

**复用后端 M2 路由**（`server/src/routes/index.ts` 已挂载 `/shifts` / `/attendance` / `/leaves` / `/overtime` / `/business-trips` / `/monthly-summaries`）：

| 模块 | 路由前缀 | 端点 | 端点路径 |
|---|---|---|---|
| B1 班次 | `/api/shifts` | 5 | GET / + GET /:id + POST / + PUT /:id + POST /assignments |
| B2 打卡 | `/api/attendance` | 5 | POST /clock-in + GET /records + GET /records/:id + POST /manual + POST /import |
| B3 请假 | `/api/leaves` | 5 | GET /balance + POST /requests + GET /requests + GET /requests/:id + POST /requests/:id/cancel |
| B4 加班 | `/api/overtime` | **3** | POST /requests + GET /requests + POST /requests/:id/cancel |
| B5 出差 | `/api/business-trips` | **3** | POST /requests + GET /requests + POST /requests/:id/cancel |
| B6 月度汇总 | `/api/monthly-summaries` | 4 | POST /generate + GET / + POST /:id/confirm + POST /:id/lock |
| **M5-2-B 合计** | — | **25** | — |

**0 新增后端端点**

### 1.4 单测目标

16-20 个新单测，按 6 子任务 + 通用测试分布：

| describe | it 数 | 目标 |
|---|---|---|
| `api/shift.ts` | 2-3 | listShifts / getShift / createShift / updateShift / assignShift 类型 + 路径 |
| `api/attendance.ts` | 2-3 | clockIn / listRecords / getRecord / manual / importData |
| `api/leave.ts` | 2-3 | getLeaveBalance / createLeaveRequest / listLeaveRequests / getLeaveRequest / cancelLeaveRequest |
| `api/overtime.ts` | 2-3 | createOvertimeRequest / listOvertimeRequests / cancelOvertimeRequest |
| `api/businessTrip.ts` | 2-3 | createBusinessTrip / listBusinessTrips / cancelBusinessTrip |
| `api/monthlySummary.ts` | 2-3 | generateMonthlySummary / listMonthlySummaries / confirmMonthlySummary / lockMonthlySummary |
| `views/__tests__/attendance.test.ts` | 4-5 | 6 个模块列表页 + 状态机（B1 3 + B2 4 + B3 5 + B4 5 + B5 5 + B6 3 = 25 状态）|

**沿用 M5-2-A1/A2 内联断言模式**

---

## 2. 上下文

### 2.1 项目位置

- **项目根**：`D:\projects\hrms`
- **主文档**：[`docs/HRMS-V1.2.md`](../../HRMS-V1.2.md) §四.6 B1-B6
- **API 契约**：[`docs/api-spec.md`](../../api-spec.md) §4.x attendance
- **AI Coding 上下文**：[`AGENTS.md`](../../../AGENTS.md)
- **M5-2-A1 基础数据**：[`docs/cursor-prompts/M5-2-A1.md`](M5-2-A1.md) commit `7689a45`
- **M5-2-A2 员工生命周期**：[`docs/cursor-prompts/M5-2-A2.md`](M5-2-A2.md) commit `fd9e4a7`

### 2.2 当前状态（M0+M0.5+M1+M2+M3+M4+M5-1+M5-2-0+M5-2-A1+M5-2-A2 已全部完成）

- **M5-2-A2 commit**：`fd9e4a7 feat(client): M5-2-A2 M1 组织人事前端 · 员工生命周期`
- **后端测试**：876/876 通过
- **M5-2-A1 + M5-2-A2 已建 30 client 文件**：
  - `api/types/organization.ts`（A1+A2+A3+A3-A7 类型）
  - `api/{company,department,employee,onboarding,regularization,transfer,offboarding,contract}.ts`（8 API 文件）
  - `router/organization.ts`（20 子路由）
  - `OrganizationLayout.vue`（9 菜单项）
  - `views/organization/*`（27 页面 = 13 A1+A2+A3 + 14 A3-A7）
  - `components/{StatusTag,TreeNode}.vue`（2 通用组件）
- **M5-2-B 严禁修改**：
  - ❌ `server/` 任何文件（0 修改）
  - ❌ `client/package.json`（0 修改）
  - ❌ `client/vite.config.ts` / `tsconfig.json` / `.eslintrc.cjs`（0 修改）
  - ❌ M5-2-0 共享基础设施（除 `router/index.ts` 追加 `attendanceRoutes`，**M5-2-A1 已追加 organizationRoutes**）
  - ❌ M5-2-A1/A2 已建 30 client 文件

**M5-2-B 必须新增 client/src/ 下 27 个文件**：

- **API 类型**（1 新文件 `api/types/attendance.ts`）：
  - Shift + ShiftAssignment + ShiftType (standard/comprehensive/flexible) + ShiftStatus (draft/active/archived)
  - AttendanceRecord + ClockType (wifi/gps/manual/imported) + AttendanceStatus (approved/pending/rejected) + Correction
  - LeaveRequest + LeaveType (annual/sick/personal/compensatory/marriage/maternity/paternity/bereavement) + LeaveStatus (draft/submitted/approved/rejected/cancelled) + LeaveBalance
  - OvertimeRequest + OvertimeType (weekday/weekend/holiday) + CompensationType (pay/comp) + OvertimeStatus (draft/submitted/approved/rejected/cancelled)
  - BusinessTrip + BusinessTripStatus (draft/submitted/approved/rejected/cancelled)
  - MonthlySummary + SummaryStatus (draft/employee_confirmed/hr_locked)
- **API 函数**（6 新文件）：
  - `client/src/api/shift.ts`（B1 5 端点函数）
  - `client/src/api/attendance.ts`（B2 5 端点函数）
  - `client/src/api/leave.ts`（B3 5 端点函数）
  - `client/src/api/overtime.ts`（B4 3 端点函数）
  - `client/src/api/businessTrip.ts`（B5 3 端点函数）
  - `client/src/api/monthlySummary.ts`（B6 4 端点函数）
- **页面**（15 新增）：
  - B1 班次（3）：`views/attendance/shift/{ShiftList,ShiftDetail,ShiftAssignment}.vue`
  - B2 打卡（3）：`views/attendance/attendance/{AttendanceList,AttendanceDetail,AttendanceCorrection}.vue`
  - B3 请假（3）：`views/attendance/leave/{LeaveList,LeaveDetail,LeaveForm}.vue`
  - B4 加班（2）：`views/attendance/overtime/{OvertimeList,OvertimeForm}.vue`（**没有详情页**）
  - B5 出差（2）：`views/attendance/businessTrip/{BusinessTripList,BusinessTripForm}.vue`（**没有详情页**）
  - B6 月度汇总（2）：`views/attendance/monthlySummary/{MonthlySummaryList,MonthlySummaryForm}.vue`（**没有详情页**）
- **路由 + 布局**（2 新增）：
  - `client/src/router/attendance.ts`（考勤假勤子路由表，6 子任务 15 页面）
  - `client/src/layouts/AttendanceLayout.vue`（模块布局：左侧子菜单 + 右侧 router-view）
- **通用组件**（1 新增）：
  - `client/src/components/Calendar.vue`（日历组件，用于排班 / 打卡 / 月度汇总）
- **测试**（7 新增）：
  - `client/src/api/__tests__/shift.test.ts`（2-3）
  - `client/src/api/__tests__/attendance.test.ts`（2-3）
  - `client/src/api/__tests__/leave.test.ts`（2-3）
  - `client/src/api/__tests__/overtime.test.ts`（2-3）
  - `client/src/api/__tests__/businessTrip.test.ts`（2-3）
  - `client/src/api/__tests__/monthlySummary.test.ts`（2-3）
  - `client/src/views/__tests__/attendance.test.ts`（4-5）

**M5-2-B 必须修改 3 个文件**：
- ✅ `client/src/api/types/index.ts`（**追加** `export * from './attendance'`，**不删** 已有 organization export）
- ✅ `client/src/router/index.ts`（**追加** `attendanceRoutes`，**不删** /login + /dashboard + organizationRoutes）
- ✅ `client/src/api/types/organization.ts`（**0 修改**，attendance 类型**独立**在 `attendance.ts`）

**实际新增文件 27 个**（含 7 个测试文件），**修改 2 个**（types/index.ts + router/index.ts），**总数 29 个文件**。

**注**：原 §1.4 概数 30 含 `api/types/index.ts` 追加 export 算 1 个修改，**实际 §1.2 改 2 文件 = 27+2=29**。

### 2.3 已有可复用模块（M5-2-B 必复用，零重写）

| 复用对象 | 来源 | 用途 | M5-2-B 用法 |
|---|---|---|---|
| `http` 实例 | M5-2-0 `client/src/api/http.ts` | axios 封装 + 401 refresh + 错误 toast | **6 个新 API 文件全部 import http** |
| `useUserStore` | M5-2-0 `client/src/stores/user.ts` | 用户信息 + 权限 + 角色 | 5 角色权限判断 |
| `usePermissionStore` | M5-2-0 `client/src/stores/permission.ts` | v-permission 指令 | 元素级权限控制 |
| `useDictStore` | M5-2-0 `client/src/stores/dict.ts` | 字典缓存 | 班次类型 / 请假类型 |
| `PageHeader` / `DataTable` / `FormDialog` / `EmptyState` / `SearchForm` | M5-2-0 5 通用组件 | UI 通用组件 | **15 页面全部用** |
| `formatDate` / `formatMoney` / `formatTime` | M5-2-0 `utils/format.ts` | 日期 / 金额 / 时间格式化 | **15 页面全部用** |
| `StatusTag` | M5-2-A1 `components/StatusTag.vue` | 5 状态标签（active / suspended / probation / resigned / merged）| 15 页面用，**追加 25 状态**（`lifecycleStatusLabel` 模式同 A2）|
| 后端 `/api/shifts` | server/src/routes | B1 5 端点 | `api/shift.ts` 调 |
| 后端 `/api/attendance` | server/src/routes | B2 5 端点 | `api/attendance.ts` 调 |
| 后端 `/api/leaves` | server/src/routes | B3 5 端点 | `api/leave.ts` 调 |
| 后端 `/api/overtime` | server/src/routes | B4 3 端点 | `api/overtime.ts` 调 |
| 后端 `/api/business-trips` | server/src/routes | B5 3 端点 | `api/businessTrip.ts` 调 |
| 后端 `/api/monthly-summaries` | server/src/routes | B6 4 端点 | `api/monthlySummary.ts` 调 |

### 2.4 业务规则来源

- **V1.2 §四.6 B1-B6**：考勤假勤 6 切片
- **V1.2 §三.6 RBAC**：5 角色 = admin / hr / dept_head / executive / employee（**无 finance**）
- **V1.2 §三.5.2 业务规则配置化**：configs 表 + configService（**前端仅消费**）

---

## 3. 必读约束（项目级）

### 3.1 代码范式

- **Vue3 Composition API + `<script setup lang="ts">`**（**不用** Options API，**不用** class 组件）
- **TypeScript 强类型**（`strict: true`，**严禁** `any`）
- **Element Plus 组件**：`ElButton` / `ElInput` / `ElTable` / `ElDialog` / `ElForm` / `ElCalendar` / `ElCard` / `ElTag`
- **ESLint 规范**：`@vue/eslint-config-airbnb` + `@vue/eslint-config-typescript`
- **API 类型**：`api/types/attendance.ts` 独立文件，**不混** organization.ts

### 3.2 数据模型约定

- **TS 类型**：PascalCase（`Shift` / `AttendanceRecord` / `LeaveRequest` / `OvertimeRequest` / `BusinessTrip` / `MonthlySummary`）
- **组件命名**：PascalCase（`ShiftList.vue` / `AttendanceDetail.vue` 等）
- **文件命名**：kebab-case（`shift.ts` / `attendance.ts` / `leave.ts` / `overtime.ts` / `businessTrip.ts` / `monthlySummary.ts`）
- **API 路径**：`/api/<module>/<resource>`，kebab-case

### 3.3 RBAC 5 角色（**无 finance**）

```
admin / hr / dept_head / executive / employee
```

**M5-2-B 权限分配**（B1-B6 25 端点，**按后端实际权限点**）：

| 端点 | admin | hr | dept_head | executive | employee |
|---|---|---|---|---|---|
| **B1 班次**（5）| ✅ 全开 | ✅ 全开 | 🔍 只读 | 🔍 只读 | ❌ 无 |
| **B2 打卡**（5）| ✅ 全开 | ✅ 全开 | 🔍 只读本部门 | 🔍 只读 | 🟡 **自己打卡**（clock-in + 自己的 records）|
| **B3 请假**（5）| ✅ 全开 | ✅ 全开 | 🔍 只读本部门 + 审批 | 🔍 只读 | 🟡 **自己申请 + 自己看** |
| **B4 加班**（3）| ✅ 全开 | ✅ 全开 | 🔍 只读本部门 + 审批 | 🔍 只读 | 🟡 **自己申请** |
| **B5 出差**（3）| ✅ 全开 | ✅ 全开 | 🔍 只读本部门 + 审批 | 🔍 只读 | 🟡 **自己申请** |
| **B6 月度汇总**（4）| ✅ 全开 | ✅ 全开 + 锁定 | 🔍 只读本部门 | 🔍 只读 | 🟡 **自己确认**（confirm）|

**employee 角色特殊权限**（**前端按后端实际权限点**）：
- employee 可调 `clock-in`（自己打卡）+ `attendance/records?employeeId=self`（自己看）+ `leave/requests`（自己申请）+ `overtime/requests`（自己申请）+ `business-trips/requests`（自己申请）+ `monthly-summaries/:id/confirm`（自己确认）
- 菜单按真实权限点过滤（**沿用 M5-2-A1 主动识别**）

**严禁** 给 finance 角色分配权限（D1 教训延续）

### 3.4 错误码段位（M5-2-B 0 新错误码）

**M5-2-B 不分配任何错误码**（前端不涉及业务错误码定义，仅消费后端错误码）。

**前端消费后端 B1-B6 错误码段位**（**M2 后端已分配**，前端仅显示 message）：
- B1-B6 段位：`TODO: M2-B1-B6 错误码段位`
- **前端不需记忆具体码**，仅显示 `apiError.message`（**沿用 M5-2-0 http.ts 自动 ElMessage.error**）

### 3.5 审计埋点（M5-2-B 0 审计）

**M5-2-B 不写任何审计**（前端不涉及，**复用**后端 `auditLog` 自动记录）。

### 3.6 业务规则配置化（M5-2-B 0 新 configs）

**M5-2-B 不新增任何 configs**（前端不涉及业务规则定义）。

---

## 4. 实施任务

### 4.1 子任务 7.2.B.1：班次（B1）— 5 端点 + 3 页面

**1 新 API 文件**（`api/shift.ts`）+ 3 新页面 + 1 新测试

**`api/shift.ts`**（B1 5 端点函数）：

```ts
import http from './http'
import type { Shift, ShiftAssignment, CreateShiftRequest, UpdateShiftRequest, AssignShiftRequest, PaginatedResponse } from './types/attendance'

export async function listShifts(filter: ListShiftFilter = {}): Promise<PaginatedResponse<Shift>> {
  return http.get<PaginatedResponse<Shift>>('/shifts', { params: filter })
}

export async function getShift(id: string): Promise<Shift> {
  return http.get<Shift>(`/shifts/${id}`)
}

export async function createShift(data: CreateShiftRequest): Promise<Shift> {
  return http.post<Shift>('/shifts', data)
}

export async function updateShift(id: string, data: UpdateShiftRequest): Promise<Shift> {
  return http.put<Shift>(`/shifts/${id}`, data)
}

export async function assignShift(data: AssignShiftRequest): Promise<ShiftAssignment> {
  return http.post<ShiftAssignment>('/shifts/assignments', data)
}
```

**3 班次页面**（**完整代码见 §5.1**）：

**`ShiftList.vue`**：
- 顶部 PageHeader + 搜索（公司 + 班次类型 + 状态）+ 创建按钮 + 分配按钮
- DataTable 列表（编码 / 名称 / 类型 / 上下班时间 / 工时 / 状态 / 操作）
- 状态用 `StatusTag`（draft / active / archived）

**`ShiftDetail.vue`**：
- 基础信息 + 班次时间表 + 排班分配列表（employeeIds / departmentIds）
- 操作：编辑 / 重新分配

**`ShiftAssignment.vue`**：
- 排班分配表单（shiftId / assigneeType [employee/department] / employeeIds[] / departmentIds[] / effectiveFrom / effectiveTo / remark）
- 校验：assigneeType 切换时清空另一组

### 4.2 子任务 7.2.B.2：打卡（B2）— 5 端点 + 3 页面

**1 新 API 文件**（`api/attendance.ts`）+ 3 新页面 + 1 新测试

**`api/attendance.ts`**（B2 5 端点函数）：

```ts
import http from './http'
import type { AttendanceRecord, ClockInRequest, ManualClockRequest, ImportDataRequest, PaginatedResponse } from './types/attendance'

export async function clockIn(data: ClockInRequest): Promise<AttendanceRecord> {
  return http.post<AttendanceRecord>('/attendance/clock-in', data)
}

export async function listRecords(filter: ListAttendanceFilter = {}): Promise<PaginatedResponse<AttendanceRecord>> {
  return http.get<PaginatedResponse<AttendanceRecord>>('/attendance/records', { params: filter })
}

export async function getRecord(id: string): Promise<AttendanceRecord> {
  return http.get<AttendanceRecord>(`/attendance/records/${id}`)
}

export async function manualClock(data: ManualClockRequest): Promise<AttendanceRecord> {
  return http.post<AttendanceRecord>('/attendance/manual', data)
}

export async function importData(data: ImportDataRequest): Promise<{ successCount: number; failedCount: number }> {
  return http.post('/attendance/import', data)
}
```

**3 打卡页面**：

**`AttendanceList.vue`**：
- 顶部 PageHeader + **自己打卡**按钮（employee 角色）+ **手动补卡**按钮（hr/admin）+ **导入**按钮（hr/admin）+ 搜索（员工 + 日期 + 状态）
- DataTable 列表（员工 / 打卡时间 / 上班 / 下班 / 打卡方式 / 状态 / 操作）
- **Calendar** 组件视图（**日历模式**显示月度打卡）

**`AttendanceDetail.vue`**：
- 基础信息 + 异常说明（迟到 / 早退 / 缺卡 / 补卡原因）

**`AttendanceCorrection.vue`**：
- 手动补卡表单（employeeId / clockInTime / clockOutTime / clockType='manual' / manualReason）
- 校验：manualReason 必填

### 4.3 子任务 7.2.B.3：请假（B3）— 5 端点 + 3 页面

**1 新 API 文件**（`api/leave.ts`）+ 3 新页面 + 1 新测试

**`api/leave.ts`**（B3 5 端点函数）：

```ts
import http from './http'
import type { LeaveRequest, LeaveBalance, CreateLeaveRequest, CancelLeaveRequest, PaginatedResponse } from './types/attendance'

export async function getLeaveBalance(employeeId: string, leaveType: string, year?: number): Promise<LeaveBalance> {
  return http.get<LeaveBalance>('/leaves/balance', { params: { employeeId, leaveType, year } })
}

export async function createLeaveRequest(data: CreateLeaveRequest): Promise<LeaveRequest> {
  return http.post<LeaveRequest>('/leaves/requests', data)
}

export async function listLeaveRequests(filter: ListLeaveFilter = {}): Promise<PaginatedResponse<LeaveRequest>> {
  return http.get<PaginatedResponse<LeaveRequest>>('/leaves/requests', { params: filter })
}

export async function getLeaveRequest(id: string): Promise<LeaveRequest> {
  return http.get<LeaveRequest>(`/leaves/requests/${id}`)
}

export async function cancelLeaveRequest(id: string, data: CancelLeaveRequest): Promise<LeaveRequest> {
  return http.post<LeaveRequest>(`/leaves/requests/${id}/cancel`, data)
}
```

**3 请假页面**：

**`LeaveList.vue`**：
- 顶部 PageHeader + **自己申请**按钮（employee 角色）+ 搜索（员工 + 类型 + 状态）
- **余额展示**：8 类假期（annual / sick / personal / compensatory / marriage / maternity / paternity / bereavement）当前余额卡片
- DataTable 列表（员工 / 类型 / 起止日期 / 天数 / 状态 / 操作）

**`LeaveDetail.vue`**：
- 基础信息 + 审批流（**M0.5-1 联动**，**留 TODO** 显示）+ 附件

**`LeaveForm.vue`**：
- 请假申请表单（employeeId / leaveType / startDate / endDate / reason / attachments）
- 自动调 `getLeaveBalance` 显示余额

### 4.4 子任务 7.2.B.4：加班（B4）— 3 端点 + 2 页面

**1 新 API 文件**（`api/overtime.ts`）+ 2 新页面 + 1 新测试

**`api/overtime.ts`**（B4 3 端点函数）：

```ts
import http from './http'
import type { OvertimeRequest, CreateOvertimeRequest, CancelOvertimeRequest, PaginatedResponse } from './types/attendance'

export async function createOvertimeRequest(data: CreateOvertimeRequest): Promise<OvertimeRequest> {
  return http.post<OvertimeRequest>('/overtime/requests', data)
}

export async function listOvertimeRequests(filter: ListOvertimeFilter = {}): Promise<PaginatedResponse<OvertimeRequest>> {
  return http.get<PaginatedResponse<OvertimeRequest>>('/overtime/requests', { params: filter })
}

export async function cancelOvertimeRequest(id: string, data: CancelOvertimeRequest): Promise<OvertimeRequest> {
  return http.post<OvertimeRequest>(`/overtime/requests/${id}/cancel`, data)
}
```

**2 加班页面**（**B4 没有 GET /:id 详情端点，详情展示在 List 内 Dialog**）：

**`OvertimeList.vue`**：
- 顶部 PageHeader + **自己申请**按钮 + 搜索（员工 + 状态 + 日期）
- DataTable 列表（员工 / 开始时间 / 结束时间 / 时长 / 类型 / 补偿方式 / 状态 / 操作）
- 操作：详情（**Dialog 展开**，**不跳独立详情页**）/ 取消

**`OvertimeForm.vue`**：
- 加班申请表单（employeeId / startTime / endTime / overtimeType [weekday/weekend/holiday] / compensationType [pay/comp] / reason / attachments）
- 校验：开始时间 < 结束时间 + 至少提前 4h 申请（**后端校验，**前端仅 UI 提示**）

### 4.5 子任务 7.2.B.5：出差（B5）— 3 端点 + 2 页面

**1 新 API 文件**（`api/businessTrip.ts`）+ 2 新页面 + 1 新测试

**`api/businessTrip.ts`**（B5 3 端点函数）：

```ts
import http from './http'
import type { BusinessTrip, CreateBusinessTripRequest, CancelBusinessTripRequest, PaginatedResponse } from './types/attendance'

export async function createBusinessTrip(data: CreateBusinessTripRequest): Promise<BusinessTrip> {
  return http.post<BusinessTrip>('/business-trips/requests', data)
}

export async function listBusinessTrips(filter: ListBusinessTripFilter = {}): Promise<PaginatedResponse<BusinessTrip>> {
  return http.get<PaginatedResponse<BusinessTrip>>('/business-trips/requests', { params: filter })
}

export async function cancelBusinessTrip(id: string, data: CancelBusinessTripRequest): Promise<BusinessTrip> {
  return http.post<BusinessTrip>(`/business-trips/requests/${id}/cancel`, data)
}
```

**2 出差页面**（**B5 没有 GET /:id 详情端点**）：

**`BusinessTripList.vue`**：
- 顶部 PageHeader + **自己申请**按钮 + 搜索（员工 + 目的地 + 状态 + 日期）
- DataTable 列表（员工 / 目的地 / 起止日期 / 事由 / 状态 / 操作）
- 操作：详情（**Dialog**）/ 取消

**`BusinessTripForm.vue`**：
- 出差申请表单（employeeId / destination / startDate / endDate / reason / projectCode）
- 校验：至少提前 3 天申请 + startDate < endDate

### 4.6 子任务 7.2.B.6：月度汇总（B6）— 4 端点 + 2 页面

**1 新 API 文件**（`api/monthlySummary.ts`）+ 2 新页面 + 1 新测试

**`api/monthlySummary.ts`**（B6 4 端点函数）：

```ts
import http from './http'
import type { MonthlySummary, GenerateSummaryRequest, PaginatedResponse } from './types/attendance'

export async function generateMonthlySummary(data: GenerateSummaryRequest): Promise<MonthlySummary> {
  return http.post<MonthlySummary>('/monthly-summaries/generate', data)
}

export async function listMonthlySummaries(filter: ListMonthlySummaryFilter): Promise<PaginatedResponse<MonthlySummary>> {
  return http.get<PaginatedResponse<MonthlySummary>>('/monthly-summaries', { params: filter })
}

export async function confirmMonthlySummary(id: string): Promise<MonthlySummary> {
  return http.post<MonthlySummary>(`/monthly-summaries/${id}/confirm`)
}

export async function lockMonthlySummary(id: string): Promise<MonthlySummary> {
  return http.post<MonthlySummary>(`/monthly-summaries/${id}/lock`)
}
```

**2 月度汇总页面**（**B6 没有 GET /:id 详情端点**）：

**`MonthlySummaryList.vue`**：
- 顶部 PageHeader + **生成**按钮（hr/admin）+ 年月选择（el-date-picker type="month"）+ 搜索
- DataTable 列表（员工 / 月份 / 应出勤 / 实际出勤 / 迟到 / 早退 / 缺卡 / 请假 / 加班 / 出差 / 状态 / 操作）
- 状态用 `StatusTag`（draft / employee_confirmed / hr_locked）
- 操作：详情（**Dialog** 显示完整月报）/ 确认（employee 确认自己的）/ 锁定（hr 锁定月报）
- **月报数据 = B1 排班 + B2 打卡 + B3 请假 + B4 加班 + B5 出差 聚合**（**后端已实现聚合逻辑**）

**`MonthlySummaryForm.vue`**：
- 生成月报表单（year / month / employeeId?）
- 校验：year ∈ [1970, 9999] + month ∈ [1, 12]

### 4.7 共用新增（2 个新文件）

**`router/attendance.ts`**（考勤假勤子路由表，6 子任务 15 页面）：

```ts
import type { RouteRecordRaw } from 'vue-router'

/**
 * 考勤假勤子路由表（M5-2-B）
 * @module router/attendance
 * @description 路径前缀 /attendance，包含 15 子路由：shift / attendance / leave / overtime / businessTrip / monthlySummary
 * @auth 通过 DefaultLayout 守卫 + AttendanceLayout 侧栏菜单控制
 * @layout AttendanceLayout 包裹所有子路由，**不修改** DefaultLayout.vue
 */
const attendanceRoutes: RouteRecordRaw[] = [
  {
    path: '/attendance',
    component: () => import('@/layouts/AttendanceLayout.vue'),
    meta: { requiresAuth: true, title: '考勤假勤' },
    children: [
      { path: '', redirect: '/attendance/shifts' },
      {
        path: 'shifts',
        name: 'ShiftList',
        component: () => import('@/views/attendance/shift/ShiftList.vue'),
        meta: { title: '班次管理' },
      },
      {
        path: 'shifts/:id',
        name: 'ShiftDetail',
        component: () => import('@/views/attendance/shift/ShiftDetail.vue'),
        meta: { title: '班次详情' },
      },
      {
        path: 'shifts/:id/assign',
        name: 'ShiftAssignment',
        component: () => import('@/views/attendance/shift/ShiftAssignment.vue'),
        meta: { title: '班次排班' },
      },
      {
        path: 'attendance',
        name: 'AttendanceList',
        component: () => import('@/views/attendance/attendance/AttendanceList.vue'),
        meta: { title: '打卡管理' },
      },
      {
        path: 'attendance/records/:id',
        name: 'AttendanceDetail',
        component: () => import('@/views/attendance/attendance/AttendanceDetail.vue'),
        meta: { title: '打卡详情' },
      },
      {
        path: 'attendance/correction',
        name: 'AttendanceCorrection',
        component: () => import('@/views/attendance/attendance/AttendanceCorrection.vue'),
        meta: { title: '手动补卡' },
      },
      {
        path: 'leaves',
        name: 'LeaveList',
        component: () => import('@/views/attendance/leave/LeaveList.vue'),
        meta: { title: '请假管理' },
      },
      {
        path: 'leaves/requests/:id',
        name: 'LeaveDetail',
        component: () => import('@/views/attendance/leave/LeaveDetail.vue'),
        meta: { title: '请假详情' },
      },
      {
        path: 'leaves/new',
        name: 'LeaveForm',
        component: () => import('@/views/attendance/leave/LeaveForm.vue'),
        meta: { title: '请假申请' },
      },
      {
        path: 'overtime',
        name: 'OvertimeList',
        component: () => import('@/views/attendance/overtime/OvertimeList.vue'),
        meta: { title: '加班管理' },
      },
      {
        path: 'overtime/new',
        name: 'OvertimeForm',
        component: () => import('@/views/attendance/overtime/OvertimeForm.vue'),
        meta: { title: '加班申请' },
      },
      {
        path: 'business-trips',
        name: 'BusinessTripList',
        component: () => import('@/views/attendance/businessTrip/BusinessTripList.vue'),
        meta: { title: '出差管理' },
      },
      {
        path: 'business-trips/new',
        name: 'BusinessTripForm',
        component: () => import('@/views/attendance/businessTrip/BusinessTripForm.vue'),
        meta: { title: '出差申请' },
      },
      {
        path: 'monthly-summaries',
        name: 'MonthlySummaryList',
        component: () => import('@/views/attendance/monthlySummary/MonthlySummaryList.vue'),
        meta: { title: '月度汇总' },
      },
      {
        path: 'monthly-summaries/new',
        name: 'MonthlySummaryForm',
        component: () => import('@/views/attendance/monthlySummary/MonthlySummaryForm.vue'),
        meta: { title: '生成月报' },
      },
    ],
  },
]

export default attendanceRoutes
```

**`layouts/AttendanceLayout.vue`**（**M5-2-B 独立 layout，不修改 DefaultLayout，**沿用 OrganizationLayout 模式**）：

```vue
<script setup lang="ts">
/**
 * 考勤假勤模块布局（M5-2-B）
 * @module layouts/AttendanceLayout
 * @description 左侧子菜单（6 项：班次/打卡/请假/加班/出差/月度汇总）+ 右侧 router-view
 * @permissions 通过 useUserStore.userInfo + hasPermission 工具过滤菜单（5 角色 RBAC）
 * @routing 接收子路由表 router/attendance.ts
 */
import { computed } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { useUserStore } from '@/stores/user'
import { hasPermission } from '@/utils/permission'

const route = useRoute()
const router = useRouter()
const userStore = useUserStore()

const menuItems = computed(() => [
  { key: 'shifts', label: '班次管理', icon: 'Clock', to: '/attendance/shifts', permission: 'shift:read' },
  { key: 'attendance', label: '打卡管理', icon: 'Timer', to: '/attendance/attendance', permission: 'attendance:read' },
  { key: 'leaves', label: '请假管理', icon: 'Calendar', to: '/attendance/leaves', permission: 'leave:read' },
  { key: 'overtime', label: '加班管理', icon: 'Sunny', to: '/attendance/overtime', permission: 'overtime:read' },
  { key: 'business-trips', label: '出差管理', icon: 'Location', to: '/attendance/business-trips', permission: 'trip:read' },
  { key: 'monthly-summaries', label: '月度汇总', icon: 'DataAnalysis', to: '/attendance/monthly-summaries', permission: 'summary:read' },
].filter((m) => hasPermission(userStore.userInfo, m.permission)))

const activeKey = computed(() => {
  const path = route.path
  if (path.startsWith('/attendance/shifts')) return 'shifts'
  if (path.startsWith('/attendance/attendance')) return 'attendance'
  if (path.startsWith('/attendance/leaves')) return 'leaves'
  if (path.startsWith('/attendance/overtime')) return 'overtime'
  if (path.startsWith('/attendance/business-trips')) return 'business-trips'
  if (path.startsWith('/attendance/monthly-summaries')) return 'monthly-summaries'
  return ''
})
</script>

<template>
  <div class="attendance-layout">
    <aside class="attendance-layout__sidebar">
      <el-menu :default-active="activeKey" @select="(k) => router.push(menuItems.find((m) => m.key === k)?.to || '/attendance')">
        <el-menu-item v-for="m in menuItems" :key="m.key" :index="m.key">
          <el-icon><component :is="m.icon" /></el-icon>
          <span>{{ m.label }}</span>
        </el-menu-item>
      </el-menu>
    </aside>
    <main class="attendance-layout__content">
      <router-view />
    </main>
  </div>
</template>
```

**`router/index.ts` 追加**（**仅追加** `...attendanceRoutes`，**不删** /login + /dashboard + organizationRoutes）：

```ts
import attendanceRoutes from './attendance'
// ...
const routes: RouteRecordRaw[] = [
  // ... 已有 /login + / + /dashboard + organizationRoutes
  ...attendanceRoutes,  // 追加 M5-2-B
]
```

**`api/types/index.ts` 追加**（**仅追加** `export * from './attendance'`，**不删** 已有 organization export）：

```ts
export * from './organization'  // M5-2-A1 + M5-2-A2 已有
export * from './attendance'    // M5-2-B 新增
```

---

## 5. 关键决策点详尽（4 类必须详尽）

### 5.1 班次（B1）JSDoc + 测试（子任务 7.2.B.1）

#### 5.1.1 JSDoc + 校验链

**`api/shift.ts`**（**B1 5 端点函数**）

```ts
/**
 * 班次管理 API（M5-2-B）
 * @module api/shift
 * @description 消费后端 /api/shifts 5 端点（list / get / create / update / assign）
 * @type 3 班次类型：standard / comprehensive / flexible
 * @status 3 状态：draft / active / archived
 * @assign POST /shifts/assignments 分配排班（employee 或 department 维度）
 */
import http from './http'
import type { Shift, ShiftAssignment, CreateShiftRequest, UpdateShiftRequest, AssignShiftRequest, PaginatedResponse } from './types/attendance'

export async function listShifts(filter: ListShiftFilter = {}): Promise<PaginatedResponse<Shift>> { ... }
export async function getShift(id: string): Promise<Shift> { ... }
export async function createShift(data: CreateShiftRequest): Promise<Shift> { ... }
export async function updateShift(id: string, data: UpdateShiftRequest): Promise<Shift> { ... }
export async function assignShift(data: AssignShiftRequest): Promise<ShiftAssignment> { ... }
```

**3 班次页面**（**详细代码见 §4.1**）：

**`ShiftList.vue`** / `ShiftDetail.vue` / `ShiftAssignment.vue`：

**JSDoc + props/slots/emits** 与 M5-2-A1 模式相同（**详见 §4.1**）

#### 5.1.2 错误码触发条件

**M5-2-B 不分配错误码**（前端不涉及）。

#### 5.1.3 测试用例（2-3 个 it）

```ts
describe('api/shift.ts', () => {
  it('listShifts 调 GET /shifts + 过滤参数', () => { ... })
  it('createShift 调 POST /shifts（不是 PUT）', () => { ... })
  it('updateShift 调 PUT /shifts/:id', () => { ... })
  it('assignShift 调 POST /shifts/assignments（不是 /shifts/:id/assign）', () => { ... })
})
```

### 5.2 打卡（B2）JSDoc + 测试（子任务 7.2.B.2）

**`api/attendance.ts`**（**B2 5 端点函数**）+ 3 页面

**JSDoc + 校验链**（**完整代码见 §4.2**）：

```ts
/**
 * 打卡管理 API（M5-2-B）
 * @module api/attendance
 * @description 消费后端 /api/attendance 5 端点
 * @clockType 4 类型：wifi / gps / manual / imported
 * @status 3 状态：approved / pending / rejected
 */
```

**3 打卡页面**（`AttendanceList.vue` / `AttendanceDetail.vue` / `AttendanceCorrection.vue`）：

#### 5.2.1 测试用例（2-3 个 it）

```ts
describe('api/attendance.ts', () => {
  it('clockIn 调 POST /attendance/clock-in（wifi/gps）', () => { ... })
  it('listRecords 调 GET /attendance/records（注意不是 /attendance）', () => { ... })
  it('getRecord 调 GET /attendance/records/:id', () => { ... })
  it('manualClock 调 POST /attendance/manual', () => { ... })
  it('importData 调 POST /attendance/import', () => { ... })
})
```

### 5.3 请假（B3）JSDoc + 测试（子任务 7.2.B.3）

**`api/leave.ts`**（**B3 5 端点函数**）+ 3 页面

**JSDoc + 校验链**（**完整代码见 §4.3**）：

```ts
/**
 * 请假管理 API（M5-2-B）
 * @module api/leave
 * @description 消费后端 /api/leaves 5 端点（balance / create / list / get / cancel）
 * @leaveType 8 类型：annual / sick / personal / compensatory / marriage / maternity / paternity / bereavement
 * @status 5 状态：draft / submitted / approved / rejected / cancelled
 */
```

**3 请假页面**（`LeaveList.vue` / `LeaveDetail.vue` / `LeaveForm.vue`）：

#### 5.3.1 测试用例（2-3 个 it）

```ts
describe('api/leave.ts', () => {
  it('getLeaveBalance 调 GET /leaves/balance（带 employeeId + leaveType + year 参数）', () => { ... })
  it('createLeaveRequest 调 POST /leaves/requests', () => { ... })
  it('listLeaveRequests 调 GET /leaves/requests（注意不是 /leaves）', () => { ... })
  it('getLeaveRequest 调 GET /leaves/requests/:id', () => { ... })
  it('cancelLeaveRequest 调 POST /leaves/requests/:id/cancel', () => { ... })
})
```

### 5.4 加班（B4）JSDoc + 测试（子任务 7.2.B.4）

**`api/overtime.ts`**（**B4 3 端点函数**）+ 2 页面

**JSDoc + 校验链**（**完整代码见 §4.4**）：

```ts
/**
 * 加班管理 API（M5-2-B）
 * @module api/overtime
 * @description 消费后端 /api/overtime 3 端点（create / list / cancel）
 * @overtimeType 3 类型：weekday / weekend / holiday
 * @compensationType 2 类型：pay / comp（加班费 / 调休 1:1）
 * @status 5 状态：draft / submitted / approved / rejected / cancelled
 * @noDetail **没有 GET /:id 详情端点**，详情展示在 List 内 Dialog
 */
```

**2 加班页面**（`OvertimeList.vue` / `OvertimeForm.vue`）：

#### 5.4.1 测试用例（2-3 个 it）

```ts
describe('api/overtime.ts', () => {
  it('createOvertimeRequest 调 POST /overtime/requests', () => { ... })
  it('listOvertimeRequests 调 GET /overtime/requests', () => { ... })
  it('cancelOvertimeRequest 调 POST /overtime/requests/:id/cancel', () => { ... })
  // **没有** getOvertimeRequest 单测（后端无此端点）
})
```

### 5.5 出差（B5）JSDoc + 测试（子任务 7.2.B.5）

**`api/businessTrip.ts`**（**B5 3 端点函数**）+ 2 页面

**JSDoc + 校验链**（**完整代码见 §4.5**）：

```ts
/**
 * 出差管理 API（M5-2-B）
 * @module api/businessTrip
 * @description 消费后端 /api/business-trips 3 端点（create / list / cancel）
 * @status 5 状态：draft / submitted / approved / rejected / cancelled
 * @noDetail **没有 GET /:id 详情端点**，详情展示在 List 内 Dialog
 */
```

**2 出差页面**（`BusinessTripList.vue` / `BusinessTripForm.vue`）：

#### 5.5.1 测试用例（2-3 个 it）

```ts
describe('api/businessTrip.ts', () => {
  it('createBusinessTrip 调 POST /business-trips/requests（注意连字符）', () => { ... })
  it('listBusinessTrips 调 GET /business-trips/requests', () => { ... })
  it('cancelBusinessTrip 调 POST /business-trips/requests/:id/cancel', () => { ... })
})
```

### 5.6 月度汇总（B6）JSDoc + 测试（子任务 7.2.B.6）

**`api/monthlySummary.ts`**（**B6 4 端点函数**）+ 2 页面

**JSDoc + 校验链**（**完整代码见 §4.6**）：

```ts
/**
 * 月度考勤汇总 API（M5-2-B）
 * @module api/monthlySummary
 * @description 消费后端 /api/monthly-summaries 4 端点（generate / list / confirm / lock）
 * @status 3 状态：draft / employee_confirmed / hr_locked
 * @flow draft → employee_confirmed（员工确认） → hr_locked（HR 锁定，**M4 薪酬读取**）
 * @noDetail **没有 GET /:id 详情端点**，详情展示在 List 内 Dialog
 */
```

**2 月度汇总页面**（`MonthlySummaryList.vue` / `MonthlySummaryForm.vue`）：

#### 5.6.1 测试用例（2-3 个 it）

```ts
describe('api/monthlySummary.ts', () => {
  it('generateMonthlySummary 调 POST /monthly-summaries/generate', () => { ... })
  it('listMonthlySummaries 调 GET /monthly-summaries（必传 year + month）', () => { ... })
  it('confirmMonthlySummary 调 POST /monthly-summaries/:id/confirm', () => { ... })
  it('lockMonthlySummary 调 POST /monthly-summaries/:id/lock（仅 hr/admin）', () => { ... })
})
```

### 5.7 通用测试（视图 + 状态机）

```ts
describe('views/__tests__/attendance.test.ts', () => {
  it('6 模块列表页（shift / attendance / leave / overtime / businessTrip / monthlySummary）渲染正确', () => { ... })
  it('25 状态机切换（B1 3 + B2 4 + B3 5 + B4 5 + B5 5 + B6 3）', () => { ... })
  it('B4/B5/B6 没有 GET /:id 详情端点（详情在 List 内 Dialog）', () => { ... })
  it('employee 角色可调 clock-in + 自己 leave/overtime/businessTrip', () => { ... })
})
```

### 5.8 通用组件：Calendar

**`components/Calendar.vue`**（**B2 打卡日历视图 + B6 月度汇总日历视图复用**）：

```vue
<script setup lang="ts">
/**
 * 日历通用组件（M5-2-B）
 * @module components/Calendar
 * @description Element Plus ElCalendar 封装，支持打卡记录 / 月度汇总 / 排班 3 种模式
 * @props mode 'attendance' | 'summary' | 'schedule'（3 模式）
 * @props data 各模式对应数据（打卡记录 / 月报 / 排班）
 * @emits dateClick(date: string) 日期点击
 */
interface CalendarData {
  [date: string]: {  // YYYY-MM-DD
    status?: 'normal' / 'late' / 'early' / 'absent' / 'leave' / 'overtime' / 'business_trip' | 'scheduled' | 'unscheduled'
    summary?: string  // 详情文本
  }
}
interface Props {
  mode: 'attendance' / 'summary' / 'schedule'
  data: CalendarData
  defaultDate?: string  // YYYY-MM-DD
}
const props = withDefaults(defineProps<Props>(), { defaultDate: () => new Date().toISOString().slice(0, 10) })
const emit = defineEmits<{
  (e: 'dateClick', date: string): void
}>()
</script>

<template>
  <el-calendar v-model:model-value="props.defaultDate">
    <template #date-cell="{ data }">
      <div :class="['calendar-cell', props.data[data.day]?.status ? `is-${props.data[data.day]?.status}` : '']" @click="emit('dateClick', data.day)">
        <div class="calendar-cell__day">{{ data.day.split('-').slice(2).join('') }}</div>
        <div v-if="props.data[data.day]" class="calendar-cell__summary">{{ props.data[data.day]?.summary }}</div>
      </div>
    </template>
  </el-calendar>
</template>
```

---

## 6. 修改文件清单（明示边界）

### 6.1 必改文件（清单以内）

#### 新增文件（27 个）

1. `client/src/api/types/attendance.ts`
2. `client/src/api/shift.ts`
3. `client/src/api/attendance.ts`
4. `client/src/api/leave.ts`
5. `client/src/api/overtime.ts`
6. `client/src/api/businessTrip.ts`
7. `client/src/api/monthlySummary.ts`
8. `client/src/api/__tests__/shift.test.ts`
9. `client/src/api/__tests__/attendance.test.ts`
10. `client/src/api/__tests__/leave.test.ts`
11. `client/src/api/__tests__/overtime.test.ts`
12. `client/src/api/__tests__/businessTrip.test.ts`
13. `client/src/api/__tests__/monthlySummary.test.ts`
14. `client/src/views/attendance/shift/ShiftList.vue`
15. `client/src/views/attendance/shift/ShiftDetail.vue`
16. `client/src/views/attendance/shift/ShiftAssignment.vue`
17. `client/src/views/attendance/attendance/AttendanceList.vue`
18. `client/src/views/attendance/attendance/AttendanceDetail.vue`
19. `client/src/views/attendance/attendance/AttendanceCorrection.vue`
20. `client/src/views/attendance/leave/LeaveList.vue`
21. `client/src/views/attendance/leave/LeaveDetail.vue`
22. `client/src/views/attendance/leave/LeaveForm.vue`
23. `client/src/views/attendance/overtime/OvertimeList.vue`
24. `client/src/views/attendance/overtime/OvertimeForm.vue`
25. `client/src/views/attendance/businessTrip/BusinessTripList.vue`
26. `client/src/views/attendance/businessTrip/BusinessTripForm.vue`
27. `client/src/views/attendance/monthlySummary/MonthlySummaryList.vue`
28. `client/src/views/attendance/monthlySummary/MonthlySummaryForm.vue`
29. `client/src/router/attendance.ts`
30. `client/src/layouts/AttendanceLayout.vue`
31. `client/src/components/Calendar.vue`
32. `client/src/views/__tests__/attendance.test.ts`

#### 修改文件（2 个）

33. `client/src/api/types/index.ts`（**追加** `export * from './attendance'`，**不删** organization export）
34. `client/src/router/index.ts`（**追加** `...attendanceRoutes`，**不删** /login + /dashboard + organizationRoutes）

**实际清单数**：32 新增 + 2 修改 = **34 个文件**（注：原 §1.4 概数 30 是粗估，**实际 34 个**才能完整覆盖 B1-B6 6 子任务 25 端点 + 15 页面 + 1 通用组件）

### 6.2 禁止修改文件（清单以外）

- **后端任何文件**（**M5-2-B 0 修改** server/）：
  - ❌ 严禁修改 `server/src/services/*` 任何 service（36 service 0 改动）
  - ❌ 严禁修改 `server/src/routes/*` 任何路由
  - ❌ 严禁修改 `server/prisma/schema.prisma`（0 新表）
  - ❌ 严禁修改 `server/src/constants/permissions.ts`（0 新权限点）
  - ❌ 严禁修改 `server/src/app.ts` / `server/src/index.ts`
  - ❌ 严禁修改 `server/src/controllers/*` 任何 controller
  - ❌ 严禁修改 `server/prisma/seed.ts`
  - ❌ 严禁修改 `docs/error-codes.md`（0 新错误码）
  - ❌ 严禁修改 `docs/api-spec.md`（M5-2-B 0 新端点）
- **M5-2-0 共享基础设施**（除 `router/index.ts` 追加 `attendanceRoutes`）：
  - ❌ 严禁修改 `client/src/api/{http,auth,types}.ts`（**M5-2-B 创建独立的 `api/types/attendance.ts`**）
  - ❌ 严禁修改 `client/src/stores/{user,permission,dict}.ts`
  - ❌ 严禁修改 `client/src/layouts/DefaultLayout.vue`（**M5-2-B 创建独立的 `AttendanceLayout.vue`**）
  - ❌ 严禁修改 `client/src/views/{Login,Dashboard}.vue`
  - ❌ 严禁修改 `client/src/utils/{auth,permission,format}.ts`
- **M5-2-A1/A2 已建 30 client 文件**（除**追加**路由到 `router/index.ts` + types 索引）：
  - ❌ 严禁修改 `client/src/api/{company,department,employee,onboarding,regularization,transfer,offboarding,contract}.ts`（8 API 文件）
  - ❌ 严禁修改 `client/src/api/types/organization.ts`（A1+A2+A3+A3-A7 类型，**与 attendance 无关**）
  - ❌ 严禁修改 `client/src/router/organization.ts`（20 子路由，**不删** A1+A2+A3+A3-A7 路由）
  - ❌ 严禁修改 `client/src/layouts/OrganizationLayout.vue`（9 菜单项，**不删** A1+A2+A3+A3-A7 菜单项）
  - ❌ 严禁修改 `client/src/views/organization/*`（27 已建页面，**不删** A1+A2+A3+A3-A7 页面）
  - ❌ 严禁修改 `client/src/components/{StatusTag,TreeNode}.vue`（如需追加 status，**仅在 `api/types/attendance.ts` 追加 enum**，**不改** StatusTag 组件，StatusTag 自动从 `STATUS_MAP` 字典扩展）
- **M5-2-B 严禁修改**：
  - ❌ 严禁修改 `client/package.json`（**禁止装新依赖**）
  - ❌ 严禁修改 `client/vite.config.ts` / `tsconfig.json` / `.eslintrc.cjs`
  - ❌ 严禁创建 M5-2-C / M5-2-D 业务页面（**留独立任务**）
  - ❌ 严禁创建 M2 后端新表 / 新 service / 新权限点
- 任何旧测试文件（`*.test.ts`）除非在 §6.1 清单内

### 6.3 越界检测（交付时自检）

```bash
git diff --name-only HEAD
# 必须只出现 §6.1 列出的 34 个文件
# 如果出现其他文件 → 越界，必须回退

# 特别检查：D1-D6 + C1-C8 共 32 service 0 行改动 + M0.5 4 service 0 行改动 = 36 service
git diff HEAD -- 'server/src/services/'
# 必须为 0 行

# 验证 0 修改后端任何文件
git diff HEAD -- 'server/'
# 必须 0 行

# 验证 0 修改 client/package.json
git diff HEAD -- 'client/package.json'
# 必须 0 行

# 验证 0 修改 M5-2-0 共享基础设施（除 router/index.ts 追加）
git diff HEAD -- 'client/src/api/http.ts' 'client/src/api/auth.ts' 'client/src/stores/' 'client/src/layouts/DefaultLayout.vue' 'client/src/views/Login.vue' 'client/src/views/Dashboard.vue' 'client/src/utils/'
# 必须 0 行

# 验证 0 修改 M5-2-A1/A2 已建 30 client 文件
git diff HEAD -- 'client/src/api/company.ts' 'client/src/api/department.ts' 'client/src/api/employee.ts' 'client/src/api/onboarding.ts' 'client/src/api/regularization.ts' 'client/src/api/transfer.ts' 'client/src/api/offboarding.ts' 'client/src/api/contract.ts' 'client/src/api/types/organization.ts' 'client/src/router/organization.ts' 'client/src/layouts/OrganizationLayout.vue' 'client/src/views/organization/' 'client/src/components/StatusTag.vue' 'client/src/components/TreeNode.vue'
# 必须 0 行
```

---

## 7. 验收标准

### 7.1 硬性验收（必须 100% 通过）

```bash
# 在项目根 D:\projects\hrms

# 1. 后端测试（**0 改动**，沿用 876）
pnpm --filter hrms-server test
# 预期：876/876 通过

# 2. 后端 type-check
pnpm --filter hrms-server type-check
# 预期：0 错

# 3. 客户端 type-check
cd client && npx vue-tsc --noEmit
# 预期：EXIT_CODE: 0

# 4. 客户端 build
cd client && pnpm build
# 预期：dist 生成成功

# 5. 客户端 lint
cd client && pnpm lint
# 预期：0 error
```

### 7.2 验收明细

- [ ] **34 个 client 文件**（32 新增 + 2 修改）齐全
- [ ] **B1 班次 3 页面**（List/Detail/Assignment）渲染正确
- [ ] **B2 打卡 3 页面**（List/Detail/Correction）渲染正确
- [ ] **B3 请假 3 页面**（List/Detail/Form）渲染正确
- [ ] **B4 加班 2 页面**（List/Form）渲染正确（**没有独立详情页，详情在 List 内 Dialog**）
- [ ] **B5 出差 2 页面**（List/Form）渲染正确（**没有独立详情页**）
- [ ] **B6 月度汇总 2 页面**（List/Form）渲染正确（**没有独立详情页**）
- [ ] **AttendanceLayout** 6 菜单项 + activeKey 正确
- [ ] **router/attendance.ts** 15 子路由（含 `attendance/records/:id` 详情路径，**不与 `attendance` 列表冲突**）
- [ ] **api/types/attendance.ts** 6 模型 + 25 状态机
- [ ] **0 修改后端任何文件**
- [ ] **0 修改 `client/package.json`**
- [ ] **0 修改 M5-2-0 共享基础设施**（除 `router/index.ts` 追加 `attendanceRoutes`）
- [ ] **0 修改 M5-2-A1/A2 已建 30 client 文件**
- [ ] **16-20 单元测试**通过（6 API 文件 + 1 视图 + 25 状态机）
- [ ] **client type-check 0 错 + build 成功**

### 7.3 红线 9 验证

- [ ] **0 修改后端任何文件**（**仅 client/src**）
- [ ] **0 修改 `client/package.json`**
- [ ] **0 修改 M5-2-0 共享基础设施**（除 `router/index.ts` 追加 `attendanceRoutes`）
- [ ] **0 修改 M5-2-A1/A2 已建 30 client 文件**
- [ ] **34 个 client 文件**（32 新增 + 2 修改，**不超出清单**）
- [ ] **D1-D6 + C1-C8 业务代码 0 行改动**（36 service + 40 表 = 0 行）
- [ ] **M0.5 4 service 0 行改动**
- [ ] **复用 M5-2-0 + M5-2-A1/A2 通用组件 + Pinia stores + http + utils + 8 已建 API 文件 + types**
- [ ] **5 角色 RBAC 严格遵守**（admin / hr / dept_head / executive / employee，**无 finance 角色**）
- [ ] **16-20 单元测试**（内联断言 + 导出 `run*Tests()`，**不引入 vitest**）

### 7.4 原有用例未改（硬要求）

```bash
git diff HEAD -- 'server/src/**/*.test.ts' 'client/src/**/*.test.ts' | grep -E '^[+-]' | grep -v '^[+-]{3}' | grep -v 'shift\.test|attendance\.test|leave\.test|overtime\.test|businessTrip\.test|monthlySummary\.test|attendance\.test$'
# 必须为 0 行
```

---

## 8. 禁止做的事（额外禁忌清单）

1. ❌ 做 M5-2-A1/A2/C/D / M5-3-M5-8
2. ❌ 修改后端任何文件（**0 修改 server/**）
3. ❌ 修改 `client/package.json`（**禁止装新依赖**）
4. ❌ 修改 `client/vite.config.ts` / `tsconfig.json` / `.eslintrc.cjs`
5. ❌ 修改 M5-2-0 共享基础设施（除 `router/index.ts` 追加 `attendanceRoutes`）
6. ❌ 修改 M5-2-A1/A2 已建 30 client 文件
7. ❌ 顺手装新依赖（**unplugin-vue-components / pinia-plugin-persistedstate / vee-validate / vueuse / dayjs / moment / lodash 等**）
8. ❌ 创建 M2 后端新表 / 新 service / 新权限点
9. ❌ 修改 `server/src/services/*` 任何 service（36 service 0 改动）
10. ❌ 修改 `server/prisma/schema.prisma`（0 新表）
11. ❌ 修改 `server/src/constants/permissions.ts`（0 新权限点）
12. ❌ 修改 `docs/error-codes.md`（0 新错误码）
13. ❌ 修改 `docs/api-spec.md`（M5-2-B 0 新端点）
14. ❌ 创建 M5-2-C/D 业务页面
15. ❌ 改 D1-D6 + C1-C8 任何 service（**不重做 M1-M4**）
16. ❌ 改 M0.5-1/2/5/6 任何 service（**不重做 M0.5**）
17. ❌ 改 M1 / M2 / M3 / M4 任何 service（**不重做**）
18. ❌ 修改任何后端已存在的 .ts 文件（除 §6.1 清单内，但 §6.1 仅 client/）
19. ❌ 修改任何旧测试文件
20. ❌ 越界做 6 角色 RBAC（M0 锁定 5 角色：admin / hr / dept_head / executive / employee，**无 finance**）
21. ❌ 用 Options API / class 组件（**仅 Vue3 Composition API + `<script setup lang="ts">`**）
22. ❌ 用 `<button>` / `<input>` 原生 HTML（**仅 Element Plus 组件**）
23. ❌ 用 `any` 类型（**严格 TypeScript**）
24. ❌ "我觉得这也有用"（任何"顺便做"越界内容一律禁止）
25. ❌ 重写 notification / configService / aiSummary
26. ❌ 给 finance 角色分配权限（**D1 教训延续**）
27. ❌ 越界实现 M1-M4 后端功能（**M5-2-B 仅消费 B1-B6 25 端点**）
28. ❌ 试图为 B4/B5/B6 创建 GET /:id 详情页面（**后端无此端点**，详情在 List 内 Dialog）

---

## 9. 完成标准

### 9.1 必跑命令（全部通过才交付）

```bash
cd D:\projects\hrms

# 1. 后端测试（**0 改动**，沿用 876）
pnpm --filter hrms-server test
# 预期：876/876 通过

# 2. 后端 type-check
pnpm --filter hrms-server type-check
# 预期：0 错

# 3. 客户端 type-check
cd client && npx vue-tsc --noEmit
# 预期：EXIT_CODE: 0

# 4. 客户端 build
cd client && pnpm build
# 预期：dist 生成成功

# 5. 客户端 lint
cd client && pnpm lint
# 预期：0 error
```

### 9.2 越界自检（必查）

```bash
# 1. 越界文件检测
git diff --name-only HEAD
# 预期：§6.1 列出 34 个文件（32 新增 + 2 修改）

# 2. 36 service 0 行改动检测
git diff HEAD -- 'server/src/services/'
# 预期：0 行

# 3. 0 修改后端任何文件检测
git diff HEAD -- 'server/'
# 预期：0 行

# 4. 0 修改 client/package.json 检测
git diff HEAD -- 'client/package.json'
# 预期：0 行

# 5. 0 修改 M5-2-0 共享基础设施检测
git diff HEAD -- 'client/src/api/http.ts' 'client/src/api/auth.ts' 'client/src/api/types.ts' 'client/src/stores/' 'client/src/layouts/DefaultLayout.vue' 'client/src/views/Login.vue' 'client/src/views/Dashboard.vue' 'client/src/utils/'
# 预期：0 行

# 6. 0 修改 M5-2-A1/A2 已建 30 client 文件检测
git diff HEAD -- 'client/src/api/company.ts' 'client/src/api/department.ts' 'client/src/api/employee.ts' 'client/src/api/onboarding.ts' 'client/src/api/regularization.ts' 'client/src/api/transfer.ts' 'client/src/api/offboarding.ts' 'client/src/api/contract.ts' 'client/src/api/types/organization.ts' 'client/src/router/organization.ts' 'client/src/layouts/OrganizationLayout.vue' 'client/src/views/organization/' 'client/src/components/StatusTag.vue' 'client/src/components/TreeNode.vue'
# 预期：0 行

# 7. 客户端 type-check
cd client && npx vue-tsc --noEmit
# 预期：EXIT_CODE: 0

# 8. 客户端 build
cd client && pnpm build
# 预期：dist 生成成功

# 9. 旧测试 0 改动检测
git diff HEAD -- 'server/src/**/*.test.ts' 'client/src/**/*.test.ts' | grep -E '^[+-]' | grep -v '^[+-]{3}' | grep -v 'shift\.test|attendance\.test|leave\.test|overtime\.test|businessTrip\.test|monthlySummary\.test|attendance\.test$'
# 预期：0 行
```

### 9.3 交付报告（Cursor 输出格式）

请在交付时输出以下内容（**M5-2-A2 交付报告格式**）：

```
# M5-2-B 交付报告

M5-2-B 已落地：M2 考勤假勤前端 · B1 班次 + B2 打卡 + B3 请假 + B4 加班 + B5 出差 + B6 月度汇总。0 修改后端 / 0 修改 package.json / 0 修改 M5-2-0 共享基础设施 / 0 修改 M5-2-A1/A2 已建文件，未改 D1-D6 / C1-C8 / M0.5 业务代码。未提交 commit。

## 改动文件清单（X 新增 + X 修改 = X 个）

**新增（X）**：
1. client/src/api/types/attendance.ts
...（共 32 个新文件，**不列**）

**修改（X）**：
1. client/src/api/types/index.ts（**追加** `export * from './attendance'`，**不删** organization export）
2. client/src/router/index.ts（**追加** `...attendanceRoutes`，**不删** /login + /dashboard + organizationRoutes）

**未改（按约束）**：
- D1-D6 + C1-C8 共 32 service（diff = 0）
- M0.5 4 service（diff = 0）
- server/ 任何文件（diff = 0）
- client/package.json（diff = 0）
- M5-2-0 共享基础设施（除 router/index.ts 追加）
- M5-2-A1/A2 已建 30 client 文件（diff = 0）

## 7 条强制声明

1. 未修改任何 M0/M0.5/M1/M2/M3/C1-C8 旧测试（仍 876 个）
2. 新单测 X 个（内联断言 + 导出 `run*Tests()`）
3. 未做 M5-2-C/D / M5-3-M5-8 任何切片
4. 未 import 跨业务 service（**M5-2-B 0 新 service**）
5. 5 角色 RBAC 严格遵守（admin / hr / dept_head / executive / employee，**无 finance**）
6. **0 修改后端 / 0 修改 package.json / 0 修改 M5-2-0 + M5-2-A1/A2 / 0 新业务 service**（纯前端 + 追加）
7. **D1-D6 + C1-C8 + M0.5 业务代码 0 行改动**（36 service diff = 0）

## 红线 9 自检

- [x] 0 修改后端任何文件
- [x] 0 修改 client/package.json
- [x] 0 修改 M5-2-0 共享基础设施（除 router/index.ts 追加）
- [x] 0 修改 M5-2-A1/A2 已建 30 client 文件
- [x] 34 个 client 文件（32 新增 + 2 修改，**不超出清单**）
- [x] 32 service 0 行改动
- [x] M0.5 4 service 0 行改动
- [x] 复用 M5-2-0 + M5-2-A1/A2 通用组件 + Pinia stores + http + utils + 8 已建 API + types
- [x] 5 角色 RBAC 严格遵守（**无 finance 角色**）
- [x] 16-20 单元测试（内联断言，**不引入 vitest**）

## 验收命令输出

| 命令 | 结果 |
|---|---|
| pnpm --filter hrms-server test | 876/876 通过（**0 改动**） |
| pnpm --filter hrms-server type-check | 0 错 |
| npx vue-tsc --noEmit（client） | EXIT_CODE: 0 |
| pnpm build（client） | dist 生成成功 |
| pnpm lint（client） | 0 error（XX warnings） |

单测分布：shift.test X + attendance.test X + leave.test X + overtime.test X + businessTrip.test X + monthlySummary.test X + attendance.test$ X = X（16-20）。

## 越界检查

git diff --name-only HEAD = §6.1 列出 34 个文件，0 越界。
server/ 任何文件 diff = 0。
client/package.json diff = 0。
M5-2-0 共享基础设施 diff = 0（除 router/index.ts 追加）。
M5-2-A1/A2 已建 30 client 文件 diff = 0。
旧 *.test.ts diff 为空。
5 角色无 finance 角色（utils/permission.ts 仅含 5 个 RoleCode）。

## 已知问题

（如有边界场景未覆盖 / TODO 留二期 / mock 限制等，请详细说明）
```

---

## 10. 参考文档

- **V1.2 主文档** §四.6 B1-B6：https://example.com/HRMS-V1.2.md#四-6-m2-考勤假勤
- **V1.2 主文档** §三.6 RBAC：https://example.com/HRMS-V1.2.md#三-6-rbac-权限模型
- **M5-2-A2 业务 commit** fd9e4a7（M1 员工生命周期 28 文件 / 20 单测）：https://example.com/commit/fd9e4a7
- **M5-2-A2 提示词** docs/cursor-prompts/M5-2-A2.md
- **M2-B1 业务 commit** 256xxx（后端 B1 班次 5 端点）：https://example.com/commit/xxx

---

**任务开始**：M5-2-B 业务实现 = 6 子任务 + 15 页面 + 25 端点消费 + 0 新增后端端点 + 34 个 client 文件 + 16-20 单测 + 0 修改后端 + 0 修改 package.json + 36 service 0 行改动 + 5 角色无 finance。
