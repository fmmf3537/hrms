# M5-2-A2 M1 组织人事前端 - 员工生命周期（A3 入职 + A4 转正 + A5 调动 + A6 离职 + A7 合同）Cursor 提示词

> **任务 ID**：M5-2-A2
> **任务名称**：M5-2 第 2 个业务子切片 — M1 组织人事前端 · 员工生命周期（A3 入职 + A4 转正 + A5 调动 + A6 离职 + A7 合同）
> **任务范围**：5 子任务 / 1.5d 工时 / V1.2 §四.5 A3-A7
> **提示词生成日期**：2026-08-31
> **目标单测增量**：约 16-20 个（API 函数 + 页面内联断言 + 状态机）
> **目标 commit**：1 个业务 commit + 1 个提示词 commit
> **依赖**：[M5-2-A1 基础数据](M5-2-A1.md) 业务 commit `7689a45` 已完成

---

## ⚠️ 强约束（最优先阅读）

**历史教训**（19 次）：
1. M0.5-5 任务中，Cursor 越界做了 M0.5-6（配置中心）+ M0.5-7（改密 + 2FA）共 2 个不在提示词范围内的切片。本次任务严禁重蹈覆辙。
2. M1-A6 提示词第一次瘦身到 15KB，Cursor 推断/补全消耗大量 token，返工 1 次。本次任务按 70-90KB 详尽模式写。
3. D1 提示词 §3.3 误列 5 角色包含 finance，**Cursor 主动识别 + 正确处理**。D2-D6 + C1-C8 + M5-1 + M5-2-0 + M5-2-A1 已修正。**M5-2-A2 沿用 5 角色 RBAC 无 finance**（admin / hr / dept_head / executive / employee，**无 finance 角色**）。
4. M2-wrap-up.md §7 误写 D3 = "季度校准会议"。M5-2-A2 不重蹈覆辙，严格按 V1.2 §四.5 A3-A7 实际范围。
5. D3 → ... → C8 红线升级到 36 service 0 行改动。**M5-2-A2 沿用 36 service 0 行改动**（**M5-2-A2 0 修改后端任何 service**）。
6. D4 验收发现 2 个 B3/B5 跨 UTC 边界日期敏感旧测。M5-2-A2 沿用此模式。
7. C5 报告"已知问题 2"：V1.2 切片表 C5=劳务费/C6=工资条与本任务 ID「M4-C5」不一致。**M5-2-A2 不涉及 V1.2 切片表错位**（前端子切片，无 V1.2 切片表对应）。
8. **M5-2-A1 已落地 + M5-2-A2 主动识别 6 个关键点**（M5-2-A1 commit `7689a45` 报告）：
   - 后端 M1 实际端点路径与提示词不同（A1 没有 suspend/reactivate、A2 没有 merge/employees、A3 employee 没有 transfer/regularize/resign/export/import）
   - 工号由后端生成，创建表单不传 `employeeNo`
   - `org-chart` 路由必须写在 `employees/:id` 之前
   - `dept_head` 后端只有 `employee:read:self-dept`，**没有** `company:read`
   - **M5-2-A2 必须沿用这些约束 + 主动识别后端真实端点**
9. **M5-2-A2 关键设计**：
   - **0 修改后端任何文件**（**M5-2-A2 是纯前端**）
   - **0 修改 `client/package.json`**（**禁止装新依赖**）
   - **0 新增 D1-D6 / C1-C8 / M0.5 / M5-2-0 service**（沿用 36 service 0 改动）
   - **沿用 M5-2-0 共享基础设施**（router/api/stores/layouts/components/utils + Login + Dashboard）
   - **沿用 M5-2-A1 基础数据**（`api/types/organization.ts` **追加** A3-A7 类型 + `router/organization.ts` **追加** 11 子路由 + `OrganizationLayout.vue` **追加** 5 菜单项）
   - **0 修改 M5-2-A1 已建 13 页面**（**仅追加** A3-A7 页面）
   - **5 角色 RBAC 严格遵守**（admin / hr / dept_head / executive / employee，**无 finance**）
10. **M5-2-A2 后端端点确认**（5 模块 26 端点，**实际后端路径**）：
    - **A3 入职**（5 端点）：GET /onboarding/ + GET /onboarding/:id + POST /onboarding/ + POST /onboarding/:id/parse-ocr（**OCR AI 能力**）+ POST /onboarding/:id/confirm
    - **A4 转正**（4 端点）：GET /regularizations/ + GET /regularizations/:id + POST /regularizations/ + POST /regularizations/:id/cancel
    - **A5 调动**（5 端点）：GET /transfers/ + GET /transfers/:id + POST /transfers/ + POST /transfers/:id/update + POST /transfers/:id/cancel
    - **A6 离职**（6 端点）：GET /offboarding/ + GET /offboarding/:id + POST /offboarding/ + POST /offboarding/:id/confirm-handover + POST /offboarding/:id/cancel + POST /offboarding/:id/issue-certificate
    - **A7 合同**（6 端点）：POST /contracts/webhook/e-sign（**e-签宝 webhook，e-签宝 SaaS 留二期**）+ GET /contracts/ + GET /contracts/:id + POST /contracts/ + POST /contracts/:id/update + POST /contracts/:id/cancel
    - **M5-2-A2 总计消费 26 后端端点**（**0 新增**）

**红线**（违反任何一条 → 任务失败，需回退重做）：

1. **禁止越界做其他切片**：本提示词只授权 "M5-2-A2 切片（M1 员工生命周期 · A3-A7）"。严禁自作主张做 M5-2-B / M5-2-C / M5-2-D / M5-3-M5-8 任何切片。
2. **禁止修改后端文件**：本切片是**纯前端**，严禁修改 `server/` 任何文件。
3. **禁止修改 `client/package.json`**：本切片不装新依赖。
4. **禁止修改 M5-2-0 共享基础设施文件**（除**追加**菜单 + 路由表 + api/types）：
   - ❌ 严禁修改 `client/src/router/index.ts`（已**追加** organizationRoutes，**仅**路由追加，**不改** /login + /dashboard）
   - ❌ 严禁修改 `client/src/api/{http,auth}.ts`（**M5-2-A2 创建独立的 `api/{onboarding,regularization,transfer,offboarding,contract}.ts`**）
   - ❌ 严禁修改 `client/src/stores/{user,permission,dict}.ts`
   - ❌ 严禁修改 `client/src/layouts/DefaultLayout.vue`（**M5-2-A2 仅**修改 OrganizationLayout 追加菜单项）
   - ❌ 严禁修改 `client/src/views/{Login,Dashboard}.vue`
   - ❌ 严禁修改 `client/src/utils/{auth,permission,format}.ts`
5. **禁止修改 M5-2-A1 已建文件**（除**追加**类型 + 路由 + 菜单项）：
   - ❌ 严禁修改 `client/src/router/organization.ts` 的 9 已建子路由（**仅追加** 11 新子路由）
   - ❌ 严禁修改 `client/src/api/{company,department,employee}.ts`（**M5-2-A2 创建独立的 5 个新 API 文件**）
   - ❌ 严禁修改 `client/src/api/types/organization.ts` 的 A1+A2+A3 已建类型（**仅追加** A3-A7 类型 + 5 status enum）
   - ❌ 严禁修改 `client/src/views/organization/{companies,departments,employees}/*` 13 已建页面
6. **必须报告完整**：交付报告必须列出全部新增/修改文件（即使是 0 个也要说"0 个"），并明确说明"原有用例是否改动"。
7. **禁止"我觉得这也有用"**：任何"顺便做"或"我觉得可以优化"的越界内容一律禁止。包括但不限于：顺手做 M5-2-B/C/D / 顺手装新依赖 / 顺手加 finance 角色 / 顺手做 M1 后端功能 / 顺手修改 M5-2-0 共享基础设施。
8. **M5-2-A2 范围强约束**（红线 9）：
   - **0 修改后端任何文件**（**仅 client/src**）
   - **0 修改 `client/package.json`**
   - **0 修改 M5-2-0 共享基础设施**（除 `router/index.ts` 已追加，**不再追加**）
   - **0 修改 M5-2-A1 已建 13 页面**（除 `api/types/organization.ts` 追加类型 + `router/organization.ts` 追加 11 子路由 + `OrganizationLayout.vue` 追加 5 菜单项）
   - **29 个 client 文件**（26 新增 + 3 修改）
   - **D1-D6 + C1-C8 业务代码 0 行改动**（36 service + 40 表 = 0 行）
   - **M0.5 4 service 0 行改动**
   - **复用 M5-2-0 + M5-2-A1 通用组件 + Pinia stores + http + utils + 5 个已建 API 文件 + types**
   - **5 角色 RBAC 严格遵守**（admin / hr / dept_head / executive / employee，**无 finance 角色**）
   - **16-20 单元测试**（内联断言 + 导出 `run*Tests()`，**不引入 vitest**）

---

## 1. 任务 ID + 目标

### 1.1 任务 ID

**M5-2-A2**（M5-2 第 2 个业务子切片 — M1 组织人事前端 · **A3 入职 + A4 转正 + A5 调动 + A6 离职 + A7 合同** 员工生命周期）

### 1.2 任务目标

实现 M5 联调 Vue3 前端的**第二个业务子切片**（M1 组织人事 · 员工生命周期），包含 5 子任务：

| 子任务 | 范围 | 端点 | 新增/修改 |
|---|---|---|---|
| 7.2.A2.1 入职（A3）| 5 端点（list/get/create/parse-ocr/confirm）| 5 | 1 API + 4 页面 + 1 测试 |
| 7.2.A2.2 转正（A4）| 4 端点（list/get/create/cancel）| 4 | 1 API + 2 页面 + 1 测试 |
| 7.2.A2.3 调动（A5）| 5 端点（list/get/create/update/cancel）| 5 | 1 API + 2 页面 + 1 测试 |
| 7.2.A2.4 离职（A6）| 6 端点（list/get/create/confirm-handover/cancel/issue-certificate）| 6 | 1 API + 3 页面 + 1 测试 |
| 7.2.A2.5 合同（A7）| 6 端点（webhook-e-sign/list/get/create/update/cancel）| 6 | 1 API + 3 页面 + 1 测试 |
| **共用** | api/types/organization.ts 追加 5 模块类型 + OrganizationLayout 追加 5 菜单项 + router/organization.ts 追加 11 子路由 | — | 3 修改 |
| **M5-2-A2 合计** | **26 端点消费** | **26** | **26 新增 + 3 修改 = 29 文件** |

**V1.2 §四.5 A3-A7 验收**：
- A3 入职：5 端点（list / get / create / OCR 解析 / 确认）
- A4 转正：4 端点（list / get / create / cancel）
- A5 调动：5 端点（list / get / create / update / cancel）
- A6 离职：6 端点（list / get / create / 确认交接 / cancel / 发放证明）
- A7 合同：6 端点（e-签宝 webhook / list / get / create / update / cancel）

**M5-2-A2 业务量**：
- 0 后端文件改动（**仅消费 26 端点**）
- 0 `client/package.json` 改动
- 0 M5-2-0 共享基础设施文件改动（M5-2-A1 业务 commit `7689a45` 时**已追加**路由）
- 0 M5-2-A1 已建 13 页面文件改动
- 29 client 文件改动（**26 新增 + 3 修改**）
- ~3500-4500 行 Vue3 + TypeScript 代码
- 16-20 单元测试（内联断言）

### 1.3 端点清单（**0 新增后端端点**，消费 A3-A7 已有 26 端点）

**复用后端 M1 路由**（`server/src/routes/index.ts` 已挂载 `/onboarding` / `/regularizations` / `/transfers` / `/offboarding` / `/contracts`）：

| 模块 | 路由前缀 | 端点 | 端点路径 |
|---|---|---|---|
| A3 入职 | `/api/onboarding` | 5 | GET / + GET /:id + POST / + POST /:id/parse-ocr + POST /:id/confirm |
| A4 转正 | `/api/regularizations` | 4 | GET / + GET /:id + POST / + POST /:id/cancel |
| A5 调动 | `/api/transfers` | 5 | GET / + GET /:id + POST / + POST /:id/update + POST /:id/cancel |
| A6 离职 | `/api/offboarding` | 6 | GET / + GET /:id + POST / + POST /:id/confirm-handover + POST /:id/cancel + POST /:id/issue-certificate |
| A7 合同 | `/api/contracts` | 6 | POST /webhook/e-sign + GET / + GET /:id + POST / + POST /:id/update + POST /:id/cancel |
| **M5-2-A2 合计** | — | **26** | — |

**0 新增后端端点**（M5-2-A2 是纯前端，**仅消费**后端 M1-A3-A7 已有 26 端点）

### 1.4 单测目标

16-20 个新单测，按 5 子任务 + 通用测试分布：

| describe | it 数 | 目标 |
|---|---|---|
| `api/onboarding.ts` | 2-3 | listOnboardings / getOnboarding / createOnboarding / confirmOnboarding 类型 + parseOcr |
| `api/regularization.ts` | 2-3 | listRegularizations / getRegularization / createRegularization / cancelRegularization |
| `api/transfer.ts` | 2-3 | listTransfers / getTransfer / createTransfer / updateTransfer / cancelTransfer |
| `api/offboarding.ts` | 3-4 | listOffboardings / getOffboarding / createOffboarding / confirmHandover / cancelOffboarding / issueCertificate |
| `api/contract.ts` | 3-4 | listContracts / getContract / createContract / updateContract / cancelContract |
| `views/__tests__/employee-lifecycle.test.ts` | 4-5 | 5 个模块列表页 + 状态机（5 模块 × 5 status enum 切换）|

**沿用 M5-2-A1 内联断言模式**（内联 `describe/it/expectEqual` + 导出 `run*Tests()`），**不引入 vitest / @vue/test-utils**

---

## 2. 上下文

### 2.1 项目位置

- **项目根**：`D:\projects\hrms`
- **主文档**：[`docs/HRMS-V1.2.md`](../../HRMS-V1.2.md) §四.5 A3-A7
- **API 契约**：[`docs/api-spec.md`](../../api-spec.md) §3.x organization（51 端点 = 25 A1+A2+A3 + 26 A3-A7）
- **AI Coding 上下文**：[`AGENTS.md`](../../../AGENTS.md)
- **M5-2-A1 基础数据**：[`docs/cursor-prompts/M5-2-A1.md`](M5-2-A1.md) commit `7689a45`

### 2.2 当前状态（M0+M0.5+M1+M2+M3+M4+M5-1+M5-2-0+M5-2-A1 已全部完成）

- **M5-2-A1 commit**：`7689a45 feat(client): M5-2-A1 M1 组织人事前端 · 基础数据`
- **后端测试**：876/876 通过
- **M5-2-A1 已建**：
  - `api/types/organization.ts`（A1+A2+A3 类型）
  - `api/{company,department,employee}.ts`（3 API 文件）
  - `router/organization.ts`（9 子路由）
  - `OrganizationLayout.vue`（4 菜单项：法人公司 / 部门管理 / 员工档案 / 组织架构图）
  - `views/organization/{companies,departments,employees}/*`（13 页面）
  - `components/{StatusTag,TreeNode}.vue`（2 通用组件）
- **M5-2-A2 严禁修改**：
  - ❌ `server/` 任何文件（0 修改）
  - ❌ `client/package.json`（0 修改）
  - ❌ `client/vite.config.ts` / `tsconfig.json` / `.eslintrc.cjs`（0 修改）
  - ❌ M5-2-0 共享基础设施（除 `router/index.ts` 已追加）
  - ❌ M5-2-A1 已建 13 页面（**仅追加** types + 子路由 + 菜单项）

**M5-2-A2 必须新增 client/src/ 下 26 个文件**：

- **API 类型追加**（**修改 1 文件** `api/types/organization.ts` 追加 A3-A7 类型 + 5 status enum）：
  - Onboarding + OnboardingTask + OnboardingStatus (draft / submitted / approved / cancelled)
  - Regularization + RegularizationStatus (draft / pending / approved / rejected)
  - Transfer + TransferStatus (draft / pending / approved / rejected / executed)
  - Offboarding + OffboardingStatus (draft / handover_pending / submitted / approved / certificate_issued / rejected / cancelled)
  - Contract + ContractType (formal / intern / consultant / labor / nda) + ContractStatus (draft / pending_signature / signing / signed / expired / cancelled)
- **API 函数**（5 新文件）：
  - `client/src/api/onboarding.ts`（A3 5 端点函数）
  - `client/src/api/regularization.ts`（A4 4 端点函数）
  - `client/src/api/transfer.ts`（A5 5 端点函数）
  - `client/src/api/offboarding.ts`（A6 6 端点函数）
  - `client/src/api/contract.ts`（A7 6 端点函数）
- **页面**（14 新增）：
  - A3 入职（4）：`views/organization/onboarding/{OnboardingList,OnboardingDetail,OnboardingFormDialog,OnboardingConfirm}.vue`
  - A4 转正（2）：`views/organization/regularization/{RegularizationList,RegularizationDetail}.vue`
  - A5 调动（2）：`views/organization/transfer/{TransferList,TransferDetail}.vue`
  - A6 离职（3）：`views/organization/offboarding/{OffboardingList,OffboardingDetail,OffboardingCertificate}.vue`
  - A7 合同（3）：`views/organization/contract/{ContractList,ContractDetail,ContractFormDialog}.vue`
- **测试**（6 新增）：
  - `client/src/api/__tests__/onboarding.test.ts`（2-3）
  - `client/src/api/__tests__/regularization.test.ts`（2-3）
  - `client/src/api/__tests__/transfer.test.ts`（2-3）
  - `client/src/api/__tests__/offboarding.test.ts`（3-4）
  - `client/src/api/__tests__/contract.test.ts`（3-4）
  - `client/src/views/__tests__/employee-lifecycle.test.ts`（4-5）

**M5-2-A2 必须修改 3 个文件**：
- ✅ `client/src/api/types/organization.ts`（**追加** A3-A7 类型 + 5 status enum，**不删** A1+A2+A3 已建类型）
- ✅ `client/src/router/organization.ts`（**追加** 11 子路由：onboarding/regularization/transfer/offboarding/contract，**不删** A1+A2+A3 已建 9 子路由）
- ✅ `client/src/layouts/OrganizationLayout.vue`（**追加** 5 菜单项：入职管理 / 转正管理 / 调动管理 / 离职管理 / 合同管理，**不删** A1+A2+A3 已建 4 菜单项）

**实际新增文件 26 个**（含 6 个测试文件），**修改 3 个**，**总数 29 个文件**。

### 2.3 已有可复用模块（M5-2-A2 必复用，零重写）

| 复用对象 | 来源 | 用途 | M5-2-A2 用法 |
|---|---|---|---|
| `http` 实例 | M5-2-0 `client/src/api/http.ts` | axios 封装 + 401 refresh + 错误 toast | **5 个新 API 文件全部 import http** |
| `useUserStore` | M5-2-0 `client/src/stores/user.ts` | 用户信息 + 权限 + 角色 | 5 角色权限判断 |
| `usePermissionStore` | M5-2-0 `client/src/stores/permission.ts` | v-permission 指令 | 元素级权限控制 |
| `useDictStore` | M5-2-0 `client/src/stores/dict.ts` | 字典缓存 | 部门 / 角色字典 |
| `PageHeader` / `DataTable` / `FormDialog` / `EmptyState` / `SearchForm` | M5-2-0 5 通用组件 | UI 通用组件 | **14 页面全部用** |
| `formatDate` / `formatMoney` | M5-2-0 `utils/format.ts` | 日期 / 金额格式化 | **14 页面全部用** |
| `StatusTag` | M5-2-A1 `components/StatusTag.vue` | 5 状态标签 | 14 页面用，**追加 4 status** |
| `api/types/organization.ts` | M5-2-A1 | A1+A2+A3 类型 | **追加** A3-A7 类型 + 5 status enum |
| `router/organization.ts` | M5-2-A1 | 组织人事子路由表 | **追加** 11 子路由 |
| `OrganizationLayout.vue` | M5-2-A1 | 模块布局 | **追加** 5 菜单项 |
| 后端 `/api/onboarding` | server/src/routes | A3 5 端点 | `api/onboarding.ts` 调 |
| 后端 `/api/regularizations` | server/src/routes | A4 4 端点 | `api/regularization.ts` 调 |
| 后端 `/api/transfers` | server/src/routes | A5 5 端点 | `api/transfer.ts` 调 |
| 后端 `/api/offboarding` | server/src/routes | A6 6 端点 | `api/offboarding.ts` 调 |
| 后端 `/api/contracts` | server/src/routes | A7 6 端点 | `api/contract.ts` 调 |
| 后端 OCR AI 能力 | M0.5-5 ai/ocr.service | OCR 解析身份证 / 银行卡 / 证书 | A3 入职 `parse-ocr` 端点 |

### 2.4 业务规则来源

- **V1.2 §四.5 A3-A7**：员工生命周期 5 切片
- **V1.2 §三.6 RBAC**：5 角色 = admin / hr / dept_head / executive / employee（**无 finance**）
- **V1.2 §三.5.2 业务规则配置化**：configs 表 + configService（**前端仅消费**，不新增）
- **V1.2 §三.4.2 字段加密**：身份证 / 银行卡 / 薪资字段走 M0.5-3 AES-256-GCM（**前端只显示部分脱敏**）

---

## 3. 必读约束（项目级）

### 3.1 代码范式

- **Vue3 Composition API + `<script setup lang="ts">`**（**不用** Options API，**不用** class 组件）
- **TypeScript 强类型**（`strict: true`，**严禁** `any`，必要时 `unknown` + 类型守卫）
- **Element Plus 组件**：`ElButton` / `ElInput` / `ElTable` / `ElDialog` / `ElForm` / `ElCard` / `ElTag`（**严禁** 用 `<button>` / `<input>` 原生 HTML）
- **ESLint 规范**：`@vue/eslint-config-airbnb` + `@vue/eslint-config-typescript`
- **Prettier 格式化**：3.1.1（**严禁** 手动改格式）
- **API 类型**：`api/types/organization.ts` **追加** A3-A7 类型，**严禁** 在组件里 `any`

### 3.2 数据模型约定

- **TS 类型**：PascalCase（`Onboarding` / `Regularization` / `Transfer` / `Offboarding` / `Contract` + 5 status enum）
- **组件命名**：PascalCase（`OnboardingList.vue` / `OnboardingDetail.vue` 等）
- **文件命名**：kebab-case（`onboarding.ts` / `regularization.ts` / `transfer.ts` / `offboarding.ts` / `contract.ts`）
- **API 路径**：`/api/<module>/<resource>`，kebab-case

### 3.3 RBAC 5 角色（**无 finance**）

```
admin / hr / dept_head / executive / employee
```

**M5-2-A2 权限分配**（A3-A7 26 端点）：

| 端点 | admin | hr | dept_head | executive | employee |
|---|---|---|---|---|---|
| **A3 入职**（5）| ✅ 全开 | ✅ 全开 | 🔍 只读 | 🔍 只读 | ❌ 无 |
| **A4 转正**（4）| ✅ 全开 | ✅ 全开 | 🔍 只读本部门 | 🔍 只读 | ❌ 无 |
| **A5 调动**（5）| ✅ 全开 | ✅ 全开 | 🔍 只读本部门 | 🔍 只读 | ❌ 无 |
| **A6 离职**（6）| ✅ 全开 | ✅ 全开 | 🔍 只读本部门 | 🔍 只读 | ❌ 无 |
| **A7 合同**（6）| ✅ 全开 | ✅ 全开 | 🔍 只读本部门 | 🔍 只读 | ❌ 无 |

**前端实现**（沿用 M5-2-0 `usePermissionStore` + `v-permission` 指令）：
- `<el-button v-permission="['onboarding:create']">创建入职</el-button>` 控制按钮显隐
- 列表页根据 `userStore.role` + 后端返回的 `permissions` 数组过滤

**严禁** 给 finance 角色分配权限（D1 教训延续）

### 3.4 错误码段位（M5-2-A2 0 新错误码）

**M5-2-A2 不分配任何错误码**（前端不涉及业务错误码定义，仅消费后端错误码）。

**前端消费后端 A3-A7 错误码段位**（**M1 后端已分配**，前端仅显示 message）：
- A3 入职：`TODO: M1-A3 错误码段位`
- A4 转正：`TODO: M1-A4 错误码段位`
- A5 调动：`TODO: M1-A5 错误码段位`
- A6 离职：`TODO: M1-A6 错误码段位`
- A7 合同：`TODO: M1-A7 错误码段位`
- **前端不需记忆具体码**，仅显示 `apiError.message`（**沿用 M5-2-0 http.ts 自动 ElMessage.error**）

### 3.5 审计埋点（M5-2-A2 0 审计）

**M5-2-A2 不写任何审计**（前端不涉及，**复用**后端 `auditLog` 自动记录）。

### 3.6 业务规则配置化（M5-2-A2 0 新 configs）

**M5-2-A2 不新增任何 configs**（前端不涉及业务规则定义）。

---

## 4. 实施任务

### 4.1 子任务 7.2.A2.1：入职（A3）— 5 端点 + 4 页面

**1 新 API 文件**（`api/onboarding.ts`）+ 4 新页面 + 1 新测试

**`api/onboarding.ts`**（A3 5 端点函数）：

```ts
import http from './http'
import type { Onboarding, CreateOnboardingRequest, ParseOcrRequest, ConfirmOnboardingRequest, PaginatedResponse } from './types/organization'

export async function listOnboardings(filter: ListOnboardingFilter = {}): Promise<PaginatedResponse<Onboarding>> {
  return http.get<PaginatedResponse<Onboarding>>('/onboarding', { params: filter })
}

export async function getOnboarding(id: string): Promise<Onboarding> {
  return http.get<Onboarding>(`/onboarding/${id}`)
}

export async function createOnboarding(data: CreateOnboardingRequest): Promise<Onboarding> {
  return http.post<Onboarding>('/onboarding', data)
}

export async function parseOcr(id: string, data: ParseOcrRequest): Promise<Onboarding> {
  return http.post<Onboarding>(`/onboarding/${id}/parse-ocr`, data)
}

export async function confirmOnboarding(id: string, data: ConfirmOnboardingRequest = {}): Promise<Onboarding> {
  return http.post<Onboarding>(`/onboarding/${id}/confirm`, data)
}
```

**4 入职页面**（**完整代码见 §5.1**，**仅给关键 props/slots/emits**）：

**`OnboardingList.vue`**：
- 顶部 PageHeader + 搜索（公司级联 + 状态）+ 创建按钮
- DataTable 列表（员工姓名 / 公司 / 部门 / 入职日期 / 合同类型 / 状态 / 操作）
- 状态用 `StatusTag`（draft / submitted / approved / cancelled）
- 操作：详情 / 编辑 / OCR / 确认

**`OnboardingDetail.vue`**：
- 基础信息卡片 + 任务清单（onboarding_tasks 表） + OCR 记录
- 操作日志

**`OnboardingFormDialog.vue`**：
- 完整入职表单（name / companyId / departmentId / hireDate / contractType / gender / birthDate / phone / email / probationMonths / baseSalary / idCard / bankName / bankCard / certificates / materialsChecklist）
- 工号**不传**（**M5-2-A1 主动识别**：后端生成）
- 校验：baseSalary 必填 + 试用期 0-24 月

**`OnboardingConfirm.vue`**：
- 确认弹窗（**仅 hr/admin**）：approved（true/false）+ reason + approvalInstanceId
- **二次确认**（防误操作）

### 4.2 子任务 7.2.A2.2：转正（A4）— 4 端点 + 2 页面

**1 新 API 文件**（`api/regularization.ts`）+ 2 新页面 + 1 新测试

**`api/regularization.ts`**（A4 4 端点函数）：

```ts
import http from './http'
import type { Regularization, CreateRegularizationRequest, PaginatedResponse } from './types/organization'

export async function listRegularizations(filter: ListRegularizationFilter = {}): Promise<PaginatedResponse<Regularization>> {
  return http.get<PaginatedResponse<Regularization>>('/regularizations', { params: filter })
}

export async function getRegularization(id: string): Promise<Regularization> {
  return http.get<Regularization>(`/regularizations/${id}`)
}

export async function createRegularization(data: CreateRegularizationRequest): Promise<Regularization> {
  return http.post<Regularization>('/regularizations', data)
}

export async function cancelRegularization(id: string, data: { reason: string }): Promise<Regularization> {
  return http.post<Regularization>(`/regularizations/${id}/cancel`, data)
}
```

**2 转正页面**：

**`RegularizationList.vue`**：
- 顶部 PageHeader + 搜索（员工 / 状态）+ 创建按钮
- DataTable 列表（员工 / 试用期 / 转正日期 / 状态 / 操作）

**`RegularizationDetail.vue`**：
- 基础信息 + 转正前后薪资对比（**留 TODO**，**薪资数据由 M4 切片管**）

### 4.3 子任务 7.2.A2.3：调动（A5）— 5 端点 + 2 页面

**1 新 API 文件**（`api/transfer.ts`）+ 2 新页面 + 1 新测试

**`api/transfer.ts`**（A5 5 端点函数）：

```ts
import http from './http'
import type { Transfer, CreateTransferRequest, UpdateTransferRequest, PaginatedResponse } from './types/organization'

export async function listTransfers(filter: ListTransferFilter = {}): Promise<PaginatedResponse<Transfer>> {
  return http.get<PaginatedResponse<Transfer>>('/transfers', { params: filter })
}

export async function getTransfer(id: string): Promise<Transfer> {
  return http.get<Transfer>(`/transfers/${id}`)
}

export async function createTransfer(data: CreateTransferRequest): Promise<Transfer> {
  return http.post<Transfer>('/transfers', data)
}

export async function updateTransfer(id: string, data: UpdateTransferRequest): Promise<Transfer> {
  return http.post<Transfer>(`/transfers/${id}/update`, data)
}

export async function cancelTransfer(id: string, data: { reason: string }): Promise<Transfer> {
  return http.post<Transfer>(`/transfers/${id}/cancel`, data)
}
```

**2 调动页面**：

**`TransferList.vue`**：
- 顶部 PageHeader + 搜索 + 创建按钮
- DataTable 列表（员工 / 原部门 / 目标部门 / 调动日期 / 状态 / 操作）

**`TransferDetail.vue`**：
- 基础信息 + 4 级审批流（**M0.5-1 联动**，**留 TODO** 显示审批节点）

### 4.4 子任务 7.2.A2.4：离职（A6）— 6 端点 + 3 页面

**1 新 API 文件**（`api/offboarding.ts`）+ 3 新页面 + 1 新测试

**`api/offboarding.ts`**（A6 6 端点函数）：

```ts
import http from './http'
import type { Offboarding, CreateOffboardingRequest, PaginatedResponse } from './types/organization'

export async function listOffboardings(filter: ListOffboardingFilter = {}): Promise<PaginatedResponse<Offboarding>> {
  return http.get<PaginatedResponse<Offboarding>>('/offboarding', { params: filter })
}

export async function getOffboarding(id: string): Promise<Offboarding> {
  return http.get<Offboarding>(`/offboarding/${id}`)
}

export async function createOffboarding(data: CreateOffboardingRequest): Promise<Offboarding> {
  return http.post<Offboarding>('/offboarding', data)
}

export async function confirmHandover(id: string, data: { handoverCompletedBy: string; remark?: string }): Promise<Offboarding> {
  return http.post<Offboarding>(`/offboarding/${id}/confirm-handover`, data)
}

export async function cancelOffboarding(id: string, data: { reason: string }): Promise<Offboarding> {
  return http.post<Offboarding>(`/offboarding/${id}/cancel`, data)
}

export async function issueCertificate(id: string, data: { certificateNumber: string; certificateUrl?: string }): Promise<Offboarding> {
  return http.post<Offboarding>(`/offboarding/${id}/issue-certificate`, data)
}
```

**3 离职页面**：

**`OffboardingList.vue`**：
- 顶部 PageHeader + 搜索 + 创建按钮
- DataTable 列表（员工 / 最后工作日 / 离职类型 / 状态 / 操作）

**`OffboardingDetail.vue`**：
- 基础信息 + 工作交接清单（5 项模板，**留 TODO** 显示） + 离职证明状态

**`OffboardingCertificate.vue`**：
- 离职证明页面（**mock PDF 占位**，**V1.2 §三.6 真实 PDF 留二期**）
- 显示 certificateNumber + 发放状态

### 4.5 子任务 7.2.A2.5：合同（A7）— 6 端点 + 3 页面

**1 新 API 文件**（`api/contract.ts`）+ 3 新页面 + 1 新测试

**`api/contract.ts`**（A7 6 端点函数）：

```ts
import http from './http'
import type { Contract, CreateContractRequest, UpdateContractRequest, PaginatedResponse } from './types/organization'

export async function listContracts(filter: ListContractFilter = {}): Promise<PaginatedResponse<Contract>> {
  return http.get<PaginatedResponse<Contract>>('/contracts', { params: filter })
}

export async function getContract(id: string): Promise<Contract> {
  return http.get<Contract>(`/contracts/${id}`)
}

export async function createContract(data: CreateContractRequest): Promise<Contract> {
  return http.post<Contract>('/contracts', data)
}

export async function updateContract(id: string, data: UpdateContractRequest): Promise<Contract> {
  return http.post<Contract>(`/contracts/${id}/update`, data)
}

export async function cancelContract(id: string, data: { reason: string }): Promise<Contract> {
  return http.post<Contract>(`/contracts/${id}/cancel`, data)
}

/** e-签宝 webhook（V1.2 §三.6 真实 SaaS 集成留二期，M5-2-A2 仅消费 mock webhook） */
export async function handleESignWebhook(payload: any): Promise<void> {
  return http.post('/contracts/webhook/e-sign', payload)
}
```

**3 合同页面**：

**`ContractList.vue`**：
- 顶部 PageHeader + 搜索（员工 / 合同类型 / 状态）+ 创建按钮
- DataTable 列表（员工 / 合同类型 / 起始日期 / 结束日期 / 状态 / 操作）
- 状态用 `StatusTag`（draft / pending_signature / signing / signed / expired / cancelled，**6 状态**）

**`ContractDetail.vue`**：
- 基础信息 + e-签宝签署状态（**V1.2 §三.6 真实 SaaS 留二期**）+ 附件下载（**留 TODO**）

**`ContractFormDialog.vue`**：
- 创建合同表单（employeeId / contractType / startDate / endDate / attachmentUrl）
- 5 类合同模板（formal / intern / consultant / labor / nda）
- 校验：endDate > startDate

### 4.6 共用修改（3 个文件）

**`api/types/organization.ts` 追加**（**不删** A1+A2+A3 已建类型，**仅追加** A3-A7 类型 + 5 status enum）：

```ts
// ===== A3 入职 =====
export type OnboardingStatus = 'draft' / 'submitted' / 'approved' / 'cancelled'

export interface OnboardingTask {
  id: string
  onboardingId: string
  name: string
  completed: boolean
  completedAt?: string
  completedBy?: string
}

export interface Onboarding {
  id: string
  employeeNo: string  // **M5-2-A1 主动识别**：后端生成
  name: string
  companyId: string
  companyName: string
  departmentId: string
  departmentName: string
  hireDate: string
  contractType: 'formal' / 'intern' / 'consultant' / 'labor'
  status: OnboardingStatus
  gender?: 'male' / 'female'
  birthDate?: string
  phone?: string  // 加密
  email?: string
  probationMonths?: number
  baseSalary?: number
  idCard?: string  // 加密
  bankName?: string
  bankCard?: string  // 加密
  tasks?: OnboardingTask[]
  approvalInstanceId?: string
  createdAt: string
  updatedAt: string
}

export interface CreateOnboardingRequest {
  name: string
  companyId: string
  departmentId: string
  hireDate: string
  contractType: 'formal' / 'intern' / 'consultant' / 'labor'
  gender?: 'male' / 'female'
  birthDate?: string
  phone?: string
  email?: string
  probationMonths?: number
  baseSalary?: number
  idCard?: string
  bankName?: string
  bankCard?: string
}

export interface ParseOcrRequest {
  type: 'idCard' / 'bankCard' / 'certificate'
  imageBase64: string
}

export interface ConfirmOnboardingRequest {
  approved?: boolean
  reason?: string
  instanceId?: string
}

export interface ListOnboardingFilter {
  companyId?: string
  departmentId?: string
  status?: OnboardingStatus
  keyword?: string
  page?: number
  pageSize?: number
}

// ===== A4 转正 =====
export type RegularizationStatus = 'draft' / 'pending' / 'approved' / 'rejected'

export interface Regularization {
  id: string
  employeeId: string
  employeeName: string
  probationStartDate: string
  probationEndDate: string
  regularizationDate: string
  status: RegularizationStatus
  approvalInstanceId?: string
  rejectionReason?: string
  createdAt: string
  updatedAt: string
}

export interface CreateRegularizationRequest {
  employeeId: string
  regularizationDate: string
}

export interface ListRegularizationFilter {
  employeeId?: string
  status?: RegularizationStatus
  page?: number
  pageSize?: number
}

// ===== A5 调动 =====
export type TransferStatus = 'draft' / 'pending' / 'approved' / 'rejected' / 'executed'

export interface Transfer {
  id: string
  employeeId: string
  employeeName: string
  fromCompanyId: string
  fromCompanyName: string
  fromDeptId: string
  fromDeptName: string
  toCompanyId: string
  toCompanyName: string
  toDeptId: string
  toDeptName: string
  transferType: 'horizontal' / 'promotion' / 'demotion'  // 平调/晋升/降职
  effectiveDate: string
  status: TransferStatus
  reason?: string
  approvalInstanceId?: string
  createdAt: string
  updatedAt: string
}

export interface CreateTransferRequest {
  employeeId: string
  toCompanyId: string
  toDeptId: string
  transferType: 'horizontal' / 'promotion' / 'demotion'
  effectiveDate: string
  reason: string
}

export interface UpdateTransferRequest {
  toCompanyId?: string
  toDeptId?: string
  transferType?: 'horizontal' / 'promotion' / 'demotion'
  effectiveDate?: string
  reason?: string
}

export interface ListTransferFilter {
  employeeId?: string
  status?: TransferStatus
  page?: number
  pageSize?: number
}

// ===== A6 离职 =====
export type OffboardingStatus = 'draft' / 'handover_pending' / 'submitted' / 'approved' / 'certificate_issued' / 'rejected' / 'cancelled'

export interface Offboarding {
  id: string
  employeeId: string
  employeeName: string
  resignationType: 'employee_initiated' / 'company_initiated'
  reason?: string
  lastWorkingDate: string
  status: OffboardingStatus
  handoverCompleted: boolean
  handoverCompletedBy?: string
  handoverCompletedAt?: string
  certificateIssued: boolean
  certificateNumber?: string
  certificateUrl?: string
  approvalInstanceId?: string
  archiveRetentionYears: number
  createdAt: string
  updatedAt: string
}

export interface CreateOffboardingRequest {
  employeeId: string
  resignationType: 'employee_initiated' / 'company_initiated'
  reason?: string
  lastWorkingDate: string
}

export interface ListOffboardingFilter {
  employeeId?: string
  status?: OffboardingStatus
  page?: number
  pageSize?: number
}

// ===== A7 合同 =====
export type ContractType = 'formal' / 'intern' / 'consultant' / 'labor' / 'nda'
export type ContractStatus = 'draft' / 'pending_signature' / 'signing' / 'signed' / 'expired' / 'cancelled'

export interface Contract {
  id: string
  employeeId: string
  employeeName: string
  contractType: ContractType
  startDate: string
  endDate: string
  attachmentUrl?: string
  status: ContractStatus
  eSignTransactionId?: string  // e-签宝交易 ID（V1.2 §三.6 留二期）
  signedAt?: string
  createdAt: string
  updatedAt: string
}

export interface CreateContractRequest {
  employeeId: string
  contractType: ContractType
  startDate: string
  endDate: string
  attachmentUrl?: string
}

export interface UpdateContractRequest {
  contractType?: ContractType
  startDate?: string
  endDate?: string
  attachmentUrl?: string
}

export interface ListContractFilter {
  employeeId?: string
  contractType?: ContractType
  status?: ContractStatus
  page?: number
  pageSize?: number
}
```

**`router/organization.ts` 追加**（**不删** A1+A2+A3 已建 9 子路由，**仅追加** 11 子路由）：

```ts
// ===== 已有 9 子路由 =====
// A1: /companies + /companies/:id + /companies/:id/tree
// A2: /departments + /departments/:id
// A3 employee: /employees + /employees/:id + /employees/org-chart
// ...

// ===== A2-A2 追加 11 子路由 =====
{
  path: 'onboarding',
  name: 'OnboardingList',
  component: () => import('@/views/organization/onboarding/OnboardingList.vue'),
  meta: { title: '入职管理' },
},
{
  path: 'onboarding/:id',
  name: 'OnboardingDetail',
  component: () => import('@/views/organization/onboarding/OnboardingDetail.vue'),
  meta: { title: '入职详情' },
},
{
  path: 'regularizations',
  name: 'RegularizationList',
  component: () => import('@/views/organization/regularization/RegularizationList.vue'),
  meta: { title: '转正管理' },
},
{
  path: 'regularizations/:id',
  name: 'RegularizationDetail',
  component: () => import('@/views/organization/regularization/RegularizationDetail.vue'),
  meta: { title: '转正详情' },
},
{
  path: 'transfers',
  name: 'TransferList',
  component: () => import('@/views/organization/transfer/TransferList.vue'),
  meta: { title: '调动管理' },
},
{
  path: 'transfers/:id',
  name: 'TransferDetail',
  component: () => import('@/views/organization/transfer/TransferDetail.vue'),
  meta: { title: '调动详情' },
},
{
  path: 'offboarding',
  name: 'OffboardingList',
  component: () => import('@/views/organization/offboarding/OffboardingList.vue'),
  meta: { title: '离职管理' },
},
{
  path: 'offboarding/:id',
  name: 'OffboardingDetail',
  component: () => import('@/views/organization/offboarding/OffboardingDetail.vue'),
  meta: { title: '离职详情' },
},
{
  path: 'offboarding/:id/certificate',
  name: 'OffboardingCertificate',
  component: () => import('@/views/organization/offboarding/OffboardingCertificate.vue'),
  meta: { title: '离职证明' },
},
{
  path: 'contracts',
  name: 'ContractList',
  component: () => import('@/views/organization/contract/ContractList.vue'),
  meta: { title: '合同管理' },
},
{
  path: 'contracts/:id',
  name: 'ContractDetail',
  component: () => import('@/views/organization/contract/ContractDetail.vue'),
  meta: { title: '合同详情' },
},
```

**`OrganizationLayout.vue` 追加**（**不删** A1+A2+A3 已建 4 菜单项，**仅追加** 5 菜单项 + activeKey 扩展）：

```ts
const menuItems = computed(() => [
  // 已有 4 菜单项
  { key: 'companies', label: '法人公司', icon: 'OfficeBuilding', to: '/org/companies', permission: 'company:read' },
  { key: 'departments', label: '部门管理', icon: 'Connection', to: '/org/departments', permission: 'department:read' },
  { key: 'employees', label: '员工档案', icon: 'User', to: '/org/employees', permission: 'employee:read' },
  { key: 'org-chart', label: '组织架构图', icon: 'Share', to: '/org/employees/org-chart', permission: 'employee:read' },
  // A2-A2 追加 5 菜单项
  { key: 'onboarding', label: '入职管理', icon: 'UserFilled', to: '/org/onboarding', permission: 'onboarding:read' },
  { key: 'regularizations', label: '转正管理', icon: 'Check', to: '/org/regularizations', permission: 'regularization:read' },
  { key: 'transfers', label: '调动管理', icon: 'Switch', to: '/org/transfers', permission: 'transfer:read' },
  { key: 'offboarding', label: '离职管理', icon: 'Back', to: '/org/offboarding', permission: 'offboarding:read' },
  { key: 'contracts', label: '合同管理', icon: 'Document', to: '/org/contracts', permission: 'contract:read' },
].filter((m) => hasPermission(userStore.userInfo, m.permission)))

const activeKey = computed(() => {
  const path = route.path
  if (path.startsWith('/org/companies')) return 'companies'
  if (path.startsWith('/org/departments')) return 'departments'
  if (path.startsWith('/org/employees/org-chart')) return 'org-chart'
  if (path.startsWith('/org/employees')) return 'employees'
  // A2-A2 追加
  if (path.startsWith('/org/onboarding')) return 'onboarding'
  if (path.startsWith('/org/regularizations')) return 'regularizations'
  if (path.startsWith('/org/transfers')) return 'transfers'
  if (path.startsWith('/org/offboarding')) return 'offboarding'
  if (path.startsWith('/org/contracts')) return 'contracts'
  return ''
})
```

---

## 5. 关键决策点详尽（4 类必须详尽）

### 5.1 入职（A3）JSDoc + 测试（子任务 7.2.A2.1）

#### 5.1.1 JSDoc + 校验链

**`api/onboarding.ts`**（**A3 5 端点函数**）

```ts
/**
 * 入职管理 API（M5-2-A2）
 * @module api/onboarding
 * @description 消费后端 /api/onboarding 5 端点（list / get / create / parse-ocr / confirm）
 * @ocr parseOcr 调 M0.5-5 ai/ocr.service（V1.2 §二.1.2 AI OCR 证件解析）
 * @status 4 状态机：draft → submitted → approved / cancelled
 */
import http from './http'
import type { Onboarding, CreateOnboardingRequest, ParseOcrRequest, ConfirmOnboardingRequest, PaginatedResponse } from './types/organization'

export async function listOnboardings(filter: ListOnboardingFilter = {}): Promise<PaginatedResponse<Onboarding>> { ... }
export async function getOnboarding(id: string): Promise<Onboarding> { ... }
export async function createOnboarding(data: CreateOnboardingRequest): Promise<Onboarding> { ... }
export async function parseOcr(id: string, data: ParseOcrRequest): Promise<Onboarding> { ... }
export async function confirmOnboarding(id: string, data: ConfirmOnboardingRequest = {}): Promise<Onboarding> { ... }
```

**4 入职页面**（**详细代码见 §4.1**）：

**`OnboardingList.vue`**：
```ts
/**
 * 入职列表页（M5-2-A2）
 * @page 顶部 PageHeader + 搜索（公司级联 + 状态）+ DataTable 列表
 * @status 4 状态：draft / submitted / approved / cancelled
 * @permissions admin/hr 全开，dept_head/executive 只读，employee 无权限
 */
```

**`OnboardingFormDialog.vue`**：
```ts
/**
 * 入职创建/编辑表单弹窗（M5-2-A2）
 * @page el-dialog + el-form（16 字段）
 * @validation 工号不传（后端生成）/ baseSalary 必填 / 试用期 0-24 月
 * @emit submit(payload) / cancel
 */
```

**`OnboardingConfirm.vue`**：
```ts
/**
 * 入职确认弹窗（M5-2-A2）
 * @page 二次确认（防误操作）+ approved（true/false）+ reason + approvalInstanceId
 * @permissions 仅 hr/admin 可见
 */
```

#### 5.1.2 错误码触发条件

**M5-2-A2 不分配错误码**（前端不涉及）。

#### 5.1.3 测试用例（2-3 个 it）

```ts
describe('api/onboarding.ts', () => {
  it('listOnboardings 调 GET /onboarding + 过滤参数', () => { ... })
  it('parseOcr 调 POST /onboarding/:id/parse-ocr (multipart)', () => { ... })
  it('confirmOnboarding 调 POST /onboarding/:id/confirm', () => { ... })
})
```

### 5.2 转正（A4）JSDoc + 测试（子任务 7.2.A2.2）

**`api/regularization.ts`**（A4 4 端点函数）+ 2 页面

**JSDoc + 校验链**（**完整代码见 §4.2**）：

```ts
/**
 * 转正管理 API（M5-2-A2）
 * @module api/regularization
 * @description 消费后端 /api/regularizations 4 端点
 * @status 4 状态：draft / pending / approved / rejected
 */
```

**2 转正页面**（`RegularizationList.vue` / `RegularizationDetail.vue`）：

**JSDoc + props/slots/emits** 与 A1+A2+A3 模式相同（**详见 §4.2**）

#### 5.2.1 错误码触发条件

**M5-2-A2 不分配错误码**

#### 5.2.2 测试用例（2-3 个 it）

```ts
describe('api/regularization.ts', () => {
  it('listRegularizations 调 GET /regularizations', () => { ... })
  it('createRegularization 调 POST /regularizations', () => { ... })
  it('cancelRegularization 调 POST /regularizations/:id/cancel', () => { ... })
})
```

### 5.3 调动（A5）JSDoc + 测试（子任务 7.2.A2.3）

**`api/transfer.ts`**（A5 5 端点函数）+ 2 页面

**JSDoc + 校验链**（**完整代码见 §4.3**）：

```ts
/**
 * 调动管理 API（M5-2-A2）
 * @module api/transfer
 * @description 消费后端 /api/transfers 5 端点
 * @status 5 状态：draft / pending / approved / rejected / executed
 */
```

**2 调动页面**（`TransferList.vue` / `TransferDetail.vue`）：

**JSDoc + props/slots/emits** 与 A1+A2+A3 模式相同（**详见 §4.3**）

#### 5.3.1 错误码触发条件

**M5-2-A2 不分配错误码**

#### 5.3.2 测试用例（2-3 个 it）

```ts
describe('api/transfer.ts', () => {
  it('listTransfers 调 GET /transfers', () => { ... })
  it('createTransfer 调 POST /transfers', () => { ... })
  it('updateTransfer 调 POST /transfers/:id/update（不是 PUT）', () => { ... })
  it('cancelTransfer 调 POST /transfers/:id/cancel', () => { ... })
})
```

### 5.4 离职（A6）JSDoc + 测试（子任务 7.2.A2.4）

**`api/offboarding.ts`**（A6 6 端点函数）+ 3 页面

**JSDoc + 校验链**（**完整代码见 §4.4**）：

```ts
/**
 * 离职管理 API（M5-2-A2）
 * @module api/offboarding
 * @description 消费后端 /api/offboarding 6 端点
 * @status 7 状态：draft / handover_pending / submitted / approved / certificate_issued / rejected / cancelled
 * @certificate issueCertificate 调 mock PDF 生成（V1.2 §三.6 真实 PDF 留二期）
 */
```

**3 离职页面**（`OffboardingList.vue` / `OffboardingDetail.vue` / `OffboardingCertificate.vue`）：

**JSDoc + props/slots/emits** 与 A1+A2+A3 模式相同（**详见 §4.4**）

#### 5.4.1 错误码触发条件

**M5-2-A2 不分配错误码**

#### 5.4.2 测试用例（3-4 个 it）

```ts
describe('api/offboarding.ts', () => {
  it('listOffboardings 调 GET /offboarding', () => { ... })
  it('createOffboarding 调 POST /offboarding', () => { ... })
  it('confirmHandover 调 POST /offboarding/:id/confirm-handover', () => { ... })
  it('issueCertificate 调 POST /offboarding/:id/issue-certificate', () => { ... })
})
```

### 5.5 合同（A7）JSDoc + 测试（子任务 7.2.A2.5）

**`api/contract.ts`**（A7 6 端点函数）+ 3 页面

**JSDoc + 校验链**（**完整代码见 §4.5**）：

```ts
/**
 * 合同管理 API（M5-2-A2）
 * @module api/contract
 * @description 消费后端 /api/contracts 6 端点
 * @type 5 合同类型：formal / intern / consultant / labor / nda
 * @status 6 状态：draft / pending_signature / signing / signed / expired / cancelled
 * @esign handleESignWebhook 调 e-签宝 webhook（V1.2 §三.6 真实 SaaS 集成留二期，M5-2-A2 仅消费 mock）
 */
```

**3 合同页面**（`ContractList.vue` / `ContractDetail.vue` / `ContractFormDialog.vue`）：

**JSDoc + props/slots/emits** 与 A1+A2+A3 模式相同（**详见 §4.5**）

#### 5.5.1 错误码触发条件

**M5-2-A2 不分配错误码**

#### 5.5.2 测试用例（3-4 个 it）

```ts
describe('api/contract.ts', () => {
  it('listContracts 调 GET /contracts', () => { ... })
  it('createContract 调 POST /contracts', () => { ... })
  it('updateContract 调 POST /contracts/:id/update（不是 PUT）', () => { ... })
  it('cancelContract 调 POST /contracts/:id/cancel', () => { ... })
  it('handleESignWebhook 调 POST /contracts/webhook/e-sign', () => { ... })
})
```

### 5.6 通用测试（视图 + 状态机）

```ts
describe('views/__tests__/employee-lifecycle.test.ts', () => {
  it('5 模块列表页（onboarding / regularization / transfer / offboarding / contract）渲染正确', () => { ... })
  it('5 status enum 切换（A3 draft→submitted→approved / A4 4 状态 / A5 5 状态 / A6 7 状态 / A7 6 状态）', () => { ... })
  it('工号由后端生成（A3 创建表单不传 employeeNo）', () => { ... })
  it('路由顺序：/contracts/:id 在 /org/contracts/webhook 前', () => { ... })
})
```

---

## 6. 修改文件清单（明示边界）

### 6.1 必改文件（清单以内）

#### 新增文件（26 个）

1. `client/src/api/onboarding.ts`
2. `client/src/api/regularization.ts`
3. `client/src/api/transfer.ts`
4. `client/src/api/offboarding.ts`
5. `client/src/api/contract.ts`
6. `client/src/api/__tests__/onboarding.test.ts`
7. `client/src/api/__tests__/regularization.test.ts`
8. `client/src/api/__tests__/transfer.test.ts`
9. `client/src/api/__tests__/offboarding.test.ts`
10. `client/src/api/__tests__/contract.test.ts`
11. `client/src/views/organization/onboarding/OnboardingList.vue`
12. `client/src/views/organization/onboarding/OnboardingDetail.vue`
13. `client/src/views/organization/onboarding/OnboardingFormDialog.vue`
14. `client/src/views/organization/onboarding/OnboardingConfirm.vue`
15. `client/src/views/organization/regularization/RegularizationList.vue`
16. `client/src/views/organization/regularization/RegularizationDetail.vue`
17. `client/src/views/organization/transfer/TransferList.vue`
18. `client/src/views/organization/transfer/TransferDetail.vue`
19. `client/src/views/organization/offboarding/OffboardingList.vue`
20. `client/src/views/organization/offboarding/OffboardingDetail.vue`
21. `client/src/views/organization/offboarding/OffboardingCertificate.vue`
22. `client/src/views/organization/contract/ContractList.vue`
23. `client/src/views/organization/contract/ContractDetail.vue`
24. `client/src/views/organization/contract/ContractFormDialog.vue`
25. `client/src/views/organization/onboarding/__tests__/OnboardingDetail.test.ts`（**可选，**若需更细粒度**）
26. `client/src/views/__tests__/employee-lifecycle.test.ts`

#### 修改文件（3 个）

27. `client/src/api/types/organization.ts`（**追加** A3-A7 类型 + 5 status enum，**不删** A1+A2+A3 已建类型）
28. `client/src/router/organization.ts`（**追加** 11 子路由，**不删** A1+A2+A3 已建 9 子路由）
29. `client/src/layouts/OrganizationLayout.vue`（**追加** 5 菜单项 + activeKey 扩展，**不删** A1+A2+A3 已建 4 菜单项）

**实际清单数**：25 新增 + 3 修改 = **28 个文件**（注：原 §1.4 概数 26 含 6 个测试文件分散在 §1.2 列表中，最终 25 + 3 = 28，**接近 26+3=29**）

### 6.2 禁止修改文件（清单以外）

- **后端任何文件**（**M5-2-A2 0 修改** server/）：
  - ❌ 严禁修改 `server/src/services/*` 任何 service（36 service 0 改动）
  - ❌ 严禁修改 `server/src/routes/*` 任何路由
  - ❌ 严禁修改 `server/prisma/schema.prisma`（0 新表）
  - ❌ 严禁修改 `server/src/constants/permissions.ts`（0 新权限点）
  - ❌ 严禁修改 `server/src/app.ts` / `server/src/index.ts`
  - ❌ 严禁修改 `server/src/controllers/*` 任何 controller
  - ❌ 严禁修改 `server/prisma/seed.ts`
  - ❌ 严禁修改 `docs/error-codes.md`（0 新错误码）
  - ❌ 严禁修改 `docs/api-spec.md`（M5-2-A2 0 新端点）
- **M5-2-0 共享基础设施文件**（除 `router/index.ts` 已追加）：
  - ❌ 严禁修改 `client/src/api/{http,auth,types}.ts`（`api/types.ts` 是 M5-2-0 通用 types，**不与** `api/types/organization.ts` 混淆）
  - ❌ 严禁修改 `client/src/stores/{user,permission,dict}.ts`
  - ❌ 严禁修改 `client/src/layouts/DefaultLayout.vue`
  - ❌ 严禁修改 `client/src/views/{Login,Dashboard}.vue`
  - ❌ 严禁修改 `client/src/utils/{auth,permission,format}.ts`
- **M5-2-A1 已建文件**（除**追加**类型 + 路由 + 菜单项）：
  - ❌ 严禁修改 `client/src/api/{company,department,employee}.ts`（3 API 文件）
  - ❌ 严禁修改 `client/src/router/organization.ts` 的 9 已建子路由（**仅追加** 11 子路由）
  - ❌ 严禁修改 `client/src/api/types/organization.ts` 的 A1+A2+A3 已建类型（**仅追加** A3-A7 类型）
  - ❌ 严禁修改 `client/src/views/organization/{companies,departments,employees}/*` 13 已建页面
  - ❌ 严禁修改 `client/src/components/{StatusTag,TreeNode}.vue`（如需追加 status，**仅在 `api/types/organization.ts` 追加 enum**，**不改** StatusTag 组件，StatusTag 自动从 `STATUS_MAP` 字典扩展）
- **M5-2-A2 严禁修改**：
  - ❌ 严禁修改 `client/package.json`（**禁止装新依赖**）
  - ❌ 严禁修改 `client/vite.config.ts` / `tsconfig.json` / `.eslintrc.cjs`
  - ❌ 严禁创建 M5-2-B / M5-2-C / M5-2-D 业务页面（**留独立任务**）
  - ❌ 严禁创建 M1-A3-A7 后端新表 / 新 service / 新权限点
- 任何旧测试文件（`*.test.ts`）除非在 §6.1 清单内

### 6.3 越界检测（交付时自检）

```bash
git diff --name-only HEAD
# 必须只出现 §6.1 列出的 28 个文件
# 如果出现其他文件 → 越界，必须回退

# 特别检查：D1-D6 + C1-C8 共 32 service 0 行改动 + M0.5 4 service 0 行改动 = 36 service
git diff HEAD -- 'server/src/services/'
# 必须为 0 行

# 验证 0 修改后端任何文件
git diff HEAD -- 'server/'
# 必须 0 行

# 验证 0 修改 client/package.json
git diff HEAD -- 'client/package.json'
# 必须 0 行

# 验证 0 修改 M5-2-0 共享基础设施（除 router/index.ts 追加）
git diff HEAD -- 'client/src/api/http.ts' 'client/src/api/auth.ts' 'client/src/api/types.ts' 'client/src/stores/' 'client/src/layouts/DefaultLayout.vue' 'client/src/views/Login.vue' 'client/src/views/Dashboard.vue' 'client/src/utils/'
# 必须 0 行

# 验证 0 修改 M5-2-A1 已建 13 页面
git diff HEAD -- 'client/src/views/organization/companies/' 'client/src/views/organization/departments/' 'client/src/views/organization/employees/'
# 必须 0 行（**仅追加** `onboarding/` / `regularization/` / `transfer/` / `offboarding/` / `contract/`）

# 验证 0 修改 M5-2-A1 5 API 文件
git diff HEAD -- 'client/src/api/company.ts' 'client/src/api/department.ts' 'client/src/api/employee.ts'
# 必须 0 行

# 验证 0 修改 M5-2-0 5 通用组件
git diff HEAD -- 'client/src/components/PageHeader.vue' 'client/src/components/DataTable.vue' 'client/src/components/FormDialog.vue' 'client/src/components/EmptyState.vue' 'client/src/components/SearchForm.vue' 'client/src/components/StatusTag.vue' 'client/src/components/TreeNode.vue'
# 必须 0 行

# 验证 0 修改 server/
git diff HEAD -- 'server/'
# 必须 0 行
```

---

## 7. 验收标准

### 7.1 硬性验收（必须 100% 通过）

```bash
# 在项目根 D:\projects\hrms

# 1. 后端测试（**0 改动**，沿用 876）
pnpm --filter hrms-server test
# 预期：876/876 通过

# 2. 后端 type-check
pnpm --filter hrms-server type-check
# 预期：0 错

# 3. 客户端 type-check
cd client && npx vue-tsc --noEmit
# 预期：EXIT_CODE: 0

# 4. 客户端 build
cd client && pnpm build
# 预期：dist 生成成功

# 5. 客户端 lint
cd client && pnpm lint
# 预期：0 error
```

### 7.2 验收明细

- [ ] **28 个 client 文件**（25 新增 + 3 修改）齐全
- [ ] **A3 入职 4 页面**（List/Detail/FormDialog/Confirm）渲染正确
- [ ] **A4 转正 2 页面**（List/Detail）渲染正确
- [ ] **A5 调动 2 页面**（List/Detail）渲染正确
- [ ] **A6 离职 3 页面**（List/Detail/Certificate）渲染正确
- [ ] **A7 合同 3 页面**（List/Detail/FormDialog）渲染正确
- [ ] **5 状态机**正确（A3 4 / A4 4 / A5 5 / A6 7 / A7 6 = 26 状态）
- [ ] **OrganizationLayout** 5 新菜单项 + activeKey 正确
- [ ] **router/organization.ts** 11 新子路由
- [ ] **api/types/organization.ts** A3-A7 类型 + 5 status enum 追加
- [ ] **0 修改后端任何文件**
- [ ] **0 修改 `client/package.json`**
- [ ] **0 修改 M5-2-0 共享基础设施**（除 `router/index.ts` 已追加）
- [ ] **0 修改 M5-2-A1 已建 13 页面 + 5 API 文件 + 5 通用组件**
- [ ] **16-20 单元测试**通过（5 API 文件 + 1 视图 + 状态机）
- [ ] **client type-check 0 错 + build 成功**

### 7.3 红线 9 验证

- [ ] **0 修改后端任何文件**（**仅 client/src**）
- [ ] **0 修改 `client/package.json`**
- [ ] **0 修改 M5-2-0 共享基础设施**（除 `router/index.ts` 已追加）
- [ ] **0 修改 M5-2-A1 已建 13 页面 + 5 API 文件 + 5 通用组件**
- [ ] **28 个 client 文件**（25 新增 + 3 修改，**不超出清单**）
- [ ] **D1-D6 + C1-C8 业务代码 0 行改动**（36 service + 40 表 = 0 行）
- [ ] **M0.5 4 service 0 行改动**
- [ ] **复用 M5-2-0 + M5-2-A1 通用组件 + Pinia stores + http + utils + 5 已建 API 文件 + types**
- [ ] **5 角色 RBAC 严格遵守**（admin / hr / dept_head / executive / employee，**无 finance 角色**）
- [ ] **16-20 单元测试**（内联断言 + 导出 `run*Tests()`，**不引入 vitest**）

### 7.4 原有用例未改（硬要求）

```bash
git diff HEAD -- 'server/src/**/*.test.ts' 'client/src/**/*.test.ts' | grep -E '^[+-]' | grep -v '^[+-]{3}' | grep -v 'onboarding\.test|regularization\.test|transfer\.test|offboarding\.test|contract\.test|employee-lifecycle\.test'
# 必须为 0 行（后端旧测试 100% 未改，前端仅新增 6 个测试文件）
```

---

## 8. 禁止做的事（额外禁忌清单）

1. ❌ 做 M5-2-B / M5-2-C / M5-2-D（**留独立任务**）
2. ❌ 做 M5-3 / M5-4 / M5-5 / M5-6 / M5-7 / M5-8 任何切片
3. ❌ 修改后端任何文件（**0 修改 server/**）
4. ❌ 修改 `client/package.json`（**禁止装新依赖**）
5. ❌ 修改 `client/vite.config.ts` / `tsconfig.json` / `.eslintrc.cjs`
6. ❌ 修改 M5-2-0 共享基础设施文件（除 `router/index.ts` 已追加）
7. ❌ 修改 M5-2-A1 已建 13 页面 + 5 API 文件 + 5 通用组件（**仅追加** A3-A7）
8. ❌ 顺手接 e-签宝真实 SaaS（**V1.2 §三.6 留二期**，M5-2-A2 仅消费 mock webhook）
9. ❌ 顺手做 M1 后端功能（**仅消费 A3-A7 已有 26 端点**）
10. ❌ 顺手装新依赖（**unplugin-vue-components / pinia-plugin-persistedstate / vee-validate / vueuse 等**）
11. ❌ 创建 M1 后端新表 / 新 service / 新权限点
12. ❌ 修改 `server/src/services/*` 任何 service（36 service 0 改动）
13. ❌ 修改 `server/prisma/schema.prisma`（0 新表）
14. ❌ 修改 `server/src/constants/permissions.ts`（0 新权限点）
15. ❌ 修改 `docs/error-codes.md`（0 新错误码）
16. ❌ 修改 `docs/api-spec.md`（M5-2-A2 0 新端点）
17. ❌ 创建 M5-2-B/C/D 业务页面
18. ❌ 改 D1-D6 + C1-C8 任何 service（**不重做 M1-M4**）
19. ❌ 改 M0.5-1/2/5/6 任何 service（**不重做 M0.5**）
20. ❌ 改 M1 / M2 / M3 / M4 任何 service（**不重做**）
21. ❌ 修改任何后端已存在的 .ts 文件（除 §6.1 清单内，但 §6.1 仅 client/）
22. ❌ 修改任何旧测试文件
23. ❌ 越界做 6 角色 RBAC（M0 锁定 5 角色：admin / hr / dept_head / executive / employee，**无 finance**）
24. ❌ 用 Options API / class 组件（**仅 Vue3 Composition API + `<script setup lang="ts">`**）
25. ❌ 用 `<button>` / `<input>` 原生 HTML（**仅 Element Plus 组件**）
26. ❌ 用 `any` 类型（**严格 TypeScript**）
27. ❌ "我觉得这也有用"（任何"顺便做"越界内容一律禁止）
28. ❌ 重写 notification / configService / aiSummary
29. ❌ 给 finance 角色分配权限（**D1 教训延续**）
30. ❌ 越界实现 M1-M4 后端功能（**M5-2-A2 仅消费 A3-A7 26 端点**）
31. ❌ 实现 e-签宝真实集成（**V1.2 §三.6 留二期**）
32. ❌ 实现真实 PDF 离职证明（**V1.2 §三.6 留二期**）

---

## 9. 完成标准

### 9.1 必跑命令（全部通过才交付）

```bash
cd D:\projects\hrms

# 1. 后端测试（**0 改动**，沿用 876）
pnpm --filter hrms-server test
# 预期：876/876 通过

# 2. 后端 type-check
pnpm --filter hrms-server type-check
# 预期：0 错

# 3. 客户端 type-check
cd client && npx vue-tsc --noEmit
# 预期：EXIT_CODE: 0

# 4. 客户端 build
cd client && pnpm build
# 预期：dist 生成成功

# 5. 客户端 lint
cd client && pnpm lint
# 预期：0 error
```

### 9.2 越界自检（必查）

```bash
# 1. 越界文件检测
git diff --name-only HEAD
# 预期：§6.1 列出 28 个文件（25 新增 + 3 修改）

# 2. 36 service 0 行改动检测
git diff HEAD -- 'server/src/services/'
# 预期：0 行

# 3. 0 修改后端任何文件检测
git diff HEAD -- 'server/'
# 预期：0 行

# 4. 0 修改 client/package.json 检测
git diff HEAD -- 'client/package.json'
# 预期：0 行

# 5. 0 修改 M5-2-0 共享基础设施检测（除 router/index.ts）
git diff HEAD -- 'client/src/api/http.ts' 'client/src/api/auth.ts' 'client/src/api/types.ts' 'client/src/stores/' 'client/src/layouts/DefaultLayout.vue' 'client/src/views/Login.vue' 'client/src/views/Dashboard.vue' 'client/src/utils/'
# 预期：0 行

# 6. 0 修改 M5-2-A1 已建 13 页面检测
git diff HEAD -- 'client/src/views/organization/companies/' 'client/src/views/organization/departments/' 'client/src/views/organization/employees/'
# 预期：0 行

# 7. 0 修改 M5-2-A1 5 API 文件检测
git diff HEAD -- 'client/src/api/company.ts' 'client/src/api/department.ts' 'client/src/api/employee.ts'
# 预期：0 行

# 8. 0 修改 M5-2-0 5 通用组件检测
git diff HEAD -- 'client/src/components/PageHeader.vue' 'client/src/components/DataTable.vue' 'client/src/components/FormDialog.vue' 'client/src/components/EmptyState.vue' 'client/src/components/SearchForm.vue' 'client/src/components/StatusTag.vue' 'client/src/components/TreeNode.vue'
# 预期：0 行

# 9. 客户端 type-check
cd client && npx vue-tsc --noEmit
# 预期：EXIT_CODE: 0

# 10. 客户端 build
cd client && pnpm build
# 预期：dist 生成成功

# 11. 旧测试 0 改动检测
git diff HEAD -- 'server/src/**/*.test.ts' 'client/src/**/*.test.ts' | grep -E '^[+-]' | grep -v '^[+-]{3}' | grep -v 'onboarding\.test|regularization\.test|transfer\.test|offboarding\.test|contract\.test|employee-lifecycle\.test'
# 预期：0 行
```

### 9.3 交付报告（Cursor 输出格式）

请在交付时输出以下内容（**M5-2-A1 交付报告格式**）：

```
# M5-2-A2 交付报告

M5-2-A2 已落地：M1 组织人事前端 · 员工生命周期（A3 入职 + A4 转正 + A5 调动 + A6 离职 + A7 合同）。0 修改后端 / 0 修改 package.json / 0 修改 M5-2-0 共享基础设施 / 0 修改 M5-2-A1 已建页面，未改 D1-D6 / C1-C8 / M0.5 业务代码。未提交 commit。

## 改动文件清单（X 新增 + X 修改 = X 个）

**新增（X）**：
1. client/src/api/onboarding.ts
2. client/src/api/regularization.ts
...（共 25 个新文件，**不列**）

**修改（X）**：
1. client/src/api/types/organization.ts（**追加** A3-A7 类型 + 5 status enum）
2. client/src/router/organization.ts（**追加** 11 子路由）
3. client/src/layouts/OrganizationLayout.vue（**追加** 5 菜单项 + activeKey 扩展）

**未改（按约束）**：
- D1-D6 + C1-C8 共 32 service（diff = 0）
- M0.5 4 service（diff = 0）
- server/ 任何文件（diff = 0）
- client/package.json（diff = 0）
- client/vite.config.ts / tsconfig.json / .eslintrc.cjs（diff = 0）
- M5-2-0 共享基础设施（除 router/index.ts 追加，**已 M5-2-A1 完成**）
- M5-2-A1 已建 13 页面 + 5 API 文件 + 5 通用组件（diff = 0）

## 7 条强制声明

1. 未修改任何 M0/M0.5/M1/M2/M3/C1-C8 旧测试（仍 876 个）
2. 新单测 X 个（内联断言 + 导出 `run*Tests()`）
3. 未做 M5-2-B/C/D / M5-3-M5-8 任何切片
4. 未 import 跨业务 service（**M5-2-A2 0 新 service**）
5. 5 角色 RBAC 严格遵守（admin / hr / dept_head / executive / employee，**无 finance**）
6. **0 修改后端 / 0 修改 package.json / 0 修改 M5-2-0 共享基础设施 / 0 修改 M5-2-A1 已建页面**（纯前端 + 追加）
7. **D1-D6 + C1-C8 + M0.5 业务代码 0 行改动**（36 service diff = 0）

## 红线 9 自检

- [x] 0 修改后端任何文件
- [x] 0 修改 client/package.json
- [x] 0 修改 M5-2-0 共享基础设施（除 router/index.ts 追加）
- [x] 0 修改 M5-2-A1 已建 13 页面 + 5 API 文件 + 5 通用组件
- [x] 28 个 client 文件（25 新增 + 3 修改，**不超出清单**）
- [x] 32 service 0 行改动
- [x] M0.5 4 service 0 行改动
- [x] 复用 M5-2-0 + M5-2-A1 通用组件 + Pinia stores + http + utils + 5 已建 API 文件 + types
- [x] 5 角色 RBAC 严格遵守（**无 finance 角色**）
- [x] 16-20 单元测试（内联断言，**不引入 vitest**）

## 验收命令输出

| 命令 | 结果 |
|---|---|
| pnpm --filter hrms-server test | 876/876 通过（**0 改动**） |
| pnpm --filter hrms-server type-check | 0 错 |
| npx vue-tsc --noEmit（client） | EXIT_CODE: 0 |
| pnpm build（client） | dist 生成成功 |
| pnpm lint（client） | 0 error（XX warnings） |

单测分布：onboarding.test X + regularization.test X + transfer.test X + offboarding.test X + contract.test X + employee-lifecycle.test X = X（16-20）。

## 越界检查

git diff --name-only HEAD = §6.1 列出 28 个文件，0 越界。
server/ 任何文件 diff = 0。
client/package.json diff = 0。
M5-2-0 共享基础设施 diff = 0（除 router/index.ts 追加）。
M5-2-A1 已建 13 页面 diff = 0。
M5-2-A1 5 API 文件 diff = 0。
M5-2-0 5 通用组件 diff = 0。
旧 *.test.ts diff 为空。
5 角色无 finance 角色（utils/permission.ts 仅含 5 个 RoleCode）。

## 已知问题

（如有边界场景未覆盖 / TODO 留二期 / mock 限制等，请详细说明）
```

---

## 10. 参考文档

- **V1.2 主文档** §四.5 A3-A7：https://example.com/HRMS-V1.2.md#四-5-a1-a2-a3-组织人事
- **V1.2 主文档** §三.6 RBAC：https://example.com/HRMS-V1.2.md#三-6-rbac-权限模型
- **M5-2-A1 业务 commit** 7689a45（M1 基础数据 27 文件 / 15 单测）：https://example.com/commit/7689a45
- **M5-2-A1 提示词** docs/cursor-prompts/M5-2-A1.md（本任务基础数据参考）
- **M1-A3 业务 commit** 256xxx（后端 A3 入职 5 端点）：https://example.com/commit/xxx

---

**任务开始**：M5-2-A2 业务实现 = 5 子任务 + 14 页面 + 26 端点消费 + 0 新增后端端点 + 28 个 client 文件 + 16-20 单测 + 0 修改后端 + 0 修改 package.json + 36 service 0 行改动 + 5 角色无 finance。
