# M5-2-D3 M5 绩效前端 · 等级计算与绩效奖金（等级评定 + 部门比例校准 + 奖金兑现全流程）执行提示词

> **任务 ID**：M5-2-D3
> **任务名称**：M5-2 第 9 个业务子切片 — M5 绩效前端 · 等级计算与绩效奖金（M5-2-D 第 3 片；D4 销售提成 / D5 申请联动 待后续）
> **任务范围**：2 子任务 / 2d 工时 / V1.2 §四.9 D4 绩效兑现前端部分（任务 ID 优先，不与 V1.2 切片表强对齐）
> **提示词生成日期**：2026-09-01
> **执行器**：codex exec（headless）；执行环境 Windows + cmd，**pnpm/node 若不在 PATH 需先 `set PATH=C:\Users\fmmf\AppData\Roaming\npm;C:\Program Files\nodejs;%PATH%`**
> **提速约定（沿用 D2）**：**不要运行 §7.1 的验收命令**（由审核方统一重跑）；**不要执行 git add / commit**；**不要新建清单外辅助脚本**
> **目标单测增量**：约 4-6 个（2 测试文件，每文件 ≥2 用例）
> **依赖**：M5-2-D2 commit `ff6bcd8` 已完成（后端 876/876 + 前端 78/78 基线；records 考核记录前端已就绪）

---

## ⚠️ 强约束（最优先阅读）

**历史教训**（沿用 M5-2-C1/C2/C3/D1/D2 提示词全集，以下为仍然适用要点）：
1. 严禁越界做其他切片。本提示词只授权 M5-2-D3（/grade 计算 3 端点 + /payouts 8 端点）。
2. **5 角色 RBAC 无 finance**（admin / hr / dept_head / executive / employee）。
3. **36 service + 40 表 0 行改动**（后端一律不碰）。
4. **后端实际端点/权限以代码为准**：§1.3 / §3.3 已逐一核对 `server/src/routes/performance.ts` + `server/src/constants/permissions.ts`；发现不符以代码为准并在交付报告说明。
5. **M5-2-D3 后端端点确认**（**11 端点，已逐一核对**；前缀 `/api/performance`）：
   - 等级计算（2）：POST /grade/calculate + POST /grade/calculate-batch
   - 部门比例校准（1）：POST /grade/calibrate-ratios（**注意权限点是 record:read，不是 grade:calculate**）
   - 兑现配置（2）：GET /payouts/config + **PATCH** /payouts/config
   - 奖金计算（2）：POST /payouts/calculate（direct 模式必传 employeeId；pool 模式走部门池）+ POST /payouts/calculate-pool（按部门）
   - 预发与结算（2）：POST /payouts/prepay（月度）+ POST /payouts/settle（**季度 quarter 1-4**）
   - 奖金单（2）：GET /payouts + GET /payouts/:id
   - **总计 11 端点消费（0 新增）**
6. **M5-2-D3 关键设计**：
   - **0 修改后端 / 0 修改 client/package.json**
   - **0 修改 D1/D2 已建文件**，唯一例外：`router/performance.ts` **追加** `PERFORMANCE_D3_MENU` 独立数组（沿用 D2 模式；**不动 `PERFORMANCE_MENU` / `PERFORMANCE_D2_MENU`**）
   - **类型独立到 `api/types/performancePayout.ts`**（不改 `types/performance.ts` / `types/performanceRecord.ts`；`unwrapPerformancePage` 从 `./types/performance` import）
   - **ESS 复用**：payout:read 五角色全有 → PayoutList 双模式（employee/dept_head 强制本人视角「我的奖金」）
7. **D2 实测教训沿用**：旧测试断言冲突允许**最小化更新断言 + 注释说明**（D2 先例：`children.length 6→8`）；本切片追加 2 路由后 children 变 10、菜单计数相应变化，**同步更新 D1 测试断言并加注释**（这是唯一允许改的旧测试文件：`views/__tests__/performance.test.ts`）。
8. **执行器差异（codex headless 特注）**：无人工交互；冲突按代码事实处理并写入交付报告；**不跑验收命令、不 git commit、不建清单外文件**。

**红线**（违反任何一条 → 任务失败，需回退重做）：

1. **禁止越界**：严禁动 D1（cycles/indicators/schemes/coefficients/thresholds）/ D2（records）/ D4（sales 8 端点）/ D5（applications 8 端点）。只消费 §1.3 的 11 端点。
2. **禁止修改后端文件**：**严禁修改 `server/` 任何文件**。
3. **禁止修改 `client/package.json`** / `pnpm-lock.yaml`。
4. **禁止修改共享基础设施**：`api/http.ts` / `api/types.ts` / `stores/` / `utils/` / `components/` / `layouts/**`（icon 复用 PerformanceLayout 已注册图标）。
5. **禁止修改既有前端文件**：
   - ❌ `api/types/{organization,attendance,salary,payroll,compensation,performance,performanceRecord}.ts`
   - ❌ `api/` 已有 20 个业务 API 文件 + `http.ts`
   - ❌ `router/{index,organization,attendance,salary}.ts`（**仅允许 `router/performance.ts` 追加**）
   - ❌ `views/{organization,attendance,salary}/**` 与 `views/performance/{cycle,indicator,scheme,coefficient,grade,record}/**`（D1/D2 已建 7 页面；**grade/GradeThresholds.vue 是 D1 的，本切片新建 grade/GradeActions.vue，不同文件**）
   - ❌ `__tests__/` 已有 37 个测试文件（**唯一例外**：`views/__tests__/performance.test.ts` 的计数断言可最小化更新 + 注释）
6. **必须报告完整**：列出全部新增/修改文件 + "原有用例是否改动"（若改了 D1 测试断言，逐行列出）。
7. **禁止"我觉得这也有用"**。
8. **禁止臆测字段**：字段以 §2.4 所列 service 与 zod 实际为准。
9. **范围强约束**：0 修改后端 / 0 修改 package.json / **10 个 client 文件（9 新增 + 1 修改 router/performance.ts）+ 可能的 1 处 D1 测试断言更新** / 5 角色无 finance。
10. **测试范式强约束（`ac43074` 规则）**：`src/**/__tests__/` 自包含 shim + `run*Tests()` 导出（命名匹配 `/^run.*Tests$/`）；**严禁 `import ... from 'vitest'`**。
11. **不要执行 git add / commit**；**不要运行验收命令**；**不要新建清单外文件**（D1 曾残留 mock-run-tests.mjs，严禁重蹈）。

---

## 1. 任务 ID + 目标

### 1.1 任务 ID

**M5-2-D3**（M5-2 第 9 个业务子切片 — M5 绩效前端 · 等级计算与绩效奖金）

### 1.2 任务目标

| 子任务 | 范围 | 端点 | 新增 |
|---|---|---|---|
| 7.2.D3.1 等级计算 | 单笔/批量等级计算 + 部门比例校准 | 3 | 1 页面（GradeActions）|
| 7.2.D3.2 绩效奖金 | 兑现配置切换 + 计算（直乘/池化）+ 预发 + 结算 + 奖金单列表/详情 | 8 | 3 页面（PayoutConfig/PayoutList/PayoutDetail）|
| **共用** | types/performancePayout.ts + api/performanceGrade.ts + api/performancePayout.ts + router 追加 + 2 测试 | — | 1 types + 2 API + 1 修改 + 2 测试 |
| **M5-2-D3 合计** | **11 端点消费** | **11** | **9 新增 + 1 修改 = 10 文件** |

### 1.3 端点清单（**0 新增后端端点**）

| # | 方法 + 路径 | 权限点 | body / query（zod 实际） |
|---|---|---|---|
| 1 | POST /api/performance/grade/calculate | performance:grade:calculate | `{recordId: uuid, force?: boolean}` |
| 2 | POST /grade/calculate-batch | performance:grade:calculate | `{recordIds: uuid[] min1, force?: boolean, batchSize?: int ≤100}` |
| 3 | POST /grade/calibrate-ratios | performance:**record:read**（**不是 grade:calculate**） | `{deptIds: uuid[] min1, cycleId?: uuid}` |
| 4 | GET /payouts/config | performance:payout:read | — |
| 5 | PATCH /payouts/config | performance:payout:**write** | `{mode: 'direct'\|'pool', effectiveFrom?: string, remark?: string(≤500)}` |
| 6 | POST /payouts/calculate | performance:payout:calculate | `{mode?: 'direct'\|'pool'（默认 direct）, employeeId?: uuid, cycleId: uuid, month: string, deptIds?: uuid[]}`；**direct 模式必须传 employeeId（后端 400）**；pool 模式走部门池 |
| 7 | POST /payouts/calculate-pool | performance:payout:calculate | `{deptId: uuid, cycleId: uuid, month: string}` |
| 8 | POST /payouts/prepay | performance:payout:**settle** | `{cycleId: uuid, month: string, employeeId?: uuid}` |
| 9 | POST /payouts/settle | performance:payout:**settle** | `{cycleId: uuid, quarter: int 1-4, employeeId?: uuid}`（**季度结算，不是月份**） |
| 10 | GET /payouts | performance:payout:read | query `cycleId? / employeeId? / mode?(direct\|pool) / status?(draft\|calculated\|prepaid\|settled\|cancelled) / period?(YYYY-MM 正则) / page / pageSize` |
| 11 | GET /payouts/:id | performance:payout:read | — |

---

## 2. 上下文

### 2.1 项目位置

- 仓库根：`D:\projects\hrms`；前端源码 `client/src/`；本提示词 `docs/cursor-prompts/M5-2-D3.md`

### 2.2 当前状态 + 必增文件清单

**当前状态**（D2 commit `ff6bcd8`）：绩效模块已有 7 页面 + PERFORMANCE_MENU（5 项）+ PERFORMANCE_D2_MENU（1 项）；后端 876/876，前端 78/78。

**本切片必增 9 文件 + 必改 1 文件（+ 可能的 D1 测试断言更新）**：
- 新增：`api/types/performancePayout.ts`、`api/performanceGrade.ts`、`api/performancePayout.ts`、`views/performance/grade/GradeActions.vue`、`views/performance/payout/PayoutConfig.vue`、`views/performance/payout/PayoutList.vue`、`views/performance/payout/PayoutDetail.vue`、`api/__tests__/performancePayout.test.ts`、`views/__tests__/performance-payout.test.ts`
- 修改：`router/performance.ts`（**仅追加** PERFORMANCE_D3_MENU 3 项 + 拼接 + 3 activeKey 分支 + 4 路由）
- 条件修改：`views/__tests__/performance.test.ts`（children 计数断言 8→10 等，**最小化 + 注释**，D2 先例）

### 2.3 可复用模块（**严禁重写**）

| 模块 | 路径 | 用法 |
|---|---|---|
| http 实例 | `api/http.ts` | 拦截器返回 response.data |
| 分页解包 | `api/types/performance.ts` 的 **`unwrapPerformancePage`** | listPayouts 用（§5.1 先确认形态） |
| 周期数据源 | `api/performance.ts` 的 `listCycles` | 计算/预发/结算的 cycleId 下拉 |
| 记录数据源 | `api/performanceRecord.ts` 的 `listRecords` | 等级计算的 record 选择（拉已归档/待评级记录） |
| 部门数据源 | `api/department.ts` 既有列表函数 | calibrate-ratios 与 calculate-pool 的部门选择 |
| 员工数据源 | `api/employee.ts` 的 `listEmployees` | direct 模式员工选择 |
| 金额格式化 | `utils/format.ts` 的 `formatAmount` | 奖金金额列 |
| 等级标签 | `api/types/performance.ts` 的 `GRADE_LABELS` / `Grade` | 等级展示（**import 复用，D2-fix1 已确立此模式**） |
| 菜单追加范式 | `router/performance.ts` 的 PERFORMANCE_D2_MENU 段 | PERFORMANCE_D3_MENU 同款 |

### 2.4 业务规则来源（**写类型前必读**）

- `server/src/services/performance_grade.service.ts`（calculateGrade / calculateBatchGrade 返回：等级+分数+分布？）
- `server/src/services/performance_calibration_ratio.service.ts`（calibrateDepartmentRatios 返回结构）
- `server/src/services/performance_payout_config.service.ts`（getCurrentConfig / switchConfig 返回）
- `server/src/services/performance_payout.service.ts`（calculateDirect / calculatePool / calculatePoolByDept / prepay / settle / listPayouts / getPayout 返回结构；**payout 状态机**：draft→calculated→prepaid→settled）

---

## 3. 必读约束

### 3.1 代码范式

- Vue3 `<script setup lang="ts">` + Element Plus + Pinia，与 D1/D2 同风格
- 操作类按钮成功后 ElMessage.success + 刷新；批量操作展示结果计数（成功/失败数）
- 金额 `formatAmount`；等级用 `GRADE_LABELS`

### 3.2 命名

- API 函数：`calculateGrade / calculateBatchGrade / calibrateDeptRatios`（performanceGrade.ts 3 个）；`getPayoutConfig / switchPayoutConfig / calculatePayout / calculateDeptPool / prepayPayout / settlePayout / listPayouts / getPayout`（performancePayout.ts 8 个）
- `GRADE_PATHS` / `PAYOUT_PATHS` as const
- 类型：`GradeResult / BatchGradeResult / DeptRatioResult / PayoutConfig / PayoutMode / PayoutStatus / Payout / PayoutListQuery / CalculatePayoutRequest / ...`

### 3.3 RBAC 权限矩阵（**已实读 ROLE_PERMISSIONS**）

| 权限点 | admin | hr | dept_head | executive | employee |
|---|---|---|---|---|---|
| performance:grade:calculate | ✅* | ✅ | ❌ | ✅ | ❌ |
| performance:record:read（calibrate-ratios 用）| ✅* | ✅ | ✅ | ✅ | ✅ |
| performance:payout:read | ✅* | ✅ | ✅ | ✅ | ✅ |
| performance:payout:write（配置切换）| ✅* | ✅ | ❌ | ❌ | ❌ |
| performance:payout:calculate | ✅* | ✅ | ❌ | ✅ | ❌ |
| performance:payout:settle（prepay+settle）| ✅* | ✅ | ❌ | ✅ | ❌ |

**反直觉点**：
1. **calibrate-ratios 权限是 `record:read`**（5 角色全可发起校准计算）——前端按此显隐，不要自作主张改成 grade:calculate
2. **executive 能计算/结算奖金但不能改兑现配置**（payout:write 仅 admin/hr）
3. **dept_head / employee 只有 payout:read**——只能看「我的奖金」，无任何操作按钮
4. **prepay 与 settle 共用 payout:settle 权限**（prepay 没有独立权限点）

### 3.4 错误码 / 3.5 审计 / 3.6 configs

0 新增。兑现配置走 performance 专用端点，不动 configs 模块。

---

## 4. 实施任务

### 4.1 `api/types/performancePayout.ts`（新增，**先做 §5.1 确认再动笔**）

- `PayoutMode = 'direct'|'pool'`；`PayoutStatus = 'draft'|'calculated'|'prepaid'|'settled'|'cancelled'`
- `GradeResult` / `BatchGradeResult`（以 grade service 返回为准：recordId/等级/分数/是否强制覆盖等）
- `DeptRatioResult`（以 calibration_ratio service 为准：部门/等级分布/比例）
- `PayoutConfig`（mode/effectiveFrom/remark/时间戳，以 config service 为准）
- `Payout`（id/cycleId/employeeId/mode/status/金额字段/期间/时间戳，以 payout service 为准）
- `PayoutListQuery`；`CalculatePayoutRequest`（对齐 zod：mode?/employeeId?/cycleId/month/deptIds?）；`PAYOUT_STATUS_MAP`（导出，供测试）
- `canPayoutAction(action: 'calculate'|'prepay'|'settle'|'config', user): boolean`（导出纯函数，按 §3.3 矩阵）
- 从 `./types/performance` import `unwrapPerformancePage` / `Grade` / `GRADE_LABELS`（**不重写**）

### 4.2 `api/performanceGrade.ts`（新增，3 函数）

`GRADE_PATHS`：`calculate / calculateBatch / calibrateRatios`。函数：`calculateGrade(recordId, force?)` / `calculateBatchGrade(recordIds, opts?)` / `calibrateDeptRatios(deptIds, cycleId?)`。每函数单行 JSDoc（端点+方法+权限点；**calibrateDeptRatios 注明权限点是 record:read**）。

### 4.3 `api/performancePayout.ts`（新增，8 函数）

`PAYOUT_PATHS`：`config / calculate / calculatePool / prepay / settle / list / detail(id)`。8 函数对应 §1.3 #4-#11；`listPayouts` 用 `unwrapPerformancePage`（形态先确认）；`calculatePayout` 内部不做模式分支（把 body 原样 POST，模式路由逻辑在后端）。

### 4.4 `views/performance/grade/GradeActions.vue`（新增）

- 三个卡片区块：
  1. **单笔计算**：记录选择（`listRecords` 拉记录，Select 展示员工+周期+状态）+ force Checkbox → calculateGrade → 结果展示（等级 ElTag + 分数）
  2. **批量计算**：记录多选（同上数据源的 multiple Select 或勾选表格）+ force + batchSize（可选，≤100）→ calculateBatchGrade → 结果表格（成功/失败逐条）
  3. **部门比例校准**：部门多选（department API）+ 周期可选 → calibrateDeptRatios → 各部门等级分布/比例展示（ElTable + ElProgress）
- 权限：区块 1/2 按 `grade:calculate`（admin/hr/executive）显隐；区块 3 按 `record:read`（全角色可见）

### 4.5 `views/performance/payout/PayoutConfig.vue`（新增）

- GET 加载当前配置：模式（直乘 direct / 池化 pool）+ 生效时间 + 备注
- 「切换模式」按钮（`payout:write` = **仅 admin/hr**）：Dialog 选 mode Radio + effectiveFrom（ElDatePicker date 可选）+ remark（≤500）→ switchPayoutConfig
- 无写权限角色只读展示 + 提示文案（executive/dept_head/employee 看得到配置但改不了）

### 4.6 `views/performance/payout/PayoutList.vue` + `PayoutDetail.vue`（新增）

**PayoutList.vue**：
- 双模式：管理角色（admin/hr/executive）→ 全量 + 筛选（cycleId/mode/status/period YYYY-MM）；**dept_head/employee → 「我的奖金」**：强制 `employeeId=自己`，隐藏操作区
- 操作区（管理角色 + 对应权限）：
  - 「计算奖金」Dialog（payout:calculate）：mode Radio（direct 时出现员工 Select 必填 / pool 时出现部门多选）+ cycleId + month（ElDatePicker month，`value-format="YYYY-MM"`）→ calculatePayout；另提供「按部门池计算」入口（deptId 单选 + cycleId + month → calculateDeptPool）
  - 「预发」Dialog（payout:settle）：cycleId + month + 可选员工 → prepayPayout
  - 「季度结算」Dialog（payout:settle）：cycleId + **quarter Select 1-4** + 可选员工 → settlePayout
- 表格：员工 / 周期 / 模式 / 金额（formatAmount）/ 状态 ElTag（PAYOUT_STATUS_MAP）/ 操作（查看）

**PayoutDetail.vue**：
- 描述列表全字段 + 金额明细（以 service 返回为准）+ 状态时间线（draft→calculated→prepaid→settled）
- 只读页面，无动作按钮（操作都在 List 发起）

### 4.7 `router/performance.ts`（仅追加，沿用 D2 模式）

- `PERFORMANCE_D3_MENU` 3 项：
  ```ts
  { key: 'grade-actions', label: '等级计算', icon: '<layout 已注册图标>', to: '/performance/grade-actions', permission: 'performance:grade:calculate' },
  { key: 'payouts', label: '奖金兑现', employeeLabel: '我的奖金', icon: '<...>', to: '/performance/payouts', permission: 'performance:payout:read' },
  { key: 'payout-config', label: '兑现配置', icon: '<...>', to: '/performance/payout-config', permission: 'performance:payout:write', hideForEmployee: true },
  ```
  （payout-config 用 **payout:write** 权限使菜单仅 admin/hr 可见——虽然页面只读也安全，但菜单语义更干净；grade-actions 菜单对 dept_head/employee 不可见，calibrate-ratios 区块随之不可达，**这是可接受的取舍**，写入交付报告）
- filterPerformanceMenu 拼接追加 D3 数组；resolvePerformanceActiveKey 追加 3 分支（`/performance/grade-actions` / `/performance/payouts` / `/performance/payout-config`，**注意 `/performance/payouts` 是 `/performance/payout-config` 的前缀吗？不是——'payouts' vs 'payout-config' 不以彼此开头，顺序安全**）
- children 追加 4 路由：`grade-actions` / `payout-config` / `payouts` / `payouts/:id`
- **追加后检查 `views/__tests__/performance.test.ts`**：children 计数断言 8→10、菜单计数断言（executive 6→8 等）若失败则**最小化更新 + 注释**（D2 先例），逐行写入交付报告

### 4.8 测试（2 个新文件，`ac43074` 自包含范式）

1. `api/__tests__/performancePayout.test.ts`（≥2）：`GRADE_PATHS` / `PAYOUT_PATHS` 拼接断言；`CalculatePayoutRequest` direct 模式 employeeId 语义的类型断言；`canPayoutAction`（executive+config → **false**；executive+settle → true；dept_head+calculate → **false**）
2. `views/__tests__/performance-payout.test.ts`（≥2）：`PAYOUT_STATUS_MAP` 五态完整映射；`filterPerformanceMenu` D3 菜单可见性（admin → 含 grade-actions/payouts/payout-config；employee → 仅 payouts「我的奖金」）

导出 `runPerformancePayoutApiTests / runPerformancePayoutViewTests`。**严禁 import vitest / @vue/test-utils**。

---

## 5. 关键决策点（详尽）

### 5.1 响应结构确认（**最优先**）

写类型前逐一打开 §2.4 四个 service，记录：等级计算返回结构（单/批量）/ 校准比例返回结构 / config GET 返回 / payout list 分页形态 / payout 详情嵌套 / prepay·settle 返回（影响几笔？金额合计？）。**结论写进交付报告「主动识别与处理」**。

### 5.2 calculate 的模式路由

- `POST /payouts/calculate` 内部按 mode 分流（direct 要 employeeId，pool 用 deptIds）——前端 Dialog 按 mode 动态切换必填项，**前端不做业务分流，只组 body**
- direct 模式忘传 employeeId 会被后端 400——前端表单校验先行拦截

### 5.3 预发与结算的语义差异

- prepay 按**月**（month 字符串）；settle 按**季度**（quarter int 1-4）——两个 Dialog 的期间控件不同，严禁复用错

### 5.4 ESS 视角

- dept_head/employee 进 PayoutList 强制本人 employeeId；`userInfo.employee?.id` 为空 → EmptyState「尚未关联员工档案」
- 「我的奖金」菜单文案经 employeeLabel（employee）展示

### 5.5 执行器环境特注（codex headless）

- shell 为 cmd；pnpm 不在 PATH 时先 `set PATH=C:\Users\fmmf\AppData\Roaming\npm;C:\Program Files\nodejs;%PATH%`
- **不要执行 git add / commit**；**不要运行验收命令**；**不要新建 §6.1 清单外文件**
- 完成后最终回复 = 完整交付报告

### 5.6 不做清单

- ❌ records / cycles / indicators / schemes / coefficients / thresholds 任何改动（D1/D2）
- ❌ sales / applications（D4/D5）
- ❌ DefaultLayout 侧栏占位（M5-2-0 范畴）
- ❌ 修改 PerformanceLayout.vue

---

## 6. 修改文件清单

### 6.1 必改文件（10 个）

**新增 9 个**：
1. `client/src/api/types/performancePayout.ts`
2. `client/src/api/performanceGrade.ts`
3. `client/src/api/performancePayout.ts`
4. `client/src/views/performance/grade/GradeActions.vue`
5. `client/src/views/performance/payout/PayoutConfig.vue`
6. `client/src/views/performance/payout/PayoutList.vue`
7. `client/src/views/performance/payout/PayoutDetail.vue`
8. `client/src/api/__tests__/performancePayout.test.ts`
9. `client/src/views/__tests__/performance-payout.test.ts`

**修改 1 个**：
10. `client/src/router/performance.ts`（**仅追加** PERFORMANCE_D3_MENU + 拼接 + 3 activeKey 分支 + 4 路由）

**条件修改（若断言失败才动）**：`client/src/views/__tests__/performance.test.ts` 的计数断言（最小化 + 注释）

### 6.2 禁止修改文件（清单以外）

- ❌ `server/**`（全部）
- ❌ `client/package.json` / `pnpm-lock.yaml` / `vitest.config.ts` / `tests/run-all.test.ts` / `vite.config.ts` / `tsconfig*.json`
- ❌ `client/src/api/types.ts` / `api/types/` 既有 7 文件 / `api/` 既有 20 业务 API + `http.ts`
- ❌ `client/src/router/{index,organization,attendance,salary}.ts`
- ❌ `client/src/layouts/**` / `components/**` / `stores/**` / `utils/**`
- ❌ `client/src/views/{organization,attendance,salary}/**` / `views/performance/{cycle,indicator,scheme,coefficient,record}/**` / `views/performance/grade/GradeThresholds.vue`
- ❌ `client/src/**/__tests__/` 既有 37 个测试文件（唯一例外见 6.1）
- ❌ **禁止新建根目录/其他目录的辅助脚本**

### 6.3 越界检测（交付时自检，写入报告）

```bash
cd D:\projects\hrms
git status --short
# 必须只出现 §6.1 列出的文件
git diff --stat server/
# 必须 0 行
git diff client/package.json
# 必须 0 行
```

---

## 7. 验收标准

### 7.1 硬性验收（**审核方执行，你不跑**；仅供了解通过线）

基线：后端 876/876；前端 78 + 新增（2 文件 × ≥2）= **≥82**；两端 type-check 0 错；lint 0 error；build 成功。

### 7.2 交付报告模板（**你的最终回复必须完整包含**）

```markdown
# M5-2-D3 交付报告

## 完成范围
- 子任务 7.2.D3.1/2 完成状态 + 端点消费清单（11/11）
## 新增文件（9）
（逐一列出）
## 修改文件（1 + 条件 1）
- client/src/router/performance.ts（+N 行）
- （若改动）views/__tests__/performance.test.ts：逐行列出断言更新
## 主动识别与处理
（§5.1 逐 service 确认结论 / payout 状态机 / 分页形态 / 与提示词不一致处，逐一说明）
## 测试
- 新增 2 个测试文件 / N 个用例（未运行，由审核方执行）
- 原有用例是否改动：（0 或逐行列出）
## 已知问题
（如有，逐一列出 + 建议处理阶段）
## 红线自检
- 0 修改后端 / 0 修改 package.json / 0 修改共享基础设施与 D1/D2 文件（除 router/performance.ts 追加）/ 5 角色无 finance / 未执行 git commit / 未运行验收命令 / 未建清单外文件
```

---

**提示词结束。按本提示词直接执行（headless 环境无人工确认环节）：先输出实施计划（文件创建顺序 + §5.1 确认结论），然后动手，最终回复给出完整交付报告。**
