# M4-C5 工资条 + 银企代发 + 个税申报 + 工资表导出 Cursor 提示词

> **任务 ID**：M4-C5
> **任务名称**：M4 薪酬核算 C5 切片 — 工资条生成 + 银企代发 + 个税申报 + 工资表导出
> **任务范围**：5 子任务 / 4d 工时 / V1.2 §四.8.1 C5 行 + §二.4.6 + §三.6
> **提示词生成日期**：2026-08-27
> **目标单测增量**：约 20-28 个（M4 起步，725 → 745-753）
> **目标 commit**：1 个业务 commit + 1 个提示词 commit

---

## ⚠️ 强约束（最优先阅读）

**历史教训**（12 次）：
1. M0.5-5 任务中，Cursor 越界做了 M0.5-6（配置中心）+ M0.5-7（改密 + 2FA）共 2 个不在提示词范围内的切片。本次任务严禁重蹈覆辙。
2. M1-A6 提示词第一次瘦身到 15KB，Cursor 推断/补全消耗大量 token，返工 1 次。本次任务按 55-65KB 详尽模式写。
3. D1 提示词 §3.3 误列 5 角色包含 finance，**Cursor 主动识别 + 正确处理**。D2-D6 + C1+C2+C3+C4 已修正。C5 沿用 5 角色 = admin / hr / dept_head / executive / employee，**无 finance**。
4. M2-wrap-up.md §7 误写 D3 = "季度校准会议"。C5 不重蹈覆辙，严格按 V1.2 §四.8.1 实际范围。
5. D3 业务代码 0 行改动 D1+D2。D4 升级到 9 service。D5 升级到 11 service。D6 升级到 14 service。C1 升级到 17 service。C2 升级到 20 service。C3 升级到 23 service。C4 升级到 **D1-D6 + C1 + C2 + C3 共 23 service**。C5 升级到 **D1-D6 + C1 + C2 + C3 + C4 共 27 service 0 行改动**。
6. D4 验收发现 2 个 B3/B5 跨 UTC 边界日期敏感旧测。已用 vi.useFakeTimers 修复。C5 测试**沿用**此模式。
7. D6 调薪/晋升用 audit_logs 不建表 + PIP 失败不调 A6 离职 + D6 实际写入 employee_salary_history 留 M4 联调 → C1 已用 employee_salary_plans 表 + C2 3 张 insurance 表 + C3 0 表（纯算法层）+ C4 3 张 payroll 表。C5 同样**0 新表**（C4 已建够 C5 需要的 payslips + payslip_items + payroll_runs）。
8. C1 路由**首次非 performance 路由**（独立 `server/src/routes/salary.ts`）。C2+C3+C4 路由**追加**在 salary.ts 内（不分新文件）。C5 路由**继续追加**在 salary.ts 内。
9. C1 验证：D1-D6 14 service 0 行改动 + 5 角色无 finance。C2 验证：17 service 0 行改动 + 5 角色无 finance + 不写 employee_salary_history。C3 验证：20 service 0 行改动 + 5 角色无 finance + 0 新表。C4 验证：23 service 0 行改动 + 5 角色无 finance + AI 复用 M0.5-5。
10. C3 已知问题：Cursor 主动识别提示词 §5.1.4 / §5.3.3 部分"预期税额"与 §3.7 实际税率表算术不一致，按 §3.7 实际算法实现（**更准确**）。C4 类似：算法细节应以 §3 描述为准。
11. **C5 关键设计**：**银企代发 + 个税申报留 mock 模式**（不接真实银行 API，不接真实税务局 API）。C5 仅生成 mock 文件 + 走 M0.5-2 通知基础设施发邮件 + 走 M0.5-6 configs 调配置。V1.2 §三.6 第三方对接（招行/建行/工行）**留二期实际集成**。
12. **C5 范围**：V1.2 §四.8.1 C5 标题 = "工资条生成 + 银企直连 + 个税申报"，5 子任务 / 4d。C5 0 新表（C4 已建够用）。

**红线**（违反任何一条 → 任务失败，需回退重做）：

1. **禁止越界做其他切片**：本提示词只授权 "C5 切片（工资条 + 银企代发 + 个税申报 + 工资表导出）"。严禁自作主张做 C1+C2+C3+C4（已完成）/ C6（销售提成核算）/ C7（人力成本预算）/ C8（调薪实际执行）。
2. **禁止修改未授权文件**：清单以外的文件全部禁止改动（清单见 §6）。**特别是 D1-D6 + C1 + C2 + C3 + C4 业务代码（27 个 service）严禁改动**。
3. **禁止修改旧测试文件**：本任务完全不需要改任何测试。如果发现"必须改测试才能跑通"的情况，立即停下，说明原因。
4. **必须报告完整**：交付报告必须列出全部新增/修改文件（即使是 0 个也要说"0 个"），并明确说明"原有用例是否改动"。
5. **禁止"我觉得这也有用"**：任何"顺便做"或"我觉得可以优化"的越界内容一律禁止。包括但不限于：顺手做 C6 销售提成 / 顺手做 C7 人力成本预算 / 顺手做 C8 调薪实际执行 / 顺手接真实银行 API / 顺手接真实税务局 API。
6. **C5 范围强约束**（红线 8）：
   - **不接真实银行 API**（**留 mock 模式**，V1.2 §三.6 第三方对接留二期）
   - **不接真实税务局 API**（**留 mock 模式**，个税申报 API 留二期）
   - **不实现 UI**（前端工资条展示留任务）
   - **0 新表**（C4 已建 payslips + payslip_items + payroll_runs 够用）
   - **D1+D2+D3+D4+D5+D6 + C1 + C2 + C3 + C4 业务代码 0 行改动**（27 service + 17 表 = 0 行改动）
   - **复用 M0.5-2 通知基础设施**（发邮件给员工）— 严禁重写
   - **复用 M0.5-6 configService**（配置模板/邮件主题/银行格式/mock 模式开关）— 严禁重写
   - **5 角色 RBAC 不含 finance**（D1-D6 + C1+C2+C3+C4 教训延续）

---

## 1. 任务 ID + 目标

### 1.1 任务 ID

**M4-C5**（薪酬核算 C5 切片 — **工资条生成 + 银企代发 + 个税申报 + 工资表导出**）

### 1.2 任务目标

实现薪酬核算 C5 切片（V1.2 §四.8.1 C5 行 + §二.4.6 + §三.6），包含 5 子任务：

| 子任务 | 范围 | 表 | Service 函数 |
|---|---|---|---|
| 5.1 工资条生成 | PDF / HTML 生成 + 邮件推送 + 系统内查看 | **0 新表**（复用 C4 payslips） | generatePayslip / getPayslipHtml / getPayslipPdf |
| 5.2 工资条发送 | 邮件 + 系统通知（**复用 M0.5-2 notification**） | **0 新表** | deliverPayslip / deliverBatchPayslips |
| 5.3 银企代发文件 | 按 3 种银行格式（工行/建行/招行）**留 mock 模式** | **0 新表** | exportBankingFile / getBankingFileList |
| 5.4 个税申报 | **留 mock 模式**，生成个税申报台账 | **0 新表** | declareTax / getDeclarationList |
| 5.5 工资表导出 | Excel / PDF 汇总表 + 明细表 | **0 新表** | exportReport / getReportList |

**V1.2 §二.4.6 工资条与发放**：
- 电子工资条：每月 10 日前推送（邮件 + 系统内查看，走 M0.5 通知基础设施）
- 工资条内容：应发合计 / 各项扣款 / 实发金额 / 个税明细
- 工资表导出：Excel / PDF（含汇总表 + 明细表）
- 银行代发文件：工行 / 建行 / 招行格式
- 薪酬审批流：HR 核算 → 财务复核 → 总经理审批 → 发放

**V1.2 §三.6 第三方对接**：
- 招行 / 建行 / 工行（**留 mock**，实际集成留二期）
- 税务局个税申报（**留 mock**，实际集成留二期）

**C5 表设计**（**0 新表**，C4 已建够用）：
- 复用 C4 `payroll_runs`（算薪批次）
- 复用 C4 `payslips`（工资单主表，含 paidAt 字段）
- 复用 C4 `payslip_items`（工资单明细）
- 业务逻辑层：工资条 PDF / HTML / 银行代发文件 / 个税申报台账 全部走 configs + audit log

### 1.3 端点清单（7 个新端点，全部挂到 `/api/salary/*`）

| # | Method | Path | 子任务 | 权限点 |
|---|---|---|---|---|
| 1 | `POST` | `/api/salary/payslips/:id/payslip/generate` | 5.1 | `SALARY_PAYSLIP_GENERATE` |
| 2 | `GET` | `/api/salary/payslips/:id/payslip/html` | 5.1 | `SALARY_PAYSLIP_GENERATE` |
| 3 | `GET` | `/api/salary/payslips/:id/payslip/pdf` | 5.1 | `SALARY_PAYSLIP_GENERATE` |
| 4 | `POST` | `/api/salary/payslips/:id/deliver` | 5.2 | `SALARY_PAYSLIP_GENERATE` |
| 5 | `POST` | `/api/salary/payrolls/runs/:id/banking-export` | 5.3 | `SALARY_BANKING_EXPORT` |
| 6 | `POST` | `/api/salary/payrolls/runs/:id/tax-declare` | 5.4 | `SALARY_TAX_DECLARE` |
| 7 | `POST` | `/api/salary/payrolls/runs/:id/report-export` | 5.5 | `SALARY_REPORT_EXPORT` |

**实际端点数**：7 个

### 1.4 单测目标

20-28 个新单测，按 5 service 分布：

| Service | describe | it 数 | 目标 |
|---|---|---|---|
| payslip_generator.service | `payslip_generator.service` | 5-7 | HTML/PDF 生成 + 内容 |
| payslip_delivery.service | `payslip_delivery.service` | 4-6 | 邮件 + 系统通知（**mock M0.5-2**）|
| banking_export.service | `banking_export.service` | 4-6 | 3 种银行格式（**mock 模式**）|
| tax_declaration.service | `tax_declaration.service` | 4-6 | 个税申报（**mock 模式**）|
| report_export.service | `report_export.service` | 3-5 | Excel / PDF 导出 |

---

## 2. 上下文

### 2.1 项目位置

- **项目根**：`D:\projects\hrms`
- **主文档**：[`docs/HRMS-V1.2.md`](../../HRMS-V1.2.md) §二.4.6 + §三.6 + §四.8.1
- **API 契约**：[`docs/api-spec.md`](../../api-spec.md) §6.x（追加 salary/payslip 端点）
- **OpenAPI**：[`docs/openapi.yaml`](../../openapi.yaml)（追加 /salary/payslip + /banking + /tax + /report 端点）
- **错误码**：[`docs/error-codes.md`](../../error-codes.md) §8.5（C5 用 73501-73510）
- **AI Coding 上下文**：[`AGENTS.md`](../../../AGENTS.md)

### 2.2 当前状态（M0+M0.5+M1+M2+M3+C1+C2+C3+C4 已全部完成）

- **C1 业务 commit**：`265316b feat(salary): M4-C1 薪级薪档 + 员工薪酬方案`
- **C2 业务 commit**：`5704c0b feat(salary): M4-C2 社保公积金方案`
- **C3 业务 commit**：`92406f1 feat(salary): M4-C3 个税引擎`
- **C4 业务 commit**：`a71f7c9 feat(salary): M4-C4 算薪引擎 + 算薪流程 + AI 算薪校验摘要`
- **测试**：725/725 通过
- **C1+C2+C4 已建 9 张 salary 表**（C3 0 表）— C5 0 新表，**复用 C4 payroll_runs + payslips + payslip_items**

**C5 严禁修改**：
- ❌ C1 3 service（salary_grade / salary_grade_level / salary_plan）
- ❌ C2 3 service（social_insurance_scheme / housing_fund_scheme / employee_insurance）
- ❌ C3 3 service（tax_calculation / tax_year_end_bonus / tax_labor_income）
- ❌ C4 4 service（payroll_calculation / payroll_run / payslip / payroll_ai_summary）
- ❌ C1+C2+C4 共 9 张表
- ❌ C1 4 + C2 4 + C3 3 + C4 5 = 16 权限点（仅追加 4 个 C5 权限点）
- ❌ C1 7 + C2 9 + C3 6 + C4 12 = 34 端点（仅追加 7 个 C5 端点）
- ❌ C1 5 + C2 6 + C3 10 + C4 6 = 27 configs（仅追加 6 项 C5 configs）
- ❌ C1+C2+C3+C4 路由（`server/src/routes/salary.ts` 内 C1-C4 子路由，C5 路由**追加**在 payslips / banking / tax / report 4 个子路由）
- ❌ D1-D6 14 service + 14 表 + 38 权限点 + 55 端点 + 59 configs
- ❌ M0.5-1 approval.service.ts
- ❌ M0.5-2 notification.service.ts（**C5 仅调用 sendNotification，**严禁重写** 通知 service**）
- ❌ M0.5-5 ai/score.service.ts
- ❌ M0.5-6 config.service.ts（**C5 仅调用 getValue/setValue，**严禁重写** config service**）

### 2.3 已有可复用模块（C5 必复用，零重写）

| 复用对象 | 来源 | 用途 | C5 用法 |
|---|---|---|---|
| `auditService.auditLog` | M0.5-8 | 增删改 + 关键读 | 必用（7 个端点全部写审计） |
| `configService.getValue` | M0.5-6 | 6 项 configs | 必用（模板/邮件主题/银行格式/mock 模式） |
| `prisma` | server | 20+ 张表直接查询 | 必用（payslips + payroll_runs + items） |
| `notificationService.sendNotification` | M0.5-2 | 邮件 + 系统通知 | **必用**（发工资条邮件，**复用不重写**）|
| C4 `payslips` | M4-C4 | 工资单主表 | 直接读（工资条内容） |
| C4 `payslip_items` | M4-C4 | 工资单明细 | 直接读（工资条明细） |
| C4 `payroll_runs` | M4-C4 | 算薪批次 | 直接读（代发文件 / 申报台账） |
| D1 `employees` | M3-D1 | 员工查询 | 直接读（员工姓名 / 邮箱 / 银行卡） |

### 2.4 业务规则来源

- **V1.2 §二.4.6 工资条与发放**：
  - 电子工资条：每月 10 日前推送（邮件 + 系统内查看）
  - 工资条内容：应发合计 / 各项扣款 / 实发金额 / 个税明细
  - 工资表导出：Excel / PDF（含汇总表 + 明细表）
  - 银行代发文件：工行 / 建行 / 招行格式
  - 薪酬审批流：HR 核算 → 财务复核 → 总经理审批 → 发放
- **V1.2 §三.6 第三方对接**：招行 / 建行 / 工行（**留 mock**，实际集成留二期）
- **V1.2 §四.8.1 C5 行**：5 子任务 / 4d

---

## 3. 必读约束（项目级）

### 3.1 代码范式

- **不用 class + 静态方法**：统一 `export function xxx() {}` 范式
- **JSDoc + 校验链**：每个 service 函数必须含 JSDoc（参数 + 返回值 + 校验链 + 算法步骤 + mock 标记）
- **错误统一三参**：抛 `new AppError(message, statusCode, code)`，code 必查错误码表
- **业务规则必走 configService**：禁止硬编码（V1.2 §三.5 强约束 + **mock 模式必须走 configs.salary.banking.mock_mode**）
- **审计必写**：7 个端点全部写 `auditService.auditLog(...)`

### 3.2 数据模型约定

- **C5 0 新表**（复用 C4 payslips + payslip_items + payroll_runs）
- **TS 类型**：PascalCase（`BankingExportResult` / `TaxDeclarationResult` / `ReportExportResult`）
- **API 路径**：kebab-case（`/api/salary/payslips/:id/payslip/generate`）

### 3.3 RBAC 5 角色（**无 finance**）

```
admin / hr / dept_head / executive / employee
```

**C5 新增 4 个权限点**（追加到 `permissions` 数组，不修改 ROLE_PERMISSIONS 结构）：
- `SALARY_PAYSLIP_GENERATE`（生成 + 下载 + 发送工资条）
- `SALARY_BANKING_EXPORT`（银企代发文件生成）
- `SALARY_TAX_DECLARE`（个税申报）
- `SALARY_REPORT_EXPORT`（工资表导出）

**C5 权限点分配**（5 角色）：
- admin：`['*']` WILDCARD 通配
- hr：4 个全开（HR 工资条 + 银企 + 个税 + 报表）
- executive：READ（决策权 + 审计）+ 4 个 WRITE（CEO 审批后导出）
- dept_head：READ（看本部门员工工资条）
- employee：仅 SALARY_PAYSLIP_GENERATE READ（看本人工资条）

**注**：C5 仍保持 5 角色 = admin / hr / dept_head / executive / employee，**无 finance**。银行代发 + 个税申报**真正对接**留二期（V1.2 §三.6 第三方对接），C5 仅 mock 模式生成文件。

### 3.4 错误码段位（C5 用 73501-73510，10 个）

| code | HTTP | 名称 | 触发条件 | 客户端处理 |
|---|---|---|---|---|
| 73501 | 400 | `PAYSLIP_NOT_FOUND` | payslipId 不存在 | 提示 ID 错误 |
| 73502 | 400 | `PAYSLIP_NOT_APPROVED` | payslip.status !== 'approved' | 提示需先审批 |
| 73503 | 400 | `PAYSLIP_GENERATION_FAILED` | PDF / HTML 生成失败 | 提示重试 |
| 73504 | 400 | `BANKING_EXPORT_FAILED` | 银行代发文件生成失败 | 提示重试 |
| 73505 | 400 | `BANKING_FORMAT_INVALID` | 银行格式不在 icbc / ccb / cmb | 提示合法格式 |
| 73506 | 400 | `TAX_DECLARE_FAILED` | 个税申报失败（mock 模式）| 提示重试 |
| 73507 | 400 | `TAX_PERIOD_INVALID` | period 格式错 | 提示格式 |
| 73508 | 400 | `DELIVERY_FAILED` | 邮件 / 系统通知发送失败 | 提示重试 |
| 73509 | 400 | `EXPORT_FAILED` | 工资表导出失败 | 提示重试 |
| 73510 | 400 | `REPORT_PERIOD_INVALID` | 报告 period 格式错 | 提示格式 |

### 3.5 审计埋点（C5 5 类 action）

| action | resource | 触发 |
|---|---|---|
| `PAYSLIP_GENERATE` | `payslip` | POST /payslips/:id/payslip/generate |
| `PAYSLIP_DELIVER` | `payslip` | POST /payslips/:id/deliver |
| `BANKING_EXPORT` | `payroll_run` | POST /payrolls/runs/:id/banking-export |
| `TAX_DECLARE` | `payroll_run` | POST /payrolls/runs/:id/tax-declare |
| `REPORT_EXPORT` | `payroll_run` | POST /payrolls/runs/:id/report-export |

`actorType`：HR/管理员/executive = 'USER'；BullMQ 自动推送 = 'SYSTEM'

### 3.6 业务规则配置化（6 项 configs，C5 全配齐）

所有 6 项必须走 `configService.getValue(...)` + fallback + TODO 注释：

| config key | 类型 | 默认值 | 用途 |
|---|---|---|---|
| `salary.payslip.template` | string | `'default'` | HTML/PDF 模板名（**留 mock**，实际模板留二期）|
| `salary.payslip.email_subject` | string | `'您的 {period} 工资条'` | 邮件主题（{period} 占位）|
| `salary.payslip.delivery_methods` | string[] | `['email', 'system']` | 发送方式（email / system）|
| `salary.banking.mock_mode` | boolean | `true` | **mock 模式**（C5 强制 true，**不接真实 API**）|
| `salary.banking.formats` | string[] | `['icbc', 'ccb', 'cmb']` | 银行格式（工行 / 建行 / 招行）|
| `salary.report.formats` | string[] | `['excel', 'pdf']` | 报告格式（Excel / PDF）|

**总 configs**：86（C1+C2+C3+C4+D1-D6）+ 6（C5）= **92 项**

**注**：C5 留 mock，**C5 业务通知发邮件用 M0.5-2 notification.sendNotification**（不重写通知 service）

---

## 4. 实施任务

### 4.1 子任务 5.1：工资条生成（payslip_generator.service.ts）

**0 新表**（复用 C4 payslips + payslip_items）

**Service 函数**：
- `generatePayslip(actorId, payslipId)` — 生成工资条（HTML + PDF 二进制）
- `getPayslipHtml(actorId, payslipId)` — 读 HTML 工资条
- `getPayslipPdf(actorId, payslipId)` — 读 PDF 工资条

**工资条内容**（V1.2 §二.4.6）：
- 应发合计 = base + performance + overtime + allowance + salesCommission + yearEndBonus
- 各项扣款 = social + housing + tax + absence
- 实发金额 = 应发 - 应扣
- 个税明细 = taxAmount（来自 C4 payslips）

**完整 schema + JSDoc + 错误码触发条件 + 测试断言**：见 §5.1。

### 4.2 子任务 5.2：工资条发送（payslip_delivery.service.ts）

**0 新表**

**Service 函数**：
- `deliverPayslip(actorId, payslipId, input)` — 单条发送（邮件 + 系统通知）
- `deliverBatchPayslips(actorId, runId, input)` — 批量发送（按 runId）

**复用 M0.5-2 notification.sendNotification**（**严禁重写**）：
- 调 `notificationService.sendNotification({ userId: employeeId, type: 'email', template: 'payslip', data: { html, pdf, period } })`
- 调 `notificationService.sendNotification({ userId: employeeId, type: 'system', template: 'payslip_published', data: { period, netAmount } })`
- D1 教训：bypassTemplate fallback（D2 既有）

**完整 schema + JSDoc + 错误码触发条件 + 测试断言**：见 §5.2。

### 4.3 子任务 5.3：银企代发文件（banking_export.service.ts）

**0 新表**

**Service 函数**：
- `exportBankingFile(actorId, runId, input)` — 导出代发文件（**mock 模式**）
- `getBankingFileList(actorId, filter)` — 查导出记录（从 audit log 查）

**3 种银行格式**（V1.2 §二.4.6 + §三.6）：
- 工行（icbc）— CSV 格式
- 建行（ccb）— TXT 格式
- 招行（cmb）— XLS 格式

**Mock 模式**：
- `salary.banking.mock_mode = true`（强制 true，C5 **不接真实 API**）
- 生成文件路径：`./tmp/banking/{runId}-{format}-{timestamp}.csv`
- 文件内容：employee_name + bank_card + amount（按银行格式）
- 写 audit（BANKING_EXPORT，details: { runId, format, filePath, recordCount, totalAmount }）

**完整 schema + JSDoc + 错误码触发条件 + 测试断言**：见 §5.3。

### 4.4 子任务 5.4：个税申报（tax_declaration.service.ts）

**0 新表**

**Service 函数**：
- `declareTax(actorId, runId, input)` — 个税申报（**mock 模式**）
- `getDeclarationList(actorId, filter)` — 查申报记录（从 audit log 查）

**Mock 模式**：
- `salary.tax.declaration.mock_mode = true`（强制 true，C5 **不接真实税务局 API**）
- 生成申报台账：employee_id + period + taxable_income + tax_amount + rate + quick_deduction
- 写 audit（TAX_DECLARE，details: { runId, period, totalTaxableIncome, totalTax, recordCount }）

**完整 schema + JSDoc + 错误码触发条件 + 测试断言**：见 §5.4。

### 4.5 子任务 5.5：工资表导出（report_export.service.ts）

**0 新表**

**Service 函数**：
- `exportReport(actorId, runId, input)` — 导出 Excel / PDF 工资表（汇总表 + 明细表）
- `getReportList(actorId, filter)` — 查导出记录（从 audit log 查）

**2 种格式**（V1.2 §二.4.6）：
- Excel：XLSX 格式（**留 mock**，实际 Excel 库留二期）
- PDF：PDF 格式（**留 mock**，实际 PDF 库留二期）

**Mock 模式**：
- 生成文件路径：`./tmp/reports/{runId}-{format}-{timestamp}.xlsx`
- 文件内容：汇总表（totalGross + totalNet + 异常数）+ 明细表（员工姓名 + 各项金额）
- 写 audit（REPORT_EXPORT，details: { runId, format, filePath, totalGross, totalNet }）

**完整 schema + JSDoc + 错误码触发条件 + 测试断言**：见 §5.5。

### 4.6 路由挂载

C5 路由**追加**在 `server/src/routes/salary.ts` 内（**不分新文件**，沿用 C1+C2+C3+C4 模式）：
```ts
// 已有 C1 + C2 + C3 + C4 路由
salaryRouter.use('/grades', ...);
salaryRouter.use('/grade-levels', ...);
salaryRouter.use('/plans', ...);
salaryRouter.use('/insurances/social', ...);
salaryRouter.use('/insurances/housing-fund', ...);
salaryRouter.use('/insurances/employees', ...);
salaryRouter.use('/tax', ...);
salaryRouter.use('/payrolls', ...);

// C5 新增（4 个子路由）
salaryRouter.use('/payslips', payslipRouter);             // 5.1 + 5.2
salaryRouter.use('/banking', bankingRouter);               // 5.3
salaryRouter.use('/tax', taxDeclarationRouter);           // 5.4（注：与 C3 /tax 同 prefix，路由内部用 /tax-declare 区分）
salaryRouter.use('/reports', reportRouter);               // 5.5
```

**注**：`server/src/routes/index.ts` 已挂载 `/api/salary`（C1 业务已做），C5 不需改 index.ts。

**重要路由设计**：C3 已有 `/api/salary/tax/calculate`，C5 个税申报是 `/api/salary/tax/tax-declare`（**避免与 C3 路由冲突**）。C3 路由在 `server/src/routes/salary.ts` 内的 `/tax` 子路由，C5 路由是 `/tax` 子路由下的 `/tax-declare` 端点（或独立 `/taxDeclaration` 子路由）。**由 Cursor 决定具体路由组织**，**确保不与 C3 冲突**。

### 4.7 权限常量追加

在 `server/src/constants/permissions.ts` 追加 4 个 C5 权限点（`SALARY_PAYSLIP_GENERATE` / `SALARY_BANKING_EXPORT` / `SALARY_TAX_DECLARE` / `SALARY_REPORT_EXPORT`），并在 5 角色（**不**含 finance）中按 §3.3 分配。

### 4.8 Migration

**C5 不创建 migration**（**0 新表**）。

### 4.9 seed.ts 追加

在 `server/prisma/seed.ts` 追加：
- 6 项 salary.payslip / banking / report configs
- 4 组 demo payslips（D4 已有 2 组 demo payroll_runs，**复用 C4 seed**）
- 0 新表

---

## 5. 关键决策点详尽（4 类必须详尽）

### 5.1 工资条生成 JSDoc + 测试（子任务 5.1）

#### 5.1.1 不创建新表（C5 0 新表）

C5 子任务 5.1 **0 新表**，全部复用：
- C4 `payslips` 主表（含 paidAt 字段，C4 业务已建）
- C4 `payslip_items` 明细表
- D1 `employees`（员工姓名 / 邮箱 / 银行卡）

#### 5.1.2 JSDoc + 校验链（每个函数 15-30 行）

**`generatePayslip`**

```ts
/**
 * 生成工资条（HTML + PDF 二进制，V1.2 §二.4.6）
 * @param actorId 操作人 ID（hr/admin）
 * @param payslipId payslip ID
 * @returns { payslipId, employeeId, period, html, pdf, fileName, generatedAt }
 * @throws AppError(404, 73501) payslip 不存在
 * @throws AppError(400, 73502) payslip.status !== 'approved'
 * @throws AppError(400, 73503) PDF / HTML 生成失败
 * 校验链：
 *  1. 查 payslip（含 items + employee）→ 不存在抛 73501
 *  2. 状态检查：status === 'approved'（或 'locked'）→ 否则抛 73502
 *  3. 读模板：configs.salary.payslip.template（默认 'default'，**留 mock**）
 *  4. 生成 HTML 字符串（按模板拼接应发/应扣/实发 + 明细）
 *  5. 生成 PDF 二进制（**留 mock**，实际 PDF 库留二期）
 *  6. 返回 { html, pdf, fileName, generatedAt }
 *  7. 写 audit（PAYSLIP_GENERATE，actorType='USER'，details: { payslipId, employeeId, period, format: 'html+pdf' }）
 *  注：**PDF 生成留 mock**（实际 PDF 库留二期），C5 仅生成 mock 二进制或 HTML 字符串
 */
export async function generatePayslip(
  actorId: string,
  payslipId: string
): Promise<GeneratePayslipResult> { ... }
```

#### 5.1.3 错误码触发条件

| code | 触发函数 | 触发条件 | HTTP | 客户端处理 |
|---|---|---|---|---|
| 73501 | generatePayslip / getPayslipHtml / getPayslipPdf | payslipId 不存在 | 400 | 提示 ID |
| 73502 | generatePayslip | payslip.status !== 'approved' | 400 | 提示需先审批 |
| 73503 | generatePayslip | PDF/HTML 生成失败 | 400 | 提示重试 |

#### 5.1.4 测试用例（5-7 个 it）

```ts
describe('payslip_generator.service', () => {
  describe('generatePayslip', () => {
    it('approved 状态生成 HTML + PDF 成功', async () => { ... });
    it('locked 状态可生成（仅 locked 不允许）', async () => { ... });
    it('calculated 状态抛 73502（未审批）', async () => { ... });
    it('payslipId 不存在抛 73501', async () => { ... });
    it('HTML 包含应发合计 + 各项扣款 + 实发金额 + 个税明细', async () => { ... });
    it('PDF 留 mock（fileName 以 .pdf 结尾）', async () => { ... });
  });

  describe('getPayslipHtml / getPayslipPdf', () => {
    it('获取已生成的 HTML 工资条', async () => { ... });
    it('获取已生成的 PDF 工资条（二进制）', async () => { ... });
  });
});
```

### 5.2 工资条发送 JSDoc + 测试（子任务 5.2）

#### 5.2.1 复用 M0.5-2 notification.sendNotification（严禁重写）

```ts
/**
 * 发送工资条（邮件 + 系统通知，V1.2 §二.4.6 + M0.5-2 通知）
 * @param actorId 操作人 ID（hr/admin）
 * @param payslipId payslip ID
 * @param input { methods?: string[] } - ['email', 'system']，默认 configs.salary.payslip.delivery_methods
 * @returns { payslipId, deliveredAt, emailStatus, systemStatus }
 * @throws AppError(404, 73501) payslip 不存在
 * @throws AppError(400, 73508) 邮件/系统通知发送失败
 * 校验链：
 *  1. 查 payslip（含 employee）→ 不存在抛 73501
 *  2. 校验 payslip.status === 'approved'
 *  3. 调 generatePayslip 生成 HTML + PDF
 *  4. 邮件发送（**复用 M0.5-2 notification.sendNotification**）：
 *     - notificationService.sendNotification({
 *         userId: employeeId,
 *         type: 'email',
 *         template: 'payslip',
 *         data: { html, pdf, period, employeeName, netAmount, emailSubject: configs.salary.payslip.email_subject },
 *         bypassTemplate: true
 *       })
 *  5. 系统通知发送（**复用 M0.5-2 notification.sendNotification**）：
 *     - notificationService.sendNotification({
 *         userId: employeeId,
 *         type: 'system',
 *         template: 'payslip_published',
 *         data: { period, netAmount }
 *       })
 *  6. update payslip.paidAt = now()（标记已发送）
 *  7. 写 audit（PAYSLIP_DELIVER，actorType='USER'，details: { payslipId, methods, emailStatus, systemStatus }）
 *  注：**严禁重写 M0.5-2 notification service**，仅调用
 */
export async function deliverPayslip(
  actorId: string,
  payslipId: string,
  input?: { methods?: string[] }
): Promise<DeliverPayslipResult> { ... }
```

#### 5.2.2 测试用例（4-6 个 it，**mock M0.5-2 notification**）

```ts
describe('payslip_delivery.service', () => {
  it('邮件 + 系统通知发送成功（mock notification）', async () => { ... });
  it('仅邮件发送（methods=["email"]）', async () => { ... });
  it('邮件发送失败抛 73508', async () => { ... });
  it('update payslip.paidAt = now()', async () => { ... });
  it('audit 记录 methods + status', async () => { ... });
});
```

### 5.3 银企代发文件 JSDoc + 测试（子任务 5.3）

#### 5.3.1 Mock 模式（**严禁接真实银行 API**）

```ts
/**
 * 导出银企代发文件（V1.2 §二.4.6 + §三.6，**mock 模式**）
 * @param actorId 操作人 ID（hr/admin）
 * @param runId payroll_run ID
 * @param input { format: 'icbc' | 'ccb' | 'cmb' }
 * @returns { runId, format, filePath, recordCount, totalAmount, mockMode: true }
 * @throws AppError(404) run 不存在
 * @throws AppError(400, 73505) format 不在 icbc / ccb / cmb
 * @throws AppError(400, 73504) 生成失败
 * 校验链：
 *  1. 查 run（含 payslips）→ 不存在抛 404
 *  2. 校验 format ∈ configs.salary.banking.formats（默认 ['icbc', 'ccb', 'cmb']）→ 否则抛 73505
 *  3. 校验 mock_mode = true（C5 强制 mock，**不接真实银行 API**）
 *  4. 准备数据：查所有 payslips（status=approved，employee.bankCard 必填）
 *  5. 按 format 生成文件：
 *     - icbc（工行）：CSV 格式，header: 工号|姓名|卡号|金额
 *     - ccb（建行）：TXT 格式，header: 序号|账号|金额
 *     - cmb（招行）：XLS 格式，**留 mock**（实际 XLS 库留二期）
 *  6. 文件路径：./tmp/banking/{runId}-{format}-{timestamp}.{ext}
 *  7. 写 audit（BANKING_EXPORT，actorType='USER'，details: { runId, format, filePath, recordCount, totalAmount, mockMode: true }）
 *  注：**V1.2 §三.6 真实银行 API 集成留二期**，C5 仅 mock 模式
 */
export async function exportBankingFile(
  actorId: string,
  runId: string,
  input: { format: 'icbc' | 'ccb' | 'cmb' }
): Promise<BankingExportResult> { ... }
```

#### 5.3.2 测试用例（4-6 个 it，**mock 模式**）

```ts
describe('banking_export.service', () => {
  it('工行 CSV 格式导出成功（mock）', async () => { ... });
  it('建行 TXT 格式导出成功（mock）', async () => { ... });
  it('招行 XLS 格式导出成功（mock）', async () => { ... });
  it('format 不在白名单抛 73505', async () => { ... });
  it('run 不存在抛 404', async () => { ... });
  it('audit 记录 mockMode: true', async () => { ... });
});
```

### 5.4 个税申报 JSDoc + 测试（子任务 5.4）

#### 5.4.1 Mock 模式（**严禁接真实税务局 API**）

```ts
/**
 * 个税申报（V1.2 §二.4.4，**mock 模式**）
 * @param actorId 操作人 ID（hr/admin/executive）
 * @param runId payroll_run ID
 * @param input { period: 'YYYY-MM' }
 * @returns { runId, period, totalTaxableIncome, totalTax, recordCount, mockMode: true }
 * @throws AppError(404) run 不存在
 * @throws AppError(400, 73507) period 格式错
 * @throws AppError(400, 73506) 申报失败
 * 校验链：
 *  1. 查 run（含 payslips）→ 不存在抛 404
 *  2. 校验 period 格式（YYYY-MM）→ 否则抛 73507
 *  3. 校验 mock_mode = true（C5 强制 mock，**不接真实税务局 API**）
 *  4. 查所有 payslips（status=approved）
 *  5. 累计 totalTaxableIncome = sum(payslip.taxAmount 的 taxableIncome 部分，**C3 算法**）
 *  6. 累计 totalTax = sum(payslip.taxAmount)
 *  7. 生成申报台账：employee_id + period + taxable_income + tax_amount + rate + quick_deduction
 *  8. 写 audit（TAX_DECLARE，actorType='USER'，details: { runId, period, totalTaxableIncome, totalTax, recordCount, mockMode: true }）
 *  注：**V1.2 §三.6 真实税务局 API 集成留二期**，C5 仅 mock 模式
 */
export async function declareTax(
  actorId: string,
  runId: string,
  input: { period: string }
): Promise<TaxDeclarationResult> { ... }
```

#### 5.4.2 测试用例（4-6 个 it，**mock 模式**）

```ts
describe('tax_declaration.service', () => {
  it('个税申报成功（mock）', async () => { ... });
  it('period 格式错抛 73507', async () => { ... });
  it('run 不存在抛 404', async () => { ... });
  it('audit 记录 mockMode: true', async () => { ... });
  it('累计 totalTaxableIncome + totalTax 正确', async () => { ... });
});
```

### 5.5 工资表导出 JSDoc + 测试（子任务 5.5）

#### 5.5.1 Mock 模式（**留 mock**）

```ts
/**
 * 工资表导出（V1.2 §二.4.6 Excel / PDF 汇总表 + 明细表，**mock 模式**）
 * @param actorId 操作人 ID（hr/admin/executive）
 * @param runId payroll_run ID
 * @param input { format: 'excel' | 'pdf' }
 * @returns { runId, format, filePath, totalGross, totalNet, mockMode: true }
 * @throws AppError(404) run 不存在
 * @throws AppError(400, 73510) period 格式错
 * @throws AppError(400, 73509) 导出失败
 * 校验链：
 *  1. 查 run（含 payslips）→ 不存在抛 404
 *  2. 校验 format ∈ configs.salary.report.formats（默认 ['excel', 'pdf']）
 *  3. 准备数据：汇总表（totalGross + totalNet + 异常数）+ 明细表（员工姓名 + 各项金额）
 *  4. 按 format 生成文件：
 *     - excel：XLSX 格式，**留 mock**（实际 Excel 库留二期）
 *     - pdf：PDF 格式，**留 mock**（实际 PDF 库留二期）
 *  5. 文件路径：./tmp/reports/{runId}-{format}-{timestamp}.{ext}
 *  6. 写 audit（REPORT_EXPORT，actorType='USER'，details: { runId, format, filePath, totalGross, totalNet, mockMode: true }）
 */
export async function exportReport(
  actorId: string,
  runId: string,
  input: { format: 'excel' | 'pdf' }
): Promise<ReportExportResult> { ... }
```

#### 5.5.2 测试用例（3-5 个 it，**mock 模式**）

```ts
describe('report_export.service', () => {
  it('Excel 格式导出成功（mock）', async () => { ... });
  it('PDF 格式导出成功（mock）', async () => { ... });
  it('format 不在白名单抛 400', async () => { ... });
  it('audit 记录 mockMode: true', async () => { ... });
});
```

---

## 6. 修改文件清单（明示边界）

### 6.1 必改文件（清单以内）

#### 新增文件（5 个）

1. `server/src/services/payslip_generator.service.ts`
2. `server/src/services/payslip_generator.service.test.ts`
3. `server/src/services/payslip_delivery.service.ts`
4. `server/src/services/payslip_delivery.service.test.ts`
5. `server/src/services/banking_export.service.ts`
6. `server/src/services/banking_export.service.test.ts`
7. `server/src/services/tax_declaration.service.ts`
8. `server/src/services/tax_declaration.service.test.ts`
9. `server/src/services/report_export.service.ts`
10. `server/src/services/report_export.service.test.ts`
11. `server/src/services/salary/C5-README.md`（C5 模块说明）

#### 修改文件（5 个）

12. `server/prisma/seed.ts` — 追加 6 项 salary.payslip / banking / report configs
13. `server/src/routes/salary.ts` — **追加** 4 个 C5 子路由（payslips / banking / tax/tax-declare / reports），**不删 C1-C4 路由**
14. `server/src/constants/permissions.ts` — 追加 4 个 C5 权限点 + 5 角色分配
15. `docs/api-spec.md` — §6.x salary C5 端点
16. `docs/openapi.yaml` — 7 端点 + 5 schema
17. `docs/error-codes.md` — §8.5 C5 错误码（73501-73510，10 个）
18. `docs/HRMS-V1.2.md` — §四.8 C5 状态 + §七.3 追加 C5.0.1
19. `docs/flow-diagrams.md` — §4.x 追加工资条/银企/个税/工资表流程图

**注**：`server/prisma/schema.prisma` 0 改动（C5 0 新表）；`server/prisma/migrations/` 0 新增；`server/src/routes/index.ts` 0 改动（C1 已挂载 /api/salary）。

### 6.2 禁止修改文件（清单以外）

- **D1-D6 + C1 + C2 + C3 + C4 业务代码**（除上述 11 个新增 + 8 个修改）：
  - ❌ 严禁修改 D1 4 service（performance_cycle/indicator/scheme/coefficient）
  - ❌ 严禁修改 D2 3 service（performance_record/score/ai_suggestion）
  - ❌ 严禁修改 D3 2 service（performance_grade/calibration_ratio）
  - ❌ 严禁修改 D4 2 service（performance_payout_config/payout）
  - ❌ 严禁修改 D5 3 service（performance_sales_product/payment/commission）
  - ❌ 严禁修改 C1 3 service（salary_grade / salary_grade_level / salary_plan）
  - ❌ 严禁修改 C2 3 service（social_insurance_scheme / housing_fund_scheme / employee_insurance）
  - ❌ 严禁修改 C3 3 service（tax_calculation / tax_year_end_bonus / tax_labor_income）
  - ❌ 严禁修改 C4 4 service（payroll_calculation / payroll_run / payslip / payroll_ai_summary）
  - ❌ 严禁修改 D1+D2+D4+D5+C1+C2+C4 共 9 张表
  - ❌ 严禁修改 D1-D6 38 + C1-C4 16 = 54 权限点
  - ❌ 严禁修改 D1-D6 55 + C1-C4 34 = 89 端点
  - ❌ 严禁修改 D1-D6 59 + C1-C4 27 = 86 configs
  - ❌ 严禁修改 `server/src/routes/salary.ts` 中 C1-C4 子路由
- **M0.5 公共底座**：
  - ❌ 严禁修改 M0.5-1 approval.service.ts（仅调用）
  - ❌ 严禁修改 M0.5-2 notification.service.ts（**仅调用，**严禁重写** 通知 service**）
  - ❌ 严禁修改 M0.5-5 ai/score.service.ts（仅调用）
  - ❌ 严禁修改 M0.5-6 config.service.ts（**仅调用，**严禁重写** config service**）
- **M1 + M2 业务代码**：
  - ❌ 严禁修改 M1 / M2 任何 service（C5 仅 prisma 读表）
  - ❌ 严禁修改 D1 employees 表
- 任何旧测试文件（`*.test.ts`）除非在 §6.1 清单内（仅 5 个新测试）

### 6.3 越界检测（交付时自检）

```bash
git diff --name-only HEAD
# 必须只出现 §6.1 列出的 19 个文件 + 11 个新增
# 如果出现其他文件 → 越界，必须回退

# 特别检查：D1-D6 + C1 + C2 + C3 + C4 共 27 service 0 行改动
git diff HEAD -- 'server/src/services/performance_*.service.ts' 'server/src/services/salary_*.service.ts' 'server/src/services/social_insurance_scheme.service.ts' 'server/src/services/housing_fund_scheme.service.ts' 'server/src/services/employee_insurance.service.ts' 'server/src/services/tax_*.service.ts' 'server/src/services/payroll_*.service.ts' 'server/src/services/payslip.service.ts'
# 必须为 0 行

# 验证 0 新表
ls server/prisma/migrations/ | grep -E "add_salary_c5"
# 必须为 0 结果（C5 0 migration）

# 验证 mock 模式
grep -rE "mockMode: true|mock_mode.*=.*true" server/src/services/banking_export.service.ts server/src/services/tax_declaration.service.ts server/src/services/report_export.service.ts
# 必须 ≥ 3 条结果

# 验证复用 notification
grep -E "notificationService\.sendNotification" server/src/services/payslip_delivery.service.ts
# 必须 ≥ 1 条结果
```

---

## 7. 验收标准

### 7.1 硬性验收（必须 100% 通过）

```bash
# 在项目根 D:\projects\hrms
pnpm --filter hrms-server type-check    # 必须 0 错
pnpm --filter hrms-server lint          # 必须 0 error
pnpm --filter hrms-server test          # 必须 745+ 个测试通过（725 旧 + 20-28 新）
pnpm --filter hrms-client type-check    # 必须 0 错
```

### 7.2 验收明细

- [ ] **0 新表**（migration 无新增；C5 仅 5 service + 1 README 新文件）
- [ ] 7 个端点全部可访问
- [ ] 10 个错误码（73501-73510）全部可触发
- [ ] 工资条生成正确（HTML / PDF mock 二进制）
- [ ] 工资条发送正确（**复用 M0.5-2 notification**）
- [ ] 银企代发文件正确（**3 种银行格式 + mock 模式**）
- [ ] 个税申报正确（**mock 模式**）
- [ ] 工资表导出正确（Excel / PDF mock 模式）
- [ ] 92 项 configs 全部走 configService
- [ ] 5 角色 RBAC 权限分配正确（**无 finance**）
- [ ] audit log 写入正确（5 类 action）
- [ ] 0 旧测试改动
- [ ] 0 越界
- [ ] **D1-D6 + C1 + C2 + C3 + C4 共 27 service 0 行改动**

### 7.3 红线 8 验证

- [ ] **不接真实银行 API**（**留 mock 模式**，V1.2 §三.6 第三方对接留二期）
- [ ] **不接真实税务局 API**（**留 mock 模式**，个税申报 API 留二期）
- [ ] **不实现 UI**（前端工资条展示留任务）
- [ ] **0 新表**（C4 已建 payslips + payslip_items + payroll_runs 够用）
- [ ] **D1-D6 + C1 + C2 + C3 + C4 业务代码 0 行改动**（27 service + 17 表 = 0 行）
- [ ] **复用 M0.5-2 notification**（仅调用，**严禁重写** 通知 service）
- [ ] **复用 M0.5-6 configService**（仅调用，**严禁重写** config service）
- [ ] **5 角色 RBAC 不含 finance**（D1-D6 + C1+C2+C3+C4 教训延续）

### 7.4 原有用例未改（硬要求）

```bash
git diff HEAD -- '*.test.ts' | grep -E '^[+-]' | grep -v '^[+-]{3}' | grep -v 'payslip_generator|payslip_delivery|banking_export|tax_declaration|report_export'
# 必须为 0 行（原有用例 100% 未改）
```

---

## 8. 禁止做的事（额外禁忌清单）

1. ❌ 接真实银行 API（**留 mock 模式**，V1.2 §三.6 第三方对接留二期）
2. ❌ 接真实税务局 API（**留 mock 模式**，个税申报 API 留二期）
3. ❌ 实现 UI（前端工资条展示留任务）
4. ❌ 创建任何新表（C5 0 新表，C4 已建够用）
5. ❌ 创建 migration 文件（C5 0 migration）
6. ❌ 改 C1 任何 service（不重做 C1）
7. ❌ 改 C2 任何 service（不重做 C2）
8. ❌ 改 C3 任何 service（不重做 C3）
9. ❌ 改 C4 任何 service（不重做 C4）
10. ❌ 改 D1-D6 任何 service（不重做 M3）
11. ❌ 改 M0.5-1 approval.service.ts（仅调用）
12. ❌ 改 M0.5-2 notification.service.ts（**仅调用，**严禁重写** 通知 service**）
13. ❌ 改 M0.5-5 ai/score.service.ts（仅调用）
14. ❌ 改 M0.5-6 config.service.ts（**仅调用，**严禁重写** config service**）
15. ❌ 改 M1 任何 service（不联动组织人事）
16. ❌ 改 M2 任何 service（C5 仅 prisma 读 M2 表）
17. ❌ 实现真实 PDF 库（**留 mock**，实际库留二期）
18. ❌ 实现真实 Excel 库（**留 mock**，实际库留二期）
19. ❌ 写 employee_salary_history（C8 调薪实际执行范围）
20. ❌ 修改任何 M0/M0.5/M1/M2/M3/C1/C2/C3/C4 已存在的 .ts 文件（除 §6.1 清单内）
21. ❌ 修改任何旧测试文件（即使 type-check 报错也只新增 import）
22. ❌ 越界做 6 角色 RBAC（M0 锁定 5 角色：admin/hr/dept_head/executive/employee，**无 finance**）
23. ❌ 用 class + 静态方法范式（统一 `export function xxx() {}`）
24. ❌ 硬编码业务规则（6 项 C5 configs 全走 configService + fallback + TODO）
25. ❌ 写 magic number 错误码（必须用 73501-73510 错误码表）
26. ❌ "我觉得这也有用"（任何"顺便做"越界内容一律禁止）
27. ❌ 重写 notification / configService / aiSummary（**M0.5 已有，**严禁重写**）
28. ❌ 给 finance 角色分配权限（D1-D6 + C1+C2+C3+C4 教训延续）
29. ❌ mock_mode = false（C5 强制 true，**不接真实 API**）

---

## 9. 完成标准

### 9.1 必跑命令（全部通过才交付）

```bash
cd D:\projects\hrms

# 1. 类型检查
pnpm --filter hrms-server type-check

# 2. Lint
pnpm --filter hrms-server lint

# 3. 测试（必须 725 旧 + 20-28 新 = 745-753 通过）
pnpm --filter hrms-server test

# 4. 客户端 type-check
pnpm --filter hrms-client type-check

# 5. 验证红线
git diff HEAD --name-only                                                # 仅清单内 19 个文件
git diff HEAD --name-only -- '*.test.ts' | grep -v 'payslip_generator|payslip_delivery|banking_export|tax_declaration|report_export'  # 必须为 0
git diff HEAD -- 'server/src/services/performance_*.service.ts' 'server/src/services/salary_*.service.ts' 'server/src/services/social_insurance_scheme.service.ts' 'server/src/services/housing_fund_scheme.service.ts' 'server/src/services/employee_insurance.service.ts' 'server/src/services/tax_*.service.ts' 'server/src/services/payroll_*.service.ts' 'server/src/services/payslip.service.ts'  # D1-D6 + C1 + C2 + C3 + C4 27 service 0 行改动
ls server/prisma/migrations/ | grep -E "add_salary_c5"
# 必须为 0 结果（C5 0 migration）

# 6. 验证 mock 模式
grep -rE "mockMode: true" server/src/services/banking_export.service.ts server/src/services/tax_declaration.service.ts server/src/services/report_export.service.ts | wc -l
# 必须 ≥ 3 条

# 7. 验证复用 notification
grep -E "notificationService\.sendNotification" server/src/services/payslip_delivery.service.ts | wc -l
# 必须 ≥ 1 条

# 8. 验证 5 角色 RBAC（无 finance）
grep -nE "finance" server/src/constants/permissions.ts
# 必须为 0 结果

# 9. 验证 0 越界
git diff --name-only HEAD | wc -l
# 必须 = 19（修改）+ 11（新增）= 30 个文件
```

### 9.2 交付物

1. **业务 commit**（Cursor 完成后由 WorkBuddy 提交）：
   ```
   feat(salary): M4-C5 工资条 + 银企代发 + 个税申报 + 工资表导出（19 文件 / 7 端点 / 0 新表 / 4 权限点 / 10 错误码 / 20-28 测试 / 745+ 通过）
   ```

2. **交付报告**（Cursor 输出，WorkBuddy 验收）：
   - 新增文件清单（必须 11 个，0 越界）
   - 修改文件清单（必须 8 个，0 越界）
   - 测试结果（必须 745+ 通过，新增 20-28 个）
   - 验收命令输出（type-check / lint / test 全通过）
   - 越界检查（`git diff --name-only` 仅清单内）
   - **D1-D6 + C1 + C2 + C3 + C4 共 27 service 0 行改动验证**
   - **0 新表验证**（migration 目录无新增）
   - **mock 模式验证**（grep `mockMode: true` ≥ 3 条）
   - **复用 M0.5-2 notification 验证**（grep ≥ 1 条）
   - 红线 8 自检清单（8 项全 ✅）
   - 已知问题（最多 1-2 个，不影响功能）

---

## 10. 关键决策点回顾

### 10.1 关键决策点 1：C5 0 新表（C4 已建够用）

V1.2 §二.4.6 工资条与发放的所有内容**完全复用** C4 已建的表：
- 工资条内容（应发 + 应扣 + 实发 + 个税明细）→ 查 C4 `payslips` + `payslip_items`
- 银行代发文件 → 查 C4 `payroll_runs` + `payslips`
- 个税申报 → 查 C4 `payroll_runs` + `payslips`（taxAmount 字段）
- 工资表导出 → 查 C4 `payroll_runs` + `payslips`
- **C5 0 新表**（C4 已建 3 张 payroll 表 + C1 3 张 salary 表 + C2 3 张 insurance 表 = 9 张表）

### 10.2 关键决策点 2：mock 模式（**不接真实银行 / 税务局 API**）

V1.2 §三.6 第三方对接（招行 / 建行 / 工行）**留二期实际集成**：
- C5 仅生成 mock 文件（CSV / TXT / XLS）
- `salary.banking.mock_mode = true`（C5 强制 true）
- 文件路径：`./tmp/banking/{runId}-{format}-{timestamp}.{ext}`
- 写 audit（`mockMode: true`）
- 个税申报同样 mock（不接真实税务局 API）
- 工资表导出同样 mock（不接真实 Excel / PDF 库）

### 10.3 关键决策点 3：复用 M0.5-2 notification（**严禁重写**）

V1.2 §二.4.6 "走 M0.5 通知基础设施"：
- C5 工资条发送 = 调 `notificationService.sendNotification({ type: 'email', template: 'payslip', data: {...}, bypassTemplate: true })`
- 严禁重写 M0.5-2 notification service
- 沿用 D1 教训（D1 反馈 bypassTemplate fallback）
- D2 教训：调 M0.5-5 aiScoreService.suggestScore 不重写
- **C5 教训：调 M0.5-2 notification.sendNotification 不重写**

### 10.4 关键决策点 4：D1-D6 + C1 + C2 + C3 + C4 业务代码 0 行改动（红线升级到 27 service）

C5 沿用 C1-C4 + D1-D6 教训 + 红线升级到 **D1+D2+D3+D4+D5+D6 + C1 + C2 + C3 + C4 共 27 个 service 0 行改动**：
- D1 4 service（cycle/indicator/scheme/coefficient）
- D2 3 service（record/score/ai_suggestion）
- D3 2 service（grade/calibration_ratio）
- D4 2 service（payout_config/payout）
- D5 3 service（sales_product/payment/commission）
- C1 3 service（salary_grade / salary_grade_level / salary_plan）
- C2 3 service（social_insurance_scheme / housing_fund_scheme / employee_insurance）
- C3 3 service（tax_calculation / tax_year_end_bonus / tax_labor_income）
- C4 4 service（payroll_calculation / payroll_run / payslip / payroll_ai_summary）

### 10.5 关键决策点 5：5 角色 RBAC（**无 finance**）

- C1-C4 + D1-D6 教训：5 角色 = admin / hr / dept_head / executive / employee
- C5 沿用：**5 角色 = admin / hr / dept_head / executive / employee**
- C5 银企代发 + 个税申报由 **hr 兼任**（**真正的 finance 角色留二期**）
- finance 角色留 C4 联调或二期（V1.2 §三.2 角色扩展）

### 10.6 关键决策点 6：复用既有 service（零重写）

C5 复用：audit / config / prisma / **M0.5-2 notification** / C4 payslips / C1 employee_salary_plans / D1 employees，**不 import 跨业务 service**：

```ts
import { auditService } from './audit.service';              // M0.5-8
import { configService } from './config.service';            // M0.5-6
import { notificationService } from './notification.service'; // M0.5-2（**复用，**严禁重写**）
import { prisma } from '../db';                              // server
// ❌ 不 import 任何业务 service（employee / department / cycle / scheme / coefficient / record / score / grade / payout / sales / payroll / offboarding 等）
```

### 10.7 关键决策点 7：C5 路由追加在 salary.ts（不分新文件）

- C1 已创建独立 `server/src/routes/salary.ts`
- C2+C3+C4 路由**追加**在 salary.ts 内
- C5 路由**继续追加**在 salary.ts 内，4 个 C5 子路由：
  ```ts
  // 已有 C1 + C2 + C3 + C4
  salaryRouter.use('/grades', ...);
  salaryRouter.use('/grade-levels', ...);
  salaryRouter.use('/plans', ...);
  salaryRouter.use('/insurances/social', ...);
  salaryRouter.use('/insurances/housing-fund', ...);
  salaryRouter.use('/insurances/employees', ...);
  salaryRouter.use('/tax', ...);            // C3 已有
  salaryRouter.use('/payrolls', ...);       // C4 已有
  
  // C5 新增（4 个子路由）
  salaryRouter.use('/payslips', payslipRouter);       // 5.1 + 5.2
  salaryRouter.use('/banking', bankingRouter);         // 5.3
  // C5 个税申报端点放在 C3 /tax 子路由下，避免与 C3 路由冲突
  // salaryRouter.use('/tax', taxDeclarationRouter);  // 5.4
  salaryRouter.use('/reports', reportRouter);         // 5.5
  ```

### 10.8 关键决策点 8：C5 工资条内容（V1.2 §二.4.6）

```
应发合计 = base + performance + overtime + allowance + salesCommission + yearEndBonus
各项扣款 = social + housing + tax + absence
实发金额 = 应发 - 应扣
个税明细 = taxAmount
```

工资条走 configs.salary.payslip.template（**留 mock**，实际模板留二期）。

### 10.9 关键决策点 9：3 种银行格式（V1.2 §二.4.6 + §三.6）

- 工行（icbc）：CSV 格式
- 建行（ccb）：TXT 格式
- 招行（cmb）：XLS 格式（**留 mock**）
- 全部走 configs.salary.banking.formats

### 10.10 关键决策点 10：2 种工资表格式（V1.2 §二.4.6）

- Excel：XLSX（**留 mock**）
- PDF：PDF（**留 mock**）
- 全部走 configs.salary.report.formats

---

## 11. 报告模板（Cursor 输出，WorkBuddy 验收）

```markdown
# M4-C5 交付报告

## 改动文件清单（11 新增 + 8 修改 = 19 个）

**新增（11）**：
1. server/src/services/payslip_generator.service.ts
2. server/src/services/payslip_generator.service.test.ts
3. server/src/services/payslip_delivery.service.ts
4. server/src/services/payslip_delivery.service.test.ts
5. server/src/services/banking_export.service.ts
6. server/src/services/banking_export.service.test.ts
7. server/src/services/tax_declaration.service.ts
8. server/src/services/tax_declaration.service.test.ts
9. server/src/services/report_export.service.ts
10. server/src/services/report_export.service.test.ts
11. server/src/services/salary/C5-README.md

**修改（8）**：
12. server/prisma/seed.ts
13. server/src/routes/salary.ts（**追加** 4 个 C5 子路由，**不删 C1-C4 路由**）
14. server/src/constants/permissions.ts
15. docs/api-spec.md
16. docs/openapi.yaml
17. docs/error-codes.md
18. docs/HRMS-V1.2.md
19. docs/flow-diagrams.md

**未改（按约束）**：
- server/prisma/schema.prisma（C5 0 新表）
- server/prisma/migrations/（C5 0 migration）
- server/src/routes/index.ts（C1 已挂载 /api/salary）
- D1-D6 + C1 + C2 + C3 + C4 共 27 service（diff = 0）
- D1+D2+D4+D5+C1+C2+C4 共 9 表
- D1-D6 55 端点 + C1 7 + C2 9 + C3 6 + C4 12 = 89 端点
- D1-D6 38 权限点 + C1-C4 16 = 54 权限点
- D1-D6 59 + C1-C4 27 = 86 configs
- M0.5-1 approval.service.ts（仅调用）
- M0.5-2 notification.service.ts（**仅调用，**严禁重写**）
- M0.5-5 ai/score.service.ts（仅调用）
- M0.5-6 config.service.ts（**仅调用，**严禁重写**）
- M1 / M2 任何 service
- employee_salary_history（C5 **不写**，留 C8）

## 7 条强制声明

1. 未修改任何 M0/M0.5/M1/M2/M3/C1/C2/C3/C4 旧测试（仍 725 个）
2. 新单测 X 个，总计 725+X 通过
3. 未做 C1/C2/C3/C4/C6-C8 任何切片
4. 未 import 跨业务 service（A1-A7/B1-B6/C1-C4/C6-C8/D1-D6/M1/M2）
5. 5 角色 RBAC 结构不变（admin/hr/dept_head/executive/employee，**无 finance**）
6. **0 新表**（C4 已建够用）
7. **D1-D6 + C1 + C2 + C3 + C4 业务代码 0 行改动**（27 service diff = 0）

## 红线 8 自检

- [x] 不接真实银行 API（**留 mock 模式**）
- [x] 不接真实税务局 API（**留 mock 模式**）
- [x] 不实现 UI（前端工资条展示留任务）
- [x] 0 新表（C4 已建 payslips + payslip_items + payroll_runs 够用）
- [x] D1-D6 + C1 + C2 + C3 + C4 业务代码 0 行改动（27 service diff = 0）
- [x] 复用 M0.5-2 notification（**仅调用，**严禁重写**）
- [x] 复用 M0.5-6 configService（**仅调用，**严禁重写**）
- [x] 5 角色 RBAC 不含 finance

## D1-D6 + C1 + C2 + C3 + C4 文件 0 行改动验证

```
$ git diff HEAD -- 'server/src/services/performance_*.service.ts' 'server/src/services/salary_*.service.ts' 'server/src/services/social_insurance_scheme.service.ts' 'server/src/services/housing_fund_scheme.service.ts' 'server/src/services/employee_insurance.service.ts' 'server/src/services/tax_*.service.ts' 'server/src/services/payroll_*.service.ts' 'server/src/services/payslip.service.ts'
$ echo $?
0
```

## 0 新表验证

```
$ ls server/prisma/migrations/ | grep -E "add_salary_c5"
$ echo $?
1
```

## mock 模式验证

```
$ grep -rE "mockMode: true" server/src/services/banking_export.service.ts server/src/services/tax_declaration.service.ts server/src/services/report_export.service.ts
（≥ 3 条结果）
```

## 复用 M0.5-2 notification 验证

```
$ grep -E "notificationService\.sendNotification" server/src/services/payslip_delivery.service.ts
（≥ 1 条结果）
```

## 验收命令输出

| 命令 | 结果 |
|---|---|
| pnpm --filter hrms-server type-check | 0 错 |
| pnpm --filter hrms-server lint | 0 error（X warnings） |
| pnpm --filter hrms-server test | 725+X/725+X 通过 |
| pnpm --filter hrms-client type-check | 0 错 |

## 工资条 + 银企 + 个税 + 工资表覆盖

- [x] 工资条生成（HTML / PDF mock）
- [x] 工资条发送（邮件 + 系统通知，**复用 M0.5-2**）
- [x] 银企代发文件（3 种银行格式 + **mock 模式**）
- [x] 个税申报（**mock 模式**）
- [x] 工资表导出（Excel / PDF mock）

## 越界检查

```
$ git diff --name-only HEAD
（清单内 19 个文件 + 0 越界）
```

## 已知问题

（如有，列出 1-2 个不影响功能的问题；如无，写"无"）
```

---

**提示词版本**：v1.0（2026-08-27）
**预计 Cursor 工时**：4 天
**预计单测增量**：20-28 个
**预计 commit**：1 业务 + 1 提示词
**下一切片**：M4-C6（销售提成核算：D5 联动 + 季度结算，约 3d）
