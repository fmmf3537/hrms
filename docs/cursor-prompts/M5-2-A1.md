# M5-2-A1 M1 组织人事前端 - 基础数据（A1 法人公司 + A2 部门 + A3 员工档案）Cursor 提示词

> **任务 ID**：M5-2-A1
> **任务名称**：M5-2 第 1 个业务子切片 — M1 组织人事前端 · 基础数据（A1 法人公司 + A2 部门 + A3 员工档案）
> **任务范围**：4 子任务 / 1.5d 工时 / V1.2 §四.5 A1+A2+A3
> **提示词生成日期**：2026-08-31
> **目标单测增量**：约 12-15 个（API 函数 + 通用组件 + 页面内联断言）
> **目标 commit**：1 个业务 commit + 1 个提示词 commit
> **依赖**：[M5-2-0 共享基础设施](M5-2-0.md) 已完成（commit `631992a`）

---

## ⚠️ 强约束（最优先阅读）

**历史教训**（18 次）：
1. M0.5-5 任务中，Cursor 越界做了 M0.5-6（配置中心）+ M0.5-7（改密 + 2FA）共 2 个不在提示词范围内的切片。本次任务严禁重蹈覆辙。
2. M1-A6 提示词第一次瘦身到 15KB，Cursor 推断/补全消耗大量 token，返工 1 次。本次任务按 60-90KB 详尽模式写。
3. D1 提示词 §3.3 误列 5 角色包含 finance，**Cursor 主动识别 + 正确处理**。D2-D6 + C1-C8 + M5-1 + M5-2-0 已修正。**M5-2-A1 沿用 5 角色 RBAC 无 finance**（admin / hr / dept_head / executive / employee，**无 finance 角色**）。
4. M2-wrap-up.md §7 误写 D3 = "季度校准会议"。M5-2-A1 不重蹈覆辙，严格按 V1.2 §四.5 A1-A3 实际范围。
5. D3 → ... → C8 红线升级到 36 service 0 行改动。**M5-2-A1 沿用 36 service 0 行改动**（**M5-2-A1 0 修改后端任何 service**）。
6. D4 验收发现 2 个 B3/B5 跨 UTC 边界日期敏感旧测。M5-2-A1 沿用此模式。
7. C5 报告"已知问题 2"：V1.2 切片表 C5=劳务费/C6=工资条与本任务 ID「M4-C5」不一致。**M5-2-A1 不涉及 V1.2 切片表错位**（前端子切片，无 V1.2 切片表对应）。
8. **M5-2-0 已落地 + M5-2-A1 主动识别 4 个关键点**（M5-2-0 commit `631992a` 报告）：
   - M0-06 已有 `stores/auth.ts` / `api/request.ts` / `views/login/index.vue` 等（**已删除**，避免双轨）
   - 后端 `UserInfo` 是 `roles[]` + `permissions[]`（**不是单数字段 `role`**）
   - 子路由 **不合并** `meta.requiresAuth`，守卫用 `to.matched`
   - 登录 401 **不能**走 refresh（后端 rotation + reuse 检测）
   - client **没有** vitest / `@vue/test-utils`，且**禁止改 `client/package.json`**（红线）
   - **M5-2-A1 必须沿用这些约束**
9. **M5-2-A1 关键设计**：
   - **0 修改后端任何文件**（**M5-2-A1 是纯前端**）
   - **0 修改 `client/package.json`**（**禁止装新依赖**，继续用 Element Plus + Pinia + Vue Router + Axios）
   - **0 新增 M0.5-1/2/5/6 service / 0 新增 D1-D6 service / 0 新增 C1-C8 service**（沿用 36 service 0 改动）
   - **沿用 M5-2-0 共享基础设施**（router / api / stores / layouts / components / utils + Login + Dashboard）
   - **复用 Element Plus 通用组件**（ElTable / ElForm / ElDialog / ElTree / ElCard / ElTag）
   - **5 角色 RBAC 严格遵守**（admin / hr / dept_head / executive / employee，**无 finance**）
10. **M5-2-A1 客户端现状摸底**（实施前必读）：
    - **M5-2-0 已建基础设施**（commit `631992a`，30 文件 = 20 新增 + 6 修改 + 4 删除）：
      - `client/src/router/index.ts`（主路由，仅有 /login + /dashboard）
      - `client/src/api/{http,auth,types}.ts`（http + auth + 通用 types）
      - `client/src/stores/{user,permission,dict}.ts`（3 stores）
      - `client/src/layouts/DefaultLayout.vue`（主布局）
      - `client/src/components/{PageHeader,DataTable,FormDialog,EmptyState,SearchForm}.vue`（5 通用组件）
      - `client/src/utils/{auth,permission,format}.ts`（3 utils）
      - `client/src/views/{Login,Dashboard}.vue`（2 页面）
    - **后端 M1 路由**（25 端点，**M5-2-A1 必须消费**）：
      - `/api/companies`（A1 法人公司，7 端点）
      - `/api/departments`（A2 部门管理，8 端点）
      - `/api/employees`（A3 员工档案，10 端点）
    - **M0-06 旧路径已删除**（`stores/auth.ts` / `api/request.ts` / `views/login/index.vue` / `views/dashboard/index.vue`）
11. **M5-2-A1 业务量**（与 M3/M4 业务切片类似 + M5-2-0 共享基础设施复用）：
    - 0 业务逻辑（**纯前端**，消费后端 25 端点）
    - **29 新文件**（7 API + 13 页面 + 2 路由/布局 + 2 组件 + 5 测试）
    - ~3500-4500 行 Vue3 + TypeScript 代码
    - 12-15 单元测试（API 函数 + 通用组件 + 页面内联断言，**沿用 M5-2-0 内联断言模式**）
    - **0 修改后端任何文件**
12. **M5-2-A1 与 M5-2-0 关系**：
    - **M5-2-0 是基础设施**（router / api / stores / layouts / components / utils + Login + Dashboard）
    - **M5-2-A1 是首个业务模块**（M1 组织人事 · 基础数据）
    - **M5-2-A1 必须消费 M5-2-0 提供的 API**（`http` 实例 + 错误 toast + 自动 refresh）
    - **M5-2-A1 必须消费 M5-2-0 提供的 5 通用组件**（`PageHeader` / `DataTable` / `FormDialog` / `EmptyState` / `SearchForm`）
    - **M5-2-A1 必须消费 M5-2-0 提供的 Pinia stores**（`useUserStore` / `usePermissionStore` / `useDictStore`）

**红线**（违反任何一条 → 任务失败，需回退重做）：

1. **禁止越界做其他切片**：本提示词只授权 "M5-2-A1 切片（M1 组织人事前端 · A1+A2+A3 基础数据）"。严禁自作主张做 M5-2-A2（M3 入职+转正+调动+离职+合同）/ M5-2-B / M5-2-C / M5-2-D / M5-3-M5-8 任何切片。
2. **禁止修改后端文件**：本切片是**纯前端**，严禁修改 `server/` 任何文件。
3. **禁止修改 `client/package.json`**：本切片不装新依赖（**沿用 Element Plus + Pinia + Vue Router + Axios**）。
4. **禁止修改 `client/vite.config.ts` / `tsconfig.json` / `.eslintrc.cjs`**：基础设施配置不动。
5. **禁止修改 M5-2-0 共享基础设施文件**（除**追加**路由表）：
   - ❌ 严禁修改 `client/src/router/index.ts`（仅**追加**组织人事子路由）
   - ❌ 严禁修改 `client/src/api/{http,auth,types}.ts`（**M5-2-A1 创建独立的 `api/types/organization.ts` + `api/{company,department,employee}.ts`**）
   - ❌ 严禁修改 `client/src/stores/{user,permission,dict}.ts`
   - ❌ 严禁修改 `client/src/layouts/DefaultLayout.vue`（**M5-2-A1 创建独立的 `layouts/OrganizationLayout.vue`**）
   - ❌ 严禁修改 `client/src/views/{Login,Dashboard}.vue`
   - ❌ 严禁修改 `client/src/utils/{auth,permission,format}.ts`
6. **必须报告完整**：交付报告必须列出全部新增/修改文件（即使是 0 个也要说"0 个"），并明确说明"原有用例是否改动"。
7. **禁止"我觉得这也有用"**：任何"顺便做"或"我觉得可以优化"的越界内容一律禁止。包括但不限于：顺手做 M5-2-A2 业务页面 / 顺手做 M5-2-B/C/D / 顺手装新依赖 / 顺手加 finance 角色 / 顺手做 M1 后端功能 / 顺手修改 M5-2-0 基础设施 / 顺手做 V1.2 切片表 C5 劳务费。
8. **M5-2-A1 范围强约束**（红线 9）：
   - **0 修改后端任何文件**（**仅 client/src**）
   - **0 修改 `client/package.json`**（沿用 Element Plus + Pinia + Vue Router + Axios）
   - **0 修改 M5-2-0 共享基础设施文件**（除**追加**路由表）
   - **29 新文件**（7 API + 13 页面 + 2 路由/布局 + 2 组件 + 5 测试 = 29，**不超出清单**）
   - **D1-D6 + C1-C8 业务代码 0 行改动**（36 service + 40 表 = 0 行）
   - **M0.5 4 service 0 行改动**
   - **复用 M5-2-0 通用组件**（`PageHeader` / `DataTable` / `FormDialog` / `EmptyState` / `SearchForm` + Pinia stores + http + utils）
   - **5 角色 RBAC 严格遵守**（admin / hr / dept_head / executive / employee，**无 finance 角色**）
   - **12-15 单元测试**（内联断言 + 导出 `run*Tests()`，**不修改任何旧测试**）

---

## 1. 任务 ID + 目标

### 1.1 任务 ID

**M5-2-A1**（M5-2 第 1 个业务子切片 — M1 组织人事前端 · **A1 法人公司 + A2 部门 + A3 员工档案** 基础数据）

### 1.2 任务目标

实现 M5 联调 Vue3 前端的**第一个业务模块**（M1 组织人事 · 基础数据），包含 4 子任务：

| 子任务 | 范围 | 新增/修改 | 关键文件 |
|---|---|---|---|
| 7.2.A1.1 路由 + 侧栏 + 模块布局 | 组织人事子路由 + 侧栏子菜单 + OrganizationLayout 布局 | 1 新 `client/src/router/organization.ts` + 1 新 `client/src/layouts/OrganizationLayout.vue` + 1 修改 `client/src/router/index.ts` | 子路由表 + DefaultLayout 侧栏子菜单 |
| 7.2.A1.2 公司 API + 类型 + 4 页面 | `/api/companies` 7 端点 + 4 页面 | 1 新 `client/src/api/types/organization.ts`（含 Company/Department/Employee 三类）+ 1 新 `client/src/api/company.ts` + 4 新页面（CompanyList/Detail/Form/Tree）| Element Plus ElTable / ElForm / ElTree |
| 7.2.A1.3 部门 API + 类型 + 4 页面 | `/api/departments` 8 端点 + 4 页面 | 1 新 `client/src/api/department.ts` + 4 新页面（DepartmentTree/Detail/Form/Head）| Element Plus ElTree（部门树）|
| 7.2.A1.4 员工 API + 类型 + 5 页面 | `/api/employees` 10 端点 + 5 页面 | 1 新 `client/src/api/employee.ts` + 5 新页面（EmployeeList/Detail/Form/OrgChart/Import）| Element Plus ElUpload（导入） + ElCascader（部门级联）|

**V1.2 §四.5 A1+A2+A3 验收**：
- A1 法人公司：列表 / 详情 / 创建 / 更新 / 删除 / 暂停 / 恢复
- A2 部门管理：树形结构 / 详情 / 创建 / 更新 / 删除 / 移动 / 合并 / 部门员工
- A3 员工档案：列表 / 详情 / 创建 / 更新 / 删除 / 调动 / 转正 / 离职 / 导出 / 导入

**M5-2-A1 业务量**：
- 0 后端文件改动（**仅消费 25 端点**）
- 0 `client/package.json` 改动
- 0 M5-2-0 共享基础设施文件改动（除**追加**路由表）
- 29 新 client 文件（7 API + 13 页面 + 2 路由/布局 + 2 组件 + 5 测试）
- ~3500-4500 行 Vue3 + TypeScript 代码
- 12-15 单元测试

### 1.3 端点清单（**0 新增后端端点**，消费 M1 已有 25 端点）

**复用后端 M1 路由**（`server/src/routes/index.ts` 已挂载）：

| 模块 | 路由前缀 | 端点数 | 消费端点 |
|---|---|---|---|
| A1 法人公司 | `/api/companies` | 7 | list / get / create / update / delete / suspend / reactivate |
| A2 部门管理 | `/api/departments` | 8 | list / get / create / update / delete / move / merge / employees |
| A3 员工档案 | `/api/employees` | 10 | list / get / create / update / delete / transfer / regularize / resign / export / import |
| **M5-2-A1 合计** | — | **25** | — |

**0 新增后端端点**（M5-2-A1 是纯前端，**仅消费**后端 M1 已有 25 端点）

### 1.4 单测目标

12-15 个新单测，按 4 子任务分布：

| describe | it 数 | 目标 |
|---|---|---|
| `api/company.ts` | 2-3 | listCompanies / getCompany / createCompany 类型 + 错误码消费 |
| `api/department.ts` | 2-3 | listDepartments（树形）/ getDepartment / createDepartment 类型 |
| `api/employee.ts` | 3-4 | listEmployees / getEmployee / createEmployee / importEmployees 类型 |
| `components/StatusTag.vue` | 1-2 | 5 角色状态映射（active / suspended / probation / resigned）|
| `views/organization` | 4-5 | CompanyList 列表渲染 / DepartmentTree 树形 / EmployeeList 列表 / 5 角色权限 |

**沿用 M5-2-0 内联断言模式**（内联 `describe/it/expectEqual` + 导出 `run*Tests()`），**不引入 vitest / @vue/test-utils**（红线禁止改 `package.json`）。

---

## 2. 上下文

### 2.1 项目位置

- **项目根**：`D:\projects\hrms`
- **主文档**：[`docs/HRMS-V1.2.md`](../../HRMS-V1.2.md) §四.5 A1+A2+A3
- **API 契约**：[`docs/api-spec.md`](../../api-spec.md) §3.x organization（25 端点）
- **AI Coding 上下文**：[`AGENTS.md`](../../../AGENTS.md)
- **M5-2-0 共享基础设施**：[`docs/cursor-prompts/M5-2-0.md`](M5-2-0.md) commit `631992a`

### 2.2 当前状态（M0+M0.5+M1+M2+M3+M4+M5-1+M5-2-0 已全部完成）

- **M5-2-0 commit**：`631992a feat(client): M5-2-0 Vue3 共享基础设施`
- **后端测试**：876/876 通过
- **后端 119 端点**已就绪（M0 5 + M0.5 5 + M1 25 + M2 25 + M3 55 + M4 63 + M5-1 1 = 179 实际端点，含 health 共 180 实际端点）
- **client/src 已就位**（M5-2-0 建基础设施）：
  - `api/{http,auth,types}.ts`（http + auth + 通用 types）
  - `router/index.ts`（主路由，仅 /login + /dashboard）
  - `stores/{user,permission,dict}.ts`（3 stores）
  - `layouts/DefaultLayout.vue`（主布局）
  - `components/{PageHeader,DataTable,FormDialog,EmptyState,SearchForm}.vue`（5 通用组件）
  - `utils/{auth,permission,format}.ts`（3 utils）
  - `views/{Login,Dashboard}.vue`（2 页面）

**M5-2-A1 严禁修改**：
- ❌ `server/` 任何文件（0 修改）
- ❌ `client/package.json`（0 修改，禁止装新依赖）
- ❌ `client/vite.config.ts` / `tsconfig.json` / `.eslintrc.cjs`（0 修改）
- ❌ M5-2-0 共享基础设施文件（除**追加**路由表）：
  - `client/src/api/{http,auth,types}.ts`（**M5-2-A1 创建独立的 `api/types/organization.ts`**）
  - `client/src/stores/{user,permission,dict}.ts`
  - `client/src/layouts/DefaultLayout.vue`（**M5-2-A1 创建独立的 `OrganizationLayout.vue`**）
  - `client/src/views/{Login,Dashboard}.vue`
  - `client/src/utils/{auth,permission,format}.ts`

**M5-2-A1 必须新增 client/src/ 下 29 个文件**：
- **API 类型 + 函数**（7 个）：
  - `client/src/api/types/organization.ts`（**独立 types 文件**，含 Company/Department/Employee 三类 + enum）
  - `client/src/api/company.ts`（A1 公司 7 端点函数）
  - `client/src/api/department.ts`（A2 部门 8 端点函数）
  - `client/src/api/employee.ts`（A3 员工 10 端点函数）
  - `client/src/api/__tests__/company.test.ts`（2-3 单测）
  - `client/src/api/__tests__/department.test.ts`（2-3 单测）
  - `client/src/api/__tests__/employee.test.ts`（3-4 单测）
- **页面**（13 个）：
  - 公司（4）：`views/organization/companies/{CompanyList,CompanyDetail,CompanyFormDialog,CompanyTree}.vue`
  - 部门（4）：`views/organization/departments/{DepartmentTree,DepartmentDetail,DepartmentFormDialog,DepartmentHead}.vue`
  - 员工（5）：`views/organization/employees/{EmployeeList,EmployeeDetail,EmployeeFormDialog,EmployeeOrgChart,EmployeeImportDialog}.vue`
- **路由 + 布局**（2 个）：
  - `client/src/router/organization.ts`（组织人事子路由表）
  - `client/src/layouts/OrganizationLayout.vue`（模块布局：左侧子菜单 + 右侧 router-view）
- **通用组件**（2 个）：
  - `client/src/components/StatusTag.vue`（5 状态标签：active / suspended / probation / resigned / merged）
  - `client/src/components/TreeNode.vue`（树形节点，**仅 DOM 抽象**，数据由父组件传）
- **测试**（5 个）：
  - `client/src/api/__tests__/company.test.ts`（2-3）
  - `client/src/api/__tests__/department.test.ts`（2-3）
  - `client/src/api/__tests__/employee.test.ts`（3-4）
  - `client/src/components/__tests__/StatusTag.test.ts`（1-2）
  - `client/src/views/__tests__/organization.test.ts`（4-5）

**M5-2-A1 必须修改 1 个文件**：
- ✅ `client/src/router/index.ts`（**追加**组织人事子路由，**不删** /login + /dashboard）

**实际新增文件 29 个**（含 5 个测试文件），**修改 1 个**，**总数 30 个文件**。

### 2.3 已有可复用模块（M5-2-A1 必复用，零重写）

| 复用对象 | 来源 | 用途 | M5-2-A1 用法 |
|---|---|---|---|
| `http` 实例 | M5-2-0 `client/src/api/http.ts` | axios 封装 + 401 refresh + 错误 toast | **`api/{company,department,employee}.ts` 全部 import http** |
| `useUserStore` | M5-2-0 `client/src/stores/user.ts` | 用户信息 + 权限 + 角色 | 5 角色权限判断 |
| `usePermissionStore` | M5-2-0 `client/src/stores/permission.ts` | v-permission 指令 | 元素级权限控制 |
| `useDictStore` | M5-2-0 `client/src/stores/dict.ts` | 字典缓存 | 部门 / 角色字典 |
| `PageHeader` | M5-2-0 `client/src/components/PageHeader.vue` | 页面标题 + 面包屑 + 操作按钮 | **所有 13 页面**用 |
| `DataTable` | M5-2-0 `client/src/components/DataTable.vue` | 分页 + 搜索 + 操作列 | **5 列表页**用（CompanyList / DepartmentTree / EmployeeList / ContractList / OffboardingList）|
| `FormDialog` | M5-2-0 `client/src/components/FormDialog.vue` | 表单弹窗 + 校验 | **所有创建/更新表单**用 |
| `EmptyState` | M5-2-0 `client/src/components/EmptyState.vue` | 空状态 | **所有列表页**用 |
| `SearchForm` | M5-2-0 `client/src/components/SearchForm.vue` | 搜索表单 | **5 列表页**用 |
| `formatDate` / `formatMoney` | M5-2-0 `client/src/utils/format.ts` | 日期 / 金额格式化 | **所有页面**用 |
| 后端 `/api/companies` | server/src/routes | A1 7 端点 | `api/company.ts` 调 |
| 后端 `/api/departments` | server/src/routes | A2 8 端点 | `api/department.ts` 调 |
| 后端 `/api/employees` | server/src/routes | A3 10 端点 | `api/employee.ts` 调 |
| Element Plus ElTable / ElForm / ElDialog / ElTree / ElCard / ElTag | client/package.json 已装 | UI 组件 | **所有 13 页面**用 |

### 2.4 业务规则来源

- **V1.2 §四.5 A1+A2+A3**：组织架构 + 部门 + 员工
- **V1.2 §三.6 RBAC**：5 角色 = admin / hr / dept_head / executive / employee（**无 finance**）
- **V1.2 §三.5 业务规则配置化**：configs 表 + configService（**前端仅消费**，不新增）

---

## 3. 必读约束（项目级）

### 3.1 代码范式

- **Vue3 Composition API + `<script setup lang="ts">`**（**不用** Options API，**不用** class 组件）
- **TypeScript 强类型**（`strict: true`，**严禁** `any`，必要时 `unknown` + 类型守卫）
- **Element Plus 组件**：`ElButton` / `ElInput` / `ElTable` / `ElDialog` / `ElForm` / `ElTree` / `ElCard` / `ElTag`（**严禁** 用 `<button>` / `<input>` 原生 HTML）
- **ESLint 规范**：`@vue/eslint-config-airbnb` + `@vue/eslint-config-typescript`
- **Prettier 格式化**：3.1.1（**严禁** 手动改格式）
- **API 类型**：`api/types/organization.ts` 定义所有响应类型，**严禁** 在组件里 `any`

### 3.2 数据模型约定

- **TS 类型**：PascalCase（`Company` / `Department` / `Employee` / `CompanyStatus` / `EmployeeStatus`）
- **组件命名**：PascalCase（`CompanyList.vue` / `EmployeeFormDialog.vue`）
- **文件命名**：kebab-case（`company.ts` / `organization.ts`）
- **API 路径**：`/api/<module>/<resource>`，kebab-case

### 3.3 RBAC 5 角色（**无 finance**）

```
admin / hr / dept_head / executive / employee
```

**M5-2-A1 权限分配**（A1+A2+A3 25 端点）：

| 端点 | admin | hr | dept_head | executive | employee |
|---|---|---|---|---|---|
| **A1 公司**（7）| ✅ 全开 | ✅ 全开 | 🔍 只读 | 🔍 只读 | ❌ 无 |
| **A2 部门**（8）| ✅ 全开 | ✅ 全开 | 🔍 只读本部门 | 🔍 只读 | ❌ 无 |
| **A3 员工**（10）| ✅ 全开 | ✅ 全开 | 🔍 只读本部门 | 🔍 只读 | ❌ 无 |

**前端实现**（沿用 M5-2-0 `usePermissionStore` + `v-permission` 指令）：
- `<el-button v-permission="['company:create']">创建</el-button>` 控制按钮显隐
- 列表页根据 `userStore.role` 过滤数据范围
  - admin / hr：全部
  - dept_head：本部门 + 下属部门
  - executive：全部（只读）
  - employee：无权限（不显示组织人事菜单）

**严禁** 给 finance 角色分配权限（D1 教训延续）

### 3.4 错误码段位（M5-2-A1 0 新错误码）

**M5-2-A1 不分配任何错误码**（前端不涉及业务错误码定义，仅消费后端错误码）。

**前端消费后端 A1+A2+A3 错误码段位**（**M1 后端已分配**，前端仅显示 message）：
- A1 公司：`TODO: M1-A1 错误码段位`（如 10101-10199 / 20101-20199 / 30101-30199 / 40101-40199 / 50101-50199 / 60101-60199 / 70101-70199 / 80101-80199 / 90101-90199，**前端不需记忆具体码**，仅显示 `apiError.message`）
- A2 部门：同上
- A3 员工：同上

**`api/types.ts` 定义**（沿用 M5-2-0）：
```ts
export type ErrorCode = number  // 5 位数字错误码
export interface ApiError {
  code: ErrorCode
  message: string
  details?: Record<string, unknown>
}
```

### 3.5 审计埋点（M5-2-A1 0 审计）

**M5-2-A1 不写任何审计**（前端不涉及，**复用**后端 `auditLog` 自动记录）。

### 3.6 业务规则配置化（M5-2-A1 0 新 configs）

**M5-2-A1 不新增任何 configs**（前端不涉及业务规则定义）。

**前端消费后端 configs**：通过 `GET /api/configs/*` 端点（如已建）或硬编码常用配置（按需）。

---

## 4. 实施任务

### 4.1 子任务 7.2.A1.1：路由 + 侧栏 + 模块布局

**2 新文件**（`router/organization.ts` + `layouts/OrganizationLayout.vue`）+ 1 修改（`router/index.ts`）

**`router/organization.ts`**（组织人事子路由表）：

```ts
import type { RouteRecordRaw } from 'vue-router'

/**
 * 组织人事子路由（M5-2-A1）
 * @description 路径前缀 /org，包含 7 子路由：companies / departments / employees
 * @auth 通过 DefaultLayout 守卫 + OrganizationLayout 侧栏菜单控制
 */
const organizationRoutes: RouteRecordRaw[] = [
  {
    path: '/org',
    component: () => import('@/layouts/OrganizationLayout.vue'),
    meta: { requiresAuth: true, title: '组织人事' },
    children: [
      { path: '', redirect: '/org/companies' },
      {
        path: 'companies',
        name: 'CompanyList',
        component: () => import('@/views/organization/companies/CompanyList.vue'),
        meta: { title: '法人公司' },
      },
      {
        path: 'companies/:id',
        name: 'CompanyDetail',
        component: () => import('@/views/organization/companies/CompanyDetail.vue'),
        meta: { title: '公司详情' },
      },
      {
        path: 'companies/:id/tree',
        name: 'CompanyTree',
        component: () => import('@/views/organization/companies/CompanyTree.vue'),
        meta: { title: '公司组织树' },
      },
      {
        path: 'departments',
        name: 'DepartmentTree',
        component: () => import('@/views/organization/departments/DepartmentTree.vue'),
        meta: { title: '部门管理' },
      },
      {
        path: 'departments/:id',
        name: 'DepartmentDetail',
        component: () => import('@/views/organization/departments/DepartmentDetail.vue'),
        meta: { title: '部门详情' },
      },
      {
        path: 'employees',
        name: 'EmployeeList',
        component: () => import('@/views/organization/employees/EmployeeList.vue'),
        meta: { title: '员工档案' },
      },
      {
        path: 'employees/:id',
        name: 'EmployeeDetail',
        component: () => import('@/views/organization/employees/EmployeeDetail.vue'),
        meta: { title: '员工详情' },
      },
      {
        path: 'employees/org-chart',
        name: 'EmployeeOrgChart',
        component: () => import('@/views/organization/employees/EmployeeOrgChart.vue'),
        meta: { title: '组织架构图' },
      },
    ],
  },
]

export default organizationRoutes
```

**`router/index.ts` 追加**（**仅追加**，不删 /login + /dashboard）：

```ts
import organizationRoutes from './organization'
// ...
const routes: RouteRecordRaw[] = [
  // ... 已有 /login + / + /dashboard
  ...organizationRoutes,  // 追加 M5-2-A1
]
```

**`layouts/OrganizationLayout.vue`**（模块布局：左侧子菜单 + 右侧 router-view）：

```vue
<script setup lang="ts">
import { computed } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { useUserStore } from '@/stores/user'
import { hasPermission } from '@/utils/permission'

const route = useRoute()
const router = useRouter()
const userStore = useUserStore()

const menuItems = computed(() => [
  { key: 'companies', label: '法人公司', icon: 'OfficeBuilding', to: '/org/companies', permission: 'company:read' },
  { key: 'departments', label: '部门管理', icon: 'Connection', to: '/org/departments', permission: 'department:read' },
  { key: 'employees', label: '员工档案', icon: 'User', to: '/org/employees', permission: 'employee:read' },
  { key: 'org-chart', label: '组织架构图', icon: 'Share', to: '/org/employees/org-chart', permission: 'employee:read' },
].filter((m) => hasPermission(userStore.userInfo, m.permission)))

const activeKey = computed(() => {
  const path = route.path
  if (path.startsWith('/org/companies')) return 'companies'
  if (path.startsWith('/org/departments')) return 'departments'
  if (path.startsWith('/org/employees/org-chart')) return 'org-chart'
  if (path.startsWith('/org/employees')) return 'employees'
  return ''
})
</script>

<template>
  <div class="org-layout">
    <aside class="org-layout__sidebar">
      <el-menu :default-active="activeKey" @select="(k) => router.push(menuItems.find((m) => m.key === k)?.to || '/org')">
        <el-menu-item v-for="m in menuItems" :key="m.key" :index="m.key">
          <el-icon><component :is="m.icon" /></el-icon>
          <span>{{ m.label }}</span>
        </el-menu-item>
      </el-menu>
    </aside>
    <main class="org-layout__content">
      <router-view />
    </main>
  </div>
</template>
```

### 4.2 子任务 7.2.A1.2：公司 API + 类型 + 4 页面

**1 新 types 文件**（`api/types/organization.ts`，**含 A1+A2+A3 三类**，避免拆 3 个 types 文件）+ 1 新 API 文件（`api/company.ts`）+ 4 新页面

**`api/types/organization.ts`**（独立 types 文件，**A1+A2+A3 全部类型**）：

```ts
/** ===== A1 法人公司 ===== */
export type CompanyStatus = 'active' | 'suspended' | 'merged'

export interface Company {
  id: string
  code: string  // XACH / XACX / SCXH
  name: string
  shortName?: string
  city?: string
  address?: string
  contact?: string
  legalRep?: string
  taxNo?: string
  status: CompanyStatus
  headcount: number
  createdAt: string
  updatedAt: string
}

export interface CreateCompanyRequest {
  code: string
  name: string
  shortName?: string
  city?: string
  address?: string
  contact?: string
  legalRep?: string
  taxNo?: string
}

export interface UpdateCompanyRequest extends Partial<CreateCompanyRequest> {}

/** ===== A2 部门管理 ===== */
export type DepartmentStatus = 'active' | 'suspended' | 'merged'

export interface Department {
  id: string
  companyId: string
  parentId?: string
  code: string
  name: string
  leaderId?: string
  leaderName?: string
  headcount: number
  order: number
  status: DepartmentStatus
  children?: Department[]  // 树形
  createdAt: string
  updatedAt: string
}

export interface CreateDepartmentRequest {
  companyId: string
  parentId?: string
  code: string
  name: string
  leaderId?: string
  order?: number
}

export interface MoveDepartmentRequest {
  newParentId?: string
  newOrder: number
}

/** ===== A3 员工档案 ===== */
export type EmployeeStatus = 'probation' / 'active' / 'resigned'
export type Gender = 'male' | 'female'

export interface Employee {
  id: string
  userId?: string
  employeeNo: string
  name: string
  gender?: Gender
  birthDate?: string
  phone?: string  // 加密
  email?: string
  idCard?: string  // 加密
  bankCard?: string  // 加密
  companyId: string
  companyName: string
  departmentId?: string
  departmentName?: string
  position?: string
  hireDate: string
  resignationDate?: string
  status: EmployeeStatus
  emergencyContact?: string
  emergencyContactPhone?: string  // 加密
  createdAt: string
  updatedAt: string
}

export interface CreateEmployeeRequest {
  employeeNo: string
  name: string
  gender?: Gender
  birthDate?: string
  phone?: string
  email?: string
  idCard?: string
  bankCard?: string
  companyId: string
  departmentId?: string
  position?: string
  hireDate: string
  emergencyContact?: string
  emergencyContactPhone?: string
}

export interface ListEmployeeFilter {
  companyId?: string
  departmentId?: string
  status?: EmployeeStatus
  keyword?: string
  page?: number
  pageSize?: number
}
```

**`api/company.ts`**（A1 7 端点函数）：

```ts
import http from './http'
import type { Company, CreateCompanyRequest, UpdateCompanyRequest, PaginatedResponse } from './types/organization'

export async function listCompanies(): Promise<PaginatedResponse<Company>> {
  return http.get<PaginatedResponse<Company>>('/companies')
}

export async function getCompany(id: string): Promise<Company> {
  return http.get<Company>(`/companies/${id}`)
}

export async function createCompany(data: CreateCompanyRequest): Promise<Company> {
  return http.post<Company>('/companies', data)
}

export async function updateCompany(id: string, data: UpdateCompanyRequest): Promise<Company> {
  return http.put<Company>(`/companies/${id}`, data)
}

export async function deleteCompany(id: string): Promise<void> {
  return http.delete(`/companies/${id}`)
}

export async function suspendCompany(id: string): Promise<Company> {
  return http.post<Company>(`/companies/${id}/suspend`)
}

export async function reactivateCompany(id: string): Promise<Company> {
  return http.post<Company>(`/companies/${id}/reactivate`)
}
```

**4 公司页面**（**完整代码见 §5.2**，**仅给关键 props/slots/emits**）：

**`CompanyList.vue`**：
- 顶部 `PageHeader title="法人公司"` + 操作按钮（v-permission="['company:create']"）
- 搜索 `SearchForm`（按 status + 名称）
- 表格 `DataTable :data=companies :columns=columns`
- 操作列：详情 / 编辑 / 暂停 / 恢复 / 删除（v-permission）
- 创建/编辑用 `FormDialog`

**`CompanyDetail.vue`**：
- `PageHeader` + 返回按钮
- 基础信息卡片 + 子公司树（用 `CompanyTree`）
- 操作日志（用 `DataTable` 显示 audit_logs）

**`CompanyFormDialog.vue`**：
- `el-dialog` + `el-form` 表单（code / name / shortName / city / address / contact / legalRep / taxNo）
- 校验：公司编码唯一（后端 409xx 错误码）
- emit `submit` + `cancel`

**`CompanyTree.vue`**：
- 顶部 PageHeader
- `el-tree` 显示公司组织树（递归子公司 + 部门）
- 点击节点跳详情

### 4.3 子任务 7.2.A1.3：部门 API + 类型 + 4 页面

**1 新 API 文件**（`api/department.ts`）+ 4 新页面

**`api/department.ts`**（A2 8 端点函数）：

```ts
import http from './http'
import type { Department, CreateDepartmentRequest, MoveDepartmentRequest, Employee, PaginatedResponse } from './types/organization'

export async function listDepartments(companyId?: string): Promise<Department[]> {
  return http.get<Department[]>('/departments', { params: { companyId } })
}

export async function getDepartment(id: string): Promise<Department> {
  return http.get<Department>(`/departments/${id}`)
}

export async function createDepartment(data: CreateDepartmentRequest): Promise<Department> {
  return http.post<Department>('/departments', data)
}

export async function updateDepartment(id: string, data: Partial<CreateDepartmentRequest>): Promise<Department> {
  return http.put<Department>(`/departments/${id}`, data)
}

export async function deleteDepartment(id: string): Promise<void> {
  return http.delete(`/departments/${id}`)
}

export async function moveDepartment(id: string, data: MoveDepartmentRequest): Promise<Department> {
  return http.post<Department>(`/departments/${id}/move`, data)
}

export async function mergeDepartment(sourceId: string, targetId: string): Promise<void> {
  return http.post(`/departments/${sourceId}/merge`, { targetId })
}

export async function listDepartmentEmployees(id: string): Promise<PaginatedResponse<Employee>> {
  return http.get<PaginatedResponse<Employee>>(`/departments/${id}/employees`)
}
```

**4 部门页面**（**完整代码见 §5.3**，**仅给关键**）：

**`DepartmentTree.vue`**：
- `PageHeader title="部门管理"` + 创建按钮
- `el-tree` 树形结构（递归显示）+ 拖拽排序
- 节点操作：新增子部门 / 编辑 / 删除 / 合并 / 查看员工

**`DepartmentDetail.vue`**：
- 基础信息卡片（部门 / 上级 / 负责人 / 编制）
- 部门员工列表（用 `DataTable` 消费 `listDepartmentEmployees`）
- 操作日志

**`DepartmentFormDialog.vue`**：
- `el-dialog` + `el-form`（companyId / parentId / code / name / leaderId / order）
- 校验：code 唯一 + leaderId 必须存在

**`DepartmentHead.vue`**：
- 部门负责人选择（el-select + 远程搜索员工）
- 显示负责人信息卡片

### 4.4 子任务 7.2.A1.4：员工 API + 类型 + 5 页面

**1 新 API 文件**（`api/employee.ts`）+ 5 新页面

**`api/employee.ts`**（A3 10 端点函数）：

```ts
import http from './http'
import type { Employee, CreateEmployeeRequest, ListEmployeeFilter, PaginatedResponse } from './types/organization'

export async function listEmployees(filter: ListEmployeeFilter = {}): Promise<PaginatedResponse<Employee>> {
  return http.get<PaginatedResponse<Employee>>('/employees', { params: filter })
}

export async function getEmployee(id: string): Promise<Employee> {
  return http.get<Employee>(`/employees/${id}`)
}

export async function createEmployee(data: CreateEmployeeRequest): Promise<Employee> {
  return http.post<Employee>('/employees', data)
}

export async function updateEmployee(id: string, data: Partial<CreateEmployeeRequest>): Promise<Employee> {
  return http.put<Employee>(`/employees/${id}`, data)
}

export async function deleteEmployee(id: string): Promise<void> {
  return http.delete(`/employees/${id}`)
}

export async function transferEmployee(id: string, data: { toDeptId: string; effectiveDate: string }): Promise<Employee> {
  return http.post<Employee>(`/employees/${id}/transfer`, data)
}

export async function regularizeEmployee(id: string, data: { newBaseSalary?: number; effectiveDate: string }): Promise<Employee> {
  return http.post<Employee>(`/employees/${id}/regularize`, data)
}

export async function resignEmployee(id: string, data: { resignationDate: string; reason?: string }): Promise<Employee> {
  return http.post<Employee>(`/employees/${id}/resign`, data)
}

export async function exportEmployees(filter: ListEmployeeFilter = {}): Promise<Blob> {
  return http.get<Blob>('/employees/export', { params: filter, responseType: 'blob' })
}

export async function importEmployees(file: File): Promise<{ successCount: number; failedCount: number; errors: string[] }> {
  const formData = new FormData()
  formData.append('file', file)
  return http.post('/employees/import', formData, { headers: { 'Content-Type': 'multipart/form-data' } })
}
```

**5 员工页面**（**完整代码见 §5.4**，**仅给关键**）：

**`EmployeeList.vue`**：
- `PageHeader` + 导入 + 导出 + 创建按钮
- `SearchForm`（按部门级联 + 状态 + 关键词）
- `DataTable` 员工列表（编号 / 姓名 / 部门 / 职位 / 状态 / 入职日期 / 操作）
- 状态用 `StatusTag`（active / probation / resigned）
- 加密字段（手机 / 身份证 / 银行卡）显示**部分脱敏**（utils/format）

**`EmployeeDetail.vue`**：
- 基础信息卡片（**敏感字段脱敏显示** + "查看完整"按钮二次授权，**留 TODO**）
- 岗位历史 / 薪资历史（用 `DataTable` 消费 `employee_position_history` / `employee_salary_history`）
- 操作日志

**`EmployeeFormDialog.vue`**：
- `el-dialog` + `el-form`（完整 13 字段：employeeNo / name / gender / birthDate / phone / email / idCard / bankCard / companyId / departmentId / position / hireDate / emergencyContact）
- 校验：employeeNo 唯一 + 身份证 18 位 + 手机 11 位

**`EmployeeOrgChart.vue`**：
- `PageHeader` + 公司/部门级联选择
- 组织架构图（树形 + 卡片展示员工头像 + 姓名 + 职位）
- 点击员工跳详情

**`EmployeeImportDialog.vue`**：
- `el-dialog` + `el-upload`（Excel 文件）
- 下载模板按钮（`/api/employees/import-template`，如未建留 TODO）
- 上传后显示导入结果（成功数 / 失败数 / 错误列表）

### 4.5 路由挂载 / 权限常量 / Migration / seed.ts

**M5-2-A1 不涉及**：
- 0 路由挂载（**仅 client/src/router/organization.ts 子路由表**，**追加**到 router/index.ts）
- 0 权限常量追加（**前端不修改 permissions.ts**）
- 0 Migration（不修改 schema.prisma）
- 0 seed.ts 追加（不修改后端 seed.ts）

---

## 5. 关键决策点详尽（4 类必须详尽）

### 5.1 路由 + 侧栏 + 模块布局 JSDoc + 测试（子任务 7.2.A1.1）

#### 5.1.1 JSDoc + 校验链

**`router/organization.ts`**

```ts
/**
 * 组织人事子路由表（M5-2-A1）
 * @module router/organization
 * @description 路径前缀 /org，包含 7 子路由：companies（列表+详情+树）/ departments（树+详情）/ employees（列表+详情+组织图）
 * @auth 通过 DefaultLayout 守卫（meta.requiresAuth=true）+ OrganizationLayout 侧栏菜单过滤
 * @layout OrganizationLayout 包裹所有子路由，**不修改** DefaultLayout.vue
 */
import type { RouteRecordRaw } from 'vue-router'

const organizationRoutes: RouteRecordRaw[] = [
  {
    path: '/org',
    component: () => import('@/layouts/OrganizationLayout.vue'),
    meta: { requiresAuth: true, title: '组织人事' },
    children: [
      { path: '', redirect: '/org/companies' },
      // ... 7 子路由
    ],
  },
]

export default organizationRoutes
```

**`layouts/OrganizationLayout.vue`**（**M5-2-A1 独立 layout，不修改 DefaultLayout**）

```vue
<script setup lang="ts">
/**
 * 组织人事模块布局（M5-2-A1）
 * @module layouts/OrganizationLayout
 * @description 左侧子菜单（4 项：法人公司 / 部门管理 / 员工档案 / 组织架构图）+ 右侧 router-view
 * @permissions 通过 useUserStore.userInfo + hasPermission 工具过滤菜单（5 角色 RBAC）
 * @routing 接收子路由表 router/organization.ts
 */
import { computed } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { useUserStore } from '@/stores/user'
import { hasPermission } from '@/utils/permission'

const route = useRoute()
const router = useRouter()
const userStore = useUserStore()

const menuItems = computed(() => [
  { key: 'companies', label: '法人公司', icon: 'OfficeBuilding', to: '/org/companies', permission: 'company:read' },
  { key: 'departments', label: '部门管理', icon: 'Connection', to: '/org/departments', permission: 'department:read' },
  { key: 'employees', label: '员工档案', icon: 'User', to: '/org/employees', permission: 'employee:read' },
  { key: 'org-chart', label: '组织架构图', icon: 'Share', to: '/org/employees/org-chart', permission: 'employee:read' },
].filter((m) => hasPermission(userStore.userInfo, m.permission)))

const activeKey = computed(() => {
  const path = route.path
  if (path.startsWith('/org/companies')) return 'companies'
  if (path.startsWith('/org/departments')) return 'departments'
  if (path.startsWith('/org/employees/org-chart')) return 'org-chart'
  if (path.startsWith('/org/employees')) return 'employees'
  return ''
})
</script>

<template>
  <div class="org-layout">
    <aside class="org-layout__sidebar">
      <el-menu :default-active="activeKey" @select="(k) => router.push(menuItems.find((m) => m.key === k)?.to || '/org')">
        <el-menu-item v-for="m in menuItems" :key="m.key" :index="m.key">
          <el-icon><component :is="m.icon" /></el-icon>
          <span>{{ m.label }}</span>
        </el-menu-item>
      </el-menu>
    </aside>
    <main class="org-layout__content">
      <router-view />
    </main>
  </div>
</template>
```

**`router/index.ts` 追加**（**仅追加**，不删 /login + /dashboard）：

```ts
import organizationRoutes from './organization'
// ...
const routes: RouteRecordRaw[] = [
  // ... 已有 /login + / + /dashboard
  ...organizationRoutes,  // 追加 M5-2-A1
]
```

#### 5.1.2 错误码触发条件

**M5-2-A1 不分配错误码**（前端不涉及）。

#### 5.1.3 测试用例（4-5 个 it，**沿用 M5-2-0 内联断言**）

```ts
import { describe, it, expectEqual } from './test-utils'  // M5-2-0 内联断言工具
// 验证路由表结构 + 权限过滤
describe('router/organization.ts', () => {
  it('包含 7 子路由（companies / departments / employees）', () => {
    const routes = organizationRoutes[0].children || []
    expectEqual(routes.length, 7)
  })

  it('默认重定向到 /org/companies', () => {
    const redirect = organizationRoutes[0].children?.[0]
    expectEqual(redirect?.path, '')
    expectEqual(redirect?.redirect, '/org/companies')
  })
})
```

### 5.2 公司 API + 4 页面 JSDoc + 测试（子任务 7.2.A1.2）

#### 5.2.1 JSDoc + 校验链

**`api/types/organization.ts`**（**A1+A2+A3 全部类型，1 个文件**）

```ts
/**
 * 组织人事 API 类型（M5-2-A1）
 * @module api/types/organization
 * @description A1+A2+A3 全部类型：Company / Department / Employee + 4 状态 enum
 * @status 3 公司状态：active / suspended / merged
 * @status 3 部门状态：active / suspended / merged
 * @status 3 员工状态：probation / active / resigned
 */
/** A1 */
export type CompanyStatus = 'active' | 'suspended' | 'merged'
export interface Company { /* 13 字段 */ }
export interface CreateCompanyRequest { /* 8 字段 */ }
export interface UpdateCompanyRequest extends Partial<CreateCompanyRequest> {}

/** A2 */
export type DepartmentStatus = 'active' | 'suspended' | 'merged'
export interface Department { /* 12 字段 + children */ }
export interface CreateDepartmentRequest { /* 7 字段 */ }
export interface MoveDepartmentRequest { /* 2 字段 */ }

/** A3 */
export type EmployeeStatus = 'probation' | 'active' | 'resigned'
export type Gender = 'male' | 'female'
export interface Employee { /* 17 字段（含 4 加密字段）*/ }
export interface CreateEmployeeRequest { /* 13 字段 */ }
export interface ListEmployeeFilter { /* 6 字段 */ }
```

**`api/company.ts`**（**A1 7 端点函数**）

```ts
/**
 * 法人公司 API（M5-2-A1）
 * @module api/company
 * @description 消费后端 /api/companies 7 端点（list / get / create / update / delete / suspend / reactivate）
 * @error 后端 5 位数字错误码 → M5-2-0 http.ts 自动 ElMessage.error(apiError.message)
 */
import http from './http'
import type { Company, CreateCompanyRequest, UpdateCompanyRequest, PaginatedResponse } from './types/organization'

export async function listCompanies(): Promise<PaginatedResponse<Company>> { ... }
export async function getCompany(id: string): Promise<Company> { ... }
export async function createCompany(data: CreateCompanyRequest): Promise<Company> { ... }
export async function updateCompany(id: string, data: UpdateCompanyRequest): Promise<Company> { ... }
export async function deleteCompany(id: string): Promise<void> { ... }
export async function suspendCompany(id: string): Promise<Company> { ... }
export async function reactivateCompany(id: string): Promise<Company> { ... }
```

**4 公司页面**（**详细代码见 §4.2**，**仅给关键 props/slots/emits + JSDoc**）：

**`CompanyList.vue`**：
```ts
/**
 * 法人公司列表页（M5-2-A1）
 * @page 顶部 PageHeader + 搜索 + 表格 + 创建按钮
 * @permissions admin/hr 全开，dept_head/executive 只读，employee 无权限
 * @data 调 listCompanies() + DataTable 渲染
 */
```

**`CompanyDetail.vue`**：
```ts
/**
 * 公司详情页（M5-2-A1）
 * @page 基础信息卡片 + 子公司树（CompanyTree）+ 操作日志
 * @data 调 getCompany(id) + CompanyTree 渲染
 */
```

**`CompanyFormDialog.vue`**：
```ts
/**
 * 公司创建/编辑表单弹窗（M5-2-A1）
 * @page el-dialog + el-form（8 字段）
 * @validation 公司编码唯一（后端 409xx 错误码）
 * @emit submit(payload: CreateCompanyRequest) / cancel
 */
```

**`CompanyTree.vue`**：
```ts
/**
 * 公司组织树（M5-2-A1）
 * @page 顶部 PageHeader + el-tree 显示公司组织树
 * @data 调 listCompanies() 递归显示子公司
 */
```

#### 5.2.2 错误码触发条件

**M5-2-A1 不分配错误码**（前端不涉及）。

#### 5.2.3 测试用例（2-3 个 it）

```ts
import { describe, it, expectEqual } from '@/api/__tests__/test-utils'

describe('api/company.ts', () => {
  it('listCompanies 调 GET /companies', () => { ... })
  it('createCompany 调 POST /companies', () => { ... })
  it('类型正确（Company 13 字段）', () => { ... })
})
```

### 5.3 部门 API + 4 页面 JSDoc + 测试（子任务 7.2.A1.3）

**`api/department.ts`**（**A2 8 端点函数**）+ 4 页面

**JSDoc + 校验链**（**完整代码见 §4.3**）：

```ts
/**
 * 部门管理 API（M5-2-A1）
 * @module api/department
 * @description 消费后端 /api/departments 8 端点（list / get / create / update / delete / move / merge / employees）
 * @tree listDepartments 返回 Department[] 含 children 字段，前端 el-tree 直接渲染
 */
```

**4 部门页面**（`DepartmentTree.vue` / `DepartmentDetail.vue` / `DepartmentFormDialog.vue` / `DepartmentHead.vue`）：

**JSDoc + props/slots/emits** 与公司 4 页面模式相同（**详见 §4.3**）

#### 5.3.1 错误码触发条件

**M5-2-A1 不分配错误码**

#### 5.3.2 测试用例（2-3 个 it）

```ts
describe('api/department.ts', () => {
  it('listDepartments 调 GET /departments', () => { ... })
  it('moveDepartment 调 POST /departments/:id/move', () => { ... })
  it('类型正确（Department 12 字段 + children）', () => { ... })
})
```

### 5.4 员工 API + 5 页面 JSDoc + 测试（子任务 7.2.A1.4）

**`api/employee.ts`**（**A3 10 端点函数**）+ 5 页面

**JSDoc + 校验链**（**完整代码见 §4.4**）：

```ts
/**
 * 员工档案 API（M5-2-A1）
 * @module api/employee
 * @description 消费后端 /api/employees 10 端点（list / get / create / update / delete / transfer / regularize / resign / export / import）
 * @encrypted 4 加密字段（phone / idCard / bankCard / emergencyContactPhone）由 M0.5-3 AES-256-GCM 加密，**前端只显示部分脱敏**
 */
```

**5 员工页面**（`EmployeeList.vue` / `EmployeeDetail.vue` / `EmployeeFormDialog.vue` / `EmployeeOrgChart.vue` / `EmployeeImportDialog.vue`）：

**JSDoc + props/slots/emits** 与公司 4 页面模式相同（**详见 §4.4**）

#### 5.4.1 错误码触发条件

**M5-2-A1 不分配错误码**

#### 5.4.2 测试用例（3-4 个 it）

```ts
describe('api/employee.ts', () => {
  it('listEmployees 调 GET /employees + filter 参数', () => { ... })
  it('createEmployee 调 POST /employees', () => { ... })
  it('importEmployees 调 POST /employees/import (multipart/form-data)', () => { ... })
  it('类型正确（Employee 17 字段含 4 加密字段）', () => { ... })
})
```

### 5.5 通用组件 + 状态映射（跨 A1+A2+A3 复用）

**`components/StatusTag.vue`**（5 状态标签：active / suspended / probation / resigned / merged）：

```vue
<script setup lang="ts">
/**
 * 状态标签通用组件（M5-2-A1）
 * @module components/StatusTag
 * @description 5 状态映射（active/suspended/probation/resigned/merged）→ Element Plus ElTag
 * @props status 状态字符串
 * @props type 可选覆盖 el-tag type（success/warning/info/danger/primary）
 */
interface Props {
  status: string
  type?: 'success' / 'warning' / 'info' / 'danger' / 'primary'
}
const props = withDefaults(defineProps<Props>(), { type: 'info' })

const STATUS_MAP: Record<string, { label: string; type: 'success' / 'warning' / 'info' / 'danger' }> = {
  active: { label: '正常', type: 'success' },
  suspended: { label: '暂停', type: 'warning' },
  merged: { label: '已合并', type: 'info' },
  probation: { label: '试用期', type: 'warning' },
  resigned: { label: '已离职', type: 'danger' },
}

const statusInfo = computed(() => STATUS_MAP[props.status] || { label: props.status, type: 'info' })
</script>

<template>
  <el-tag :type="props.type || statusInfo.type">{{ statusInfo.label }}</el-tag>
</template>
```

**`components/TreeNode.vue`**（树形节点抽象，**仅 DOM 抽象**，数据由父组件传）：

```vue
<script setup lang="ts">
/**
 * 树形节点通用组件（M5-2-A1）
 * @module components/TreeNode
 * @description 递归树形节点（公司组织树 + 部门树）
 * @props node 当前节点 { id, label, children? }
 * @slot default 节点右侧操作按钮 slot
 */
interface TreeNodeData {
  id: string
  label: string
  children?: TreeNodeData[]
}
interface Props {
  node: TreeNodeData
}
const props = defineProps<Props>()
</script>

<template>
  <div class="tree-node">
    <div class="tree-node__content">
      <span class="tree-node__label">{{ props.node.label }}</span>
      <slot :node="props.node" />
    </div>
    <div v-if="props.node.children?.length" class="tree-node__children">
      <TreeNode v-for="child in props.node.children" :key="child.id" :node="child">
        <template #default="slotProps">
          <slot :node="slotProps.node" />
        </template>
      </TreeNode>
    </div>
  </div>
</template>
```

#### 5.5.1 测试用例（1-2 个 it）

```ts
describe('components/StatusTag.vue', () => {
  it('5 状态正确映射（active→success / resigned→danger）', () => { ... })
  it('未知状态显示原字符串', () => { ... })
})
```

---

## 6. 修改文件清单（明示边界）

### 6.1 必改文件（清单以内）

#### 新增文件（29 个）

1. `client/src/router/organization.ts`
2. `client/src/layouts/OrganizationLayout.vue`
3. `client/src/api/types/organization.ts`
4. `client/src/api/company.ts`
5. `client/src/api/department.ts`
6. `client/src/api/employee.ts`
7. `client/src/api/__tests__/company.test.ts`
8. `client/src/api/__tests__/department.test.ts`
9. `client/src/api/__tests__/employee.test.ts`
10. `client/src/views/organization/companies/CompanyList.vue`
11. `client/src/views/organization/companies/CompanyDetail.vue`
12. `client/src/views/organization/companies/CompanyFormDialog.vue`
13. `client/src/views/organization/companies/CompanyTree.vue`
14. `client/src/views/organization/departments/DepartmentTree.vue`
15. `client/src/views/organization/departments/DepartmentDetail.vue`
16. `client/src/views/organization/departments/DepartmentFormDialog.vue`
17. `client/src/views/organization/departments/DepartmentHead.vue`
18. `client/src/views/organization/employees/EmployeeList.vue`
19. `client/src/views/organization/employees/EmployeeDetail.vue`
20. `client/src/views/organization/employees/EmployeeFormDialog.vue`
21. `client/src/views/organization/employees/EmployeeOrgChart.vue`
22. `client/src/views/organization/employees/EmployeeImportDialog.vue`
23. `client/src/components/StatusTag.vue`
24. `client/src/components/TreeNode.vue`
25. `client/src/components/__tests__/StatusTag.test.ts`
26. `client/src/views/__tests__/organization.test.ts`

#### 修改文件（1 个）

27. `client/src/router/index.ts`（**仅追加** `...organizationRoutes`，**不删** /login + /dashboard）

**实际清单数**：26 新增 + 1 修改 = **27 个文件**（注：原 §1.4 概数 29 含 5 个测试文件，实际 5 个测试分散在 §1.2 列表中，最终 26+1=27）

### 6.2 禁止修改文件（清单以外）

- **后端任何文件**（**M5-2-A1 0 修改** server/）：
  - ❌ 严禁修改 `server/src/services/*` 任何 service（36 service 0 改动）
  - ❌ 严禁修改 `server/src/routes/*` 任何路由
  - ❌ 严禁修改 `server/prisma/schema.prisma`（0 新表）
  - ❌ 严禁修改 `server/src/constants/permissions.ts`（0 新权限点）
  - ❌ 严禁修改 `server/src/app.ts` / `server/src/index.ts`
  - ❌ 严禁修改 `server/src/controllers/*` 任何 controller
  - ❌ 严禁修改 `server/prisma/seed.ts`
  - ❌ 严禁修改 `docs/error-codes.md`（0 新错误码）
  - ❌ 严禁修改 `docs/api-spec.md`（M5-2-A1 0 新端点）
- **M5-2-0 共享基础设施文件**（除**追加**路由表）：
  - ❌ 严禁修改 `client/src/router/index.ts` 的 `/login` / `/dashboard` 路由（**仅追加** `...organizationRoutes`）
  - ❌ 严禁修改 `client/src/api/{http,auth,types}.ts`（**M5-2-A1 创建独立的 `api/types/organization.ts` + `api/{company,department,employee}.ts`**）
  - ❌ 严禁修改 `client/src/stores/{user,permission,dict}.ts`
  - ❌ 严禁修改 `client/src/layouts/DefaultLayout.vue`（**M5-2-A1 创建独立的 `OrganizationLayout.vue`**）
  - ❌ 严禁修改 `client/src/views/{Login,Dashboard}.vue`
  - ❌ 严禁修改 `client/src/utils/{auth,permission,format}.ts`
- **M5-2-A1 严禁修改**：
  - ❌ 严禁修改 `client/package.json`（**禁止装新依赖**）
  - ❌ 严禁修改 `client/vite.config.ts` / `tsconfig.json` / `.eslintrc.cjs`
  - ❌ 严禁创建 M5-2-A2 业务页面（A3 入职+转正+调动+离职+合同，**留独立任务**）
  - ❌ 严禁创建 M5-2-B/C/D 业务页面（**留独立任务**）
- 任何旧测试文件（`*.test.ts`）除非在 §6.1 清单内

### 6.3 越界检测（交付时自检）

```bash
git diff --name-only HEAD
# 必须只出现 §6.1 列出的 27 个文件
# 如果出现其他文件 → 越界，必须回退

# 特别检查：D1-D6 + C1-C8 共 32 service 0 行改动 + M0.5 4 service 0 行改动 = 36 service
git diff HEAD -- 'server/src/services/'
# 必须为 0 行

# 验证 0 修改后端任何文件
git diff HEAD -- 'server/'
# 必须 0 行

# 验证 0 修改 client/package.json
git diff HEAD -- 'client/package.json'
# 必须 0 行

# 验证 0 修改 M5-2-0 基础设施（除 router/index.ts 追加）
git diff HEAD -- 'client/src/api/http.ts' 'client/src/api/auth.ts' 'client/src/api/types.ts' 'client/src/stores/' 'client/src/layouts/DefaultLayout.vue' 'client/src/views/Login.vue' 'client/src/views/Dashboard.vue' 'client/src/utils/'
# 必须 0 行（除 router/index.ts 追加 organizationRoutes）

# 验证 0 修改 server/
git diff HEAD -- 'server/'
# 必须 0 行
```

---

## 7. 验收标准

### 7.1 硬性验收（必须 100% 通过）

```bash
# 在项目根 D:\projects\hrms

# 1. 后端测试（**0 改动**，沿用 876）
pnpm --filter hrms-server test
# 预期：876/876 通过

# 2. 后端 type-check
pnpm --filter hrms-server type-check
# 预期：0 错

# 3. 客户端 type-check
cd client && npx vue-tsc --noEmit
# 预期：EXIT_CODE: 0

# 4. 客户端 build
cd client && pnpm build
# 预期：dist 生成成功

# 5. 客户端 lint
cd client && pnpm lint
# 预期：0 error
```

### 7.2 验收明细

- [ ] **27 个 client 文件**（26 新增 + 1 修改）齐全
- [ ] **A1 公司 4 页面**（List/Detail/Form/Tree）渲染正确
- [ ] **A2 部门 4 页面**（Tree/Detail/Form/Head）渲染正确
- [ ] **A3 员工 5 页面**（List/Detail/Form/OrgChart/Import）渲染正确
- [ ] **OrganizationLayout 模块布局**（左侧子菜单 + 右侧 router-view）
- [ ] **5 角色 RBAC** 端点 + 菜单 + 操作按钮过滤正确（admin/hr 全开，dept_head/executive 只读，employee 无权限）
- [ ] **M1 后端 25 端点**消费正确（list / get / create / update / delete / suspend / reactivate / move / merge / employees / transfer / regularize / resign / export / import）
- [ ] **加密字段脱敏显示**（手机 / 身份证 / 银行卡）
- [ ] **0 修改后端任何文件**
- [ ] **0 修改 `client/package.json`**（不装新依赖）
- [ ] **0 修改 M5-2-0 共享基础设施**（除 `router/index.ts` 追加）
- [ ] **12-15 单元测试**通过（内联断言 + 导出 `run*Tests()`）
- [ ] **client type-check 0 错 + build 成功**

### 7.3 红线 9 验证

- [ ] **0 修改后端任何文件**（**仅 client/src**）
- [ ] **0 修改 `client/package.json`**（沿用 Element Plus + Pinia + Vue Router + Axios）
- [ ] **0 修改 M5-2-0 共享基础设施**（除 `router/index.ts` 追加 `...organizationRoutes`）
- [ ] **27 个 client 文件**（26 新增 + 1 修改，**不超出清单**）
- [ ] **D1-D6 + C1-C8 业务代码 0 行改动**（36 service + 40 表 = 0 行）
- [ ] **M0.5 4 service 0 行改动**
- [ ] **复用 M5-2-0 通用组件 + Pinia stores + http + utils**
- [ ] **5 角色 RBAC 严格遵守**（admin / hr / dept_head / executive / employee，**无 finance 角色**）
- [ ] **12-15 单元测试**（内联断言 + 导出 `run*Tests()`，**不修改任何旧测试**）

### 7.4 原有用例未改（硬要求）

```bash
git diff HEAD -- 'server/src/**/*.test.ts' 'client/src/**/*.test.ts' | grep -E '^[+-]' | grep -v '^[+-]{3}' | grep -v 'company\.test|department\.test|employee\.test|StatusTag\.test|organization\.test'
# 必须为 0 行（后端旧测试 100% 未改，前端仅新增 5 个测试文件）
```

---

## 8. 禁止做的事（额外禁忌清单）

1. ❌ 做 M5-2-A2（M3 入职+转正+调动+离职+合同，**留独立任务**）
2. ❌ 做 M5-2-B / M5-2-C / M5-2-D（**留独立任务**）
3. ❌ 做 M5-3 / M5-4 / M5-5 / M5-6 / M5-7 / M5-8 任何切片
4. ❌ 修改后端任何文件（**0 修改 server/**）
5. ❌ 修改 `client/package.json`（**禁止装新依赖**）
6. ❌ 修改 `client/vite.config.ts` / `tsconfig.json` / `.eslintrc.cjs`
7. ❌ 修改 M5-2-0 共享基础设施文件（除 `router/index.ts` 追加 `...organizationRoutes`）
8. ❌ 顺手接 Element Plus Pro / Ant Design Vue / Naive UI
9. ❌ 顺手做国际化 i18n
10. ❌ 顺手做主题切换
11. ❌ 顺手做实时通知（WebSocket / SSE）
12. ❌ 顺手装 unplugin-vue-components / pinia-plugin-persistedstate / vee-validate / vueuse 等
13. ❌ 创建 M1 后端新表 / 新 service / 新权限点
14. ❌ 修改 `server/src/services/*` 任何 service（36 service 0 改动）
15. ❌ 修改 `server/prisma/schema.prisma`（0 新表）
16. ❌ 修改 `server/src/constants/permissions.ts`（0 新权限点）
17. ❌ 修改 `docs/error-codes.md`（0 新错误码）
18. ❌ 修改 `docs/api-spec.md`（M5-2-A1 0 新端点）
19. ❌ 创建 M1-M4 后端功能（**仅消费 M1 已有 25 端点**）
20. ❌ 创建 M5-2-A2 业务页面（**留 M5-2-A2 独立任务**）
21. ❌ 改 D1-D6 + C1-C8 任何 service（**不重做 M1-M4**）
22. ❌ 改 M0.5-1/2/5/6 任何 service（**不重做 M0.5**）
23. ❌ 改 M1 / M2 / M3 / M4 任何 service（**不重做**）
24. ❌ 修改任何后端已存在的 .ts 文件（除 §6.1 清单内，但 §6.1 仅 client/）
25. ❌ 修改任何旧测试文件
26. ❌ 越界做 6 角色 RBAC（M0 锁定 5 角色：admin / hr / dept_head / executive / employee，**无 finance**）
27. ❌ 用 Options API / class 组件（**仅 Vue3 Composition API + `<script setup lang="ts">`**）
28. ❌ 用 `<button>` / `<input>` 原生 HTML（**仅 Element Plus 组件**）
29. ❌ 用 `any` 类型（**严格 TypeScript**）
30. ❌ "我觉得这也有用"（任何"顺便做"越界内容一律禁止）
31. ❌ 重写 notification / configService / aiSummary
32. ❌ 给 finance 角色分配权限（**D1 教训延续**）
33. ❌ 越界实现 M1-M4 后端功能（**M5-2-A1 仅消费 25 端点**）

---

## 9. 完成标准

### 9.1 必跑命令（全部通过才交付）

```bash
cd D:\projects\hrms

# 1. 后端测试（**0 改动**，沿用 876）
pnpm --filter hrms-server test
# 预期：876/876 通过

# 2. 后端 type-check
pnpm --filter hrms-server type-check
# 预期：0 错

# 3. 客户端 type-check
cd client && npx vue-tsc --noEmit
# 预期：EXIT_CODE: 0

# 4. 客户端 build
cd client && pnpm build
# 预期：dist 生成成功

# 5. 客户端 lint
cd client && pnpm lint
# 预期：0 error
```

### 9.2 越界自检（必查）

```bash
# 1. 越界文件检测
git diff --name-only HEAD
# 预期：§6.1 列出 27 个文件（26 新增 + 1 修改）

# 2. 36 service 0 行改动检测
git diff HEAD -- 'server/src/services/'
# 预期：0 行

# 3. 0 修改后端任何文件检测
git diff HEAD -- 'server/'
# 预期：0 行

# 4. 0 修改 client/package.json 检测
git diff HEAD -- 'client/package.json'
# 预期：0 行

# 5. 0 修改 M5-2-0 共享基础设施检测（除 router/index.ts）
git diff HEAD -- 'client/src/api/http.ts' 'client/src/api/auth.ts' 'client/src/api/types.ts' 'client/src/stores/' 'client/src/layouts/DefaultLayout.vue' 'client/src/views/Login.vue' 'client/src/views/Dashboard.vue' 'client/src/utils/'
# 预期：0 行

# 6. 客户端 type-check
cd client && npx vue-tsc --noEmit
# 预期：EXIT_CODE: 0

# 7. 客户端 build
cd client && pnpm build
# 预期：dist 生成成功

# 8. 旧测试 0 改动检测
git diff HEAD -- 'server/src/**/*.test.ts' 'client/src/**/*.test.ts' | grep -E '^[+-]' | grep -v '^[+-]{3}' | grep -v 'company\.test|department\.test|employee\.test|StatusTag\.test|organization\.test'
# 预期：0 行
```

### 9.3 交付报告（Cursor 输出格式）

请在交付时输出以下内容（**M5-2-0 交付报告格式**）：

```
# M5-2-A1 交付报告

M5-2-A1 已落地：M1 组织人事前端 · 基础数据（A1 法人公司 + A2 部门 + A3 员工档案）。0 修改后端 / 0 修改 package.json / 0 新业务 service，未改 D1-D6 / C1-C8 / M0.5 业务代码。未提交 commit。

## 改动文件清单（X 新增 + X 修改 = X 个）

**新增（X）**：
1. client/src/router/organization.ts
2. client/src/layouts/OrganizationLayout.vue
3. client/src/api/types/organization.ts
4. client/src/api/company.ts
5. client/src/api/department.ts
6. client/src/api/employee.ts
7. client/src/api/__tests__/company.test.ts
8. client/src/api/__tests__/department.test.ts
9. client/src/api/__tests__/employee.test.ts
10. client/src/views/organization/companies/CompanyList.vue
11. client/src/views/organization/companies/CompanyDetail.vue
12. client/src/views/organization/companies/CompanyFormDialog.vue
13. client/src/views/organization/companies/CompanyTree.vue
14. client/src/views/organization/departments/DepartmentTree.vue
15. client/src/views/organization/departments/DepartmentDetail.vue
16. client/src/views/organization/departments/DepartmentFormDialog.vue
17. client/src/views/organization/departments/DepartmentHead.vue
18. client/src/views/organization/employees/EmployeeList.vue
19. client/src/views/organization/employees/EmployeeDetail.vue
20. client/src/views/organization/employees/EmployeeFormDialog.vue
21. client/src/views/organization/employees/EmployeeOrgChart.vue
22. client/src/views/organization/employees/EmployeeImportDialog.vue
23. client/src/components/StatusTag.vue
24. client/src/components/TreeNode.vue
25. client/src/components/__tests__/StatusTag.test.ts
26. client/src/views/__tests__/organization.test.ts

**修改（X）**：
27. client/src/router/index.ts（**仅追加** organizationRoutes，**不删** /login + /dashboard）

**未改（按约束）**：
- D1-D6 + C1-C8 共 32 service（diff = 0）
- M0.5 4 service（approval / notification / config / ai/score，diff = 0）
- server/ 任何文件（diff = 0）
- client/package.json（diff = 0）
- client/vite.config.ts / tsconfig.json / .eslintrc.cjs（diff = 0）
- M5-2-0 共享基础设施（除 router/index.ts 追加）

## 7 条强制声明

1. 未修改任何 M0/M0.5/M1/M2/M3/C1-C8 旧测试（仍 876 个）
2. 新单测 X 个（内联断言 + 导出 `run*Tests()`）
3. 未做 M5-2-A2 / M5-2-B/C/D / M5-3-M5-8 任何切片
4. 未 import 跨业务 service（**M5-2-A1 0 新 service**）
5. 5 角色 RBAC 严格遵守（admin / hr / dept_head / executive / employee，**无 finance**）
6. **0 修改后端 / 0 修改 package.json / 0 新业务 service**（纯前端切片）
7. **D1-D6 + C1-C8 + M0.5 业务代码 0 行改动**（36 service diff = 0）

## 红线 9 自检

- [x] 0 修改后端任何文件
- [x] 0 修改 client/package.json
- [x] 0 修改 M5-2-0 共享基础设施（除 router/index.ts 追加）
- [x] 27 个 client 文件（26 新增 + 1 修改，**不超出清单**）
- [x] 32 service 0 行改动
- [x] M0.5 4 service 0 行改动
- [x] 复用 M5-2-0 通用组件 + Pinia stores + http + utils
- [x] 5 角色 RBAC 严格遵守（**无 finance 角色**）
- [x] 12-15 单元测试（内联断言，**不引入 vitest**）

## 验收命令输出

| 命令 | 结果 |
|---|---|
| pnpm --filter hrms-server test | 876/876 通过（**0 改动**） |
| pnpm --filter hrms-server type-check | 0 错 |
| npx vue-tsc --noEmit（client） | EXIT_CODE: 0 |
| pnpm build（client） | dist 生成成功 |
| pnpm lint（client） | 0 error（XX warnings） |

单测分布：company.test X + department.test X + employee.test X + StatusTag.test X + organization.test X = X（12-15）。

## 越界检查

git diff --name-only HEAD = §6.1 列出 27 个文件，0 越界。
server/ 任何文件 diff = 0。
client/package.json diff = 0。
M5-2-0 共享基础设施 diff = 0（除 router/index.ts 追加）。
旧 *.test.ts diff 为空。
5 角色无 finance 角色（utils/permission.ts 仅含 5 个 RoleCode）。

## 已知问题

（如有边界场景未覆盖 / TODO 留二期 / mock 限制等，请详细说明）
```

---

## 10. 参考文档

- **V1.2 主文档** §四.5 A1+A2+A3：https://example.com/HRMS-V1.2.md#四-5-a1-a2-a3-组织人事
- **V1.2 主文档** §三.6 RBAC：https://example.com/HRMS-V1.2.md#三-6-rbac-权限模型
- **M5-2-0 业务 commit** 631992a（共享基础设施 30 文件 / 12 单测）：https://example.com/commit/631992a
- **M5-2-0 提示词** docs/cursor-prompts/M5-2-0.md（本任务基础设施参考）
- **M1-A1-A2 commit** 265316b（后端 A1+A2 25 端点）：https://example.com/commit/265316b

---

**任务开始**：M5-2-A1 业务实现 = 4 子任务 + 13 页面 + 25 端点消费 + 0 新增后端端点 + 27 个 client 文件 + 12-15 单测 + 0 修改后端 + 0 修改 package.json + 36 service 0 行改动 + 5 角色无 finance。
