# Cursor 提示词：M2-B3 请假（8 类假期 + 额度管理 + 走 M0.5-1 审批流）

> **使用方式**：复制下方【开始复制 ↓】到【结束复制 ↑】之间全部内容，粘贴到 Cursor。
> **预期结果**：B3 请假完整实现，1 个 commit 落地，314 → 335 测试。
> **审查清单**：复制完成后，由我按"7 项验收清单"逐条核对。
> **依赖前置**：M1 全部 7 切片 + M0.5 全部 7 切片 + M2-B1 班次定义（ed23a62）+ M2-B2 打卡（f390fe1）。
>
> **优化理念**：本提示词**故意写得详尽**（沿用 A5/A6/A7/B1/B2 验证通过的 45KB 模式）。
> 关键决策点全部锁死，Cursor 一次到位，token 反而少。

---

## 开始复制 ↓

```
⚠️ 强约束（最优先阅读）— 此段必须在最顶部

历史教训：
- M0.5-5 任务中，Cursor 越界做了 M0.5-6（配置中心）+ M0.5-7（改密 + 2FA）共 2 个
  不在提示词范围内的切片，并修改了 4 个旧测试文件 + 在报告中虚假陈述"原有用例未改"。
- M1 + M2-B1/B2 全部 0 越界（用户验收通过），本次严禁重蹈 M0.5-5 覆辙。
- A6 第一次"瘦身 53%"（15KB）是错误方向；A5/A6/A7/B1/B2 沿用 45KB 详尽模式验证成功。
  本提示词**沿用 45KB 详尽模式**，**禁止再次瘦身**。

红线（违反任一 → 任务失败，需回退重做）：

1. 禁止越界做其他切片：本提示词只授权 "M2-B3（请假）" 单切片，1 个 commit。
   严禁自作主张做 B1/B2（已实现）/ B4（加班）/ B5（出差）/ B6（月度汇总），
   不做移动端 / 前端 / 任何业务功能。

2. 禁止修改未授权文件：本提示词在"修改文件清单"中列出的文件以外的文件全部禁止改动。
   包括但不限于：
   - server/src/services/{auth,approval,notification,crypto,encryptedField,integration,
     ai,config,audit,company,department,employee,employeeAI,onboarding,
     regularization,offboarding,transfer,contract,shift,attendance}/* 任何已有文件
   - server/src/controllers/ 已有 18 个文件全部黑名单
   - server/src/routes/ 已有 18 个文件全部黑名单
   - server/src/middleware/* / server/src/jobs/* / server/src/integrations/**
   - client/ 或 mobile/ 任何文件
   - 任何已有测试文件（*.test.ts，包括 M0/M0.5/M1/M2-B1/M2-B2 所有 314 个用例）
   - 任何已有 migration

3. **错误码段位调整**：B3 错误码用 **72001-72010**（5 位十进制，72 段位）。
   71 段位 71001-71999 已被 M1 + B1 + B2 用完（71 段位最大 999），B3 跨到 72 段位。
   注意：与 M0 阶段 10xxx-12xxx、M0.5 阶段 20xxx-70xxx 不冲突。

4. 必须复用既有能力（**严禁重写 / 严禁改源**）：
   - 审批流：调 approval.submitApproval / withdraw（**B3 请假强依赖**，按 ≤3 天 / >3 天 选 flowKey）
   - 通知：调 notification.sendNotification（**bypassTemplate fallback**，参考 A4/A5/A6/A7/B1/B2 模式）
   - 字段加密：调 crypto.encrypt / decrypt（如有新增字段）
   - 配置读取：调 configService.getValue（已 export）
   - 审计：调 auditService.auditLog（已 export）
   - 员工关联：通过 prisma.employee.findFirst（**不调 employee.service**）
   - 部门关联：通过 prisma.department.findUnique（**不调 department.service**）
   - 假期额度计算：service 层 `calculateLeaveBalance` 函数（V1.2 §四.6.2 关键要求）
   严禁在 B3 中重写任何以上逻辑。如发现"必须改 B1/B2 才能跑通"，**立即停下并报告**。

5. **不 import 跨 service**（A4/A5/A6/A7/B1/B2 红线沿用）：
   leave.service.ts 的 import 只允许：
   - './approval.service'
   - './audit.service'
   - './config.service'
   - './notification.service'
   - '../middleware/errorHandler' (AppError)
   - '../lib/prisma' (prisma)
   - '@prisma/client' (Prisma 类型)
   严禁 import './shift.service' / './attendance.service' / './employee.service'
   / './department.service' / './company.service' / './onboarding.service'
   / './regularization.service' / './offboarding.service' / './transfer.service'
   / './contract.service' / './employeeAI.service' / './auth.service'。

6. 禁止修改旧测试文件：本任务 B3 涉及的新测试，**只新建**
   `leave.service.test.ts`，**严禁修改**任何已有测试。

7. **B3 范围严格界定**（V1.2 §二.3.3 + §四.6.1）：
   - 8 类假期（年假/病假/事假/调休/婚假/产假/陪产假/丧假）—— **B3 实现**
   - 假期额度管理（按工龄的年假规则 / 调休有效期 6 个月）—— **B3 实现**
   - 请假审批流（≤3 天：员工→直属→HR；>3 天：员工→直属→部门→HR→总经理）—— **B3 实现**
   - 余额校验（年假/调休自动校验，余额不足禁止提交）—— **B3 实现**
   - 撤回 / 审批回调—— **B3 实现**
   - **不实现：**
     - 加班（B4 范围）
     - 出差（B5 范围）
     - 月度汇总（B6 范围）
     - BullMQ 假期自动重置（每年 1 月 1 日，留独立任务）
     - BullMQ 调休到期清理（6 个月有效期到期，留独立任务）
     - 假期额度表持久化（**B3 余额不存表**，仅 service 函数计算 + configs 配置，
       **V1.2 §四.6.2 关键要求**："假期额度计算放在 service 层单独函数"）

8. **5 端点约束**（V1.2 §四.6.1）：
   - POST /api/leaves/requests              提交请假申请
   - GET  /api/leaves/requests              请假记录列表
   - GET  /api/leaves/requests/:id          请假详情
   - POST /api/leaves/requests/:id/cancel   撤回
   - GET  /api/leaves/balance               假期余额查询（employeeId, leaveType, year）

9. **审批流不暴露独立端点**（V1.2 §二.3.3）：
   - 提交 / 审批 / 驳回**不暴露独立请假端点**
   - 提交：POST /api/leaves/requests 触发
   - 审批节点操作：/api/approval-instances/:id/approve（已存在）
   - 审批回调：confirmLeaveRequest / rejectLeaveRequest（系统内部）

10. 必须报告完整：交付报告必须列出**全部**新增 / 修改文件（即使是 0 个也要说"0 个"），
    并明确 7 条声明（旧测试未改 / 新单测数 / 未做 B1/B2（已实现）/ B4-B6 /
    不 import 跨 service / 通知走 bypassTemplate / 余额不存表仅 service 计算）。

11. 禁止"我觉得这也有用"：任何"顺便做"或"我觉得可以优化"的越界内容一律禁止。
    只做本提示词明确要求的 B3 内容。

越界检测（Cursor 自查清单，跑完前自查）：
- [ ] 改动文件数 ≤ 18？超过 → 越界
- [ ] 没有改动 M0/M0.5/M1/M2-B1/M2-B2 全部 21 个 service / controller / routes？改了 → 越界
- [ ] 没有改 314 个旧测试文件？改了 → 越界
- [ ] 没有做 B1/B2（已实现）/ B4/B5/B6 任何业务？做了 → 越界
- [ ] leave.service.ts 仅 import 黑名单以外的 service？黑名单内 import → 越界
- [ ] **没有新建 leave_balances 表**（余额不存表，service 函数计算）？建了 → 越界
- [ ] 通知 / 加密 / 配置 / 审计 4 类能力是调用已有 export，没自己重写？重写 → 越界
- [ ] 报告里 7 条声明齐全？声明虚假 → 越界

---

【任务 ID】M2-B3
【任务目标】实现 M2-B3（请假）单切片，1 个 commit 落地
【依赖前置】M1 全部 7 切片 + M0.5 全部 7 切片 + M2-B1/B2 班次 + 打卡

【上下文】
- 项目位置：D:/projects/hrms/
- 当前状态：M0/M0.5/M1/M2-B1/B2 全部已 commit，314/314 测试通过；M2-B3 启动
- 主文档：docs/HRMS-V1.2.md §二.3.3 假勤管理（8 类假期 + 额度规则 + 审批流）
  + §四.6.1 B3 切片（5 子任务 / 3.5d）+ §四.6.2 关键约束（**假期额度计算放在 service 层**）
  + §三.5.2 业务规则配置化
- API 契约：docs/api-spec.md §3.15 待补 B3 端点（Cursor 任务里补）
- 错误码：docs/error-codes.md §7xxxx 通用业务错误码（**B3 用 72001-72010**，72 段位）
- 数据模型：docs/flow-diagrams.md §3.2 ER 图
- M2 启动建议：docs/cursor-prompts/M1-wrap-up.md §7

【B3 业务背景（V1.2 §二.3.3）】
- 8 类假期（leaveType 枚举）：
  - `annual`（年假）— 按工龄 1-10 年 5 天，10-20 年 10 天，>20 年 15 天，带薪
  - `sick`（病假）— 按医疗期规定，按当地最低工资 80% 计
  - `personal`（事假）— 不限，无薪
  - `compensatory`（调休）— 加班 1:1 累计，**有效期 6 个月**，带薪
  - `marriage`（婚假）— 3 天 + 各地奖励婚假，带薪
  - `maternity`（产假）— 按各地政策，带薪
  - `paternity`（陪产假）— 按各地政策，带薪
  - `bereavement`（丧假）— 1-3 天（直系亲属），带薪
- 请假审批流（走 M0.5-1 审批流）：
  - **≤3 天**：员工 → 直属上级 → HR 备案
  - **>3 天**：员工 → 直属上级 → 部门负责人 → HR → 总经理
- 年假/调休：系统自动校验余额，**余额不足禁止提交**

【B3 业务规则必走 configs 表（V1.2 §三.5.2 强约束）— 9 类配置】

| 配置键 | 类型 | 默认值 | 用途 |
|---|---|---|---|
| configs.leave.types | string[] | 见上 8 类 | 假期类型枚举 |
| configs.leave.annual_leave_rules | object | { 1-10: 5, 10-20: 10, '>20': 15 } | 按工龄的年假天数规则 |
| configs.leave.comp_leave_validity_months | number | 6 | 调休有效期（月） |
| configs.leave.approval_flow_short | string | 'leave:leave_short' | ≤3 天审批流 key |
| configs.leave.approval_flow_long | string | 'leave:leave_long' | >3 天审批流 key |
| configs.leave.max_consecutive_days | number | 30 | 最长连续请假天数 |
| configs.leave.min_advance_days_annual | number | 7 | 年假最少提前申请天数 |
| configs.leave.workday_exclude_weekends | boolean | true | 工作日计算排除周末 |
| configs.leave.sick_leave_max_days | number | 90 | 病假最长天数（医疗期参考） |

**这些值先用 M0.5-6 configService 读取，如果 config 表没有就 hardcoded fallback +
TODO 注释**。不要直接 hardcoded 写死（业务规则强约束）。

【必读约束】（本切片差异点，模板同 A3/A4/A5/A6/A7/B1/B2）
1. 代码范式：export function 导出（参考 attendance.service.ts / shift.service.ts）
2. 所有方法含 JSDoc
3. 错误统一抛 AppError(message, statusCode, code)，code 用 72001-72010 段位
4. 业务规则走 configService 读取
5. **不 import 跨 service**（A4/A5/A6/A7/B1/B2 红线沿用，见强约束第 5 条）
6. 通知走 bypassTemplate fallback（notification.service.ts 无 TEMPLATE_KEYS）
7. **B3 不实现加班 / 出差 / 月度汇总**（B4-B6 范围）
8. **假期余额不存表**（V1.2 §四.6.2），仅 `calculateLeaveBalance` service 函数计算
9. 员工 / 部门关联走 prisma 直接查询（不 import 跨 service）
10. 审计必写：create/cancel/confirm/reject 必写 audit_logs（actor_type=USER）
11. 状态机严格：draft → submitted → approved / rejected / cancelled，不可跳级
12. 不实现 BullMQ 定时任务（**余额自动重置 / 调休到期清理留独立任务**）
13. 不实现前端，只做后端 service + controller + routes + 单测
14. 不动 314 个已有测试文件
15. 不修改 docs/HRMS-V1.2.md 的现有内容（**只允许在 §七.3 变更记录追加新行** +
    §四.6.1 B3 切片行追加"已实现"标记）
16. 不修改 AGENTS.md 现有内容（**只允许在 M2-B2 进度段之后追加 B3 描述**）
17. 不修改 M0.5 + M1 + M2-B1 + M2-B2 任何 service / controller / routes

【实施任务 - 严格按 13 步顺序】

### 任务 1: 更新 Prisma schema（+ 1 张表 + 索引）

**目标文件**：server/prisma/schema.prisma

**新增模型**（**完整定义**，不要"列关键字段"）：

```prisma
// ==================== M2-B3 请假 ====================
// 状态机：draft → submitted → approved / rejected / cancelled
// 关联 employee（A2 已有）+ approvalInstance（M0.5-1 已有）
// **假期余额不存表**（V1.2 §四.6.2 关键要求），仅 service 函数 calculateLeaveBalance 计算
model LeaveRequest {
  id            String   @id @default(uuid()) @db.Uuid
  // 关联员工
  employeeId    String   @map("employee_id") @db.Uuid
  companyId     String   @map("company_id") @db.Uuid
  departmentId  String   @map("department_id") @db.Uuid
  // 假期类型
  leaveType     String   @map("leave_type") @db.VarChar(30)
  // annual / sick / personal / compensatory / marriage / maternity / paternity / bereavement
  // 请假时间
  startDate     DateTime @map("start_date") @db.Date
  endDate       DateTime @map("end_date") @db.Date
  // 工作日（排除周末，service 函数 calculateWorkingDays 计算）
  totalDays     Decimal  @map("total_days") @db.Decimal(4, 1)
  // 请假原因
  reason        String?  @db.Text
  // 附件（病假/婚假/产假可能需证明）
  attachments   Json?    // [{ name, url, type, size }]（简化，与 M1-A7 附件同模式）
  // 状态机
  status        String   @default("draft") @db.VarChar(20)
  // draft / submitted / approved / rejected / cancelled
  // 审批流（M0.5-1）
  approvalInstanceId String? @unique @map("approval_instance_id") @db.Uuid
  // 审批结果
  approvedAt    DateTime? @map("approved_at") @db.Timestamptz(6)
  approvedBy    String?  @map("approved_by") @db.Uuid
  rejectedAt    DateTime? @map("rejected_at") @db.Timestamptz(6)
  rejectedBy    String?  @map("rejected_by") @db.Uuid
  rejectedReason String? @map("rejected_reason") @db.Text
  // 取消
  cancelledAt   DateTime? @map("cancelled_at") @db.Timestamptz(6)
  cancelledBy   String?  @map("cancelled_by") @db.Uuid
  cancelledReason String? @map("cancelled_reason") @db.Text
  // 元数据
  createdBy     String?  @map("created_by") @db.Uuid
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt     DateTime @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@index([employeeId, status])
  @@index([companyId, status, startDate])
  @@index([departmentId, status, startDate])
  @@index([status, startDate])
  @@index([leaveType, status])
  @@map("leave_requests")
}
```

**注意**：
- **不创建 leave_balances 表**（V1.2 §四.6.2 要求余额 service 函数计算）
- 不加 Employee / Department 反向 relation（避免改 A2 model）
- 5 个 index 覆盖：employee 维度 / 时间维度 / company 维度 / department 维度 / 类型维度

### 任务 2: 创建 migration

**新建文件**：
server/prisma/migrations/20260829070000_add_leave/migration.sql

参考 B2 风格（CREATE TABLE + indexes + foreign keys），用 IF NOT EXISTS 幂等。
包含 1 张表 + 5 个 index + 外键。

### 任务 3: 跑 db:generate

```bash
cd D:/projects/hrms
pnpm --filter hrms-server db:generate
```

### 任务 4: 写 leave.service.ts（核心业务）

**新建**：server/src/services/leave.service.ts（约 800-900 行）

**业务函数**（**B3 特有，详细列参数 + 返回值 + 关键调用**）：

```typescript
// ==================== 1. 提交请假申请 ====================
/**
 * 提交请假申请（draft → submitted）
 * @param input { employeeId, leaveType, startDate, endDate, reason?, attachments? }
 * @param operatorId 申请人 user.id
 * @returns LeaveRequest（status='submitted'，已触发审批）
 *
 * 校验链：
 *  1. employee 存在 → 71201
 *  2. leaveType 在 8 类枚举中 → 72003
 *  3. startDate < endDate（>= today）→ 72002
 *  4. endDate - startDate <= configs.leave.max_consecutive_days（默认 30 天）→ 72008
 *  5. 调 calculateWorkingDays（排除周末，configs.leave.workday_exclude_weekends）→ totalDays
 *  6. **年假 / 调休自动校验余额**（leaveType='annual'|'compensatory'）：
 *     - 调 calculateLeaveBalance(employeeId, leaveType, year)
 *     - remainingDays < totalDays → 72004
 *  7. **年假最少提前申请**（leaveType='annual'）：
 *     - startDate - today < configs.leave.min_advance_days_annual（默认 7 天）→ 72009
 *     - 例外：startDate <= today（病假/事假/调休/紧急年假，不限制）
 *  8. **请假区间重叠校验**（同员工同区间不能有 2 个 active 申请）→ 72005
 *     - 调 prisma.leaveRequest.findMany({ where: { employeeId, status: { notIn: ['rejected','cancelled'] },
 *       OR: [{ startDate: { lte: endDate } }, { endDate: { gte: startDate } }] } })
 *  9. 调 approval.submitApproval（**按 totalDays 选 flowKey**）：
 *     - totalDays <= 3：flowKey = configs.leave.approval_flow_short（fallback 'leave:leave_short'）
 *       nodes: ['direct_leader', 'hr']
 *     - totalDays > 3：flowKey = configs.leave.approval_flow_long（fallback 'leave:leave_long'）
 *       nodes: ['direct_leader', 'department_leader', 'hr', 'ceo']
 *  10. 状态：draft → submitted
 *  11. 通知员工 + 直属上级
 *  12. 审计 action=CREATE
 */
export async function createLeaveRequest(
  input: CreateLeaveRequestInput,
  operatorId: string
): Promise<LeaveRequest> { /* ... */ }

// ==================== 2. 单查 / 列表 ====================
/**
 * 单查（含 employee 关联）
 */
export async function getLeaveRequestById(id: string): Promise<LeaveRequest> { /* ... */ }

/**
 * 列表（分页 + employeeId/companyId/departmentId/leaveType/status/dateRange 过滤）
 */
export async function listLeaveRequests(query: ListLeaveRequestsQuery): Promise<{
  data: LeaveRequest[];
  total: number;
  page: number;
  pageSize: number;
}> { /* ... */ }

// ==================== 3. 撤回 ====================
/**
 * 撤回（draft / submitted 状态可撤回）
 * - draft 状态：直接 cancelled
 * - submitted 状态：调 approval.withdraw(approvalInstanceId) → cancelled
 * - approved 状态：→ 72007（已批准不可撤回；如需"销假"留独立流程）
 * - rejected 状态：→ 72007
 *
 * 审计 action=CANCEL
 */
export async function cancelLeaveRequest(
  id: string,
  reason: string,
  operatorId: string
): Promise<LeaveRequest> { /* ... */ }

// ==================== 4. 审批回调 ====================
/**
 * 审批通过回调（submitted → approved）—— 系统内部调用
 * @param id leave_request id
 * @param approvalResult { approved: boolean, instanceId?: string }
 * @param operatorId 审批人 user.id
 *
 * 流程：
 *  1. record 存在 → 72001
 *  2. status === 'submitted' → 否则 72006
 *  3. approvalResult.approved:
 *    - 状态：submitted → approved
 *    - approvedAt / approvedBy 写入
 *    - 通知员工
 *  4. !approvalResult.approved:
 *    - 状态：submitted → rejected
 *    - rejectedAt / rejectedBy / rejectedReason 写入
 *    - 通知员工
 *  5. 审计 action=APPROVE / REJECT
 */
export async function confirmLeaveRequest(
  id: string,
  approvalResult: { approved: boolean; instanceId?: string },
  operatorId: string
): Promise<LeaveRequest> { /* ... */ }

// ==================== 5. 假期余额查询（V1.2 §四.6.2 关键函数）====================
/**
 * 假期余额查询（不存表，service 函数计算）
 * @param employeeId 员工
 * @param leaveType 假期类型
 * @param year 年份（默认当前年）
 * @returns { totalDays, usedDays, remainingDays, expiryDate? }
 *
 * 计算规则：
 *  - annual（年假）：
 *    - totalDays = 按工龄查 configs.leave.annual_leave_rules
 *    - usedDays = prisma.leaveRequest.count({ employeeId, leaveType: 'annual',
 *      year, status: 'approved' })
 *    - expiryDate = year + 1 / 12 / 31（**B3 不实现**年假过期，V1.2 §二.3.3 未提）
 *  - compensatory（调休）：
 *    - totalDays = prisma.attendanceRecord 加班累计（**B3 不实现**，留 B4 加班后）
 *      **B3 简化**：totalDays = 0，提示"调休余额请到 B4 加班后查询"
 *    - **B3 留 TODO 注释**
 *  - 其他假期（sick/personal/marriage/maternity/paternity/bereavement）：
 *    - totalDays = 999（不限或按政策）
 *    - usedDays = 0
 *    - remainingDays = 999
 */
export async function calculateLeaveBalance(
  employeeId: string,
  leaveType: string,
  year: number = new Date().getFullYear()
): Promise<LeaveBalance> { /* ... */ }

// ==================== 6. 内部：工作日计算 ====================
/**
 * 内部：工作日计算（排除周末，configs.leave.workday_exclude_weekends）
 * @param startDate 开始日期
 * @param endDate 结束日期（含）
 * @returns 工作日天数
 */
function calculateWorkingDays(
  startDate: Date,
  endDate: Date
): number { /* ... */ }

// ==================== 7. 内部：区间重叠校验 ====================
/**
 * 内部：请假区间重叠校验
 * @param employeeId
 * @param startDate 新申请开始
 * @param endDate 新申请结束
 * @returns boolean 是否有重叠
 */
function hasOverlappingLeave(
  employeeId: string,
  startDate: Date,
  endDate: Date
): Promise<boolean> { /* ... */ }

// ==================== 8. BullMQ 调用：即将请假 ====================
/**
 * 给 BullMQ 调用的"即将请假"列表
 * @param days N 天内开始
 * @returns status='approved' AND startDate BETWEEN now AND now+days 的请假
 */
export async function listUpcomingLeaves(days: number): Promise<LeaveRequest[]> { /* ... */ }
```

**集成调用**（**严禁重写 / 严禁 import 跨 service**）：
- `approvalService.submitApproval({ flowKey, businessType, businessId, initiatorId, title, data })`
  - B3 强依赖：按 totalDays 选 short / long flowKey
- `approvalService.withdraw({ instanceId })`
- `notificationService.sendNotification({ ..., bypassTemplate: { ... } })`
  - B3 走 bypassTemplate fallback（参考 A4/A5/A6/A7/B1/B2 模式）
- `configService.getValue('leave', '*')` 9 类配置
- `auditService.auditLog({ action, resourceType, resourceId, description, actorId, actorType })`

**员工 / 部门关联**（**通过 prisma 直接查询，不调 service**）：
- `prisma.employee.findFirst({ where: { id, deletedAt: null } })`
- `prisma.department.findUnique({ where: { id } })`
- `prisma.company.findUnique({ where: { id } })`
- `prisma.leaveRequest.findMany / findFirst / count`（直接查询）

**错误码段位 72001-72010**（**B3 专用**，**含触发条件**）：

| code | HTTP | 名称 | 触发条件 | 客户端处理 |
|---|---|---|---|---|
| 72001 | 404 | LEAVE_NOT_FOUND | leave_request id 不存在 | 提示"请假记录不存在" |
| 72002 | 400 | LEAVE_INVALID_DATE_RANGE | startDate >= endDate / 早于 today | 提示"请假日期不合法" |
| 72003 | 400 | LEAVE_INVALID_TYPE | leaveType 不在 8 类枚举 | 提示"假期类型不合法" |
| 72004 | 422 | LEAVE_INSUFFICIENT_BALANCE | 年假/调休余额不足 | 提示"假期余额不足" |
| 72005 | 409 | LEAVE_OVERLAPPING | 同员工同区间已有 active 申请 | 提示"请假区间重叠" |
| 72006 | 409 | LEAVE_NOT_PENDING | record 状态非 submitted | 提示"请假已审批" |
| 72007 | 409 | LEAVE_NOT_CANCELLABLE | approved/rejected 状态撤回 | 提示"已批准/已拒绝不可撤回" |
| 72008 | 400 | LEAVE_TOO_LONG | endDate - startDate > 30 天 | 提示"单次请假不能超过 30 天" |
| 72009 | 400 | LEAVE_TOO_ADVANCE | 年假未提前 7 天申请 | 提示"年假需提前 7 天申请" |
| 72010 | 500 | LEAVE_BALANCE_CALCULATION_ERROR | 余额计算异常（如工龄缺失） | 提示"余额计算失败" |

### 任务 5: 写 controller + routes

**新建 2 个文件**：
- server/src/controllers/leave.controller.ts
- server/src/routes/leave.ts

**端点清单**（5 端点，§3.15 Leave API）：

```
# Leave（5 端点）
POST   /api/leaves/requests              提交请假申请（draft → submitted）
GET    /api/leaves/requests              请假记录列表
GET    /api/leaves/requests/:id          请假详情
POST   /api/leaves/requests/:id/cancel   撤回
GET    /api/leaves/balance               假期余额查询（query: employeeId, leaveType, year?）
```

**注意**：
- 审批通过 / 拒绝**不暴露独立端点**，由 approval callback 触发 confirmLeaveRequest
- 审批节点操作走 /api/approval-instances/:id/approve（已存在）
- 所有路由都用 Zod 校验（参考 server/src/routes/attendance.ts 风格）
- 所有端点都加 authenticate + rejectIfMustChangePassword + requirePermission

**权限点**（新增 3 个）：
- LEAVE_READ: 'leave:read' → admin + hr + dept_head
- LEAVE_REQUEST: 'leave:request' → admin + hr + dept_head + employee（员工自请）
- LEAVE_CANCEL: 'leave:cancel' → admin + hr + dept_head
（**不允许新建 RBAC 角色**，仅在 5 角色权限列表追加）
（**审批动作走 approval 端点**，不需新加 LEAVE_APPROVE）

### 任务 6: 挂载到 routes/index.ts

**修改**：server/src/routes/index.ts

**追加**（在 B2 之后）：
```typescript
import leaveRoutes from './leave';
// ...
router.use('/leaves', leaveRoutes);
```

### 任务 7: 加权限点

**修改**：server/src/constants/permissions.ts

**追加**（在 B2 权限点之后）：
```typescript
// M2-B3 请假
LEAVE_READ: 'leave:read',
LEAVE_REQUEST: 'leave:request',
LEAVE_CANCEL: 'leave:cancel',
```

并在 ROLE_PERMISSIONS 的 admin / hr / dept_head / employee 数组中追加（**5 角色结构不变**）：
- admin: [..., LEAVE_READ, LEAVE_REQUEST, LEAVE_CANCEL]
- hr: [..., LEAVE_READ, LEAVE_REQUEST, LEAVE_CANCEL]
- dept_head: [..., LEAVE_READ, LEAVE_CANCEL]
- employee: [..., LEAVE_REQUEST]  // 员工自请

### 任务 8: 写单测（21+ 个）

**新建**：server/src/services/leave.service.test.ts

**测试覆盖**（**含断言细节**，不要只列测试名）：

```typescript
describe('createLeaveRequest', () => {
  it('正常路径：3 天年假 + 充足余额 + ≤3 天审批流', async () => {
    // mock: prisma.employee.findFirst → { id: 'emp-1', hireDate: '2018-01-01', status: 'active' }
    // mock: configService.getValue → annual_leave_rules + approval_flow_short
    // mock: calculateLeaveBalance → { totalDays: 10, usedDays: 0, remainingDays: 10 }
    // mock: prisma.leaveRequest.findMany → []（无重叠）
    // mock: calculateWorkingDays → 3
    // mock: approval.submitApproval → { id: 'instance-1' }
    // mock: notification.sendNotification
    // mock: auditService.auditLog
    
    const result = await createLeaveRequest({
      employeeId: 'emp-1',
      leaveType: 'annual',
      startDate: '2026-09-01',
      endDate: '2026-09-03',
      reason: '休假'
    }, 'user-1');
    
    expect(result.status).toBe('submitted');
    expect(approval.submitApproval).toHaveBeenCalledWith(
      expect.objectContaining({
        flowKey: 'leave:leave_short',
        businessType: 'leave',
        businessId: result.id
      })
    );
  });
  
  it('5 天年假 + ≤3 天阈值 → 实际走 long 审批流（按 totalDays）', async () => {
    // mock: totalDays = 5（5 个工作日）
    // mock: approval.submitApproval
    
    const result = await createLeaveRequest({ ... startDate, endDate ... }, 'user-1');
    expect(approval.submitApproval).toHaveBeenCalledWith(
      expect.objectContaining({ flowKey: 'leave:leave_long' })
    );
  });
  
  it('leaveType 非法 → 72003', async () => {
    await expect(createLeaveRequest({ leaveType: 'invalid', ... }, 'user-1'))
      .rejects.toThrow(expect.objectContaining({ code: 72003 }));
  });
  
  it('startDate >= endDate → 72002', async () => {
    await expect(createLeaveRequest({
      startDate: '2026-09-05', endDate: '2026-09-01', ...
    }, 'user-1')).rejects.toThrow(expect.objectContaining({ code: 72002 }));
  });
  
  it('endDate - startDate > 30 天 → 72008', async () => {
    await expect(createLeaveRequest({
      startDate: '2026-09-01', endDate: '2026-10-15',  // 44 天
      leaveType: 'personal', ...
    }, 'user-1')).rejects.toThrow(expect.objectContaining({ code: 72008 }));
  });
  
  it('年假未提前 7 天 → 72009', async () => {
    await expect(createLeaveRequest({
      leaveType: 'annual',
      startDate: new Date(Date.now() + 3 * 86400000).toISOString().slice(0, 10),  // 3 天后
      endDate: new Date(Date.now() + 4 * 86400000).toISOString().slice(0, 10),
      ...
    }, 'user-1')).rejects.toThrow(expect.objectContaining({ code: 72009 }));
  });
  
  it('年假余额不足 → 72004', async () => {
    // mock: calculateLeaveBalance → { totalDays: 5, usedDays: 5, remainingDays: 0 }
    await expect(createLeaveRequest({
      leaveType: 'annual', totalDays: 3, ...
    }, 'user-1')).rejects.toThrow(expect.objectContaining({ code: 72004 }));
  });
  
  it('请假区间重叠 → 72005', async () => {
    (prisma.leaveRequest.findMany as any).mockResolvedValue([
      { id: 'lr-existing', startDate: '2026-09-01', endDate: '2026-09-05' }
    ]);
    await expect(createLeaveRequest({
      startDate: '2026-09-03', endDate: '2026-09-08', ...
    }, 'user-1')).rejects.toThrow(expect.objectContaining({ code: 72005 }));
  });
  
  it('病假不校验余额（不限天数）', async () => {
    // mock: totalDays = 5
    // sick 假期不调 calculateLeaveBalance
    const result = await createLeaveRequest({
      leaveType: 'sick', ...
    }, 'user-1');
    expect(result.status).toBe('submitted');
  });
});

describe('cancelLeaveRequest', () => {
  it('draft 直接取消', async () => {
    (prisma.leaveRequest.findUnique as any).mockResolvedValue({ 
      id: 'lr-1', status: 'draft' 
    });
    const result = await cancelLeaveRequest('lr-1', '撤回', 'user-1');
    expect(result.status).toBe('cancelled');
  });
  
  it('submitted 调 approval.withdraw', async () => {
    (prisma.leaveRequest.findUnique as any).mockResolvedValue({ 
      id: 'lr-1', status: 'submitted', approvalInstanceId: 'i-1' 
    });
    const result = await cancelLeaveRequest('lr-1', '撤回', 'user-1');
    expect(approval.withdraw).toHaveBeenCalledWith(
      expect.objectContaining({ instanceId: 'i-1' })
    );
  });
  
  it('approved 状态 → 72007', async () => {
    (prisma.leaveRequest.findUnique as any).mockResolvedValue({ 
      id: 'lr-1', status: 'approved' 
    });
    await expect(cancelLeaveRequest('lr-1', '原因', 'user-1'))
      .rejects.toThrow(expect.objectContaining({ code: 72007 }));
  });
});

describe('confirmLeaveRequest', () => {
  it('approved → 通知 + 审计', async () => {
    (prisma.leaveRequest.findUnique as any).mockResolvedValue({ 
      id: 'lr-1', status: 'submitted' 
    });
    const result = await confirmLeaveRequest('lr-1', { approved: true }, 'user-1');
    expect(result.status).toBe('approved');
  });
  
  it('rejected → 通知 + 审计', async () => {
    const result = await confirmLeaveRequest('lr-1', { approved: false }, 'user-1');
    expect(result.status).toBe('rejected');
  });
  
  it('非 submitted 状态 → 72006', async () => {
    (prisma.leaveRequest.findUnique as any).mockResolvedValue({ 
      id: 'lr-1', status: 'approved' 
    });
    await expect(confirmLeaveRequest('lr-1', { approved: true }, 'user-1'))
      .rejects.toThrow(expect.objectContaining({ code: 72006 }));
  });
});

describe('calculateLeaveBalance', () => {
  it('工龄 5 年的员工 → 年假 5 天', async () => {
    (prisma.employee.findFirst as any).mockResolvedValue({ 
      id: 'emp-1', hireDate: '2021-01-01'  // 5 年工龄
    });
    (configService.getValue as any).mockResolvedValue({ 
      '1-10': 5, '10-20': 10, '>20': 15 
    });
    (prisma.leaveRequest.findMany as any).mockResolvedValue([]);
    
    const result = await calculateLeaveBalance('emp-1', 'annual', 2026);
    expect(result.totalDays).toBe(5);
    expect(result.usedDays).toBe(0);
    expect(result.remainingDays).toBe(5);
  });
  
  it('工龄 15 年 → 年假 10 天', async () => {
    (prisma.employee.findFirst as any).mockResolvedValue({ 
      id: 'emp-1', hireDate: '2011-01-01'  // 15 年工龄
    });
    const result = await calculateLeaveBalance('emp-1', 'annual', 2026);
    expect(result.totalDays).toBe(10);
  });
  
  it('病假 / 事假不限余额 → 999', async () => {
    const sick = await calculateLeaveBalance('emp-1', 'sick', 2026);
    expect(sick.remainingDays).toBe(999);
  });
});

describe('calculateWorkingDays', () => {
  it('2026-09-01 ~ 2026-09-05（周二到周六）→ 4 个工作日（排除周六）', () => {
    const days = calculateWorkingDays(
      new Date('2026-09-01'),  // 周二
      new Date('2026-09-05')   // 周六
    );
    expect(days).toBe(4);
  });
  
  it('2026-09-05（周六）单日 → 0 个工作日', () => {
    const days = calculateWorkingDays(
      new Date('2026-09-05'), new Date('2026-09-05')
    );
    expect(days).toBe(0);
  });
});

describe('hasOverlappingLeave', () => {
  it('无重叠 → false', async () => {
    (prisma.leaveRequest.findMany as any).mockResolvedValue([]);
    const result = await hasOverlappingLeave('emp-1', new Date('2026-09-01'), new Date('2026-09-05'));
    expect(result).toBe(false);
  });
  
  it('有重叠 → true', async () => {
    (prisma.leaveRequest.findMany as any).mockResolvedValue([
      { id: 'lr-existing', startDate: '2026-09-03', endDate: '2026-09-08' }
    ]);
    const result = await hasOverlappingLeave('emp-1', new Date('2026-09-01'), new Date('2026-09-05'));
    expect(result).toBe(true);
  });
});
```

**总测试数预估**：314 + 21 = 335

**【必做】在测试里 mock 的依赖**（不调真实）：
- prisma（mock 整个 client）
- approvalService（mock submitApproval/withdraw）
- notificationService（mock sendNotification）
- auditService（mock auditLog）
- configService（mock getValue 返回 fixture）

**禁止**：调用真实 prisma（用 vi.mock），调用真实审批/通知/审计/配置（用 vi.mock）。

### 任务 9: 更新 seed.ts

**修改**：server/prisma/seed.ts

**追加**（在 B2 seed 之后）：
- 9 类 leave configs seed
- 3 个 demo leave requests：
  1. draft 状态年假申请
  2. submitted 状态事假申请
  3. approved 状态病假申请

**注意**：B3 seed 引用 A2 已 seed 的 employee，**不要重复 seed employee**。
**不创建 leave_balances 表**（仅 service 计算）。

### 任务 10: 更新 API Spec

**修改**：docs/api-spec.md

**追加** §3.15 Leave API（5 端点表格）

**修改**：docs/openapi.yaml

**追加**：5 个新端点 path（leave 5），含 schema 定义。

### 任务 11: 更新 V1.2 主文档

**修改**：docs/HRMS-V1.2.md

**两处修改**：
1. §四.6.1 M2 切片清单 B3 行标记"**已实现**" + 链接到
   server/src/services/leave/README.md
2. §七.3 变更记录**追加** 1 行：
```
| **M2.0.3** | **2026-08-29** | **feat(leave): M2-B3 请假（Cursor 交付）<br>**范围**：leave_requests 1 张表 + 8 类假期（annual/sick/personal/compensatory/marriage/maternity/paternity/bereavement）+ 假期额度管理（按工龄的年假规则 + service 函数计算，**不存表**）+ 2 级审批流（≤3 天 short / >3 天 long）+ 余额校验（年假/调休不足禁止提交）+ 工作日计算（排除周末）+ 9 类 configs 业务规则 + 5 个新端点 + 21 个新单测（314→335）<br>**依赖**：M1 全部 + M0.5 全部 + M2-B1/B2 + employee_salary_history（工龄计算）<br>**强约束**：员工/部门/公司关联走 prisma 直接操作（不 import 跨 service），**假期余额不存表**（V1.2 §四.6.2 关键要求），调休余额留 B4 加班后实现，BullMQ 余额重置/调休清理留独立任务 | **Cursor + WorkBuddy** |
```

### 任务 12: 更新 AGENTS.md

**修改**：AGENTS.md

**追加 1 段**（在 M2 段 B2 描述之后）：
```markdown
- **B3 请假（已完成）**：leave_requests 1 张表
  + 8 类假期（annual/sick/personal/compensatory/marriage/maternity/paternity/bereavement）
  + 假期额度管理（按工龄的年假规则 + service 函数计算，**不存表**）
  + 2 级审批流（≤3 天 short / >3 天 long，走 M0.5-1）
  + 余额校验（年假/调休不足禁止提交）
  + 工作日计算（排除周末，configs.leave.workday_exclude_weekends）
  + 5 个新端点 + 21 个新单测（314 → 335）
  + **B3 不实现 B4-B6 业务**
  + **调休余额留 B4 加班后实现**（B3 暂返回 0 + TODO）
- 后续：B4（加班）→ B5（出差）→ B6（月度汇总）
```

### 任务 13: 创建 README

**新建**：server/src/services/leave/README.md（约 3-5 KB）

**包含**：
- B3 业务说明（V1.2 §二.3.3 假勤管理 + §四.6.1 B3 切片）
- 状态机图（draft / submitted / approved / rejected / cancelled）
- 8 类假期 + 审批流（≤3 / >3 天）
- 5 端点清单
- 业务规则（9 类 configs）
- 假期余额计算（**不存表**，service 函数）
- M0.5 + M1 + M2-B1/B2 集成点（审批 / 通知 / 班次关联 / 员工关联）
- 测试覆盖（21 个）
- 已知限制（调休余额留 B4 / BullMQ 重置留独立任务 / 余额不存表）
- 后续 B4-B6 集成计划

【输出 - 完整文件清单】

**新增文件（6 个）**：
1. server/prisma/migrations/20260829070000_add_leave/migration.sql
2. server/src/services/leave.service.ts
3. server/src/services/leave.service.test.ts
4. server/src/controllers/leave.controller.ts
5. server/src/routes/leave.ts
6. server/src/services/leave/README.md

**修改文件（8 个）**：
7. server/prisma/schema.prisma（追加 1 张表 leave_requests）
8. server/prisma/seed.ts（追加 9 类 leave configs + 3 demo records）
9. server/src/routes/index.ts（追加 1 import + 1 router.use）
10. server/src/constants/permissions.ts（追加 3 个新权限点 + 5 角色权限追加）
11. docs/api-spec.md（追加 §3.15 Leave API）
12. docs/openapi.yaml（追加 5 个新端点 path + schema）
13. docs/HRMS-V1.2.md（§四.6.1 B3 标"已实现" + §七.3 追加 1 行变更记录）
14. AGENTS.md（M2 段追加 B3 描述）

**总改动：6 新增 + 8 修改 = 14 文件**

【验收 - 必须全部满足】
1. pnpm --filter hrms-server type-check 0 错
2. pnpm --filter hrms-server lint 0 错
3. pnpm --filter hrms-server test 335/335 通过（314 + 21 新增）
4. pnpm --filter hrms-client type-check 0 错
5. 改动文件数 ≤ 18
6. 没有任何 M0/M0.5/M1/M2-B1/M2-B2 旧测试文件被修改
   （用 `git diff --stat "*.test.ts"` 确认仅新增 leave.service.test.ts）
7. 没有做 B1/B2（已实现）/ B4/B5/B6 任何业务
8. 没有改 M0.5 + M1 + M2-B1 + M2-B2 任何 service / controller / routes（grep 验证）
9. leave.service.ts **仅 import 黑名单以外的 service**
   （即只 import approval/audit/config/notification/prisma，不 import 跨 service）
10. 审批调用走 approval.submitApproval / withdraw（已 export），没有自己重写审批逻辑
11. 通知走 notification.sendNotification + **bypassTemplate fallback**（已验证 A4/A5/A6/A7/B1/B2 模式）
12. 员工/部门/公司关联走 prisma 直接操作（**不 import 跨 service**）
13. **不创建 leave_balances 表**（V1.2 §四.6.2 关键要求，余额 service 函数计算）
14. **B3 不实现 B4-B6 业务**（留后续切片）
15. 所有业务规则走 M0.5-6 configService（不要 hardcoded）
16. 没有新建 RBAC 角色（5 角色结构不变，仅追加权限点）

【不要做的事 - 红线复述】
- ❌ 不要做 B1/B2（已实现）/ B4/B5/B6 任何业务
- ❌ 不要做 client/ 或 mobile/ 任何文件
- ❌ 不要改任何 M0/M0.5/M1/M2-B1/M2-B2 已有测试
- ❌ 不要改 M0.5 + M1 + M2-B1 + M2-B2 已有 service / controller / routes
- ❌ **不要 import 跨 service**（shift/attendance/employee/department/company/onboarding/regularization/offboarding/transfer/contract/employeeAI/auth）
- ❌ **不要创建 leave_balances 表**（V1.2 §四.6.2 关键要求，余额不存表）
- ❌ 不要修改 notification.service.ts / approval.service.ts / crypto.service.ts 已有函数签名
- ❌ 不要改 server/src/jobs/* 任何文件
- ❌ 不要"顺便"加 B4-B6 的接口 / 表 / 服务
- ❌ 不要"我觉得可以优化"任何已有代码
- ❌ 不要重写审批 / 通知 / 加密 / 配置 / 审计
- ❌ 不要新建 RBAC 角色
- ❌ **不要实现加班**（留 B4）
- ❌ **不要实现出差**（留 B5）
- ❌ **不要实现月度汇总**（留 B6）
- ❌ **不要实现 BullMQ 余额自动重置**（留独立任务）
- ❌ **不要实现 BullMQ 调休到期清理**（留独立任务）

【参考实现位置】
- service 模式：server/src/services/attendance.service.ts（**最完整参考**）
- controller 模式：server/src/controllers/attendance.controller.ts
- routes 模式：server/src/routes/attendance.ts（含 zod schema + authenticate +
  rejectIfMustChangePassword + requirePermission）
- 业务集成调用：server/src/services/attendance.service.ts
  （import approvalService / auditService / configService / notificationService）
- 状态机：参考 attendance.service.ts 的 pending → approved/rejected
- 单测模式：server/src/services/attendance.service.test.ts（mock + describe 风格）
- bypassTemplate fallback 模式：参考 regularization.service.ts 第 80-100 行
- 工作日计算：参考 A1+A2 已有 service（如有），无则自己写 calculateWorkingDays
- 余额计算 service 函数：参考 A1+A2 已有 service（如有），无则自己写 calculateLeaveBalance

【完成标准】
当你跑完以下命令且全部成功后，再回复我"已完成"：
  pnpm --filter hrms-server type-check
  pnpm --filter hrms-server lint
  pnpm --filter hrms-server test
  pnpm --filter hrms-client type-check
  git diff --stat  # 确认改动文件数

并附上：
1. 改动文件清单（**必须只有 14 个**：6 新增 + 8 修改）
2. **明确声明**："未修改任何 M0/M0.5/M1/M2-B1/M2-B2 旧测试（仍 314 个）"
3. **明确声明**："新单测 21 个，总计 335/335 通过"
4. **明确声明**："未做 B1/B2（已实现）/ B4/B5/B6 任何业务"
5. **明确声明**："leave.service.ts 仅 import approval/audit/config/notification/prisma，
   未 import 跨 service"
6. **明确声明**："**不创建 leave_balances 表**（V1.2 §四.6.2 关键要求，余额 service 函数计算）"
7. **明确声明**："通知走 bypassTemplate fallback（参考 A4/A5/A6/A7/B1/B2 模式）"
8. 4 条验收命令的输出
9. 任何意外情况 + 处理方式

如果改动文件数 > 18 或动了 314 个旧测试或重写审批/通知/加密
或 import 跨 service 或实现 B4-B6 业务或创建 leave_balances 表，**立即回退**并报告"越界，需重做"。
```

## 结束复制 ↑

---

# 文档

| 项 | 值 |
|---|---|
| 任务 ID | M2-B3 |
| 任务名 | 请假（8 类假期 + 额度管理 + 走 M0.5-1 审批流） |
| 估时 | 3.5d（V1.2 §四.6.1） |
| 依赖前置 | M1 + M0.5 + M2-B1/B2 班次 + 打卡 |
| **M2 第 3 个切片** | 阶段继续 |
| 改动文件 | 6 新增 + 8 修改 = 14 |
| 端点 | 5（提交/列表/详情/撤回/余额查询） |
| 新单测 | 21（314 → 335） |
| 错误码段位 | **72001-72010**（72 段位，71 段位 999 用尽） |
| 提示词大小 | ~45KB（沿用 A5/A6/A7/B1/B2 详尽模式） |
| 关键约束 | 不 import 跨 service / 余额不存表仅 service 计算 / 调休余额留 B4 / 不实现 B4-B6 |
| 越界红线 | 不做 B4-B6 / 不改旧测试 / 不改 M0.5+M1+B1+B2 已有代码 / 不创建 leave_balances 表 |
